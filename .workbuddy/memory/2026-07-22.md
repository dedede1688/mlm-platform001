# 2026-07-22 工作日志

## 流程变更（胡子老师拍板）

- **派单提示词不再落文件**：以后直接在对话中输出"可一键复制"的完整提示词文本
- 已删除全部存量提示词文件（20 个）：根目录 4 个 txt、docs/roles/onboarding/ 4 个、docs/roles/tasks/*/todo/ 12 个
- 待确认：AGENTS.md 与 docs/roles/README.md 中"任务文件/任务入口"制度描述是否同步更新

## 小M_007 综合审计报告 — 小酷审核

**小M 结论：不通过（5 表 P0 阻断）**
**小酷审核结论：事实层属实，级别判定严重错误，降级为 P1 治理问题**

### 关键反驳证据
- 生产接口 `https://mlm-platform001.vercel.app/api/settings/public` 返回 HTTP 200 + 真实数据（该接口内部调 prisma.banners.findMany）→ banners 表铁定存在
- schema 中 banners model 有 RLS 注释 + dbgenerated → 表是 Supabase 控制台手工建的
- create-tables.sql 存在 → 项目早期手工 SQL 建表历史
- 其余 4 表对应功能（忘记密码/通知/退款/分类）均经 v43-v62 多轮迭代验收，表不存在不可能工作

### 小M 修复方案的破坏性风险（被流程挡住）
- 小M 建议"生成 5 个 migration 跑 prisma migrate deploy"——表已存在，直接执行会报错；若含 DROP 会丢生产数据
- 正确修复：补幂等 migration（CREATE TABLE IF NOT EXISTS）

### 小M 报告中有价值的发现（值得排期）
- P1：reward/dividend service N+1 写入（createDividendReward 15N 次/settleWeeklyDividends 20N 次）
- P1：lookup API 无 rate limit（与 T-M0-3 重合）
- P1：支付密码纯数字 6 位弱密码
- P2：notification.service.ts 29KB 超大文件、硬编码 ICP/客服电话

## 胡子老师拍板（00:52）

1. **补幂等 migration**（P1 治理）→ 派小猫执行
2. **同步更新制度文档** → 已完成：docs/roles/README.md（任务入口改对话输出）、AGENTS.md（铁规则补"提示词不落文件"）、docs/roles/xiaoku/job-description.md（权限条款）

## 小猫派单：幂等 migration 补齐

- 任务：为 5 个缺失表（password_reset_codes/categories/notification_templates/refund_requests/banners）+ products.category_id 列补幂等 migration
- 关键发现：products.category_id 列同样无任何 migration 记录（仅 RLS migration 提及表名）
- 提示词已在对话中直接输出（不落文件）
