# 猫爪_035号任务：订单详情售后服务卡片最终发布

> **任务类型：** 033 + 034 最终发布闭环  
> **执行角色：** 猫爪  
> **已获授权：** 完整验证、精确提交、推送、部署核验  
> **禁止：** 修改业务代码、扩大范围、提交任务文件或旧文档、数据库操作

---

## 一、发布范围

只允许提交以下 5 个文件：

```text
src/app/dashboard/orders/[id]/page.tsx
src/app/api/orders/[id]/refund/route.ts
src/components/orders/OrderAfterSalesCard.tsx
__tests__/components/order-after-sales-card.test.ts
__tests__/api/orders/refund-route.test.ts
```

说明：组件测试保留 `.test.ts`，因为当前 `vitest.config.ts` 只匹配 `__tests__/**/*.test.ts`。禁止重命名，禁止修改测试配置。

---

## 二、基线确认

固定工作目录：

```text
D:\mlm-platform-source\mlm-platform
```

开始时确认：

```text
本地 HEAD   = f7e01929feca42c092b60ef07677bc0fa3746ac2
origin/main = 0c076092f232c7aef3a27791c34daa27feb363ef
```

本地领先的两个提交是售后卡片设计和实施计划，属于预期提交历史：

```text
f7e0192 docs: plan order detail after-sales card
5e818c2 docs: design order detail after-sales card
```

不得 reset、回滚或改写这两个提交。

---

## 三、发布前完整验证

所有验证必须成功后才能提交。任一失败立即停止，不得 commit 或 push。

### 3.1 范围和差异

```powershell
git diff --check -- "src/app/dashboard/orders/[id]/page.tsx" "src/app/api/orders/[id]/refund/route.ts" "src/components/orders/OrderAfterSalesCard.tsx" "__tests__/components/order-after-sales-card.test.ts" "__tests__/api/orders/refund-route.test.ts"
git status --short
```

### 3.2 代码规范检查

```powershell
npx next lint --file "src/app/dashboard/orders/[id]/page.tsx" --file "src/components/orders/OrderAfterSalesCard.tsx" --file "src/app/api/orders/[id]/refund/route.ts"
```

要求：0 Error。既有 Warning 如实记录，不扩大范围处理。

### 3.3 类型检查

```powershell
npx tsc --noEmit -p tsconfig.typecheck.json
```

### 3.4 完整测试和覆盖率

```powershell
npm run test:coverage:check
```

记录测试文件数、测试数、失败数和覆盖率，覆盖率门槛必须通过。

### 3.5 生产构建

```powershell
npm run build
```

必须退出码 0。

### 3.6 页面验证

启动本地开发服务器并尝试真实浏览器访问订单详情页：

```powershell
npm run dev
```

重点确认：

1. 原底部大号橙色“申请退款”按钮消失；
2. 金额明细后只有一个紧凑“售后服务”卡片；
3. 无退款时只有一个小号“申请退款”；
4. 页面无明显错位；
5. 如受登录认证或浏览器工具限制，必须如实记录，不得用构建成功冒充页面验收。

验证后关闭开发服务器，不得残留后台进程。

---

## 四、精确暂存

禁止 `git add .`、`git add src/` 或任何目录级暂存。

逐个精确暂存：

```powershell
git add -- "src/app/dashboard/orders/[id]/page.tsx"
git add -- "src/app/api/orders/[id]/refund/route.ts"
git add -- "src/components/orders/OrderAfterSalesCard.tsx"
git add -- "__tests__/components/order-after-sales-card.test.ts"
git add -- "__tests__/api/orders/refund-route.test.ts"
```

硬检查：

```powershell
git diff --cached --name-only
git diff --cached --stat
git status --short
```

暂存区必须恰好 5 个文件。不得包含：

- `docs/agent-tasks/`
- 5 个旧 docs 修改
- `vercel_ls.txt`
- 其他临时文件
- `AGENTS.md`
- 数据库结构或迁移文件

---

## 五、提交和推送

提交信息：

```text
feat(orders): refine after-sales refund card
```

执行提交后检查提交文件树确实只有 5 个目标文件，再执行：

```powershell
git push origin main
```

### 铁律 1 验证

推送后立即执行：

```powershell
git log origin/main --oneline -1
git rev-parse HEAD
git rev-parse origin/main
```

本地 HEAD 与 `origin/main` 必须完全一致，最新远程提交必须是本次提交。

---

## 六、线上验证

1. GitHub Actions（持续集成）必须 completed / success；
2. Lint、TypeScript、Tests、Coverage 和覆盖率门槛步骤必须成功；
3. Vercel 最新 Production（生产环境）部署 commit 必须等于本次提交；
4. Vercel 状态必须 Ready；
5. 无法独立打开 Vercel 时，必须明确记录限制，不能推断成 Ready。

---

## 七、最终状态

运行：

```powershell
git status --short
git log origin/main --oneline -1
```

确认 5 个目标文件已经提交，剩余历史文档和任务文件仍未提交，无临时文件。

---

## 八、结果文件

写入：

```text
docs/agent-tasks/catpaw/done/猫爪_035号结果.md
```

必须包含：

1. 发布前全部验证结果；
2. 页面验证证据或限制；
3. 精确暂存的 5 文件；
4. commit hash 和信息；
5. push 输出；
6. 铁律 1 验证；
7. GitHub Actions 状态；
8. Vercel commit 和 Ready 状态；
9. 最终 `git status --short`；
10. 是否误提交任务文件、旧文档或临时文件；
11. 胡子老师浏览器验收路径。

完成后停止，等待小 M 035 号上线后最终只读复审和胡子老师浏览器验收。
