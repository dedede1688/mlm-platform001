# 猫爪_032号任务：商品详情待支付恢复最终发布验证

> **执行模式：** 只做最终验证、精确提交、推送和部署闭环。  
> **任务目标：** 发布 024-031 号已经通过复审的商品详情待支付恢复功能及真实 JSX 测试基础设施。  
> **禁止事项：** 不再修改业务代码或测试；验证失败立即停止；禁止 `git add .`；不得提交任务文件、旧文档或临时文件。

---

## 一、固定工作目录

```text
D:\mlm-platform-source\mlm-platform
```

---

## 二、开始前必须读取

1. `AGENTS.md`
2. `docs/agent-tasks/README.md`
3. `docs/agent-tasks/catpaw/todo/猫爪_024号任务.md`
4. `docs/agent-tasks/catpaw/done/猫爪_024号结果.md`
5. `docs/agent-tasks/xiaom/done/小M_024号结果.md`
6. 025-031 号猫爪任务、猫爪结果和小M结果
7. 本任务文件

---

## 三、基线与提交关系

开始时预期：

```text
本地 HEAD   = baff1843a394fe1e2b21b9bc3e10ccc9d232b53b
origin/main = 49d3583a68f0f5f5e5794ce16c3a76594028752f
```

说明：本地 HEAD 比远程领先 1 个已经提交的设计文档 commit：

```text
baff184 docs: design product pending payment recovery
```

这是 024 号功能的设计文档提交，不得 reset、回滚或改写。032 号业务提交完成后，push 会把 `baff184` 和新的业务 commit 一起推送。

先执行并记录：

```powershell
git status --short
git log --oneline -3
git rev-parse HEAD
git rev-parse origin/main
```

如果基线与上述预期不一致，停止并写结果，不得自行 reset。

---

## 四、最终提交只能包含以下 13 个文件

```text
src/lib/services/order.service.ts
src/app/api/orders/[id]/route.ts
src/lib/utils/pending-payment-session.ts
src/components/checkout/CheckoutDialog.tsx
src/app/products/[id]/page.tsx
__tests__/services/order.test.ts
__tests__/api/orders/order-detail-route.test.ts
__tests__/lib/pending-payment-session.test.ts
__tests__/components/checkout-pending-order-lock.test.ts
__tests__/components/product-pending-payment-flow.test.ts
package.json
pnpm-lock.yaml
vitest.config.ts
```

不得提交：

- `docs/agent-tasks/`
- 5 个历史 docs 修改
- `vercel_ls.txt`
- 临时输出、测试日志、诊断脚本
- 任何不在上述 13 项中的文件

---

## 五、发布前只执行一次完整验证

### 5.1 范围和空白检查

确认 8 个 tracked 修改和 5 个 untracked 新文件恰好组成上述 13 项。

对 13 个目标文件运行 `git diff --check`。CRLF 提示可以记录，但真实 whitespace error（空白错误）必须为 0。

### 5.2 Lint（代码规范）

```powershell
pnpm lint
```

必须退出码 0、Error 为 0。Warning 如为项目既有项可如实记录。

### 5.3 Typecheck（类型检查）

```powershell
pnpm typecheck
```

等价于：

```powershell
npx tsc --noEmit -p tsconfig.typecheck.json
```

必须退出码 0。

### 5.4 完整测试与覆盖率

只启动一次：

```powershell
pnpm test:coverage:check
```

不得因暂时没有输出而重复启动第二个测试进程。必须记录测试文件数、测试数、失败数和 statements（语句）覆盖率；覆盖率门槛必须通过。

### 5.5 Build（生产构建）

```powershell
pnpm build
```

必须退出码 0，记录页面生成结果。

### 5.6 UI（界面）真实验证尝试

这是 UI 改动，必须启动本地开发服务器并尝试真实浏览器验证：

```powershell
pnpm dev
```

至少核对：

1. 商品详情页可以打开结算弹窗；
2. 首次下单保持可编辑收货信息；
3. 待支付恢复时显示锁定收货摘要；
4. 锁定信息缺失或空白时显示红色异常提示并禁用“重新确认支付”；
5. 完整锁定信息时按钮可用；
6. 恢复失败时不显示“已恢复”；
7. 页面无明显重叠、溢出或按钮错位。

如登录认证阻止浏览器验证，必须如实记录并截图登录拦截页面；允许以 031 号真实 JSX 渲染测试和源码级路径核对作为替代证据，但不得写成“浏览器验收通过”。停止开发服务器后再继续。

---

## 六、失败停止规则

以下任一项失败时立即停止，不 commit、不 push、不部署：

- 目标文件不是恰好 13 个；
- Lint 有 Error；
- Typecheck 失败；
- 完整测试或覆盖率失败；
- Build 失败；
- 出现新的业务问题；
- 需要修改第 13 项清单以外的文件才能通过。

只把失败证据写入结果文件，等待重新派单；本任务不允许现场修代码。

---

## 七、精确暂存与硬检查

所有验证通过后，逐个精确暂存上述 13 个文件。必须使用 `git add -- "路径"`，禁止 `git add .`、`git add -A` 或目录级批量暂存。

暂存后执行：

```powershell
git diff --cached --name-status
git diff --cached --stat
git status --short
```

硬性要求：暂存区恰好 13 个目标文件，其他历史文档、任务文件和临时文件全部不得进入暂存区。

---

## 八、Commit（提交）与 Push（推送）

提交信息：

```text
feat(checkout): restore pending product payment
```

提交成功后确认 commit 文件树仍恰好是 13 个文件，再执行：

```powershell
git push origin main
git log origin/main --oneline -2
git rev-parse HEAD
git rev-parse origin/main
```

铁律 1 要求：

1. 本地 HEAD 必须等于 `origin/main`；
2. `origin/main` 最新 commit 必须是本次业务提交；
3. 远程倒数第二条必须能看到 `baff184 docs: design product pending payment recovery`；
4. push 输出必须记录。

任一不一致都不得宣称推送成功。

---

## 九、GitHub Actions 与 Vercel 闭环

### 9.1 GitHub Actions（持续集成）

等待本次 commit 对应的工作流完成，确认：

- status = completed；
- conclusion = success；
- Lint、TypeScript type check、tests、coverage 全部通过。

如果失败，记录失败步骤和日志摘要，不得把 Vercel Ready 当作 CI 通过。

### 9.2 Vercel（部署平台）

确认生产环境最新部署：

- Source = `main`；
- commit hash = 本次业务 commit；
- Status = Ready；
- Latest / Current（最新）标记正确。

无法独立查看 Dashboard 时必须如实写“未独立验证”，不得仅凭 push 成功推断 Ready。

---

## 十、最终状态检查

执行：

```powershell
git status --short
git log origin/main --oneline -2
```

确认：

- 13 个目标文件已提交，不再出现在工作区；
- 旧 docs 修改仍未提交；
- `docs/agent-tasks/` 仍未提交；
- 无临时脚本、输出日志或测试诊断文件；
- 未修改数据库结构，未执行迁移，未写生产数据库。

---

## 十一、结果文件

写入：

```text
docs/agent-tasks/catpaw/done/猫爪_032号结果.md
```

必须包含：

1. 开始基线与本地领先 1 commit 的说明；
2. 13 个提交文件清单；
3. diff check、Lint、Typecheck、完整测试、覆盖率、Build 结果；
4. UI 真实验证结果或认证限制；
5. commit hash 和提交信息；
6. push 原始摘要；
7. `git log origin/main --oneline -2`；
8. HEAD 与 origin/main 是否一致；
9. GitHub Actions 状态；
10. Vercel commit 与 Ready 状态；
11. 最终 `git status --short`；
12. 是否误提交任务文件、旧文档或临时文件；
13. 是否改数据库、执行迁移或写生产数据；
14. 胡子老师部署后浏览器验收清单。

结果文件只留本地，不得补进本次 commit。写完后停止，等待小M 032 号上线后只读复审。

