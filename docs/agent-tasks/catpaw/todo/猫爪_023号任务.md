# 猫爪_023号任务：支付密码重置后四位校验发布闭环

## 一、任务身份与授权

你继续担任本项目“猫爪执行岗位”。

猫爪 022 号业务修复已经通过小酷初审和小 M 022 号独立只读复审。胡子老师现已明确授权本次执行：

- 完整发布前验证。
- 精确提交 022 号的 3 个文件。
- 推送到 `main` 分支。
- 验证 GitHub Actions（自动检查）。
- 验证 Vercel（部署平台）生产部署。

本任务不得再修改业务代码或测试代码。若验证失败，立即停止并报告，不得为了通过验证继续改代码。

## 二、当前基线

开始时必须确认：

```text
本地 HEAD   = ec4e66bc7cc6b781af308753be63ab225bd1f2ec
origin/main = ec4e66bc7cc6b781af308753be63ab225bd1f2ec
最新说明    = ec4e66b feat(security): add payment password reset flow
```

运行：

```powershell
git rev-parse HEAD
git rev-parse origin/main
git log origin/main --oneline -1
git status --short
```

如果本地 HEAD 或 `origin/main` 已发生变化，立即停止并报告，不得提交。

## 三、本次唯一允许提交的文件

恰好 3 个：

```text
src/app/admin/users/page.tsx
__tests__/components/payment-password-reset-flow.test.ts
__tests__/api/admin/users/payment-password-reset.test.ts
```

不得提交：

- `docs/agent-tasks/` 下任何任务或结果文件。
- 5 个旧文档修改。
- `vercel_ls.txt`。
- `AGENTS.md`。
- Prisma schema、迁移、seed 或任何数据库文件。
- 其他业务代码、测试文件或临时输出文件。

本任务**不需要同步任何生产通知模板**。022 号没有修改通知模板，禁止执行 seed、upsert、SQL 或任何生产数据库写入。

## 四、效率规则

每个重命令只运行一次：

1. Lint（代码规范检查）一次。
2. Typecheck（类型检查）一次。
3. 完整测试及覆盖率一次。
4. Build（构建）一次。

禁止因为终端输出没有立即显示就重复启动同一命令。

命令运行中时：

- 先检查对应终端是否仍在运行。
- 等待原命令完成。
- 只有明确确认原命令没有启动、终端已经失效或输出丢失时，才允许补跑一次，并在结果报告说明原因。

不要生成 `*_output.txt`、`build_out.txt`、`test-output.log` 等仓库内临时文件。

## 五、发布前验证

### 5.1 范围和差异检查

```powershell
git diff --name-only -- src/app/admin/users/page.tsx __tests__/components/payment-password-reset-flow.test.ts __tests__/api/admin/users/payment-password-reset.test.ts
git diff --stat -- src/app/admin/users/page.tsx __tests__/components/payment-password-reset-flow.test.ts __tests__/api/admin/users/payment-password-reset.test.ts
git diff --check -- src/app/admin/users/page.tsx __tests__/components/payment-password-reset-flow.test.ts __tests__/api/admin/users/payment-password-reset.test.ts
```

预期：022 增量恰好 3 个文件，`git diff --check` 退出码 0。

### 5.2 Lint（代码规范检查）

只运行一次：

```powershell
pnpm lint --file src/app/admin/users/page.tsx
```

要求：0 Error（错误）。既有 Warning（警告）可记录，不得顺手修改。

### 5.3 Typecheck（类型检查）

只运行一次：

```powershell
pnpm typecheck
```

要求：退出码 0。

### 5.4 完整测试与覆盖率

只运行一次：

```powershell
pnpm test:coverage:check
```

该命令已经包含完整 Vitest（测试框架）测试和覆盖率门槛检查，不要再单独运行 `pnpm test` 或 `pnpm test:coverage`。

要求：

- 所有测试通过。
- 失败数为 0。
- statements（语句覆盖率）不低于 70%。
- 记录测试文件数、测试数和覆盖率。

### 5.5 Build（构建）

只运行一次：

```powershell
pnpm build
```

要求：退出码 0，构建完成。

### 5.6 界面验证说明

猫爪 022 号已经尝试浏览器验证，但后台登录认证拦截，已完成源码级验证；小 M 022 号也已完成三层拦截独立审查。

本次发布任务不再重复启动 dev server（开发服务器），避免无登录状态下重复耗时。

部署完成后由胡子老师使用真实后台登录态验证：

1. 用户 `13800138001` 输入错误后四位 `0000`。
2. 页面显示红色“不匹配”提示。
3. “重置支付密码”按钮保持禁用。
4. 不打开二次确认弹窗。
5. 改为正确后四位 `8001` 后提示消失、按钮恢复可用。
6. 不真正执行第二次重置，避免再次影响测试账号。

## 六、精确暂存

只有第 5 节全部通过后才能暂存。

必须使用：

```powershell
git add -- "src/app/admin/users/page.tsx" "__tests__/components/payment-password-reset-flow.test.ts" "__tests__/api/admin/users/payment-password-reset.test.ts"
```

禁止：

```text
git add .
git add -A
git add src/
git add __tests__/
```

暂存后运行：

```powershell
git diff --cached --name-only
git diff --cached --stat
git status --short
```

硬性要求：`git diff --cached --name-only` 必须恰好显示以下 3 行：

```text
__tests__/api/admin/users/payment-password-reset.test.ts
__tests__/components/payment-password-reset-flow.test.ts
src/app/admin/users/page.tsx
```

多一行或少一行都立即停止，不得提交。

## 七、提交和推送

### 7.1 Commit（提交）

提交说明固定为：

```text
fix(security): validate payment reset phone suffix
```

运行：

```powershell
git commit -m "fix(security): validate payment reset phone suffix"
```

如果 PowerShell 引号解析异常，可使用仓库外临时提交说明文件；使用后立即删除，不能留在仓库内。

### 7.2 Push（推送）

```powershell
git push origin main
```

禁止 force push（强制推送）。

### 7.3 铁律 1 验证

推送完成后立即运行：

```powershell
git log origin/main --oneline -1
git rev-parse HEAD
git rev-parse origin/main
```

必须确认：

- `origin/main` 最新提交编号等于本次提交编号。
- 本地 HEAD 等于 `origin/main`。

不一致时不得声称推送成功。

## 八、GitHub Actions 和 Vercel 验证

### 8.1 GitHub Actions（自动检查）

等待本次提交对应的流水线完成，确认：

- commit（提交编号）是本次新提交。
- 总状态为 Success（成功）。
- Lint（代码规范检查）成功。
- TypeScript type check（类型检查）成功。
- Tests（测试）成功。
- Coverage（覆盖率）成功。

如果外部网络或工具不可用，如实记录“未独立确认”，不得猜测成功。

### 8.2 Vercel（部署平台）

确认：

- 项目：`mlm-platform001`。
- 分支：`main`。
- commit（提交编号）等于本次新提交。
- 环境：Production（生产环境）。
- 状态：Ready（就绪）。
- 标记为 Latest 或 Current（最新生产部署）。

仅看到 `Deployment has completed` 但无法确认提交编号时，必须如实说明未完成提交编号核对。

## 九、最终状态检查

运行：

```powershell
git status --short
git log origin/main --oneline -1
```

确认：

- 3 个目标文件不再出现在未提交状态中。
- 旧文档和任务文件仍未提交。
- 没有新增临时脚本或输出文件。
- 没有误提交任务文件、旧文档或 `vercel_ls.txt`。

## 十、停止条件

出现以下任一情况立即停止并报告：

- 当前基线不是 `ec4e66b`。
- 3 个目标文件以外出现本次业务增量。
- Lint 出现 Error。
- Typecheck 失败。
- 完整测试或覆盖率检查失败。
- Build 失败。
- 暂存区不是恰好 3 个文件。
- push 失败或本地 HEAD 不等于 `origin/main`。
- GitHub Actions 失败。
- Vercel 部署失败。

不得在本任务内修复任何新问题；停止后等待胡子老师和小酷重新派单。

## 十一、结果文件

完成后写入：

```text
docs/agent-tasks/catpaw/done/猫爪_023号结果.md
```

结果必须包含：

1. 开始时本地 HEAD、`origin/main` 和基线检查结果。
2. 发布前真实修改范围。
3. `git diff --check` 结果。
4. Lint 结果和 Error/Warning 数。
5. Typecheck 结果。
6. 完整测试文件数、测试数、失败数。
7. 覆盖率和 70% 门槛结果。
8. Build 结果。
9. 为什么本轮不重复运行 dev server。
10. 暂存区 3 个文件清单。
11. commit hash（提交编号）和提交说明。
12. push 结果。
13. 铁律 1 验证结果。
14. GitHub Actions 各步骤状态。
15. Vercel 提交编号、环境和 Ready 状态。
16. 最终 `git status --short`。
17. 是否误提交任务文件、旧文档、`vercel_ls.txt` 或临时文件。
18. 是否写数据库、同步模板或执行迁移（必须为否）。
19. 胡子老师部署后浏览器验收清单。
20. 未完成项、P0/P1/P2 风险和执行方案建议。

结果文件不要提交。完成后停止，等待胡子老师进行真实浏览器验收。
