# 猫爪_010号任务：已取消

> 2026-07-13 胡子老师选择停止本长任务，改用猫爪 011 号快速执行任务。本文件不得继续执行。

原任务标题：修复全局代码规范阻断错误并恢复远程自动检查

## 一、任务身份

你是本任务的执行代理“猫爪”。

本任务只处理会让 GitHub Actions（自动检查）失败的 `Lint Error（代码规范阻断错误）`。

不处理 `Warning（警告）`，不重构业务，不改变页面视觉，不修改资金、支付、提现、充值、订单或通知行为。

## 二、背景

提交 `2255440` 已部署成功，但 GitHub Actions（自动检查）的 `test`（测试任务）在 `Lint（代码规范检查）`步骤失败，导致后续类型检查和测试被跳过。

小酷已在本地运行：

```powershell
.\node_modules\.bin\next.cmd lint
```

确认当前共有：

```text
21 个 Error（阻断错误）
分布于 15 个文件
```

这些错误均为项目既有的未使用导入、未使用变量或未使用参数，不是收益作废功能引入的问题。

## 三、目标

1. 将 21 个阻断错误全部清零。
2. 保留现有 `Warning（警告）`，本轮不处理。
3. 保证 `next lint`（代码规范检查）退出码为 0。
4. 保证 typecheck（类型检查）、完整测试和 build（构建）通过。
5. 只提交本任务规定的 15 个文件。
6. 推送后验证远程提交，并确认 GitHub Actions（自动检查）和 Vercel（部署平台）状态。

## 四、允许修改的 15 个文件和精确动作

### 1. `src/app/admin/dashboard/page.tsx`

删除未使用的图标导入：

```text
FileText
```

不得修改页面逻辑或布局。

### 2. `src/app/admin/finance/page.tsx`

删除未使用的图标导入：

```text
FileText
Bell
SquareCheck
Square
```

不得处理本文件中的 `any`、依赖数组或 `<img>` 警告。

### 3. `src/app/admin/layout.tsx`

删除未使用的图标导入：

```text
FileText
```

### 4. `src/app/admin/notification-history/page.tsx`

删除未使用的图标导入：

```text
Search
```

不得修改请求逻辑或依赖数组。

### 5. `src/app/admin/reports/page.tsx`

只做两处最小修改：

1. 将：

```typescript
funnel.funnel.map((step, i) => {
```

改为：

```typescript
funnel.funnel.map((step) => {
```

2. 将 `StatCard` 中：

```typescript
const [textColor, bgColor] = color.split(' ')
```

改为：

```typescript
const [textColor] = color.split(' ')
```

注意：漏斗主体中另一个 `bgColor` 仍被页面使用，不得删除。

### 6. `src/app/api/admin/dashboard/summary/route.ts`

从 `next/server` 导入中删除未使用的：

```text
NextResponse
```

保留 `NextRequest`，不得修改接口响应。

### 7. `src/app/api/admin/role-permissions/route.ts`

从 `next/server` 导入中删除未使用的：

```text
NextResponse
```

保留 `NextRequest`。

### 8. `src/app/api/admin/roles/route.ts`

从 `next/server` 导入中删除未使用的：

```text
NextResponse
```

保留 `NextRequest`。

### 9. `src/app/api/admin/users/route.ts`

现有代码通过对象解构排除 `orders` 和 `referrerId`，不能直接删除解构字段，否则可能把关系数据或内部字段带入接口响应。

将：

```typescript
const { orders, _count, referrerId, ...rest } = u
```

改为：

```typescript
const { orders: _orders, _count, referrerId: _referrerId, ...rest } = u
```

这样保留字段过滤行为，并使用项目允许的下划线前缀消除未使用变量错误。

不得改变接口返回字段。

### 10. `src/app/api/admin/withdrawals/batch-review/route.ts`

删除未使用的导入：

```text
WITHDRAWAL_STATUS
```

不得修改批量审核逻辑。

### 11. `src/app/api/admin/withdrawals/route.ts`

删除两个未使用的服务导入：

```text
WithdrawalAuditLogService
NotificationService
```

注意：本文件中的 `WITHDRAWAL_STATUS` 正在被使用，必须保留。

不得修改提现审核逻辑、通知逻辑或操作日志。

### 12. `src/app/dashboard/balance/page.tsx`

删除未使用的图标导入：

```text
Loader2
```

不得修改收益转余额入口、资金数据或页面布局。

### 13. `src/app/dashboard/page.tsx`

从 `recharts` 导入中删除未使用的：

```text
Legend
```

不得修改图表逻辑。

### 14. `src/app/forgot-password/page.tsx`

删除未使用的图标导入：

```text
Loader2
```

不得修改找回密码流程。

### 15. `src/components/layout/Header.tsx`

将：

```typescript
const { user, logout, syncFromStorage } = useAuthStore()
```

改为：

```typescript
const { user, logout } = useAuthStore()
```

注意：文件中通过 `useAuthStore.getState().syncFromStorage()` 调用的同步逻辑必须保留，不能删除或改写。

## 五、禁止事项

1. 禁止修改上述 15 个文件以外的任何业务或测试文件。
2. 禁止处理任何 `Warning（警告）`。
3. 禁止修改 ESLint（代码规范工具）配置、关闭规则或添加忽略注释。
4. 禁止使用 `eslint-disable` 绕过错误。
5. 禁止修改 `package.json`、锁文件或工作流文件。
6. 禁止修改数据库结构、迁移文件或生产数据库。
7. 禁止修改 `AGENTS.md`（项目规则文件）。
8. 禁止修改、删除或提交 `docs/agent-tasks/`（本地任务文件夹）。
9. 禁止修改、删除或提交旧文档和 `vercel_ls.txt`。
10. 禁止顺手格式化整文件。
11. 禁止改变任何接口返回结构、权限规则或业务状态机。

如果发现完成任务必须越界修改，立即停止并写结果文件，不得自行扩展范围。

## 六、执行步骤

### 第 1 步：记录初始状态

运行：

```powershell
git status --short
git log origin/main --oneline -1
```

确认业务基线为：

```text
2255440 feat(admin): add earnings void adjustment
```

旧文档、任务文件和 `vercel_ls.txt` 保持原样。

### 第 2 步：记录修复前代码规范结果

运行：

```powershell
.\node_modules\.bin\next.cmd lint
```

预期修复前为 21 个阻断错误。保存关键输出到结果文件，不要创建临时输出文件。

### 第 3 步：按第四节逐文件做最小修改

只允许执行明确列出的删除或别名调整。

### 第 4 步：检查修改范围

运行：

```powershell
git diff --name-only
git diff --stat
git diff --check
```

必须恰好只包含第四节的 15 个文件。

### 第 5 步：运行代码规范检查

运行：

```powershell
.\node_modules\.bin\next.cmd lint
```

验收标准：

- 退出码为 0。
- `Error（阻断错误）`为 0。
- 允许保留 `Warning（警告）`。
- 不得通过修改配置或忽略规则达到通过。

### 第 6 步：运行类型检查

运行：

```powershell
npx tsc --noEmit -p tsconfig.typecheck.json
```

预期：退出码 0，0 错误。

### 第 7 步：运行完整测试

运行：

```powershell
npx vitest run
```

必须记录测试文件数、测试数量和失败数量。

如果完整测试失败，不允许跳过或只跑局部测试冒充通过。

### 第 8 步：运行构建

运行：

```powershell
npx next build
```

预期：构建成功，页面生成完成。

### 第 9 步：提交前精确检查

再次运行：

```powershell
git status --short
git diff --name-only
git diff --check
```

确认只有 15 个目标文件发生业务代码变化。

### 第 10 步：暂停并等待小 M 复审

本任务第一阶段默认：

- 不执行 `git add`。
- 不执行 commit（提交）。
- 不执行 push（推送）。
- 不触发部署。

完成代码和验证后先写结果文件，等待小 M 010 号只读复审。

只有胡子老师在小 M 复审通过后明确授权，才能另行执行精确提交、推送和部署。

## 七、结果文件

完成后写入：

```text
docs/agent-tasks/catpaw/done/猫爪_010号结果.md
```

结果文件必须包含：

1. 最终结论。
2. 修复前阻断错误数量和文件数量。
3. 实际修改的 15 个文件清单。
4. 每个文件的精确修改说明。
5. 是否处理了警告。
6. 是否修改业务逻辑。
7. 修复后代码规范检查退出码、错误数和警告数。
8. typecheck（类型检查）结果。
9. 完整测试的测试文件数、测试数和失败数。
10. build（构建）结果。
11. `git diff --check` 结果。
12. `git status --short` 结果。
13. 是否修改 ESLint 配置或使用忽略注释。
14. 是否修改数据库、工作流、依赖或锁文件。
15. 是否修改或提交任务文件、旧文档、`vercel_ls.txt`。
16. 是否 commit（提交）。
17. 是否 push（推送）。
18. 是否部署。
19. P0/P1/P2 风险。
20. 是否建议进入小 M 010 号只读复审。

## 八、验收口径

满足以下条件才可建议进入小 M 复审：

1. 只修改 15 个目标文件。
2. 21 个阻断错误全部清零。
3. 代码规范检查退出码为 0。
4. 类型检查通过。
5. 完整测试通过。
6. 构建通过。
7. 没有修改业务行为。
8. 没有修改配置、数据库、工作流、依赖和锁文件。
9. 没有提交、推送或部署。
