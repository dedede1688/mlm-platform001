# 猫爪_001号任务

## 1. 任务身份

- 任务编号：001
- 执行代理：猫爪
- 任务类型：本地任务文件读取测试
- 创建人：小酷
- 创建时间：2026-07-12

## 2. 背景

这是智能体任务文件夹机制的第一次本地读取测试。

本任务不涉及业务代码，不需要运行测试，不需要构建，不需要提交，不需要推送。

## 3. 目标

验证猫爪是否能读取当前项目中的任务文件，并按要求生成结果文件。

## 4. 任务验证码

```text
TASK_TEST_001_CATPAW
```

## 5. 允许操作

- 读取本任务文件。
- 读取 `docs/agent-tasks/README.md`。
- 生成结果文件：

```text
docs/agent-tasks/catpaw/done/猫爪_001号结果.md
```

## 6. 禁止操作

- 不修改业务代码。
- 不修改数据库。
- 不运行数据库迁移。
- 不 commit（提交）。
- 不 push（推送）。
- 不部署。
- 不修改 `AGENTS.md（项目规则文件）`。

## 7. 必须输出的结果文件内容

请在结果文件中写入：

```text
# 猫爪_001号结果

## 结论
读取成功 / 读取失败

## 读取文件路径
docs/agent-tasks/catpaw/todo/猫爪_001号任务.md

## 任务验证码
TASK_TEST_001_CATPAW

## 执行代理
猫爪

## 是否修改业务代码
否

## 是否 commit（提交）
否

## 是否 push（推送）
否

## git status --short
粘贴命令结果
```

## 8. 停止条件

如果无法读取本任务文件，请不要猜内容，直接报告“读取失败”和失败原因。

