# 猫爪_029号任务：商品详情待支付测试可信度最终补强

> **执行模式：** 本地测试补强，只允许修改两个测试文件。  
> **任务目标：** 在 024-028 号未提交业务改动基础上，为 `CheckoutDialog`（结算弹窗）的锁定收货信息分支补充真实 JSX 渲染证据，并清除剩余宽松断言和固定字符窗口。  
> **禁止事项：** 不修改业务代码，不 commit（提交），不 push（推送），不部署，不新增依赖，不改测试配置。

---

## 一、固定工作目录

```text
D:\mlm-platform-source\mlm-platform
```

所有命令必须在该目录执行。

---

## 二、开始前必须读取

依次完整读取：

1. `AGENTS.md`
2. `docs/agent-tasks/README.md`
3. `docs/agent-tasks/catpaw/猫爪执行岗位说明.md`
4. `docs/agent-tasks/catpaw/todo/猫爪_028号任务.md`
5. `docs/agent-tasks/catpaw/done/猫爪_028号结果.md`
6. `docs/agent-tasks/xiaom/todo/小M_028号任务.md`
7. `docs/agent-tasks/xiaom/done/小M_028号结果.md`
8. 本任务文件
9. `src/components/checkout/CheckoutDialog.tsx`
10. `__tests__/components/checkout-pending-order-lock.test.ts`
11. `__tests__/components/product-pending-payment-flow.test.ts`
12. `vitest.config.ts`
13. `package.json`

必须以真实代码为准，不能只复述报告。

---

## 三、基线与现有改动保护

预期：

```text
本地 HEAD   = baff1843a394fe1e2b21b9bc3e10ccc9d232b53b
origin/main = 49d3583a68f0f5f5e5794ce16c3a76594028752f
```

本地领先远程的设计文档提交和 024-028 号未提交业务改动均属于预期状态，严禁重置、覆盖、清理或推送。

开始前运行并记录：

```powershell
Get-Location
git status --short
git log --oneline -3
git log origin/main --oneline -1
git rev-parse HEAD
git rev-parse origin/main
```

基线不一致时立即停止并报告。

---

## 四、唯一允许修改的文件

```text
__tests__/components/checkout-pending-order-lock.test.ts
__tests__/components/product-pending-payment-flow.test.ts
```

本任务不允许修改任何 `src/`、`prisma/`、配置、依赖、文档规则或其他测试文件。

特别禁止修改：

- `src/app/products/[id]/page.tsx`
- `src/components/checkout/CheckoutDialog.tsx`
- `src/lib/utils/pending-payment-session.ts`
- `vitest.config.ts`
- `package.json`
- `pnpm-lock.yaml`
- `prisma/schema.prisma`
- `AGENTS.md`

如果真实渲染测试暴露业务实现错误，必须停止并报告，不得扩大修改范围。

---

## 五、已确认的测试可信度缺口

小 M 028 号复审已确认：

1. 当前没有真实 JSX 渲染测试，现有相关测试只是读取源码字符串。
2. `checkout-pending-order-lock.test.ts` 仍有 1 处用于放宽结论的 `A || B` 断言。
3. 两个测试文件仍有 8 处固定字符窗口：7 处 `+ 200`，1 处 `+ 30`。
4. `vitest.config.ts` 确实存在，测试环境是 `node`；不得再报告“没有测试配置”。
5. 项目已经安装 `react` 和 `react-dom`，不需要新增依赖。

本任务只修测试可信度，不再改 028 号已经正确的业务实现。

---

## 六、真实 JSX 渲染测试

### 6.1 必须真实导入生产组件

在 `checkout-pending-order-lock.test.ts` 中使用：

```typescript
import React from 'react'
import { renderToStaticMarkup } from 'react-dom/server'
import { CheckoutDialog } from '../../src/components/checkout/CheckoutDialog'
```

必须渲染真实 `CheckoutDialog`，禁止复制一个简化组件来测试。

测试文件扩展名保持 `.test.ts`，使用 `React.createElement(...)`，不得把文件改成 `.tsx`。

如直接导入需要隔离 Next.js 或浏览器依赖，只允许在该测试文件内使用 `vi.mock(...)`。允许 mock：

- `@/components/ToastProvider`
- `@/components/address/AddressPicker`
- `next/link`

禁止 mock `CheckoutDialog` 本身，禁止 mock `hasCompleteLockedShipping` 业务判断。

### 6.2 统一的最小渲染参数

建立一个测试辅助函数，例如：

```typescript
function renderPendingDialog(
  shipping: CheckoutLockedShipping | null
): string {
  return renderToStaticMarkup(
    React.createElement(CheckoutDialog, {
      open: true,
      product: {
        id: 'product-1',
        name: '测试商品',
        memberPrice: 500,
      },
      onClose: vi.fn(),
      onConfirm: vi.fn().mockResolvedValue(null),
      hasPendingOrder: true,
      pendingOrderShipping: shipping,
    })
  )
}
```

可根据真实 props 做最小调整，但不得绕过真实组件分支。

### 6.3 至少覆盖 5 个运行时场景

每个场景必须独立渲染、独立断言：

1. `pendingOrderShipping=null`
2. `recipientName='   '`，其他字段完整
3. `recipientPhone='   '`，其他字段完整
4. `shippingAddress='   '`，其他字段完整
5. 三个字段都完整，建议值前后带空格以确认 `.trim()` 判断可通过

前 4 个场景必须同时断言：

- HTML 包含 `订单收货信息缺失，请联系客服`
- HTML 不包含 `订单已创建，收货信息以首次提交为准`
- HTML 不包含首次下单地址表单的关键文案 `选择收货地址`
- “重新确认支付”按钮为 disabled（禁用）

第 5 个场景必须同时断言：

- HTML 包含 `订单已创建，收货信息以首次提交为准`
- HTML 包含真实姓名、手机号和地址
- HTML 不包含 `订单收货信息缺失，请联系客服`
- “重新确认支付”按钮不是 disabled（未禁用）

按钮断言必须限定到包含“重新确认支付”的真实 `<button>` 标签，不能只检查全文是否出现 `disabled`。

### 6.4 真实渲染证据要求

结果报告必须明确写出：

- 使用了 `renderToStaticMarkup`
- 使用了真实 `CheckoutDialog`
- 5 个场景各自的关键 HTML 断言
- 是否需要 mock，以及 mock 了哪些外部模块

不得把源码字符串检查称为“真实 JSX 渲染”。

---

## 七、清除宽松 OR 断言

在 `checkout-pending-order-lock.test.ts` 中删除类似：

```typescript
const completeCheck = hasPendingBranch.includes('hasCompleteLockedShipping') ||
  (...三个字段分别出现...)
expect(completeCheck).toBe(true)
```

改成唯一、严格的业务约束：

```typescript
expect(hasPendingBranch).toContain('hasCompleteLockedShipping')
```

禁止使用另一条路径兜底通过。

允许业务正则本身使用 `||` 时必须逐项说明；不得保留用于放宽测试结论的 `A || B`。

---

## 八、清除 8 处固定字符窗口

### 8.1 `+ 200` 的 7 处

禁止继续使用：

```typescript
source.indexOf(')', varIdx + 200)
source.substring(index, index + 200)
```

为 `Boolean(...)` 或其他括号表达式新增一个真正的括号平衡提取函数，例如：

```typescript
function extractParenthesizedExpression(src: string, marker: string): string {
  const markerIndex = src.indexOf(marker)
  expect(markerIndex, `未找到标记：${marker}`).toBeGreaterThanOrEqual(0)

  const openIndex = src.indexOf('(', markerIndex)
  expect(openIndex).toBeGreaterThan(markerIndex)

  let depth = 0
  for (let index = openIndex; index < src.length; index += 1) {
    if (src[index] === '(') depth += 1
    if (src[index] === ')') depth -= 1
    if (depth === 0) return src.slice(markerIndex, index + 1)
  }

  throw new Error(`未找到完整括号表达式：${marker}`)
}
```

所有 7 处必须改用完整表达式或完整代码块提取，找不到目标必须直接失败。

### 8.2 `+ 30` 的 1 处

`product-pending-payment-flow.test.ts` 中检查 `orderId` 来源时，禁止截取赋值后 30 个字符。

建议使用完整赋值右值匹配：

```typescript
const assignments = [...successBlock.matchAll(/orderId\s*=\s*([^;\n]+)/g)]
expect(assignments.length).toBeGreaterThan(0)

for (const assignment of assignments) {
  expect(assignment[1].trim()).toBe('pendingPayment.orderId')
}
```

可采用等价的严格实现，但必须：

- 先断言确实找到了赋值；
- 检查完整右值；
- 不得使用固定字符窗口；
- 不得用 `if (match)` 静默跳过。

---

## 九、残留模式硬检查

完成修改后运行：

```powershell
Select-String -Path `
  '__tests__/components/product-pending-payment-flow.test.ts', `
  '__tests__/components/checkout-pending-order-lock.test.ts' `
  -Pattern 'substring\([^,]+,[^\)]*\+\s*(30|200|300|400|500)','includes\([^\)]*\)\s*\|\|','if\s*\(.*Match','if\s*\(.*Idx\s*===\s*-1'
```

预期：与放宽测试结论有关的匹配为 0。

如果仍有 `||`，必须逐条确认是生产代码文本、正则或正常测试逻辑，不是“任一条件通过即可”的假通过。

---

## 十、高效验证顺序

029 号只改测试，不重复跑全量项目测试和 Next.js build（项目构建）。按以下顺序执行：

### 10.1 两个直接测试

```powershell
npx vitest run `
  __tests__/components/checkout-pending-order-lock.test.ts `
  __tests__/components/product-pending-payment-flow.test.ts
```

### 10.2 七文件相关回归

```powershell
npx vitest run `
  __tests__/services/order.test.ts `
  __tests__/api/orders/order-detail-route.test.ts `
  __tests__/lib/pending-payment-session.test.ts `
  __tests__/components/checkout-pending-order-lock.test.ts `
  __tests__/components/product-pending-payment-flow.test.ts `
  __tests__/components/cart-earnings-transfer-flow.test.ts `
  __tests__/api/orders/verify-payment-route.test.ts
```

### 10.3 类型与差异

```powershell
npx tsc --noEmit -p tsconfig.typecheck.json

git diff --check -- `
  '__tests__/components/checkout-pending-order-lock.test.ts' `
  '__tests__/components/product-pending-payment-flow.test.ts'
```

本任务明确不运行 `next build`，因为没有业务或配置改动；不得把“未运行 build”写成失败。

---

## 十一、最终范围检查

必须分别记录：

```powershell
git diff --name-only
git diff --stat
git status --short
```

029 号新增差异必须只在两个授权测试文件内。024-028 号累计业务文件改动属于已有状态，不得回滚或改写。

---

## 十二、结果文件

完成后写入：

```text
docs/agent-tasks/catpaw/done/猫爪_029号结果.md
```

结果必须包含：

1. 开始时本地 HEAD 和 origin/main
2. 029 实际修改文件清单
3. 是否真实导入 `CheckoutDialog`
4. 是否使用 `renderToStaticMarkup`
5. 外部模块 mock 清单
6. 5 个真实渲染场景逐项结果
7. 无效快照时确认按钮是否禁用
8. 完整快照时确认按钮是否可用
9. 宽松 OR 清理结果
10. 8 处固定字符窗口清理结果
11. 残留模式搜索结果
12. 两个直接测试结果
13. 七文件回归结果
14. TypeScript 类型检查结果
15. `git diff --check` 结果
16. `git status --short`
17. 是否修改业务代码
18. 是否 commit
19. 是否 push
20. 是否部署
21. 未完成项和风险

---

## 十三、停止条件

出现以下任一情况立即停止并报告：

- 基线不一致；
- 真实渲染测试证明业务代码仍有错误；
- 必须修改任何 `src/` 文件；
- 必须修改测试配置或新增依赖；
- 必须增加第三个测试文件；
- 必须修改数据库、后端事务或支付流程；
- 无法在 Node 测试环境真实渲染生产组件；
- 两个直接测试或七文件回归失败且原因不是本任务测试写法。

---

## 十四、完成后必须停止

本任务不允许提交、推送或部署。写完结果文件后停止，等待小 M 029 号只读复审。
