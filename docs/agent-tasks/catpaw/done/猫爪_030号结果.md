# 猫爪_030号结果

## 任务目标

为 Vitest 增加 `@vitejs/plugin-react@6.0.3` 转译插件，使真实 TSX 组件可在测试中导入，并补齐 CheckoutDialog 5 个真实 JSX 渲染场景（使用 `renderToStaticMarkup`）。

## 执行过程

### 1. 基线验证
- checkout-pending-order-lock.test.ts: 37/37 通过
- product-pending-payment-flow.test.ts: 54/54 通过

### 2. 红灯阶段
- 在测试文件添加 `await import('@/components/checkout/CheckoutDialog')` smoke test
- 运行结果：**1 failed**
- 错误信息：`Failed to parse source for import analysis because the content contains invalid JS syntax. If you use tsconfig.json, make sure to not set jsx to preserve.`
- 错误位置：`CheckoutDialog.tsx:276:71`（JSX `<button>` 标签）
- 根因确认：vite 的 import-analysis 插件不转译 JSX（tsconfig.json 设置 `jsx: "preserve"`）

### 3. 安装 @vitejs/plugin-react@6.0.3
- 命令：`npx --yes pnpm@10 add --save-dev --save-exact @vitejs/plugin-react@6.0.3 --ignore-scripts`
- 注意：项目使用 pnpm v10 store（`D:\.pnpm-store\v10`），必须用 `pnpm@10` 而非 `pnpm@11`
- 首次用 pnpm@11 安装报 `ERR_PNPM_UNEXPECTED_STORE` 错误
- 改用 pnpm@10 后成功：`devDependencies: + @vitejs/plugin-react 6.0.3`

### 4. 修改 vitest.config.ts
```typescript
import react from '@vitejs/plugin-react'
// ...
export default defineConfig({
  plugins: [react()],
  // ... 其余不变
})
```
- 只加了 `plugins: [react()]`，不改 `environment: 'node'`

### 5. 绿灯阶段
- 同一个 smoke test 由失败变通过：38/38 passed
- 0 原有测试受影响

### 6. 补齐 5 个真实渲染场景
- 使用 `renderToStaticMarkup` (from `react-dom/server`) 渲染真实 CheckoutDialog 组件
- Mock 3 个外部模块（文件顶层 `vi.mock`）：
  - `@/components/ToastProvider` → `{ toast: { error: vi.fn(), success: vi.fn() } }`
  - `@/components/address/AddressPicker` → 简单 div 替代
  - `next/link` → `<a>` 标签替代
- 5 个场景及断言：

| # | pendingOrderShipping | 预期 | 断言 |
|---|---------------------|------|------|
| 1 | `null` | 红色缺失 | 红色错误 + 无黄色摘要 + 无选择收货地址 + 按钮 disabled |
| 2 | `{ recipientName: "   ", ... }` | 红色缺失 | 红色错误 + 无黄色摘要 + 按钮 disabled |
| 3 | `{ recipientPhone: "   ", ... }` | 红色缺失 | 红色错误 + 无黄色摘要 + 按钮 disabled |
| 4 | `{ shippingAddress: "   ", ... }` | 红色缺失 | 红色错误 + 无黄色摘要 + 按钮 disabled |
| 5 | `{ recipientName: "  张三  ", ... }` | 黄色摘要 | 黄色摘要含姓名/手机/地址 + 无红色缺失 + 按钮 NOT disabled |

- 按钮断言用正则 `/<button[^>]*disabled[^>]*>[\s\S]*?重新确认支付[\s\S]*?<\/button>/` 限定到包含"重新确认支付"的真实 `<button>` 标签

### 7. vi.mock 位置修正
- 初始版本在 `beforeAll` 内调用 `vi.mock`，触发 vitest hoisting 警告
- 修正：移到文件顶层（import 之后），警告消除

## 修改文件清单

| 文件 | 变更 |
|------|------|
| `package.json` | 新增 `@vitejs/plugin-react: "6.0.3"` devDependency |
| `pnpm-lock.yaml` | 锁文件更新 |
| `vitest.config.ts` | 新增 `import react from '@vitejs/plugin-react'` + `plugins: [react()]` |
| `__tests__/components/checkout-pending-order-lock.test.ts` | 新增 `import { renderToStaticMarkup }` + `import React` + 3 个顶层 `vi.mock` + 6 个渲染测试（1 smoke + 5 场景） |

## 验证结果

| 验证项 | 结果 |
|--------|------|
| checkout-pending-order-lock.test.ts 直接测试 | 43/43 passed |
| product-pending-payment-flow.test.ts 直接测试 | 54/54 passed |
| 7 文件回归测试 | 213/213 passed |
| 完整 vitest run | 815/815 passed (40 files) |
| TypeScript 类型检查（030 修改的文件） | 0 错误 |
| 029 清理保持（宽松 OR / 固定窗口） | 0 残留 |
| git diff --check | 无冲突标记（仅 CRLF 警告） |

## 029 清理保持确认

- 宽松 OR（`||`）：0 处残留
- 固定窗口（`+200` / `+30`）：0 处残留
- `extractParenthesizedExpression` 括号平衡提取函数：保留完好
- `matchAll` 完整右值匹配：保留完好

## 注意事项

1. **pnpm 版本**：项目使用 pnpm v10 store，安装新依赖必须用 `pnpm@10`（`pnpm@11` 会报 store 版本不匹配）
2. **vi.mock 位置**：必须在文件顶层，不能放在 `beforeAll` 或 `describe` 内（vitest hoisting 机制）
3. **React SSR 布尔属性**：`disabled` 在 `renderToStaticMarkup` 输出为 `disabled=""`，正则用 `disabled` 而非 `disabled=""` 匹配
4. **typecheck 预存错误**：`notification-history.test.ts`、`recharge-settings-route.test.ts`、`balance.test.ts`、`detail.test.ts`、`payment-password-reset.test.ts` 有预存 TS 类型错误（非 030 引入）