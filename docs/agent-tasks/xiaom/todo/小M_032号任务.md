# 小M_032号任务：商品详情待支付恢复上线后最终只读复审

> **复审模式：** 严格只读。  
> **复审对象：** 猫爪_032号最终发布结果。  
> **目标：** 独立确认 024-031 号累计改动按 13 文件提交，远程、持续集成和生产部署闭环。  
> **禁止事项：** 不修改代码、不安装依赖、不 commit、不 push、不部署、不写数据库。

---

## 一、开始前读取

1. `AGENTS.md`
2. `docs/agent-tasks/catpaw/todo/猫爪_032号任务.md`
3. `docs/agent-tasks/catpaw/done/猫爪_032号结果.md`
4. `docs/agent-tasks/xiaom/done/小M_031号结果.md`
5. 本任务文件

---

## 二、独立核验项目

### 2.1 Git 基线和远程

执行：

```powershell
git status --short
git log --oneline -5
git log origin/main --oneline -3
git rev-parse HEAD
git rev-parse origin/main
```

确认本地 HEAD = origin/main，远程最新提交为 032 号业务提交，远程倒数第二条包含：

```text
baff184 docs: design product pending payment recovery
```

### 2.2 Commit 文件树

用 `git show --name-status --stat <032 commit>` 独立确认 commit 只包含以下 13 个文件：

```text
src/lib/services/order.service.ts
src/app/api/orders/[id]/route.ts
src/lib/utils/pending-payment-session.ts
src/components/checkout/CheckoutDialog.tsx
src/app/products/[id]/page.tsx
__tests__/services/order.test.ts
__tests__/api/orders/order-detail-route.test.ts
__tests__/lib/pending-payment-session.test.ts
__tests__/components/checkout-pending-order-lock.test.ts
__tests__/components/product-pending-payment-flow.test.ts
package.json
pnpm-lock.yaml
vitest.config.ts
```

不得包含 `docs/agent-tasks/`、旧 docs、`vercel_ls.txt` 或临时文件。

### 2.3 验证结果可信度

独立检查猫爪报告中的：

- Lint 退出码与 Error 数；
- Typecheck 命令是否为 `tsc --noEmit -p tsconfig.typecheck.json`；
- 完整测试和覆盖率是否只启动一次并成功；
- Build 是否退出码 0；
- UI 浏览器验证是否真实完成，若被登录拦截是否如实标注；
- 是否把源码级或 JSX 渲染测试错误写成浏览器验收。

本复审不重复运行完整测试和 Build；优先使用 GitHub Actions 的独立结果。只有发现证据冲突时才运行最小必要命令。

### 2.4 GitHub Actions

独立确认 032 commit 对应工作流：

- completed；
- success；
- Lint、Typecheck、tests、coverage 均通过。

### 2.5 Vercel

独立确认生产部署：

- Source = main；
- commit = 032 commit；
- Status = Ready；
- Latest / Current 标记正确。

无法查看时必须写“未独立验证”，不得推断。

### 2.6 工作区隔离

确认：

- 13 个目标文件已提交；
- 旧 docs 和任务文件未提交；
- 无临时脚本、测试输出或诊断文件；
- 未修改 `prisma/schema.prisma`；
- 未执行 migration（迁移）；
- 未写生产数据库。

---

## 三、业务风险抽查

只读抽查以下链路没有在最终提交中被破坏：

1. 创建订单前先验证资金数据可计算，非法数据不得 POST 新订单；
2. 已有待支付订单恢复时不得重复创建订单；
3. 收货信息快照锁定，不允许恢复时静默换地址；
4. 缺失或空白锁定信息显示异常提示并禁用支付；
5. 完整锁定信息使用 trim 后快照提交；
6. shortage 非法时不得转成 0 或写入会话；
7. `paymentVerified` 字段仍正常返回；
8. 真实 JSX 测试仍使用生产 `CheckoutDialog`，目标按钮定位唯一且不跨标签。

---

## 四、结果文件

写入：

```text
docs/agent-tasks/xiaom/done/小M_032号结果.md
```

结果必须包含：

1. 最终结论：通过 / 有条件通过 / 不通过；
2. P0、P1、P2；
3. 032 commit hash；
4. 13 文件范围核验；
5. HEAD 与 origin/main；
6. GitHub Actions；
7. Vercel；
8. 工作区隔离；
9. 数据库与迁移确认；
10. 业务风险抽查；
11. 胡子老师仍需执行的浏览器验收步骤；
12. 是否确认发布闭环完成。

严格只读，不得修复任何问题。写完后停止，等待胡子老师和小酷最终审核。

