# 小M_008号任务：未知异常 500 测试快速只读复审

## 一、任务性质

本任务是快速只读复审。

- 不允许修改业务代码。
- 不允许修改测试代码。
- 不允许 commit（提交）。
- 不允许 push（推送）。
- 不允许部署。
- 不允许连接生产数据库。
- 只允许读取文件、运行只读验证命令，并写结果文件。

## 二、复审对象

猫爪 008 号任务：

```text
docs/agent-tasks/catpaw/todo/猫爪_008号任务.md
docs/agent-tasks/catpaw/done/猫爪_008号结果.md
```

本轮唯一允许修改的文件：

```text
__tests__/api/admin/users/balance.test.ts
```

## 三、检查项

### 1. 修改范围

确认 008 号只在 `balance.test.ts` 增加测试，没有继续修改任何业务文件。

结合：

```powershell
git status --short
git diff -- __tests__/api/admin/users/balance.test.ts
```

注意：该测试文件是未跟踪文件，普通 `git diff` 可能看不到全部内容，需要直接读取文件末尾。

### 2. 测试真实性

确认新增测试 21 真实执行路由：

```text
POST /api/admin/users/[id]/balance
```

确认不是直接构造假响应。

### 3. 未知异常模拟

确认测试让：

```typescript
prisma.$transaction
```

直接抛出：

```typescript
new Error('数据库连接断开')
```

### 4. 必须断言

确认测试明确断言：

```typescript
expect(res.status).toBe(500)
expect(data.success).toBe(false)
expect(data.message).toBe('数据库连接断开')
expect(OrderNotificationService.notifyEarningsVoid).not.toHaveBeenCalled()
expect(logOperation).not.toHaveBeenCalled()
```

### 5. 测试数量

确认：

- `balance.test.ts` 为 21 个测试。
- `order-notification.test.ts` 为 66 个测试。
- 两个文件合计 87 个测试。
- 87 个测试全部通过。

### 6. 最终回归状态

确认：

- typecheck（类型检查）通过。
- build（构建）通过。
- `git diff --check` 无空白错误。
- 未修改数据库结构。
- 未同步生产模板。
- 未 commit、未 push、未部署。

## 四、只读验证命令

运行：

```powershell
npx vitest run __tests__/api/admin/users/balance.test.ts __tests__/services/order-notification.test.ts
npx tsc --noEmit -p tsconfig.typecheck.json
npx next build
git diff --check
git status --short
```

不得修改代码让验证通过。

## 五、结果文件

完成后写入：

```text
docs/agent-tasks/xiaom/done/小M_008号结果.md
```

结果文件必须包含：

1. 最终结论：通过 / 不通过。
2. P0 问题。
3. P1 问题。
4. P2 观察。
5. 修改范围检查。
6. 测试真实性检查。
7. 未知异常 500 断言检查。
8. 不通知、不写日志断言检查。
9. 测试数量和结果。
10. typecheck（类型检查）结果。
11. build（构建）结果。
12. git diff --check 结果。
13. git status --short 结果。
14. 是否修改业务代码。
15. 是否修改数据库结构。
16. 是否同步生产模板。
17. 是否 commit（提交）。
18. 是否 push（推送）。
19. 是否部署。
20. 是否建议进入生产模板单条同步和精确提交阶段。

## 六、结论口径

满足以下条件可判定通过：

- 008 号只补 1 条测试。
- 测试真实执行路由。
- 未知异常返回 500。
- 不发送通知。
- 不写操作日志。
- 87 个测试通过。
- 类型检查和构建通过。
- 未修改业务代码和数据库结构。

通过后建议进入：

```text
earnings_voided 单模板生产同步
→ 精确暂存 6 个业务/测试文件
→ 提交推送部署
```
