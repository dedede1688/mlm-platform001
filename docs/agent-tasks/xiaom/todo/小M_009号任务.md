# 小M_009号任务：收益作废上线结果最终只读复审

## 一、任务性质

本任务是上线后的最终只读复审。

- 不允许修改业务代码。
- 不允许修改测试代码。
- 不允许修改数据库。
- 不允许执行数据库写入或迁移。
- 不允许 commit（提交）。
- 不允许 push（推送）。
- 不允许重新部署。
- 只允许读取本地文件、读取 Git 信息、运行只读验证命令，并写结果文件。

## 二、复审对象

猫爪 009 号任务及结果：

```text
docs/agent-tasks/catpaw/todo/猫爪_009号任务.md
docs/agent-tasks/catpaw/done/猫爪_009号结果.md
```

目标提交：

```text
2255440 feat(admin): add earnings void adjustment
```

目标业务和测试文件必须恰好为以下 6 个：

```text
src/app/api/admin/users/[id]/balance/route.ts
src/lib/services/order-notification.service.ts
prisma/seed.ts
src/app/admin/users/page.tsx
__tests__/api/admin/users/balance.test.ts
__tests__/services/order-notification.test.ts
```

## 三、必须检查的项目

### 1. 提交范围

确认提交 `2255440` 只包含上述 6 个文件，没有包含：

- `docs/agent-tasks/`（本地任务文件夹）
- 旧文档
- `vercel_ls.txt`
- 临时脚本
- `AGENTS.md`（项目规则文件）
- `prisma/schema.prisma`（数据库结构文件）
- 数据库迁移文件

运行：

```powershell
git show --name-status --format=fuller 2255440
git show --stat --oneline 2255440
```

### 2. 远程提交闭环

确认：

```text
本地 HEAD = origin/main = 2255440
```

运行：

```powershell
git rev-parse --short HEAD
git rev-parse --short origin/main
git log origin/main --oneline -1
```

### 3. 部署平台状态

只读确认部署平台最新生产部署：

- 提交编号为 `2255440`
- 状态为 Ready（已就绪）或 success（成功）
- 环境为 Production（生产环境）

不得触发重新部署。

### 4. 生产通知模板

只读确认生产数据库中存在：

```text
id: seed-tpl-earnings_voided-in_app
type: earnings_voided
channel: in_app
subject: 收益作废通知
enabled: true
```

同时确认模板内容与 `prisma/seed.ts` 一致。

本项只允许查询，不允许新增、更新或删除。

如果无法安全连接生产数据库，必须如实写“未复查”，不得把猫爪报告当作自己的查询结果。

### 5. 资金逻辑最终核对

确认 `earnings_void`（收益作废）满足：

- 只允许正有限数字。
- 使用 `updateMany`（条件更新）和 `earningsAvailable >= amount` 防并发。
- `earningsAvailable` 减少。
- `earningsVoided` 增加。
- `balance`、`frozenBalance`、`consumeBalance`、`earningsFrozen`、`earningsPending` 不变。
- 可用收益不足返回 400。
- 未知异常返回 500。
- 写入 `BalanceRecord`（资金流水）。
- 写入 `OperationLog`（操作日志）。
- 通知失败不影响资金接口成功结果。
- 通知关联 `sourceType = balance_record` 和真实 `sourceId = balanceRecord.id`。

### 6. 自动化验证

运行：

```powershell
npx vitest run __tests__/api/admin/users/balance.test.ts __tests__/services/order-notification.test.ts
npx tsc --noEmit -p tsconfig.typecheck.json
npx next build
git diff --check
```

预期：

- 2 个测试文件全部通过。
- 87 个测试全部通过。
- typecheck（类型检查）0 错误。
- build（构建）成功。
- `git diff --check` 没有空白错误；Windows 换行提示可单独说明。

### 7. 持续集成失败项必须如实记录

提交 `2255440` 的代码托管平台检查并非全部成功：

- Vercel（部署平台）检查：成功。
- GitHub Actions（自动检查）中的 `test`（测试任务）：失败。
- 失败步骤：`Lint`（代码规范检查）。
- 后续类型检查和测试步骤因此被跳过。

小酷已经在本地复现，失败原因是项目既有的全局未使用导入/变量错误，分布在多个旧文件中，例如：

```text
src/app/admin/dashboard/page.tsx
src/app/admin/finance/page.tsx
src/app/admin/layout.tsx
src/app/admin/reports/page.tsx
src/app/api/admin/dashboard/summary/route.ts
src/app/api/admin/users/route.ts
src/app/dashboard/balance/page.tsx
src/components/layout/Header.tsx
```

本次 6 个文件中：

- `src/app/admin/users/page.tsx` 只有既有警告，没有阻断错误。
- `src/lib/services/order-notification.service.ts` 只有既有警告，没有阻断错误。
- 其余本次文件没有出现在本地阻断错误列表中。

复审报告必须将其列为“项目既有持续集成缺口”，不得写成“所有远程检查全部通过”，也不得误判为本次收益作废功能失败。

### 8. 工作区隔离

运行：

```powershell
git status --short
```

确认本次 6 个业务/测试文件已经干净。旧文档、任务文件和 `vercel_ls.txt` 即使仍存在，也不得修改、删除、暂存或提交。

## 四、禁止事项

- 禁止修改任何业务或测试文件。
- 禁止修复全局代码规范检查问题。
- 禁止重新同步通知模板。
- 禁止运行全量 `prisma db seed`（数据库种子初始化）。
- 禁止执行 SQL 写入。
- 禁止 commit（提交）。
- 禁止 push（推送）。
- 禁止重新部署。
- 禁止清理旧文档和本地任务文件。

## 五、停止条件

出现以下任一情况，立即停止并在结果文件中报告：

1. `2255440` 不只包含 6 个目标文件。
2. `origin/main` 不是 `2255440`。
3. 部署平台不是目标提交或不是已就绪状态。
4. 生产模板字段不一致。
5. 资金逻辑与上述规则不一致。
6. 87 个专项测试失败。
7. 类型检查或构建失败。
8. 发现目标业务文件仍有未提交改动。

## 六、结果文件

完成后写入：

```text
docs/agent-tasks/xiaom/done/小M_009号结果.md
```

结果文件必须包含：

1. 最终结论：通过 / 有条件通过 / 不通过。
2. P0 问题。
3. P1 问题。
4. P2 观察。
5. 提交的 6 个文件清单。
6. 是否混入任务文件、旧文档、临时文件或数据库结构文件。
7. 本地 HEAD、origin/main 和部署平台提交编号。
8. 部署平台状态。
9. 生产模板只读查询结果；若未查询，必须明确写“未复查”。
10. 资金逻辑最终核对结果。
11. 87 个测试结果。
12. typecheck（类型检查）结果。
13. build（构建）结果。
14. `git diff --check` 结果。
15. `git status --short` 结果。
16. 持续集成失败项名称、失败步骤和影响说明。
17. 是否修改代码。
18. 是否修改数据库。
19. 是否 commit（提交）。
20. 是否 push（推送）。
21. 是否部署。
22. 是否建议进入浏览器真实验收。
23. 单独提出“全局代码规范检查修复包”的建议范围和优先级，但本任务不得执行。

## 七、结论口径

满足以下条件可判定“有条件通过”：

- 目标提交只含 6 个文件。
- 远程和部署平台均为 `2255440`。
- 部署平台已就绪。
- 生产模板存在且正确，或猫爪报告明确成功但小 M 无法独立复查并如实标注。
- 资金逻辑正确。
- 87 个专项测试、类型检查和构建通过。
- 唯一未闭合项是项目既有的全局代码规范检查失败。

不得判定“全部检查通过”，因为代码托管平台仍有一项失败检查。
