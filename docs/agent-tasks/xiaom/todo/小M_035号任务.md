# 小M_035号任务：订单售后服务卡片上线后最终只读复审

> **模式：** 严格只读  
> **复审对象：** 猫爪_035号发布结果  
> **禁止：** 修改代码、测试或文档，commit、push、部署、数据库写入

---

## 一、独立核验

1. 本地 HEAD、`origin/main` 和猫爪报告 commit 完全一致；
2. 本次业务 commit 恰好包含 5 个目标文件；
3. 前面的设计和实施计划提交历史保持完整；
4. 独立检查 GitHub Actions 全部关键步骤为 success；
5. 独立检查 Vercel Production 最新部署 commit 和 Ready 状态；
6. 抽查业务规则：
   - pending 和 approved 退款阻止重复申请；
   - rejected 可以重新申请；
   - 订单详情只有一个售后服务卡片；
   - 大号橙色退款按钮已删除；
   - 最新退款记录按创建时间倒序；
   - `.test.ts` 与当前测试配置一致；
7. 确认未修改数据库、退款资金、审核、完成或通知链路；
8. 确认任务文件、旧文档和临时文件未进入提交；
9. 如未完成登录态浏览器验收，结论必须标记为“有条件通过”，等待胡子老师验收。

---

## 二、结果文件

写入：

```text
docs/agent-tasks/xiaom/done/小M_035号结果.md
```

必须包含最终结论、P0/P1/P2、提交文件树、远程闭环、持续集成、Vercel、业务抽查、浏览器验收状态和后续执行建议。写完立即停止。
