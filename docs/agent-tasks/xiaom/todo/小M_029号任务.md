# 小M_029号任务：商品详情待支付测试可信度最终只读复审

> **复审模式：** 严格只读。  
> **复审对象：** 猫爪_029号任务及执行结果。  
> **核心目标：** 独立确认真实 JSX 渲染测试、宽松断言清理和固定字符窗口清理是否真实有效。  
> **禁止事项：** 不修改业务或测试代码，不 commit，不 push，不部署，不同步数据库。

---

## 一、固定工作目录

```text
D:\mlm-platform-source\mlm-platform
```

---

## 二、开始前必须读取

1. `AGENTS.md`
2. `docs/agent-tasks/README.md`
3. `docs/agent-tasks/xiaom/小M审核岗位说明.md`（如存在）
4. `docs/agent-tasks/catpaw/todo/猫爪_028号任务.md`
5. `docs/agent-tasks/catpaw/done/猫爪_028号结果.md`
6. `docs/agent-tasks/xiaom/todo/小M_028号任务.md`
7. `docs/agent-tasks/xiaom/done/小M_028号结果.md`
8. `docs/agent-tasks/catpaw/todo/猫爪_029号任务.md`
9. `docs/agent-tasks/catpaw/done/猫爪_029号结果.md`
10. 本任务文件
11. `src/components/checkout/CheckoutDialog.tsx`
12. `__tests__/components/checkout-pending-order-lock.test.ts`
13. `__tests__/components/product-pending-payment-flow.test.ts`
14. `vitest.config.ts`

必须读真实 diff 和测试代码，不能依据猫爪报告直接通过。

---

## 三、基线核验

预期：

```text
本地 HEAD   = baff1843a394fe1e2b21b9bc3e10ccc9d232b53b
origin/main = 49d3583a68f0f5f5e5794ce16c3a76594028752f
```

运行：

```powershell
Get-Location
git status --short
git log --oneline -3
git log origin/main --oneline -1
git rev-parse HEAD
git rev-parse origin/main
```

基线不一致则停止报告。

---

## 四、修改范围检查

029 号净新增修改只允许：

```text
__tests__/components/checkout-pending-order-lock.test.ts
__tests__/components/product-pending-payment-flow.test.ts
```

重点确认：

- 029 未修改任何 `src/` 文件；
- 未修改 `vitest.config.ts`、`package.json`、锁文件；
- 未新增依赖；
- 未覆盖或回滚 024-028 号业务改动；
- 未清理历史 docs、任务文件或 `vercel_ls.txt`。

若 029 修改范围越界，结论不得写“通过”。

---

## 五、真实 JSX 渲染测试核验

必须逐项检查：

1. 测试真实导入 `CheckoutDialog`，不是复制的简化组件。
2. 使用 `renderToStaticMarkup` 和 `React.createElement`。
3. 测试文件仍为 `.test.ts`。
4. 没有修改生产测试配置。
5. 若有 `vi.mock`，只 mock 外部依赖，没有 mock `CheckoutDialog` 或业务判断。
6. 每个场景都执行独立的真实渲染，不是只读源码字符串。

必须独立核验 5 个场景：

| 场景 | 应显示 | 不应显示 | 按钮 |
|---|---|---|---|
| `shipping=null` | 红色缺失提示 | 黄色已创建摘要、首次下单地址表单 | 禁用 |
| 姓名纯空格 | 红色缺失提示 | 黄色已创建摘要、首次下单地址表单 | 禁用 |
| 手机纯空格 | 红色缺失提示 | 黄色已创建摘要、首次下单地址表单 | 禁用 |
| 地址纯空格 | 红色缺失提示 | 黄色已创建摘要、首次下单地址表单 | 禁用 |
| 三字段完整 | 黄色已创建摘要和真实字段 | 红色缺失提示 | 可用 |

重点防止以下假测试：

- 只对源码做 `toContain`，却声称是渲染测试；
- 只检查全文存在 `disabled`，没有锁定“重新确认支付”按钮；
- 用 `if (match)` 找不到就跳过；
- 用 `A || B` 任一路径通过；
- mock 了被测生产组件。

任何一个场景缺少正向和反向断言，至少列为 P1 测试缺口。

### 5.1 猫爪“无法渲染”停止报告专项核验

猫爪结果报告称：

```text
Failed to parse source for import analysis because the content contains invalid JS syntax.
If you use tsconfig.json, make sure to not set jsx to preserve.
```

并推断必须安装 `jsdom` 或 `@vitejs/plugin-react`。该推断尚未被独立证明，小 M 必须做根因审计：

1. 确认当前结果文件中确实没有保留 5 个真实渲染测试，不能把已删除的尝试算作完成。
2. 完整读取 `tsconfig.json`、`vitest.config.ts`、`package.json` 和锁文件中的 Vite/Vitest/esbuild 现有依赖。
3. 核对报错发生在：TSX 转译、Vite import-analysis、模块别名、外部模块副作用还是 DOM 环境，不能只依据报错最后一句推断。
4. 明确指出 `renderToStaticMarkup` 本身不需要浏览器 DOM；不得把“缺少 jsdom”未经验证地写成根因。
5. 检查是否存在**不新增依赖**的最小可行路径，例如 Vitest/Vite 现有 esbuild JSX 配置能力；这里只做只读论证，不得修改配置。
6. 若需要最小配置改动，给出准确的候选配置、影响范围、风险和验证命令，但不得执行。
7. 若确实必须新增依赖，必须说明为何现有 Vite/esbuild 能力无法解决，并给出证据。

允许执行只读命令和一次性内联命令收集信息；禁止在 `src/`、`__tests__/` 或配置目录创建临时文件。若必须临时复现，只能放到系统临时目录，结束前必须删除，并在报告中列明。

专项结论必须从以下三种中选择：

- `A：无需新增依赖，只需最小配置调整即可真实渲染`
- `B：必须新增测试依赖才能真实渲染`
- `C：证据不足，需单独诊断任务`

不得直接接受猫爪报告中的根因描述。

---

## 六、宽松 OR 清理核验

确认原有类似：

```typescript
hasPendingBranch.includes('hasCompleteLockedShipping') || (...)
```

已改为唯一严格断言：

```typescript
expect(hasPendingBranch).toContain('hasCompleteLockedShipping')
```

搜索两个测试文件中的 `||`，逐条判断：

- 业务文本或正则正常使用可以保留；
- 用于放宽测试结论的 `A || B` 必须为 0。

---

## 七、固定字符窗口清理核验

独立搜索并确认：

1. `checkout-pending-order-lock.test.ts` 中 7 处 `+ 200` 已全部清除；
2. `product-pending-payment-flow.test.ts` 中 1 处 `+ 30` 已清除；
3. 替代方案使用括号平衡、代码块提取或完整赋值右值匹配；
4. 找不到目标时直接失败；
5. 不得把窗口数字改成另一个固定数字伪装清理。

运行：

```powershell
Select-String -Path `
  '__tests__/components/product-pending-payment-flow.test.ts', `
  '__tests__/components/checkout-pending-order-lock.test.ts' `
  -Pattern 'substring\([^,]+,[^\)]*\+\s*(30|200|300|400|500)','includes\([^\)]*\)\s*\|\|','if\s*\(.*Match','if\s*\(.*Idx\s*===\s*-1'
```

逐条报告真实结果，不能只写“已清理”。

---

## 八、独立验证命令

### 8.1 两个直接测试

```powershell
npx vitest run `
  __tests__/components/checkout-pending-order-lock.test.ts `
  __tests__/components/product-pending-payment-flow.test.ts
```

### 8.2 七文件相关回归

```powershell
npx vitest run `
  __tests__/services/order.test.ts `
  __tests__/api/orders/order-detail-route.test.ts `
  __tests__/lib/pending-payment-session.test.ts `
  __tests__/components/checkout-pending-order-lock.test.ts `
  __tests__/components/product-pending-payment-flow.test.ts `
  __tests__/components/cart-earnings-transfer-flow.test.ts `
  __tests__/api/orders/verify-payment-route.test.ts
```

### 8.3 类型与差异

```powershell
npx tsc --noEmit -p tsconfig.typecheck.json

git diff --check -- `
  '__tests__/components/checkout-pending-order-lock.test.ts' `
  '__tests__/components/product-pending-payment-flow.test.ts'
```

029 只改测试，未运行 build 属于任务设计，不得因此判失败；但不得把 build 写成“已通过”。

---

## 九、结论规则

### 可以写“通过”

必须同时满足：

- 真实生产组件渲染测试存在且 5 场景完整；
- 宽松 OR 为 0；
- 8 处固定字符窗口为 0；
- 两个直接测试通过；
- 七文件回归通过；
- typecheck 通过；
- diff --check 通过；
- 029 只改两个测试文件；
- 未 commit、未 push、未部署。

### 必须写“不通过”或“有条件通过”

包括但不限于：

- 所谓渲染测试仍是源码字符串检查；
- mock 了 `CheckoutDialog`；
- 按钮 disabled 断言未锁定真实按钮；
- 任一场景缺少反向断言；
- 仍有用于放宽结论的 OR；
- 仍有固定字符窗口；
- 范围越界；
- 猫爪报告与真实代码不一致。

---

## 十、结果文件

完成后写入：

```text
docs/agent-tasks/xiaom/done/小M_029号结果.md
```

必须包含：

1. 最终结论：通过 / 有条件通过 / 不通过
2. P0 问题
3. P1 问题
4. P2 观察
5. 029 修改范围
6. 真实导入与真实渲染证据
7. 5 场景逐项核验表
8. 确认按钮 disabled 精确断言证据
9. 宽松 OR 搜索结果
10. 固定字符窗口搜索结果
11. 静默跳过搜索结果
12. 两个直接测试结果
13. 七文件回归结果
14. typecheck 结果
15. diff --check 结果
16. git status --short
17. 是否修改业务代码
18. 是否 commit
19. 是否 push
20. 是否部署
21. 是否建议进入最终发布验证阶段
22. 执行方案建议（按 P0/P1/P2、负责人和预计工时列出）
23. “无法渲染”原始错误的独立根因审计
24. A/B/C 环境方案结论及证据
25. 若选 A，给出不新增依赖的最小配置候选；若选 B，给出必须新增依赖的证明

---

## 十一、只读边界

小 M 不得修改任何业务或测试代码，不得删除文件，不得创建临时脚本到业务或测试目录，不得 commit、push、部署或改数据库。

写完结果后停止，等待小酷审核并安排最终发布任务。
