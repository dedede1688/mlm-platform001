# 小M_008号任务：未知异常 500 测试快速只读复审报告

**复审对象**：猫爪 008 号任务（补"未知异常返回 500"测试）
**复审时间**：2026-07-13 04:20
**复审范围**：仅 `__tests__/api/admin/users/balance.test.ts` 新增的测试 21
**本次结论**：✅ **通过**——可进入生产模板同步 + 精确提交阶段

---

## 1. 最终结论

**✅ 通过。**

猫爪 008 号只新增 1 个测试（line 488-504），未修改任何业务代码，**所有要求 5 项断言全部真实存在**，87 个测试全部通过，typecheck + build EXIT 0。

**所有验证通过**：
- 修改范围：仅 `balance.test.ts`（?? 状态，git diff --name-only 看不到，必须读文件末尾）✓
- 测试真实性：测试 21 真实调用 `POST /api/admin/users/[id]/balance` 路由 ✓
- 未知异常模拟：`prisma.$transaction.mockRejectedValueOnce(new Error('数据库连接断开'))` ✓
- 5 项断言全部存在（status=500 / success=false / message=数据库连接断开 / notifyEarningsVoid 未调用 / logOperation 未调用）✓
- 测试数量：balance.test.ts = 21, order-notification.test.ts = 66, 合计 **87 全过** ✓
- typecheck EXIT 0 / build EXIT 0 / git diff --check 无冲突 ✓
- 未修改业务代码 / 数据库结构 / 生产模板 / commit / push / 部署 ✓

---

## 2. P0 问题

**无 P0。**

---

## 3. P1 问题

**无 P1。**

007 号报告中的 P1-1（未知异常 500 无测试）已通过 008 号补测试 21 闭合。

---

## 4. P2 观察

**无 P2。**

008 号任务非常窄（只补 1 个测试），没有引入新风险点。

---

## 5. 修改范围检查

**实际改动**：
- `__tests__/api/admin/users/balance.test.ts`：??（未跟踪，未变状态，但内部新增测试 21，从 487 行扩到 505 行，**新增 18 行**）
- 其他文件：全部保持 007 报告时的状态（5 M + 1 ??），**008 号没新增任何 M**

| 文件 | 状态 | 008 是否动过 |
|---|---|---|
| `src/app/api/admin/users/[id]/balance/route.ts` | M（007 末态）| ❌ 未动 |
| `src/lib/services/order-notification.service.ts` | M（007 末态）| ❌ 未动 |
| `prisma/seed.ts` | M（005 保留）| ❌ 未动 |
| `src/app/admin/users/page.tsx` | M（005 保留）| ❌ 未动 |
| `__tests__/api/admin/users/balance.test.ts` | ??（内部新增测试 21）| ✅ **只动这个** |
| `__tests__/services/order-notification.test.ts` | M（006/007 改动）| ❌ 未动 |
| 5 旧 docs + vercel_ls.txt + agent-tasks/ | 历史遗留 | ❌ 与 008 无关 |

**008 号只动了 1 个测试文件**，符合任务文件第 24-28 行"本轮唯一允许修改的文件"要求。

**注**：`balance.test.ts` 是 ?? 状态（006 新建），`git diff --name-only` 看不到，需要直接读文件末尾。任务文件第 43 行已明确提示。

---

## 6. 测试真实性检查

`balance.test.ts` line 488-504 完整内容：

```typescript
// ===== 场景 21: v008 未知异常返回 500，不发送通知，不写操作日志 =====
it('21. 未知异常返回 500，不发送通知', async () => {
  // 模拟 prisma.$transaction 直接抛出未知异常
  prisma.$transaction.mockRejectedValueOnce(new Error('数据库连接断开'))

  const { POST } = await import('@/app/api/admin/users/[id]/balance/route')
  const res = await POST(makeRequest({
    type: 'earnings_void', amount: 40, reason: '测试作废收益原因',
  }) as any, { params: Promise.resolve({ id: 'u1' }) })
  const data = await res.json()

  expect(res.status).toBe(500)
  expect(data.success).toBe(false)
  expect(data.message).toBe('数据库连接断开')
  expect(OrderNotificationService.notifyEarningsVoid).not.toHaveBeenCalled()
  expect(logOperation).not.toHaveBeenCalled()
})
```

**测试真实性**：
- ✅ **真实调用 `POST /api/admin/users/[id]/balance` 路由**（line 494-496，不是直接构造假响应）
- ✅ **`prisma.$transaction.mockRejectedValueOnce(new Error('数据库连接断开'))`**（line 491，完全匹配任务文件第 60-66 行要求）
- ✅ **amount = 40**（大于 0 的合法值，确保通过 line 73 校验进入事务）
- ✅ **reason 长度 5+**（'测试作废收益原因' = 7 字符）
- ✅ **type = 'earnings_void'**（走 earnings_void 路径）
- ✅ 通过 `vi.mock('@/lib/prisma', ...)` mock 的 prisma 让 `$transaction` 抛错，**真实走完路由的 try/catch 链**

**不是"假测试"**——`res` 是真实 `NextResponse` 对象，`data` 是真实 JSON，`status` 是真实 HTTP 状态码。

---

## 7. 未知异常 500 断言检查

5 项必须断言**全部存在**：

| 断言 | 任务要求 | 文件位置 | 真实？ |
|---|---|---|---|
| `expect(res.status).toBe(500)` | ✓ | line 499 | ✅ |
| `expect(data.success).toBe(false)` | ✓ | line 500 | ✅ |
| `expect(data.message).toBe('数据库连接断开')` | ✓ | line 501 | ✅ |
| `expect(OrderNotificationService.notifyEarningsVoid).not.toHaveBeenCalled()` | ✓ | line 502 | ✅ |
| `expect(logOperation).not.toHaveBeenCalled()` | ✓ | line 503 | ✅ |

**5/5 全部匹配** ✓

**逻辑验证**：
- `prisma.$transaction` 抛 `Error('数据库连接断开')` → 事务从未开始 → `result` 未生成
- 路由 line 211 `if (!result.updated) { return 500 }` **不会**触发（因为 throw 跳出整个 try 块）
- 路由 line 218 `await logOperation(...)` **不会**执行（throw 后代码不执行）
- 路由 line 233 `if (adjustType === 'earnings_void')` **不会**执行（throw 后代码不执行）
- catch 块 line 288-296：`message = '数据库连接断开'`，`isBusinessError = false`（不等于 '可用收益不足'）→ 返回 500 ✓

**测试断言与代码逻辑完全吻合** ✓

---

## 8. 不通知、不写日志断言检查

**通知不调用**（line 502）：
```typescript
expect(OrderNotificationService.notifyEarningsVoid).not.toHaveBeenCalled()
```

**操作日志不调用**（line 503）：
```typescript
expect(logOperation).not.toHaveBeenCalled()
```

**为什么必然成立**：
- `prisma.$transaction` 抛错 → 整个 try 块中断
- try 块内 line 218 `await logOperation(...)` **还没执行**就 throw
- try 块内 line 233 `if (adjustType === 'earnings_void') { await notifyEarningsVoid(...) }` **还没执行**就 throw
- 直接跳到外层 catch line 288 → 返回 500

**两个 not.toHaveBeenCalled() 断言 100% 成立** ✓

---

## 9. 测试数量和结果

```
npx vitest run __tests__/api/admin/users/balance.test.ts __tests__/services/order-notification.test.ts
```

**结果**：
- Test Files: 2 passed (2)
- Tests: **87 passed (87)** ← 比 007 的 86 多了 1 个（测试 21）
- Duration: 12.85s

**测试数量明细**：

| 文件 | 任务要求 | 实际 | 状态 |
|---|---|---|---|
| `__tests__/api/admin/users/balance.test.ts` | 21 个 | **21 个** | ✅ |
| `__tests__/services/order-notification.test.ts` | 66 个 | **66 个** | ✅ |
| **合计** | **87 个** | **87 个** | ✅ |
| **通过率** | 100% | 100% | ✅ |

---

## 10. typecheck（类型检查）结果

```
npx tsc --noEmit -p tsconfig.typecheck.json
```

**结果：EXIT 0，0 错误**（无输出 = 全部通过）

---

## 11. build（构建）结果

```
npx next build
```

**结果：EXIT 0**
- ✓ Compiled successfully in 69s
- ✓ Generating static pages (110/110)
- 无 error、无 warning

---

## 12. git diff --check 结果

```
git diff --check
```

**结果**：仅有 LF→CRLF 换行符警告（Windows 正常现象，不影响功能）。**无冲突标记、无空白错误**。

---

## 13. git status --short 结果

```
 M __tests__/services/order-notification.test.ts
 M docs/admin-api-audit.md
 M "docs/归档/报告/xiaojing-onboarding.md"
 M "docs/归档/计划/plan-v47-system-optimization.md"
 M "docs/归档/计划/plan-v48-comprehensive.md"
 M "docs/归档/计划/plan-v49-engineering.md"
 M prisma/seed.ts
 M src/app/admin/users/page.tsx
 M src/app/api/admin/users/[id]/balance/route.ts
 M src/lib/services/order-notification.service.ts
?? __tests__/api/admin/users/balance.test.ts  ← 006 新建 + 007/008 修改
?? docs/agent-tasks/...
?? vercel_ls.txt
```

**008 号未新增任何 M 文件**——工作区状态跟 007 报告时完全一致。

---

## 14. 是否修改业务代码

**否。** 008 号只修改了 `__tests__/api/admin/users/balance.test.ts`，未修改 `route.ts`、通知服务、前端页面、`prisma/seed.ts` 或任何其他业务文件。

---

## 15. 是否修改数据库结构

**否。** 未修改 `prisma/schema.prisma`，未新增迁移，未执行生产数据库 SQL。

---

## 16. 是否同步生产模板

**否。** 未连接生产数据库，未运行 `npx prisma db seed`。`earnings_voided` 模板**仍在 `prisma/seed.ts`** 待单条 upsert 同步。

---

## 17. 是否 commit（提交）

**否。** 业务代码改动全部在 working tree，未执行 `git add` / `git commit`。

---

## 18. 是否 push（推送）

**否。** 未执行 `git push`。

---

## 19. 是否部署

**否。** 未部署到 Vercel 或任何环境。

---

## 20. 是否建议进入生产模板单条同步和精确提交阶段

**✅ 是。** 所有验证命令通过，**强烈建议按以下顺序执行**：

### 🔴 步骤 1（胡子哥，10 min）— 部署前置，必须先做

在 Supabase SQL Editor 跑通知模板 upsert SQL（不依赖 `npx prisma db seed` 全量）：

```sql
INSERT INTO "notification_template" (id, type, channel, subject, content, variables, enabled, created_at, updated_at)
VALUES (
  'seed-tpl-earnings_voided-in_app',
  'earnings_voided',
  'in_app',
  '收益作废通知',
  '【敏维科技】{{userName}} 您好，您的可用收益 ¥{{amount}} 已被后台作废。剩余可用收益：¥{{earningsAvailable}}，累计作废收益：¥{{earningsVoided}}。如有疑问请联系客服。',
  '["userName","amount","earningsAvailable","earningsVoided"]',
  true,
  NOW(),
  NOW()
)
ON CONFLICT (type, channel) DO UPDATE SET
  subject = EXCLUDED.subject,
  content = EXCLUDED.content,
  updated_at = NOW();

-- 验证
SELECT * FROM "notification_template" WHERE type = 'earnings_voided';
-- 应返回 1 条
```

**禁止运行 `npx prisma db seed` 全量**。

### 🟢 步骤 2（猫爪，10 min）— 精确提交 6 个文件

```bash
# 只 add 业务/测试 6 个文件，不要 add 旧 docs / vercel_ls.txt / agent-tasks
git add src/app/api/admin/users/[id]/balance/route.ts
git add src/lib/services/order-notification.service.ts
git add prisma/seed.ts
git add src/app/admin/users/page.tsx
git add __tests__/api/admin/users/balance.test.ts
git add __tests__/services/order-notification.test.ts

# 确认 staged 文件清单（必须只显示 6 个 A 状态）
git status --short

git commit -m "feat(balance): add earnings void with traceable notification

- 后台资金调整新增「作废收益」选项，原子条件更新 earningsAvailable -= / earningsVoided +=
- notifyEarningsVoid 整体异常保护 + 路由层 .catch() 双层防御
- Number.isFinite 拦截所有非有限数字
- 并发安全：读取更新后真实值 + 反推真实前值
- 业务校验：可用收益不足返回 400，其他异常返回 500
- 通知追踪：关联 BalanceRecord.id (sourceType=balance_record)
- 自动化测试：21 个资金接口测试 + 7 个通知服务测试（87 个全过）"
git push origin main
```

### 🚀 步骤 3（Vercel 自动部署）

Vercel 自动从 main 分支部署。**部署前必须先完成步骤 1**（胡子哥跑 SQL），否则用户收不到通知。

### ✅ 步骤 4（胡子哥，10 min）— 部署后必做验证

按 v68.6 铁律走 6 步：

1. 强刷浏览器 → 后台 /admin/users/[id] → 资金账户 → 资金调整 → 选「作废收益」→ 输入 ¥40 → 提交
2. 验证：可用收益减少、累计作废增加、资金流水 1 条、操作日志 1 条、用户端「消息中心」1 条通知
3. 检查 Supabase `notification_template` 表已有 `earnings_voided` 记录
4. 用测试账号登录 → 确认铃铛 +1 → 点开通知 → 验证能跳转到「资金流水详情」页（v007 新增的 sourceType='balance_record' + sourceId=BalanceRecord.id 关联）
5. 故意提交 `amount: 1e308` → 验证 400 拒绝
6. 故意提交 `amount > 用户可用收益` → 验证 **400** 返回 + 提示"可用收益不足"
7. **新增**：故意断开 Supabase 连接（或在 Vercel 环境变量改错 DB URL 模拟）→ 验证 **500** 返回 + "数据库连接断开" message + 没有通知（**对应 008 号新增的测试 21 真实场景**）

---

## 总结

**本次 008 号快速只读复审通过**。

- 008 号**只补 1 个测试**（line 488-504），未修改任何业务代码
- 测试 21 真实调用路由，mock `prisma.$transaction` 抛 `Error('数据库连接断开')`
- 5 项必须断言全部真实存在（status=500 / success=false / message=数据库连接断开 / notifyEarningsVoid 未调用 / logOperation 未调用）
- 87/87 测试通过（balance 21 + order-notification 66）
- typecheck EXIT 0 / build EXIT 0 / git diff --check 无冲突

**007 号 P1-1（未知异常 500 无测试）已通过 008 号补测试 21 完整闭合**。所有 005/006/007/008 报告中的 P0/P1/P2 已全部处理或确认不阻塞。

**建议下一步**：
1. **胡子哥**（10 min）：在 Supabase 跑通知模板 upsert SQL（部署前置）
2. **猫爪**（10 min）：精确 add 6 个业务/测试文件 → commit → push
3. **Vercel**：自动部署
4. **胡子哥**（10 min）：按 7 步浏览器验证（新增第 7 步：故意 DB 失败 → 验证 500）
5. **下轮派单**（不阻塞）：P2-1 catch 字符串匹配 → 改 BusinessError 类（30 min）

---

**复审完成时间**：2026-07-13 04:25
**本次复审完全只读**，未修改任何业务代码、测试代码、AGENTS.md、prisma/schema.prisma、迁移文件，未 commit，未 push，未部署。
