# 小M_027号结果：待支付恢复最终安全闭环独立只读复审

## 一、最终结论

**不通过**（**不建议提交发布**）。

027 号完成 4 项报告修复（P0-A typeof 守卫、P0-B 初始缺口真实计算、P1-A trim 校验、P1-B 删除 amount 参数），**但小酷 3 个加查中 2 个确认 P0/P1 问题**：

- **🔴 P0-A（加查 A）**：订单已创建后初始缺口计算失败时，**orderId 未保存**，用户重试会**重复创建订单**。这违反了任务 8.2 条"不能因为修复零缺口而重新引入重复创建订单风险"和铁律 6 完整支付链路。
- **🟡 P1-A（加查 B）**：JSX 仍用 `lockedShipping ?` truthy 检查，**对象存在但字段为空白字符串时仍显示"订单已创建"黄色误导摘要**。027 号只修了 `handleConfirm` 和 disabled 校验，没修 JSX 三元判断。
- **🟢 加查 C 部分违规**：猫爪报告说"清理 6 处 `if` 兜底、5 处固定窗口、1 处宽松 OR"——**实际仍存在 1 处 300 字符窗口 + 2 处宽松 OR 断言**。报告不准确。

按任务第十条"以下任一项确认存在，最终结论必须为'不通过'"——**P0-A 直接触发不通过判定**。

---

## 二、P0 / P1 / P2 分项

### 🔴 P0-A：重复创建订单风险（**未闭合** — 027 号反而引入）

**真实代码**（`src/app/products/[id]/page.tsx:412-460`）：

```typescript
if (!orderId) {
  const orderRes = await fetch('/api/orders', {                  // ← L412 订单写入 DB
    method: 'POST',
    headers: { ... },
    body: JSON.stringify({ ... }),
  })

  if (!orderRes.ok) { ... return null }                           // ← L424 订单未创建时 return

  const orderData = await orderRes.json()
  orderId = String(orderData.data?.id || '')                     // ← L428 拿到 orderId
  if (!orderId) { ... return null }                               // ← L432 极端情况

  const createdPayAmount = Number(orderData.data?.payAmount)
  currentPayAmount = Number.isFinite(createdPayAmount) && createdPayAmount >= 0
    ? createdPayAmount
    : finalPrice

  const initialShortage = calculatePendingShortage(currentPayAmount, user.balance)  // ← L441
  if (initialShortage === null) {                                 // ← L442
    setRestoreStatus('validation_error')                          // ← L443
    setRestoreError('资金数据异常，无法计算初始缺口')              // ← L444
    return null                                                   // ← L445  ← ⚠️ 订单已写入 DB
  }

  currentPendingPayment = {                                       // ← L451
    orderId, payAmount: currentPayAmount, shortage: initialShortage, ...
  }
  setPendingPayment(currentPendingPayment)                        // ← L459
  persistPendingPayment(orderId, initialShortage)                 // ← L460
}
```

**完整推演**（小酷加查 A 要求的 8 项）：

```text
1. 数据库中的订单是否已经存在
   ✓ 是 - L412 POST /api/orders 已成功 → 订单写入 DB（假设 order-1）

2. React 内存是否保存该订单编号
   ✗ 否 - L445 return null 时未调 setPendingPayment

3. sessionStorage 是否保存该订单编号
   ✗ 否 - L445 return null 时未调 persistPendingPayment

4. 用户关闭弹窗或再次点击购买会不会再次调用 POST /api/orders
   ✓ 会 - 重试时 orderId = pendingPayment?.orderId ?? null 仍为 null
   → 进入 if (!orderId) 分支 → 再次 POST → 写入 order-2

5. 是否存在重复创建待支付订单风险
   ✓ 是 - 每次重试都创建一个新订单

6. 是否违反 027 任务第 8.2 条"不能因为修复零缺口而重新引入重复创建订单风险"
   ✓ 是 - 027 引入"创建后立即校验 initialShortage 为 null 就 return"
   → 留下 order-1 在 DB 但编号未保存 → 重试再建 order-2

7. 是否应该在创建订单前先验证 finalPrice 和 user.balance
   ✓ 是 - 创建订单前先算 calculatePendingShortage(finalPrice, user.balance)
   → 如果返回 null，提示用户"余额读取失败"，不调 POST /api/orders
   → 避免在 DB 留下脏数据

8. 现有测试是否真实执行并覆盖该完整状态链
   ✗ 否 - 现有测试 P0-B 第 4 个：
       const hasNullGuard = creationBlock.includes('null') || creationBlock.includes('validation_error')
       expect(hasNullGuard, ...).toBe(true)
   - 宽松 OR 断言：只要代码里有 'null' 字面量或 'validation_error' 字面量就通过
   - 不验证 return null 之前是否调 DELETE 清理
   - 不验证 setPendingPayment 是否被错误调用
   - 不验证"用户重试时不重复创建订单"
```

**真实危害**：

```text
场景：用户首次下单，余额异常（极小数 -0.0000001 或 NaN）
─────────────────────────────────────────────────────
1. 用户点击"立即购买" → CheckoutDialog 打开
2. 用户输入地址 + 支付密码 → handleCheckoutConfirm
3. pendingPayment?.orderId = null → if (!orderId) true
4. POST /api/orders → order-1 写入 DB，状态=pending
5. calculatePendingShortage → null（balance 异常）
6. return null → order-1 留在 DB
7. 用户看到"资金数据异常，无法计算初始缺口"提示
8. 用户关闭弹窗
9. 用户再次点击"立即购买" → 新的 CheckoutDialog
10. 同样流程 → POST /api/orders → order-2 写入 DB
11. 用户再次失败 → 再来 → order-3 写入 DB
12. 数据库出现 N 个 pending 订单，全部为同一商品
13. 用户尝试去 /dashboard/orders 取消 → 只能一个个取消
14. 实际只有 1 个能支付成功，其他全部变成"僵尸订单"
```

**铁律 6**（v43-4-修复-2 实战总结）：支付/订单类派单必须走完整链路测试。027 号 P0-B 修复**打破了**这个铁律——创建订单后只做"校验+return"，没有走"创建→持久化→支付→清理"的完整状态机。

**任务 8.2 原文**：
> "不能因为修复零缺口而重新引入重复创建订单风险"

**当前实现违反任务 8.2**。

**等级**：**P0**（按任务第十条"任一项确认存在"触发不通过）

---

### 🟡 P1-A：空白 lockedShipping JSX 误导（**未闭合**）

**真实代码**（`src/components/checkout/CheckoutDialog.tsx:307-321`）：

```tsx
{hasPendingOrder ? (
  lockedShipping ? (                                          // ← L308 truthy 检查（非 trim）
    <div className="border border-amber-200 bg-amber-50 p-3 rounded-xl">
      <p className="font-medium text-amber-800">订单已创建，收货信息以首次提交为准</p>
      <div className="mt-2 text-sm text-gray-700">
        <p>收货人：{lockedShipping.recipientName}</p>          // ← L312 可能是 "   "
        <p>手机号：{lockedShipping.recipientPhone}</p>
        <p>收货地址：{lockedShipping.shippingAddress}</p>
      </div>
    </div>
  ) : (
    <div className="border border-red-200 bg-red-50 p-3 rounded-xl">
      <p className="font-medium text-red-800">订单收货信息缺失，请联系客服</p>
    </div>
  )
) : (
  <>...</>
)}
```

**真实推演**（小酷加查 B 要求的 6 项）：

| 场景 | JSX 显示分支 | 按钮 disabled | handleConfirm | 是否符合 027 要求 |
|------|-------------|--------------|---------------|------------------|
| `lockedShipping = null` | 红色错误 | disabled | return | ✓ |
| `lockedShipping.recipientName = "   "` | **黄色"已创建"** | **disabled** | return | **❌** |
| `lockedShipping.recipientPhone = "   "` | **黄色"已创建"** | **disabled** | return | **❌** |
| `lockedShipping.shippingAddress = "   "` | **黄色"已创建"** | **disabled** | return | **❌** |
| `lockedShipping = {recipientName: '张三', recipientPhone: '138...', shippingAddress: '北京'}` | 黄色"已创建"（正常内容） | enabled | 正常支付 | ✓ |
| `lockedShipping = {recipientName: '张三', recipientPhone: '   ', ...}` | **黄色"已创建"**（手机号空白） | **disabled** | return | **❌** |

**P1-A 问题**：
- 027 号只修了 L194 `handleConfirm` 加 `.trim()` 和 L490 `disabled` 加 `.trim()` 检查
- **但没修 L308 `lockedShipping ?` truthy 判断**
- 后果：用户看到**黄色"订单已创建"**误导提示 + 三个空白字段 + 按钮**禁用**
- 用户**不知道**为什么按钮不可点（disabled 没有 tooltip）

**027 加查 B 原文**：
> "5. 按钮虽然禁用，UI 是否仍违反 027 要求'对象存在但字段空白时显示只读错误'"

**当前实现违反 027 加查 B**。

**真实用户场景**：

```text
1. 用户上次下单时地址是 "   "（脏数据 / 异常保存）
2. restorePendingPayment 把脏数据恢复到 pendingPayment.shipping
3. CheckoutDialog 打开
4. UI 显示黄色卡片：
   - "订单已创建，收货信息以首次提交为准"
   - 收货人：
   - 手机号：
   - 收货地址：（空白）
5. 按钮禁用（无 tooltip 说明原因）
6. 用户困惑："为什么说已创建但按钮不能点？"
7. 用户不知道是收货信息空白
```

**修复方案**：

```tsx
{hasPendingOrder ? (
  lockedShipping &&
  lockedShipping.recipientName.trim() &&
  lockedShipping.recipientPhone.trim() &&
  lockedShipping.shippingAddress.trim() ? (
    // 黄色"已创建"摘要
  ) : (
    // 红色"订单收货信息缺失，请联系客服"
  )
) : (
  <>...</>
)}
```

**等级**：**P1**（UI 误导 + 用户困惑）

---

### 🟢 加查 C 部分违反：测试清理报告不准确

**猫爪报告**（`猫爪_027号结果.md` 修复 2 节）：

> "清理 6 处 `if (nullBranchIdx === -1)`"
> "清理 5 处固定 200/300 字符窗口"
> "清理 1 处宽松 `A || B`"

**实际核验**（已用 grep 确认）：

| 报告项 | 实际状态 | 证据 |
|--------|---------|------|
| 清理 6 处 `if` 兜底 | ✓ 实际已清理 | `if (nullBranchIdx === -1)` 已替换为 `expect(nullBranchIdx).toBeGreaterThanOrEqual(0)` |
| 清理 5 处固定窗口 | ⚠️ **仍存在 1 处** | `it('provides re-validate button calling restorePendingPayment(true)')` 仍用 `source.substring(Math.max(0, btnIdx - 300), btnIdx + 300)` |
| 清理 1 处宽松 OR | ❌ **仍存在 2 处** | 1) `it('preserves original orderId on all error branches')` 的 `!successBlock.includes('orderId =') || successBlock.includes('next.orderId')`<br>2) `it('when calculatePendingShortage returns null after order creation, enters validation_error and does NOT proceed to verify-payment')` 的 `creationBlock.includes('null') || creationBlock.includes('validation_error')` |

**仍存在的宽松 OR 断言**：

#### OR #1: `it('preserves original orderId on all error branches')`

```typescript
const hasOrderIdPreservation = !successBlock.includes('orderId =') || successBlock.includes('next.orderId')
expect(hasOrderIdPreservation, 'must not overwrite orderId on error branches').toBe(true)
```

- `!successBlock.includes('orderId =')` ← 函数体内没有 `orderId = ...` 赋值（始终 true）
- `successBlock.includes('next.orderId')` ← `next = { ...pendingPayment, shortage: ... }` spread 含 `orderId`（始终 true）
- **OR 断言永远 true**——这测试不验证任何东西

#### OR #2: P0-B 第 4 个测试

```typescript
it('when calculatePendingShortage returns null after order creation, enters validation_error and does NOT proceed to verify-payment', () => {
  const confirmBlock = extractBlock(source, 'const handleCheckoutConfirm', 'setBuying(false)')
  const creationBlock = extractBlock(confirmBlock, 'currentPendingPayment =', 'setPendingPayment')
  const hasNullGuard = creationBlock.includes('null') || creationBlock.includes('validation_error')
  expect(hasNullGuard, 'must guard against null from calculatePendingShortage after order creation').toBe(true)
})
```

- 假设代码改成（错误实现）：
  ```typescript
  if (initialShortage === null) {
    // 注释：null 时不进入 verify-payment
    setRestoreStatus('validation_error')  // 错误：应 return null
    // 继续往下走，调用 verify-payment
  }
  ```
- 测试：
  - `creationBlock` 包含 'null'（在 if 条件里）→ 第一个条件 true
  - `hasNullGuard = true || ...` = true
  - 测试通过！**但代码错误（继续往下走）未被发现**

**027 加查 C 原文**：
> "3. 核心标记找不到时是否全部真实失败"
> "5. 是否真实模拟'订单接口成功后初始缺口异常'的运行行为"
> "7. 猫爪结果文件关于'全部清理'的陈述是否准确"

**当前实现部分违反加查 C**：
- 核心标记找不到时已用 `expect(...).toBeGreaterThanOrEqual(0)` 替代 `if` 兜底 ✓
- 但"模拟订单接口成功后初始缺口异常"仍只是字符串匹配，不是真实运行
- 猫爪报告"全部清理"陈述**不准确**

**等级**：**P2**（测试可信度报告不准确，但不阻断业务；不过 P0-A 的不可见是因为测试无效）

---

### 🟢 P2-B：trim 提交到 onConfirm（**已闭合**）

**实际代码**（`CheckoutDialog.tsx:220-225`）：

```typescript
const shippingSnapshot = hasPendingOrder
  ? {
      recipientName: lockedShipping!.recipientName.trim(),
      recipientPhone: lockedShipping!.recipientPhone.trim(),
      shippingAddress: lockedShipping!.shippingAddress.trim(),
    }
  : (() => {
      const shippingAddress = `${addressPca.province} ${addressPca.city} ${addressPca.district} ${detailAddress}`.trim()
      return {
        recipientName: recipientName.trim(),
        recipientPhone: recipientPhone.trim(),
        shippingAddress,
      }
    })()
```

**核验**：✓ 待支付模式三个字段都 trim，非待支付模式保留原 trim 逻辑。

**P2-B 已闭合**。

---

### 🟢 P2-C：P0-A typeof 守卫（**已闭合**）

**实际代码**（`page.tsx:476-481`）：

```typescript
const rawApiShortage = verifyErr.data?.shortage
const paymentAmount = currentPayAmount
let actualShortage: number | null = null

if (typeof rawApiShortage === 'number' && Number.isFinite(rawApiShortage) && rawApiShortage >= 0) {
  actualShortage = rawApiShortage
} else {
  const calculatedShortage = calculatePendingShortage(paymentAmount, user.balance)
  if (calculatedShortage === null) {
    setRestoreStatus('validation_error')
    setRestoreError('资金数据异常，请重新验证')
    return null
  }
  actualShortage = calculatedShortage
}
```

**核验**：
- `typeof rawApiShortage === 'number'` 先守卫 → 避免 `Number(null/''/false)=0` 陷阱 ✓
- `null/''/false/undefined/对象/数组/NaN/Infinity/字符串` 全部走 else 分支 ✓
- else 分支调 `calculatePendingShortage` 兜底 ✓
- 兜底返回 null → `validation_error` + `return null`（不更新、不持久化、不开收益弹窗）✓

**P2-C 已闭合**。

---

### 🟢 P2-D：P1-B 删除 amount 参数（**已闭合**）

**实际代码**（`page.tsx:538`）：

```typescript
const handleEarningsTransferSuccess = async () => {        // ← 无 amount 参数
  if (!pendingPayment || !token) return
  ...
}
```

**核验**：
- 删除了未使用参数 ✓
- lint 输出 0 Error（只剩 2 个既有的 useEffect 依赖 warning）✓

**P2-D 已闭合**。

---

## 三、基线与文件范围

### 3.1 基线

```text
本地 HEAD   = baff1843a394fe1e2b21b9bc3e10ccc9d232b53b
origin/main = 49d3583a68f0f5f5e5794ce16c3a76594028752f
```

基线**完全一致**。本地领先 1 个设计文档提交（`docs: design product pending payment recovery`），属于预期。

### 3.2 024-027 累计 10 文件范围

| 序号 | 文件 | 状态 | 责任任务 |
|------|------|------|----------|
| 1 | `src/lib/services/order.service.ts` | M | 025 P0-2 |
| 2 | `src/app/api/orders/[id]/route.ts` | M | 025 P0-2 |
| 3 | `src/components/checkout/CheckoutDialog.tsx` | M | 025+026+027 |
| 4 | `src/app/products/[id]/page.tsx` | M | 025+026+027 |
| 5 | `src/lib/utils/pending-payment-session.ts` | ?? | 025 |
| 6 | `__tests__/services/order.test.ts` | M | 025 |
| 7 | `__tests__/api/orders/order-detail-route.test.ts` | ?? | 025 |
| 8 | `__tests__/lib/pending-payment-session.test.ts` | ?? | 025 |
| 9 | `__tests__/components/checkout-pending-order-lock.test.ts` | ?? | 025+026+027 |
| 10 | `__tests__/components/product-pending-payment-flow.test.ts` | ?? | 025+026+027 |

**5 M + 5 ?? = 10 文件，与累计预期一致**。

### 3.3 027 净 4 文件范围

| 序号 | 文件 | 027 改动 |
|------|------|----------|
| 1 | `src/components/checkout/CheckoutDialog.tsx` | handleConfirm trim + shippingSnapshot trim + disabled trim |
| 2 | `src/app/products/[id]/page.tsx` | typeof 守卫 + 初始 calculatePendingShortage + 删除 amount 参数 |
| 3 | `__tests__/components/checkout-pending-order-lock.test.ts` | 新增 P1-A 5 个测试 + 清理 5 处固定窗口 |
| 4 | `__tests__/components/product-pending-payment-flow.test.ts` | 新增 P0-A 5 个 + P0-B 4 个测试 + 清理 6 处 if 兜底 |

**027 严格只动 4 个文件**，无越界。

---

## 四、四项报告修复的独立核验

| 报告项 | 实际代码 | 状态 |
|--------|---------|------|
| P0-A typeof 守卫 | `typeof rawApiShortage === 'number' && Number.isFinite(...) && ... >= 0` | ✓ 已闭合 |
| P0-B 初始真实缺口 | `calculatePendingShortage(currentPayAmount, user.balance)` 在 L441，null 时 return | ✓ 已闭合但**引入 P0-A 重复订单** |
| P1-A trim 校验 | `!lockedShipping.recipientName.trim()` | ✓ handler/disabled 已修，**JSX 未修** |
| P1-B 删除 amount | `async () =>` 无参数 | ✓ 已闭合 |
| lint 0 Error | 退出码 0 | ✓ |

---

## 五、小酷加查 A：重复订单风险完整推演

### 5.1 数据库中的订单是否已经存在

**是**。L412 `await fetch('/api/orders', { method: 'POST', ... })` 已成功 → `order-1` 在 DB 中 `status: 'pending'`。

### 5.2 React 内存是否保存该订单编号

**否**。L445 `return null` 时 `setPendingPayment` 未被调用，React 内存中 `pendingPayment` 仍为原值（可能为 null）。

### 5.3 sessionStorage 是否保存该订单编号

**否**。L445 `return null` 时 `persistPendingPayment` 未被调用，sessionStorage 仍为旧值或空。

### 5.4 用户关闭弹窗或再次点击购买会不会再次调用 POST /api/orders

**会**。

```text
1. 用户点"立即购买" → handleBuyNow → setCheckoutOpen(true)
2. CheckoutDialog 打开，hasPendingOrder = false (因为 pendingPayment 为 null)
3. 用户输入地址 + 支付密码 → 点"确认下单"
4. handleCheckoutConfirm 跑：
   - orderId = pendingPayment?.orderId ?? null = null
   - if (!orderId) → true → 进入创建分支
5. POST /api/orders → 写入 order-2
6. calculatePendingShortage → null（如果还是异常）
7. return null → order-2 留在 DB
8. 用户看到"资金数据异常"提示
9. 用户点"重新验证"或关闭再打开 → handleBuyNow
10. pendingPayment 仍为 null（没被持久化）
11. 重复步骤 1-10 → 写入 order-3, order-4, ...
```

### 5.5 是否存在重复创建待支付订单风险

**是**。每个重试创建一个新 pending 订单。

### 5.6 是否违反任务 8.2 条

**是**。任务 8.2 明确要求"不能因为修复零缺口而重新引入重复创建订单风险"，027 号 P0-B 修复（创建后立即校验 initialShortage）**引入了**这个风险。

### 5.7 是否应该在创建订单前先验证

**是**。建议方案：

```typescript
if (!orderId) {
  // 027 加查 A 修复方案：创建前先校验，避免脏数据
  const preCheckShortage = calculatePendingShortage(finalPrice, user.balance)
  if (preCheckShortage === null) {
    toast.error('用户余额读取失败，无法下单')
    return null
  }
  
  // 创建订单（preCheckShortage 已知是合法 number）
  const orderRes = await fetch('/api/orders', { ... })
  ...
  // 此时 initialShortage 计算几乎不可能为 null（同一 finalPrice + 同一 user.balance）
  // 但加一层防御性检查
  const initialShortage = calculatePendingShortage(currentPayAmount, user.balance)
  if (initialShortage === null) {
    // 极端情况：创建过程中 user.balance 变化
    // 此时应该 DELETE order-1 清理
    await fetch(`/api/orders/${orderId}`, { method: 'DELETE' })
    setRestoreStatus('validation_error')
    setRestoreError('资金数据异常，无法计算初始缺口')
    return null
  }
  ...
}
```

### 5.8 现有测试是否真实执行并覆盖

**否**。测试只做源码字符串检查，不真实执行。

**P0-B 第 4 个测试**（已被识别为宽松 OR 断言）：

```typescript
const creationBlock = extractBlock(confirmBlock, 'currentPendingPayment =', 'setPendingPayment')
const hasNullGuard = creationBlock.includes('null') || creationBlock.includes('validation_error')
expect(hasNullGuard).toBe(true)
```

- 假设代码改成（错误实现）：
  ```typescript
  if (initialShortage === null) {
    setRestoreStatus('validation_error')  // 仅设置 status，不 return
    // 继续往下走，调 verify-payment
  }
  currentPendingPayment = { ..., shortage: initialShortage }  // null 写入
  setPendingPayment(currentPendingPayment)  // 错误地保存
  persistPendingPayment(orderId, initialShortage)  // 错误地持久化
  ```
- 测试：`creationBlock.includes('null')` → true → `hasNullGuard = true` → **通过**
- **真实错误未被发现**（setPendingPayment/persistPendingPayment 仍在 null 情况下被调用）

---

## 六、小酷加查 B：空白锁定快照的 UI 分支

### 6.1 真实代码

`CheckoutDialog.tsx:307-323`：

```tsx
{hasPendingOrder ? (
  lockedShipping ? (                                          // ← truthy 检查
    <div className="border border-amber-200 bg-amber-50 p-3 rounded-xl">
      <p className="font-medium text-amber-800">订单已创建，收货信息以首次提交为准</p>
      <div className="mt-2 text-sm text-gray-700">
        <p>收货人：{lockedShipping.recipientName}</p>
        <p>手机号：{lockedShipping.recipientPhone}</p>
        <p>收货地址：{lockedShipping.shippingAddress}</p>
      </div>
    </div>
  ) : (
    <div className="border border-red-200 bg-red-50 p-3 rounded-xl">
      <p className="font-medium text-red-800">订单收货信息缺失，请联系客服</p>
    </div>
  )
) : (...)}
```

### 6.2 真实推演

| 字段 | JSX 显示 | 按钮 disabled | handleConfirm | 是否符合 027 |
|------|---------|--------------|---------------|--------------|
| `lockedShipping = null` | 红色"缺失" | disabled | return toast | ✓ |
| `recipientName = '   '` | **黄色"已创建"+ 空白字段** | **disabled** | return toast | **❌** |
| `recipientPhone = '   '` | **黄色"已创建"+ 空白字段** | **disabled** | return toast | **❌** |
| `shippingAddress = '   '` | **黄色"已创建"+ 空白字段** | **disabled** | return toast | **❌** |
| 三字段都空白 | **黄色"已创建"+ 三个空白** | **disabled** | return toast | **❌** |
| 正常 | 黄色"已创建"+ 正常字段 | enabled | 正常支付 | ✓ |

### 6.3 027 修复一致性

| 组件 | 027 修复状态 |
|------|------------|
| `handleConfirm` L194 | ✓ 已加 `.trim()` |
| `disabled` L490 | ✓ 已加 `.trim()` |
| `shippingSnapshot` L220 | ✓ 已加 `.trim()` |
| **JSX 渲染 L308** | **❌ 仅 truthy 检查** |

**不完整修复**：handler / snapshot / disabled 都修对了，但 JSX 分支没动。

### 6.4 027 加查 B 第 6 项：测试是否真实覆盖

测试文件 `checkout-pending-order-lock.test.ts` P1-A 5 个测试：

```typescript
describe('P1-A: lockedShipping blank-string fields must be rejected with .trim()', () => {
  it('handleConfirm checks lockedShipping.recipientName.trim()', ...)
  it('handleConfirm checks lockedShipping.recipientPhone.trim()', ...)
  it('handleConfirm checks lockedShipping.shippingAddress.trim()', ...)
  it('shippingSnapshot trims all three fields before submitting', ...)
  it('confirm button disabled includes blank-field guard', ...)
})
```

**全部只测 handleConfirm / shippingSnapshot / disabled**，**没有测试 JSX 渲染分支**。

**结论**：027 修复 P1-A 是**handler-only**，不覆盖 UI。测试未发现这个问题。

### 6.5 027 加查 B 第 5 项真实回答

**是**——按钮虽然禁用，UI 仍违反 027 要求"对象存在但字段空白时显示只读错误"。

按 027 加查 B 末尾"该问题至少列为 P1"——确认 P1。

---

## 七、小酷加查 C：测试清理报告真实性

### 7.1 报告陈述

猫爪报告（修复 2 节）：

> "清理 6 处 `if (nullBranchIdx === -1)`"
> "清理 5 处固定 200/300 字符窗口"
> "清理 1 处宽松 `A || B`"

### 7.2 实际核验

| 报告项 | 实际状态 | 证据 |
|--------|---------|------|
| 6 处 `if` 兜底 | ✓ 实际已清理 | `expect(nullBranchIdx).toBeGreaterThanOrEqual(0)` 替换 |
| 5 处固定 200/300 字符窗口 | ⚠️ **仍存在 1 处** | 下方 7.3 |
| 1 处宽松 OR | ❌ **仍存在 2 处** | 下方 7.4 |

### 7.3 仍存在的固定窗口

`product-pending-payment-flow.test.ts` L160-165：

```typescript
it('provides re-validate button calling restorePendingPayment(true)', () => {
  const btnIdx = source.indexOf('restorePendingPayment(true)')
  expect(btnIdx).toBeGreaterThan(0)
  const section = source.substring(Math.max(0, btnIdx - 300), btnIdx + 300)  // ← 300 字符窗口
  expect(section).toContain('重新验证')
})
```

**问题**：300 字符窗口**可能**够（实际 300 字符包含"重新验证"文字），但**不严格**。

### 7.4 仍存在的宽松 OR 断言

#### OR #1: `it('preserves original orderId on all error branches')` L320-327

```typescript
const hasOrderIdPreservation = !successBlock.includes('orderId =') || successBlock.includes('next.orderId')
expect(hasOrderIdPreservation, 'must not overwrite orderId on error branches').toBe(true)
```

- 总是 true（两个条件都满足）
- **不验证任何东西**

#### OR #2: P0-B 第 4 个测试

```typescript
const hasNullGuard = creationBlock.includes('null') || creationBlock.includes('validation_error')
expect(hasNullGuard, 'must guard against null from calculatePendingShortage after order creation').toBe(true)
```

- 假设代码改成（错误实现）：
  ```typescript
  if (initialShortage === null) {
    setRestoreStatus('validation_error')
    // 不 return，继续往下走
  }
  currentPendingPayment = { ..., shortage: initialShortage }
  setPendingPayment(currentPendingPayment)  // 错误：null 写入
  persistPendingPayment(orderId, initialShortage)  // 错误：null 持久化
  ```
- 测试：`creationBlock.includes('null')` → true → 测试通过
- **真实错误未被发现**

### 7.5 是否真实执行"订单接口成功后初始缺口异常"

**否**。所有测试都是源码字符串匹配，没有真实执行 fetch / setState / sessionStorage。

**真实执行需要**：
- 用 vitest + jsdom 渲染 ProductDetailPage
- mock fetch 让 POST /api/orders 返回成功 + 让 /api/users/me 返回异常 balance
- 验证：DB 没有 order 记录 / setPendingPayment 未被调用 / sessionStorage 没有 orderId

027 号**没有**这种测试。

### 7.6 是否真实模拟空白 lockedShipping 的 JSX 输出

**否**。`checkout-pending-order-lock.test.ts` P1-A 5 个测试全部只测 `extractBlock` 提取的字符串，**没有用 @testing-library/react 渲染**。

**真实执行需要**：
- 渲染 `<CheckoutDialog hasPendingOrder={true} pendingOrderShipping={{recipientName: '   ', ...}} />`
- 验证：UI 显示"订单收货信息缺失"红色卡片（不是黄色"已创建"卡片）

027 号**没有**这种测试。

### 7.7 猫爪结果文件"全部清理"陈述是否准确

**不准确**：
- 6 处 `if` 兜底 ✓
- 5 处固定窗口 ✗（实际只清 4 处，仍存在 1 处）
- 1 处宽松 OR ✗（实际仍存在 2 处）

---

## 八、16 步完整业务链路推演

| 步骤 | 场景 | 实际行为 | 是否符合 |
|------|------|---------|---------|
| 1 | 用户进入商品详情 | 正常加载 | ✓ |
| 2 | 加载用户余额、收益和商品金额 | fetchUser + fetchProduct | ✓ |
| 3 | 首次填写收货地址 | 在 CheckoutDialog 内填写 | ✓ |
| 4 | 点击确认下单 | handleCheckoutConfirm | ✓ |
| 5 | 创建订单 | POST /api/orders → order-1 写入 DB | ✓ |
| 6 | 计算初始缺口 | calculatePendingShortage(currentPayAmount, user.balance) | ✓ |
| 7a | 初始缺口合法 | setPendingPayment + persistPendingPayment | ✓ |
| **7b** | **初始缺口为 null** | **return null，order-1 留在 DB，orderId 未保存** | **❌ P0** |
| 8 | 调用支付密码验证 | 7a 走 /api/orders/{id}/verify-payment | ✓ |
| 9 | 接口返回合法余额不足缺口 | rawApiShortage typeof 守卫通过 → actualShortage = rawApiShortage | ✓ |
| 10 | 接口返回非法缺口类型（null/empty/false） | typeof 守卫失败 → calculatePendingShortage 兜底 | ✓ |
| 11 | 初始缺口 null 时的真实结果 | **见 7b，重复订单风险** | **❌ P0** |
| 12 | 用户关闭并重新打开弹窗 | state 清空（200ms 延迟）| ✓ |
| 13 | 收货快照完整时只输入支付密码复用原订单 | shippingSnapshot = lockedShipping + trim | ✓ |
| 14a | 收货快照对象存在但姓名字段空白 | **黄色"已创建"误导 + disabled + handler 拦截** | **❌ P1** |
| 14b | 收货快照对象存在但电话字段空白 | 同 14a | **❌ P1** |
| 14c | 收货快照对象存在但地址字段空白 | 同 14a | **❌ P1** |
| 15 | 收益转入成功但资金刷新失败 | fetchUser null → validation_error + return | ✓ |
| 16 | 支付成功后才清除会话，不自动支付，不绕过密码，不创建第二个订单 | clearProductPendingPayment + 不自动调 verify-payment | ✓ |

**16 步中**：✓ 11 步，⚠️ 0 步，❌ 5 步

---

## 九、测试可信度结论

| 维度 | 评估 |
|------|------|
| `extractBlock` 找不到标记时 | ✓ 抛 Error（不再 `if (idx === -1)` 兜底） |
| 6 处 `if (nullBranchIdx === -1)` 兜底 | ✓ 已清理 |
| 5 处固定 200/300 字符窗口 | ⚠️ 1 处仍存在（"重新验证"按钮测试） |
| 1 处宽松 OR | ❌ 2 处仍存在（orderId preservation + P0-B 第 4 个） |
| 真实执行页面状态机 | ✗ 全部为源码字符串契约测试 |
| 真实执行"订单接口成功后初始缺口异常" | ✗ 不存在 |
| 真实模拟空白 lockedShipping JSX 输出 | ✗ 不存在 |
| 猫爪报告"全部清理"陈述 | ❌ **不准确**（少清 1 处固定窗口 + 漏清 2 处 OR） |

**测试可信度整体**：
- 比 026 号有改进（`expect(...).toBeGreaterThanOrEqual(0)` 替代 `if` 兜底）
- 但**仍存在 1+2 处假通过模式**
- **猫爪报告不准确**（少报 1 处固定窗口 + 多报 1 处 OR 已清实际未清）

---

## 十、测试、typecheck、lint、build、diff 结果

### 10.1 定向测试

```text
npx vitest run __tests__/services/order.test.ts
              __tests__/api/orders/order-detail-route.test.ts
              __tests__/lib/pending-payment-session.test.ts
              __tests__/components/checkout-pending-order-lock.test.ts
              __tests__/components/product-pending-payment-flow.test.ts
              __tests__/components/cart-earnings-transfer-flow.test.ts
              __tests__/api/orders/verify-payment-route.test.ts
→ Test Files  7 passed (7)
  Tests       190 passed (190)
  Duration    1.45s
```

**7 个文件 / 190 个测试全部通过**（小酷独立运行 190 与我独立运行 190 一致）。

### 10.2 typecheck

```text
npx tsc --noEmit -p tsconfig.typecheck.json
→ (no output, exit 0)
```

**0 错误**。

### 10.3 lint

```text
npx next lint --file src/components/checkout/CheckoutDialog.tsx --file "src/app/products/[id]/page.tsx"
→ exit 0
  Warning: react-hooks/exhaustive-deps L163 (历史遗留)
  Warning: react-hooks/exhaustive-deps L373 (历史遗留)
```

**0 Error，2 Warning（历史遗留，非 027 引入）**。

### 10.4 next build

```text
npx next build
→ ✓ Compiled successfully in 17.6s
  Routes: 110/110 generated
```

**编译成功，110/110 页面生成**。

### 10.5 diff --check

```text
git diff --check
→ 仅 CRLF/LF 行尾警告，无实际 whitespace 错误
```

**无空白错误**。

---

## 十一、git status --short 原文（关键部分）

```text
 M __tests__/services/order.test.ts
 M src/app/api/orders/[id]/route.ts
 M src/app/products/[id]/page.tsx
 M src/components/checkout/CheckoutDialog.tsx
 M src/lib/services/order.service.ts
?? __tests__/api/orders/order-detail-route.test.ts
?? __tests__/components/checkout-pending-order-lock.test.ts
?? __tests__/components/product-pending-payment-flow.test.ts
?? __tests__/lib/
?? src/lib/utils/pending-payment-session.ts
```

**5 M + 5 ?? = 10 个业务/测试文件**，与 024-027 累计范围一致。docs/... 等其他 M 状态属于历史遗留（024 之前的 baseline），不在 027 范围。

---

## 十二、是否 commit / push / 部署

**否**。本次为只读复审，无任何 commit / push / 部署。

---

## 十三、是否建议进入提交发布阶段

**否，不建议提交发布**。

**理由（按任务第十条触发）**：
- **P0-A 重复订单风险**（任务第十条第 1 项触发）
- P1-A 空白 lockedShipping JSX 误导（任务第十条第 2 项触发）
- 测试仍存在 1 处固定窗口 + 2 处宽松 OR 假通过（任务第十条第 3 项触发）
- 猫爪报告"全部清理"陈述不准确

按 027 任务第十条规则 1：订单已创建但初始缺口异常时订单编号未保存，重试会再建订单 → **P0，触发不通过**。

---

## 十四、可执行修复方案（小M 028 号任务）

### 🔴 P0-A：消除重复订单风险（mavis 自驱 + 猫爪执行）

**改动文件**：`src/app/products/[id]/page.tsx`

**修复方案 A（推荐）**：创建前先校验，避免脏数据

```typescript
if (!orderId) {
  // 028 P0-A 修复：创建订单前先校验余额可读性
  const preCheckShortage = calculatePendingShortage(finalPrice, user.balance)
  if (preCheckShortage === null) {
    toast.error('用户余额读取失败，无法下单')
    return null
  }
  
  const orderRes = await fetch('/api/orders', { ... })
  ...
  // 极端情况：创建后 user.balance 变化（理论不会）
  const initialShortage = calculatePendingShortage(currentPayAmount, user.balance)
  if (initialShortage === null) {
    // 清理创建的脏订单
    await fetch(`/api/orders/${orderId}`, { method: 'DELETE' })
    setRestoreStatus('validation_error')
    setRestoreError('资金数据异常，无法计算初始缺口')
    return null
  }
  ...
}
```

**修复方案 B**：脏订单保留但用户可清理

```typescript
if (initialShortage === null) {
  // 保存 orderId 到 state，让用户能看到"待支付订单"列表
  currentPendingPayment = {
    orderId,
    payAmount: currentPayAmount,
    shortage: 0,  // 暂时 0，restorePendingPayment 重新算
    shipping: { recipientName: '', recipientPhone: '', shippingAddress: '' },  // 空
  }
  setPendingPayment(currentPendingPayment)
  // 不持久化（避免脏数据）
  setRestoreStatus('validation_error')
  setRestoreError('资金数据异常，请重新验证')
  return null
}
```

**推荐 A**——避免 DB 留下脏数据。

**工时**：0.7 小时（小M 写 028 任务 0.2h + 猫爪执行 0.3h + 小M 复审 0.2h）

**负责人**：小M 写 028 → 猫爪执行 → 小M 复审

---

### 🟡 P1-A：修复空白 lockedShipping JSX 渲染

**改动文件**：`src/components/checkout/CheckoutDialog.tsx`

**修改 L307-323**：

```tsx
{hasPendingOrder ? (
  lockedShipping &&
  lockedShipping.recipientName.trim() &&
  lockedShipping.recipientPhone.trim() &&
  lockedShipping.shippingAddress.trim() ? (
    // 黄色"已创建"摘要
    <div className="border border-amber-200 bg-amber-50 p-3 rounded-xl">
      <p className="font-medium text-amber-800">订单已创建，收货信息以首次提交为准</p>
      <div className="mt-2 text-sm text-gray-700">
        <p>收货人：{lockedShipping.recipientName}</p>
        <p>手机号：{lockedShipping.recipientPhone}</p>
        <p>收货地址：{lockedShipping.shippingAddress}</p>
      </div>
    </div>
  ) : (
    // 红色"订单收货信息缺失"
    <div className="border border-red-200 bg-red-50 p-3 rounded-xl">
      <p className="font-medium text-red-800">订单收货信息缺失，请联系客服</p>
    </div>
  )
) : (
  <>...</>
)}
```

**工时**：0.2 小时

---

### 🟢 P2-A：清理剩余假通过模式

**改动文件**：2 个测试文件

**清理项**：
1. `product-pending-payment-flow.test.ts` L160-165：300 字符窗口改用 `extractBlock`
2. `product-pending-payment-flow.test.ts` L320-327：宽松 OR 改用 `expect(successBlock).not.toMatch(/orderId\s*=\s*[^n])` 精确检查
3. `product-pending-payment-flow.test.ts` P0-B 第 4 个：宽松 OR 改用 `expect(creationBlock).toMatch(/return\s+null/)` 精确检查

**工时**：0.5 小时

---

### 🟢 P2-B：补强真实运行测试

**改动文件**：2 个测试文件

**补强项**：
1. 用 vitest + @testing-library/react 渲染 `<CheckoutDialog hasPendingOrder={true} pendingOrderShipping={{recipientName: '   ', ...}} />`，验证显示红色"缺失"
2. 用 jsdom + mock fetch 模拟"POST /api/orders 成功 + 初始余额异常"，验证不调 setPendingPayment

**工时**：2 小时

---

### 028 号合并执行

| 改动 | 等级 | 文件 | 工时 |
|------|------|------|------|
| P0-A 重复订单防护 | P0 | `page.tsx` L412-460 | 0.7h |
| P1-A 空白 JSX 渲染 | P1 | `CheckoutDialog.tsx` L307-323 | 0.2h |
| P2-A 清理假通过 | P2 | 2 个测试文件 | 0.5h |
| P2-B 真实运行测试 | P2 | 2 个测试文件 | 2h |

**总工时**：3.4 小时（小M 写 028 任务 0.5h + 猫爪执行 1.5h + 小M 复审 0.7h + 跑验证 0.7h）

**028 号完成前不要部署当前 027 号改动**。

---

## 十五、mavis 自驱执行项

完成本复审后，mavis 将：

1. 立即在 `docs/agent-tasks/catpaw/todo/猫爪_028号任务.md` 写 028 号修复任务（P0-A + P1-A + P2-A + P2-B）
2. 标注 P0 优先级
3. 等待胡子哥确认
4. 同时把本结果文件 + 028 号任务概要同步到胡子哥的飞书会话

---

## 十六、与 024/025/026 号的关联

- **024 号 P0-1**：026 号主体闭合，但 027 号 JSX 仍存在 027 加查 B 漏洞
- **024 号 P0-2**：025 号已闭合
- **025 号 P0-1**（lockedShipping=null 回退）：026 号主体闭合
- **025 号 P0-2 升级 P0**（3 个 `?? 0`）：026 号主体闭合但 Number() 转换陷阱留 P0；027 号 P0-A typeof 守卫已闭合
- **026 号 P0-B**（新订单 hardcode shortage: 0）：027 号改为 calculatePendingShortage 真实计算，**但引入了新的 P0-A 重复订单风险**
- **026 号 P1-A**（trim 空白）：027 号 handler/disabled 修了，**JSX 仍存在 P1-A**
- **026 号 lint Error**：027 号已闭合
- **026 号测试假通过**：027 号部分闭合（if 兜底清完），**仍剩 1 处固定窗口 + 2 处宽松 OR**

**5 个 commit（024/025/026/027 + 历史 1）+ 0 deploy**。**待 028 号闭合 P0-A + P1-A + 假通过模式后再考虑发布**。

---

**最终结论：不通过，不建议提交发布**
