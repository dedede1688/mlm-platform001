# {{代理名}}_{{编号}}号任务

## 1. 任务身份

- 任务编号：
- 执行代理：猫爪 / 小 M
- 任务类型：执行 / 只读复审 / 部署验证 / 实测
- 创建人：小酷
- 创建时间：

## 2. 背景

说明当前模块、上一包状态、相关 commit（提交编号）、业务目标。

## 3. 目标

本任务要完成什么，用 3-5 条写清楚。

## 4. 允许操作

- 

## 5. 禁止操作

- 不修改无关文件。
- 不修改 `AGENTS.md（项目规则文件）`。
- 不执行数据库迁移，除非本任务明确允许。
- 不 commit（提交），除非本任务明确允许。
- 不 push（推送），除非本任务明确允许。
- 不部署，除非本任务明确允许。

## 6. 文件范围

允许读取：

```text

```

允许修改：

```text

```

如果出现范围外文件，必须停止并报告。

## 7. 检查清单

1. 
2. 
3. 

## 8. 验证命令

```bash
npx tsc --noEmit -p tsconfig.typecheck.json
```

说明：上面命令是 typecheck（类型检查）。

如涉及 build（构建）：

```bash
npx next build
```

如涉及 test（测试）：

```bash
npx vitest run <测试文件>
```

## 9. 输出要求

完成后生成结果文件：

```text
docs/agent-tasks/{{代理目录}}/done/{{代理名}}_{{编号}}号结果.md
```

说明：`done（已完成）` 是结果文件目录。

结果文件必须使用：

```text
docs/agent-tasks/templates/result-template.md
```

## 10. 停止条件

遇到以下任一情况必须停止：

1. 发现任务范围外文件被修改。
2. 发现需求和现有代码冲突。
3. 测试或构建失败且无法在本任务范围内修复。
4. 需要数据库迁移但任务未授权。
5. 需要 commit（提交）或 push（推送）但任务未授权。

