# {{代理名}}_{{编号}}号任务

> 变更级别：A / P（S 级变更不需要任务文件）

## 1. 任务目标

本任务要完成什么，用 3-5 条写清楚。

## 2. 允许修改的文件

```text

```

如果出现范围外文件，必须停止并报告。

## 3. 禁止操作

- 不修改无关文件。
- 不修改 `AGENTS.md`。
- 不修改 `prisma/schema.prisma`、迁移文件或数据库结构。
- 不执行数据库迁移或生产写入。
- 不 commit（提交）、push（推送）、部署，除非本任务明确允许。
- 不修改通知模板或通知链路，除非本任务明确允许。

## 4. 业务规则要点

本任务涉及的业务规则，逐条列出。

## 5. 验证命令

```bash
# typecheck（类型检查）
npx tsc --noEmit -p tsconfig.typecheck.json

# test（测试）
npx vitest run <测试文件>

# 如涉及 build（构建）
npx next build
```

## 6. 完成标准

1. 
2. 
3. 
