# 小M_004号任务

## 1. 任务目标

独立只读复审 Supabase Data API lockdown 生产执行日志，确认执行内容、前置门槛、结果记录与制度一致，并给出"通过""有条件通过"或"不通过"的明确结论。

## 2. 基线与范围

- 变更等级：P级（数据库权限边界）。
- 基线提交：`e0b4cdc`（含铁律18的最新版）。
- 目标提交：`3cf9093`（生产执行日志提交）。
- 允许读取：
  - `docs/runbooks/supabase-data-api-lockdown-execution-log.md`
  - `docs/runbooks/supabase-data-api-lockdown.md`
  - `prisma/migrations/20260719_lock_down_supabase_data_api/migration.sql`
  - `docs/superpowers/specs/2026-07-19-supabase-data-api-lockdown-design.md`
  - `docs/superpowers/plans/2026-07-19-supabase-data-api-lockdown-plan.md`
  - `docs/roles/tasks/xiaom/todo/小M_003号任务.md`
  - 本任务文件、Git 状态、差异和历史。
- 允许修改：无。复审结果只能在当前对话中完整交付给 Codex 小酷。

## 3. 禁止操作

- 禁止修改、创建或删除任何项目文件。
- 禁止 `git add`、`git commit`、`git push`、合并、部署或改变分支状态。
- 禁止执行迁移、生产 SQL、数据库写入或任何外部系统写操作。
- 禁止自行扩大任务范围、修复问题或降低验收标准。
- 禁止把小酷提供的结论直接当作独立复审证据。

## 4. 业务与安全规则

- 执行日志必须记录完整的 10 项前置门槛检查结果。
- 执行 SQL 必须与已复审的 `migration.sql` 完全一致（28 表 + 序列）。
- 执行结果必须为零报错、零数据变更。
- 执行时间、执行人、执行方式必须明确记录。
- 必须确认执行后仍有待补充的验证项（操作手册§5）。
- `password_reset_codes` 仍不在生产表清单中。

## 5. 验证命令

```powershell
git status --short --branch
git diff --stat e0b4cdc..3cf9093
git diff --name-only e0b4cdc..3cf9093
git show --stat --oneline 3cf9093
git show 3cf9093:docs/runbooks/supabase-data-api-lockdown-execution-log.md
```

预期：目标提交仅新增执行日志文件；`git diff --check` 为0；执行日志内容与制度一致。

## 6. 完成标准

- 输出"通过""有条件通过"或"不通过"。
- 逐项检查执行日志的前置门槛、执行内容、结果记录与制度一致性。
- 明确是否存在日志缺失、门槛漏检、SQL 不一致或范围越界。
- 报告基线、目标、实际文件范围和工作区状态。
- 全程只读，结果只在对话中交付给 Codex 小酷。
