# 小M_003号任务

## 1. 任务目标

对“Supabase 数据 API 封锁”P0 权限变更实施稿进行提交前独立只读复审，确认迁移范围精确、审计脚本无写操作、验证证据可信，并给出“通过”“有条件通过”或“不通过”的明确结论。

## 2. 基线与范围

- 变更等级：P级（数据库权限边界）。
- 基线提交：`ed7c254`。
- 目标工作区：分支 `codex/supabase-data-api-lockdown` 上尚未提交的工作区差异。
- 允许读取：
  - `docs/superpowers/specs/2026-07-19-supabase-data-api-lockdown-design.md`
  - `docs/superpowers/plans/2026-07-19-supabase-data-api-lockdown-plan.md`
  - `__tests__/security/supabase-data-api-lockdown.test.ts`
  - `prisma/migrations/20260719_lock_down_supabase_data_api/migration.sql`
  - `scripts/audit-supabase-data-api.sql`
  - `scripts/README.md`
  - `docs/runbooks/supabase-data-api-lockdown.md`
  - 本任务文件、Git 状态、差异和历史。
- 如具备 Supabase 只读工具，可只读取生产数据库的表清单、RLS 状态、角色授权、策略清单和指定表行数；不得读取或展示密码散列、支付密码散列、银行卡号等敏感行数据。
- 允许修改：无。复审结果只能在当前对话中完整交付给 Codex 小酷，由小酷决定是否落盘。

## 3. 禁止操作

- 禁止修改、创建或删除任何项目文件，包括复审结果文件。
- 禁止 `git add`、`git commit`、`git push`、合并、部署或改变分支状态。
- 禁止执行迁移、生产 SQL、数据库写入、Storage 修改或任何外部系统写操作。
- 禁止运行会生成或改写文件的格式化、自动修复、依赖安装、Prisma 生成、覆盖率或快照更新命令。
- 禁止自行扩大任务范围、修复问题、降低验收标准或增加权限例外。
- 禁止把小酷提供的结论直接当作独立复审证据。

## 4. 业务与安全规则

- 生产 `public` 架构本次只处理已确认存在的 28 张表，表名必须与设计和测试中的清单完全一致。
- `password_reset_codes` 当前不在生产表清单中，迁移不得引用或创建它。
- 每张目标表必须撤销 `anon`、`authenticated` 的全部表权限，并启用 RLS（行级安全）。
- 必须撤销 `public` 架构内全部序列对 `anon`、`authenticated` 的权限。
- 迁移不得授予新权限、关闭 RLS、创建宽松策略、修改业务数据、删除表或改变表结构。
- 审计脚本必须严格只读，只允许查询元数据、策略和指定表行数。
- 本次不得改变 Prisma 服务端连接、`service_role`（服务角色）或 Storage（对象存储）策略；Storage 风险属于后续独立任务。
- 应用继续通过受信任服务端 Prisma/数据库连接运行；本次目标是封锁浏览器通过 Supabase Data API 绕过服务端业务逻辑的直接访问。
- 任何生产执行必须等到：小M通过、胡子老师批准、备份与回滚门槛确认；本任务本身不授权生产执行。

## 5. 验证命令

在隔离工作树根目录运行以下只读命令；若命令会因工具行为改写文件，应停止而不是执行。

```powershell
git status --short --branch
git rev-parse --short HEAD
git rev-parse --short origin/main
git -c core.quotePath=false diff --name-only ed7c254
git diff --check ed7c254
git diff ed7c254 -- __tests__/security/supabase-data-api-lockdown.test.ts prisma/migrations/20260719_lock_down_supabase_data_api/migration.sql scripts/audit-supabase-data-api.sql scripts/README.md docs/runbooks/supabase-data-api-lockdown.md docs/roles/tasks/xiaom/todo/小M_003号任务.md
npx vitest run __tests__/security/supabase-data-api-lockdown.test.ts
npm run typecheck
npm test
```

独立统计迁移语句数量：

```powershell
$sql = Get-Content -Raw -Encoding UTF8 prisma/migrations/20260719_lock_down_supabase_data_api/migration.sql
([regex]::Matches($sql, '(?im)^REVOKE ALL PRIVILEGES ON TABLE public\.')).Count
([regex]::Matches($sql, '(?im)^ALTER TABLE public\..+ ENABLE ROW LEVEL SECURITY;')).Count
([regex]::Matches($sql, '(?im)^REVOKE ALL PRIVILEGES ON ALL SEQUENCES IN SCHEMA public FROM anon, authenticated;')).Count
```

预期分别为 `28`、`28`、`1`。逐表比对 28 张表清单，确认没有缺失、重复或额外表。另需人工确认迁移和审计脚本不存在授权、关闭 RLS、数据写入、结构删除等越界 SQL。

小酷已报告但小M必须独立核对的证据：

- 专项测试：1 个文件、4 项测试通过。
- 类型检查：通过。
- 全量测试：47 个文件、928 项测试通过。
- 生产构建：退出码 0；隔离环境未配置 `DATABASE_URL`，预渲染阶段出现数据库地址为空警告，但未连接或写入数据库。

如使用生产只读检查，预期确认当前 28 张表及现存风险即可；不得执行迁移内容，不得抽取敏感数据。

## 6. 完成标准

- 独立确认实际差异没有超出任务允许范围。
- 独立确认 28/28/1 语句数量和逐表对应关系准确。
- 独立确认迁移只收紧 Data API 权限，不修改数据或业务结构。
- 独立确认审计脚本严格只读，运行手册含前置检查、停止条件、执行后复核、应用冒烟和恢复原则。
- 报告所有 P0/P1/P2/P3 问题及可复现证据。
- 仅在当前对话向 Codex 小酷输出完整复审结果，不写入项目文件。
- 结论必须是“通过”“有条件通过”或“不通过”之一；存在 P0/P1、范围冲突或基线不明时不得给出“通过”。
