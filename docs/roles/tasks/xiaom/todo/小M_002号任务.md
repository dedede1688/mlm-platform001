# 小M_002号任务

## 1. 任务目标

独立只读复审提交 `698e67c` 新增的首批治理任务，确认小M与小猫任务的身份、权限、范围、命令和完成标准符合现行岗位制度。

## 2. 基线与范围

- 基线提交：`007388e`
- 目标提交：`698e67c`
- 变更范围：仅 `docs/roles/tasks/xiaom/todo/小M_001号任务.md` 与 `docs/roles/tasks/xiaomao/todo/小猫_001号任务.md`。
- 允许读取：上述两个任务、`AGENTS.md`、`docs/roles/README.md`、`docs/roles/{xiaom,xiaomao}/**`、`docs/roles/templates/**`及Git历史。
- 允许修改：无。

## 3. 禁止操作

- 禁止修改任何文件或使用 `apply_patch`。
- 禁止git add、commit、push、merge、rebase、reset或创建PR。
- 禁止部署、数据库写入或外部系统操作。
- 禁止自行修复、自行扩大范围或自行批准发布。
- 复审结果只能在对话中交付给Codex小酷。

## 4. 业务与安全规则

- 小M任务必须绝对只读，不得隐含任何结果文件写入权限。
- 小猫任务默认只读；任何写入、commit、push或部署必须逐项明确授权。
- 两个任务都必须正确映射Codex=小酷、mavis/Mavis=小M、coder/Coder=小猫、verifier/Verifier=退役。
- 两个任务都必须明确只有胡子老师可以批准上岗。
- 验证命令不得修改文件、数据库或外部系统。
- 扫描命令的反例文本和任务自指命中必须人工区分，不得直接以命中数下结论。

## 5. 验证命令

```powershell
git status --short --branch
git show --stat --oneline 698e67c
git -c core.quotePath=false diff --name-only 007388e..698e67c
git diff --check 007388e..698e67c
git show 698e67c:docs/roles/tasks/xiaom/todo/小M_001号任务.md
git show 698e67c:docs/roles/tasks/xiaomao/todo/小猫_001号任务.md
```

预期：目标提交仅新增两个任务文件；`git diff --check` 为0；任务权限和现行岗位一致；命令全部只读。

## 6. 完成标准

- 输出“通过”“有条件通过”或“不通过”。
- 逐项检查两个任务的身份映射、允许范围、禁止事项、验证命令和完成标准。
- 明确是否存在自设写权限、错误发布建议、目标范围缺失或危险命令。
- 报告基线、目标、实际文件范围和工作区状态。
- 全程只读，结果只在对话中交付给Codex小酷。
