# 小M_001号任务

## 1. 任务目标

对角色改岗结果做第一次独立只读复审，确认Codex=小酷、mavis/Mavis=小M、coder/Coder=小猫、verifier/Verifier=退役，且旧任务历史完整归档。

## 2. 基线与范围

- 基线提交：`341ebd9`
- 目标提交：`007388e`
- 允许读取：`AGENTS.md`、`docs/roles/**`、`docs/archive/agent-tasks-v70/**`、`docs/superpowers/specs/2026-07-19-codex-mavis-role-remap-design.md`、`docs/superpowers/plans/2026-07-19-codex-mavis-role-remap-plan.md`及Git历史。
- 允许修改：无。

## 3. 禁止操作

- 禁止修改任何文件或使用 `apply_patch`。
- 禁止git add、commit、push、merge、rebase、reset或创建PR。
- 禁止部署、数据库写入、Supabase/Vercel/生产环境操作。
- 禁止自行修复发现的问题。
- 禁止自行宣布项目可发布或自己已上岗。

## 4. 业务与安全规则

- 现行文件中mavis只能对应小M，Codex只能对应小酷，coder只能对应小猫。
- verifier只能以“已退役”出现，不得存在现行任务入口。
- 小M必须保持只读独立，不得拥有实现、提交、推送或部署权限。
- 历史归档正文不得因为新角色映射而改写。
- `src/`、`prisma/`、依赖和数据库不得发生变化。

## 5. 验证命令

```powershell
git status --short --branch
git diff --stat 341ebd9..007388e
git diff --name-only 341ebd9..007388e
rg -n "Codex.*小酷|mavis.*Mavis.*小M|coder.*Coder.*小猫|verifier.*Verifier.*退役" AGENTS.md docs/roles
$activeFiles=@('AGENTS.md')+(Get-ChildItem -LiteralPath 'docs/roles' -Recurse -File | Select-Object -ExpandProperty FullName)
$activeFiles | Select-String -Pattern 'mavis[^\r\n]{0,50}小酷|verifier[^\r\n]{0,50}小M|猫爪|docs/agent-tasks|胡子哥'
Get-ChildItem -LiteralPath 'docs/archive/agent-tasks-v70/catpaw/todo' -File -Filter '*.md' | Measure-Object
Get-ChildItem -LiteralPath 'docs/archive/agent-tasks-v70/catpaw/done' -File -Filter '*.md' | Measure-Object
Get-ChildItem -LiteralPath 'docs/archive/agent-tasks-v70/xiaom/todo' -File -Filter '*.md' | Measure-Object
Get-ChildItem -LiteralPath 'docs/archive/agent-tasks-v70/xiaom/done' -File -Filter '*.md' | Measure-Object
Get-ChildItem -LiteralPath 'docs/archive/agent-tasks-v70/review' -File -Filter '*.md' | Measure-Object
```

预期：非法现行引用扫描为0；归档数量依次为38、38、32、32、1；业务代码和数据库相关路径无变更。

## 6. 完成标准

- 使用 `docs/roles/templates/review.md` 输出“通过”“有条件通过”或“不通过”。
- 结论包含基线、实际范围、独立命令证据和逐项风险。
- 全程只读，工作区没有因复审新增改动。
- 结果先交付给Codex小酷，不自行宣布发布或上岗。
