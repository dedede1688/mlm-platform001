# 小M 005 号任务：复审分红周结 + UI 清理（P 级，2026-07-20）

## 复审任务

### 批次 A：分红周结（P 级，资金 + 数据库结构）

复审以下文件：

1. `prisma/schema.prisma`（Dividend 模型加 settled/settleBatchId/settleDate + 索引）
2. `prisma/migrations/20260720120000_dividend_weekly_settle_fields/migration.sql`
3. `src/lib/services/dividend.service.ts`（拆为每日快照 snapshotDailyDividends + 周结 settleWeeklyDividends + 幂等）
4. `src/lib/utils/cron.ts`（每日快照 + 新增 runWeeklyTasks）
5. `vercel.json`（新增周日 cron）
6. `src/app/api/cron/weekly-tasks/route.ts`（新增周结 cron 路由）
7. `src/app/api/admin/settle-dividends/route.ts`（支持 mode=snapshot/weekly）
8. `src/app/api/cron/daily-tasks/route.ts`（注释更新）
9. `__tests__/services/dividend.test.ts`（19 项周结测试）

重点审查：
- [ ] 幂等：周结是否只发未结算明细？
- [ ] 原子性：settle 与 balance increment 是否同事务？
- [ ] 每日快照是否只生成 dividend 不做入账？
- [ ] migrate 之后旧数据是否会重复触发？
- [ ] cron 调度是否正确（日快照 + 周结）？

### 批次 B：UI 清理（S 级，无资金/权限）

1. `src/app/admin/system/roles/page.tsx`（6 emoji → 纯文字）
2. `src/app/admin/settings/system-parameters/page.tsx`（7 emoji → 纯文字）
3. `src/app/admin/products/page.tsx`（3 处调试 console.log 删除）
4. `src/lib/services/admin.service.ts`（删除 settleDividends 死代码 + 清理 import）
5. `__tests__/services/admin.test.ts`（删除 4 个废弃测试）
6. `src/lib/notification/sendEmail.ts` / `sendSms.ts`（TODO → 站内信-only 说明）
7. `PRD_多级分销电商平台_完整版.md`（品牌管理奖差异说明 + 站内信决策）
8. `AGENTS.md`（新增工作规则）

---

## 小酷独立验证结果（供参考，小M 需独立核验）

- 全量测试：47 文件 928 测试全绿
- Typecheck：0 错误
- Build：进行中（待补）

---

## 输出要求

- 三态：通过 / 有条件通过 / 不通过
- 必须带具体证据（文件路径 + 行号 + 问题描述）
- 资金/权限相关必查项全部覆盖
