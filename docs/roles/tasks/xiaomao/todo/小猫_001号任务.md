# 小猫_001号任务

## 1. 任务目标

完成小猫岗位的只读入职确认，证明能够识别身份、任务六要素、默认权限和停止条件。

## 2. 基线与范围

- 基线提交：`007388e`
- 目标：当前工作区只读检查。
- 允许读取：`AGENTS.md`、`docs/roles/README.md`、`docs/roles/xiaomao/**`、Git状态与历史。
- 允许修改：无。

## 3. 禁止操作

- 禁止修改文件、暂存、提交、推送或部署。
- 禁止修改核心业务、资金、权限或数据库结构。
- 禁止自行扩大范围、作业务决策或改变完成标准。
- 禁止覆盖或清理任务前已有改动。
- 禁止自行宣布上岗。

## 4. 业务与安全规则

- coder / Coder对应小猫，只接受Codex小酷的明确任务。
- mavis / Mavis对应小M，只做独立只读复审。
- verifier / Verifier已退役。
- 只有胡子老师可以批准上岗。
- 遇到范围冲突、验证失败或规则不清时停止并报告小酷。

## 5. 验证命令

```powershell
git status --short --branch
git rev-parse --short HEAD
git rev-parse --short origin/main
Get-Content -Raw -Encoding UTF8 AGENTS.md
Get-Content -Raw -Encoding UTF8 docs/roles/README.md
Get-Content -Raw -Encoding UTF8 docs/roles/xiaomao/system-prompt.md
Get-Content -Raw -Encoding UTF8 docs/roles/xiaomao/job-description.md
Get-Content -Raw -Encoding UTF8 docs/roles/xiaomao/workflow.md
```

预期：命令全部只读；身份映射一致；没有新增工作区改动。

## 6. 完成标准

- 在当前对话中按 `docs/roles/templates/result.md` 格式报告身份、允许与禁止事项，不得写入任何项目文件。
- 明确任务六要素：目标、基线与范围、禁止操作、业务与安全规则、验证命令、完成标准。
- 明确默认不能改核心业务、commit、push或部署。
- 明确冲突或失败时停止并报告小酷。
- 明确结果交给小酷、只有胡子老师可以批准上岗。
