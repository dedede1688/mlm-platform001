# 小猫_002号任务

## 1. 任务目标

清理角色改岗后遗留的旧 `docs/agent-tasks/` 空目录及其子目录，完成角色改岗计划 Task 6 Step 3。

## 2. 基线与范围

- 基线提交：`3cf9093`。
- 目标：当前工作区。
- 允许修改/删除：`docs/agent-tasks/` 及其所有子目录和 `.gitkeep` 文件。
- 允许读取：`AGENTS.md`、`docs/roles/README.md`、`docs/superpowers/plans/2026-07-19-codex-mavis-role-remap-plan.md`、Git 状态与历史。

## 3. 禁止操作

- 禁止修改 `docs/archive/` 下的任何文件（历史归档必须保留）。
- 禁止修改 `docs/roles/` 下的任何文件。
- 禁止修改 `src/`、`prisma/`、业务代码、测试或配置。
- 禁止提交、推送或部署（本任务只清理工作区文件，不触发部署）。
- 禁止覆盖任务开始前已有改动。
- 禁止自行扩大范围到非 `docs/agent-tasks/` 目录。

## 4. 业务与安全规则

- `docs/agent-tasks/` 是角色改岗前的旧任务系统根目录。
- 所有 Markdown 文件已迁移到 `docs/archive/agent-tasks-v70/`（141 份）。
- 旧目录中应只剩空子目录（catpaw/review/templates/xiaoku/xiaom）和 `.gitkeep` 文件。
- 删除前必须确认：旧目录中没有未被归档的 Markdown 文件。
- 删除后必须确认：`docs/archive/agent-tasks-v70/` 中对应文件仍然存在。

## 5. 验证命令

```powershell
# 1. 确认旧目录中无 Markdown 文件
Get-ChildItem -LiteralPath 'docs/agent-tasks' -Recurse -File -Filter '*.md' | Measure-Object

# 2. 确认归档中文件仍然存在（抽查）
Get-ChildItem -LiteralPath 'docs/archive/agent-tasks-v70/catpaw/done' -File -Filter '*.md' | Measure-Object
Get-ChildItem -LiteralPath 'docs/archive/agent-tasks-v70/xiaom/done' -File -Filter '*.md' | Measure-Object

# 3. 确认删除后旧目录已不存在
Test-Path 'docs/agent-tasks'

# 4. 确认 Git 状态干净
git status --short
```

预期：旧目录中 0 个 Markdown；归档中文件数与之前一致；删除后 `Test-Path` 返回 False；Git 状态只有删除记录。

## 6. 完成标准

- `docs/agent-tasks/` 及其所有子目录已完全删除（包括 `.gitkeep`）。
- `docs/archive/agent-tasks-v70/` 中的历史文件完整保留，未受任何影响。
- 其他项目文件（`src/`、`prisma/`、`docs/roles/` 等）未受任何影响。
- Git 状态仅反映 `docs/agent-tasks/` 的删除，无其他改动。
- 结果按 `docs/roles/templates/result.md` 格式向小酷报告。
