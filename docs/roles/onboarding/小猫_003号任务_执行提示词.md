# 小猫 003 号任务：执行提示词

> **角色**：小猫（coder），辅助执行者
> **派单人**：小酷
> **任务级别**：S（无资金/权限/数据库结构变更）
> **任务 ID**：xiaomao-exec-003

---

## 上下文

小酷在 2026-07-20 完成了两批代码改动（分红周结、死代码删除 + UI 清理），所有改动在本地工作区（HEAD `3cf9093`），尚未 commit/push。

你的任务是**机械执行**以下三项，不涉及任何判断、设计或业务决策。

---

## 任务 1：整理本轮改动清单（结构化表格）

### 输入
```bash
cd D:/mlm-platform-source/mlm-platform
git diff HEAD --stat
git diff HEAD --name-only
```

### 输出（按以下分组，Markdown 表格）

```
## 本轮改动清单

### A. 分红周结（P级，8文件）
| 文件 | 新增行 | 删除行 | 说明 |
|------|--------|--------|------|
| prisma/schema.prisma | XX | XX | Dividend 加 settled/settleBatchId/settleDate + 索引 |
| ... | ... | ... | ... |

### B. 死代码删除（2文件）
| 文件 | 新增行 | 删除行 | 说明 |
| ... | ... | ... | ... |

### C. UI 清理（3文件）
| ... | ... | ... | ... |

### D. 站内信终态（2文件）
| ... | ... | ... | ... |

### E. 文档补注（2文件）
| ... | ... | ... | ... |

### F. 新增文件
| 文件路径 | 类型 | 说明 |
| ... | ... | ... |

总计：N 个文件，+X insertions / -Y deletions
```

---

## 任务 2：跑全量测试并报告结果

### 执行
```bash
cd D:/mlm-platform-source/mlm-platform
./node_modules/.bin/vitest run 2>&1
```

### 输出格式
```
## 测试结果

- 测试文件：X passed / Y total
- 测试用例：X passed / Y total
- Duration: X.XXs
- 失败/跳过：无（如有，逐条列出文件名+错误信息）
- 结论：全部通过 / 有 X 项失败
```

---

## 任务 3：更新今日工作日志

### 输入
文件路径：`D:\mlm-platform-source\mlm-platform\.workbuddy\memory\2026-07-20.md`

如果文件存在：在末尾追加本轮记录。
如果文件不存在：创建文件并写记录。

### 追加内容格式
```markdown
## 小猫 003 号任务 — 2026-07-20 晚间

- 整理本轮改动清单（XX 文件，+X/-X 行），详见上方报告。
- 全量测试：XX 文件 XX 测试全部通过。
- 小M_005 号复审任务已派（docs/roles/onboarding/小M_005号任务_复审提示词.md）。
```

---

## 边界规则（铁律，违反了会闯祸）

1. **默认无权**：不得修改任何代码或文件。只读操作（grep、cat、vitest run）。
2. **禁止执行**：不得跑 `git commit`、`git push`、`npm run build`、`prisma migrate` 等命令——这些是小酷的事。
3. **禁止判断**：不要评价代码好坏、不要建议怎么改、不要说"我觉得"。你只需要汇报事实。
4. **输出严格按格式**：不要自作主张加创意、不要换表达方式、不要省略表格列。
5. **遇到异常立即停**：vitest 报错、文件不存在、git 命令失败——立即汇报给派单人（小酷），不要自己尝试修复。

---

## 执行完后

把完整的输出直接回复给小酷。任务完成。
