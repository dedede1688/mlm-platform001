# MLM Platform v60 全量盘点报告

> **盘点时间**: 2026-07-01 12:49
> **执行者**: Mavis
> **触发**: 胡子哥要求"梳理整个项目 + 分析铁律/规则/沟通方式 + 给出报告"
> **当前版本**: v60 step2 (commit `3eff22f`) · 总 commit 数: **188**

---

## 📊 项目画像

| 维度 | 数据 |
|------|------|
| **业务定位** | 多级分销（三三复制）电商平台 — 8 级会员、双轨制（直推 + 安置） |
| **代码规模** | 50 个 admin API + 36 个用户/API 路由 + 14 个 service + 9 个 util + 23 张数据库表 |
| **测试** | 87 passed / coverage **71.85% Stmts**（README badge）/ 70% 强制门槛 / typecheck + lint 双门 |
| **技术栈** | Next.js 15 + React 19 + TS 5.7 + Tailwind + Prisma 6 + Supabase（Postgres）+ JWT + Vercel |
| **UI 库** | TipTap（富文本）+ lucide-react（图标）+ echarts/recharts（图表）+ reactflow（关系图） |
| **状态管理** | zustand（5 stores）+ bcryptjs + jsonwebtoken + zod 校验 |
| **部署** | Vercel 自动部署 main 分支 · Vercel Cron 每日 0:0 触发 `/api/cron/daily-tasks` |
| **部署版本链** | v0.0 → v6/v7 UI 修复 → v17 注册 500 修复 → v43-7 余额 4 字段 → v47 业务 v2 → v50 核心重构 → v52-v54 体验/SQL/通知 → v55.1 积分原子 → v56 改密/转赠 → v57 历史清理 → v60 P/I/G 收尾 |

---

## 🛡️ 铁律全景（13 条实战总结）

> 全部沉淀在 `AGENTS.md`,派单必读。

### 🔵 部署与验证（铁律 1-3）
| # | 铁律 | 来源事故 |
|---|------|----------|
| 1 | **commit + push 成功 ≠ 部署完成** — push 后必跑 `git log origin/main --oneline -1` 对比 Vercel 部署 commit | v6 胡乱部署 30 分钟 |
| 2 | **UI 改动必须本地 dev server 真实截图** — 不能只信 "build 通过" | v6/v7 |
| 3 | **中间件鉴权 + 路由内 verifyPermission 双保险** — 50 个 admin API 全部 100% 双保险 | v55.2 |

### 🔴 业务正确性（铁律 4-6）
| # | 铁律 | 来源事故 |
|---|------|----------|
| 4 | **`$queryRaw` / `$queryRawUnsafe` 是最后手段** — 默认禁止,优先 Prisma 原生 ORM | v17 注册 500 血泪 7 版 |
| 5 | **`$queryRaw` 错误链必须一次修到底** — 修完一层就 push 必爆下一层 | v12 注册 500 五轮迭代 |
| 6 | **支付/订单/库存派单必须走完整链路** — build 0 错 + 看代码 ≠ 业务跑通 | v43-4 verify-payment 100% 失败 |

### 🟡 前端安全（铁律 7-11）
| # | 铁律 | 来源事故 |
|---|------|----------|
| 7 | **新建 admin 页面 fetch 必须含 `Authorization: Bearer ${token}`** — middleware 会拦 | v46.6 发件箱空 |
| 8 | **派单前 6 步检查清单** — 页面存在/有入口/有鉴权/有 await/有 catch/有链路测试 | v46.5-v46.8 |
| 9 | **useEffect 依赖别放 zustand store 函数引用** — 用 `getState()` 拿稳定引用 | v46.10 React #185 死循环 |
| 10 | **所有 admin 状态变更路由必须自动通知用户** — 退款/调账/发货/完成通用模板 | v46.11/v46.12 |
| 11 | **派单前 grep service 方法的所有真实调用入口** — 0 行调用 = 死代码 | v46.10.3 通知死代码 |

### 🟣 工程纪律（铁律 12-13）
| # | 铁律 | 来源事故 |
|---|------|----------|
| 12 | **业务决策以 v2 需求文档为准** — 任何冲突停下来找胡子哥 | 项目伊始 |
| 13 | **所有分析报告必须包含可执行的方案建议** — 纯诊断不给方案 = 半成品 | v60.1 本次报告直接触发 |

---

## 🤝 协作三角

```
┌──────────────────┐  拍板  ┌──────────────────┐
│    胡子哥（用户）  │◄──────►│    mavis（我）    │
│  业务决策 + 验收   │  反馈  │   方案 + 审核     │
└──────────────────┘        └────────┬─────────┘
                                     │ 派单
                                     ▼
                            ┌──────────────────┐
                            │    猫爪（执行者）  │
                            │  改代码+build+push │
                            └──────────────────┘
```

- **派单文档格式**: 4 个反引号 ```` + 单个 markdown 块（反垃圾 prompt 解析）
- **派单前 4 步必做**: grep 枚举 / grep 鉴权 / grep read 函数 / grep 业务冲突
- **执行后必做**: dev server 截图（UI）+ grep 验证（后端）+ git log 验证（部署）

---

## 🧩 完整功能模块清单

### 4.1 用户端（`src/app/` 除 admin/api）
- **首页 / 商品列表 / 商品详情** — TipTap 富文本 + JSON-LD（v52.3）+ 多图轮播（v11+）
- **购物车** — 一单一品 + 积分抵扣
- **登录 / 注册 / 找回密码 / 改密** — JWT + bcryptjs + 6 位验证码（v56.1）
- **Dashboard** — 身份双轨卡（v50 E）+ 4 字段余额卡片 + 积分明细（v58 改版）+ 流水页（v53b）
- **订单流程** — 创建 → 支付密码弹窗（v43-7）→ verify-payment → 发货 → 自动完成（v50 L cron）
- **退款** — 申请 → 审核 → 完成 → 全链路通知（v46.12 + 3c9e0be）
- **提现** — 申请 → 批量审核 → 拒绝理由模板 → 站内信（v46.3.1）
- **收货地址簿** — 省市区三级联动（v43-5）
- **支付密码** — set/update/verify（v43-2）
- **积分转赠** — 手机号 lookup + 手续费预览（v56.2）
- **推荐关系图** — reactflow + dagre 自动布局 + 浮动面板 + 三态聚焦（v18-v42）
- **通知中心** — Header 铃铛（v46.8）+ unread-count + 站内信全模板

### 4.2 后台（`src/app/admin/` 21 个页面）
按角色分（5 角色）:`super_admin` / `goods_admin` / `finance_admin` / `support_admin` / `auditor`

- **仪表盘** — 环比% + 90 天切换（v51.0）
- **运营报表** — 销售/会员/财务 3 tab + CSV 导出（v51.1/v51.2）+ 转化漏斗 5 级（v51.3）+ 内存缓存（v51.5）
- **轮播图 / 分类 / 商品 / 订单** — 批量上下架 + 库存预警（v53.2）+ 商品复制（v5）+ 操作列排版（v7）
- **会员 / 推荐树 / 详情 / 余额 / 流水** — 4 字段调账 + 8 卡片布局（v43-7 Batch 2.2.b）
- **退款** — 审核/完成/拒绝理由 + 全通知（v46.12）
- **财务管理** — 奖励流水 + 提现审核
- **通知模板** — 7 个种子模板 + 通用通知 + 系统公告（v46.3 系列）
- **通知发件箱** — 列表 + 详情 + 阅读统计（v46.5/v46.6）
- **拒绝理由模板** — 提现拒绝模板管理
- **系统设置 / 系统参数** — 33 项配置 7 分组（v50 C）+ 站点信息 / 协议 / FAQ 可视化编辑
- **操作日志** — auditor 角色专属只读

### 4.3 后端 services（14 个）
| Service | 行数 | 职责 |
|---------|------|------|
| `order.service.ts` | 238（精简 67%） | 订单 CRUD,facade 模式 |
| `order-lifecycle.service.ts` | 7 个状态机 | 支付/履约/退款/取消（v50-n-1） |
| `order-notification.service.ts` | 6 个 notify | 站内信/邮件/短信（v50-n-2） |
| `points.service.ts` | 256 | 积分记录 + 解锁调度 + 转赠 |
| `reward.service.ts` | - | 直推/品牌管理（v4）/退款扣回 |
| `dividend.service.ts` | - | **v2 5 级独立池算法**（v50 B） |
| `user.service.ts` | 256 | 升级规则 + 关系绑定 |
| `withdrawal.service.ts` | - | 申请/审核/打款 |
| `notification.service.ts` | - | 7 模板 + 站内信 |
| `product.service.ts` | - | CRUD + 富文本处理 |
| `admin.service.ts` | - | 调账/手动奖励 |
| `auth.service.ts` | - | JWT + bcryptjs |
| `withdrawal-reject-template.service.ts` | - | 拒绝模板 |
| `withdrawal-audit-log.service.ts` | - | 审核日志 |

### 4.4 自动化与定时任务
- **Vercel Cron** `0 0 * * *` → `/api/cron/daily-tasks` → 3 任务链：
  1. `PointsService.dailyUnlock()` 积分每日释放
  2. `DividendService.settleDailyDividends()` 分红结算（5 级独立池）
  3. `OrderLifecycleService.autoCompleteOrders()` 自动确认收货
- **CRON_SECRET 鉴权** — 防止外部恶意触发（v50 L 安全）

---

## 🗄️ 数据库模型（23 张表）

**核心 6 表**:`User` / `Product` / `Category` / `Order` / `OrderItem` / `Cart`

**奖励 + 积分 4 表**:`PointsRecord` / `PointsUnlockSchedule` / `Reward` / `ManualReward`

**财务 5 表**:`BalanceRecord` / `Withdrawal` / `WithdrawalAuditLog` / `WithdrawalRejectTemplate` / `Dividend`

**系统 4 表**:`SystemConfig`（单例 key='site_settings'）/ `LevelSnapshot` / `OperationLog` / `banners`（PG RLS）

**用户相关 3 表**:`Address` / `PasswordResetCode` / `Notification`

**通知 2 表**:`NotificationTemplate` / `NotificationBatch`

**关键索引**:User（referrerId/parentId/level/status）、Order（userId/status/orderNo）、Product（status/isUpgradeProduct/categoryId）、Notification（userId/isRead/createdAt/batchId）

---

## 🔍 TODO / FIXME 实际扫描

| 文件 | 行 | 内容 | 评估 |
|------|----|------|------|
| `src/lib/notification/sendSms.ts:44` | - | `TODO: 对接真实短信发送服务` | ⚠️ 真实未做,目前是 mock |
| `src/lib/notification/sendEmail.ts:46` | - | `TODO: 对接真实邮件发送服务` | ⚠️ 真实未做,目前是 mock |

**就这 2 个！** 其他搜出来的"粤ICP备XXXXXXXX号"是数据库默认值占位符,不是 TODO。

> 💡 **生产化前必做项**: 短信 + 邮件真实通道对接（阿里云/腾讯云/Twilio）。

---

## 🚨 重点发现：`docs/项目清单.md` 严重过期 → 已重写

**项目清单**最后更新 **2026-06-26**,而 v57-v60 都在 6 月底 ~ 7 月初完成。**12 个待办任务实际状态**:

| 任务 | 文档状态 | **实际验证（2026-07-01）** | commit |
|------|---------|------------------------|--------|
| **A** 团队奖清理 | ❌ 未做 | ✅ 已完成（v47 业务 v2 一并清） | `878f1c6` |
| **B** 分红 5 级独立池 | ❌ 未做 | ✅ **已重写**（`dividend.service.ts:67/92/145/197/250/283` 全 v2 注释） | `e2bf304` v50 B |
| **C** v2 22 项配置 | 🟡 4/22 | ✅ **33 项 / 7 分组**（`system-parameters.ts` 完整实现） | `8b3dda9` v50 C |
| **D** 释放规则 % + 天数自动算 | ❌ 未做 | ✅ **已实现**（`user.service.ts:153-156` 读 `upgrade.daily_unlock_rate` + `Math.ceil(1/rate)`） | `8fc5e3a` v54 |
| **E** 前端会员双轨制 UI | 🟡 | ✅ 已完成（身份卡片 + Toast） | `90f1f64` v50 E |
| **F** 推荐奖未解锁 Toast | 🟡 | ✅ **5 个 hook 全实现** + 后端透传 | `218d2cf` |
| **G** 品牌管理奖 v4 细节优化 | 🟢 80% | ✅ **A 是会员时跳过 A**（细节收尾） | `d000b53` v60 step1-3 |
| **H** 升级品订单计入销售额 | 🟡 待验证 | ✅ 已实现（v54 D+H 闭环） | `8fc5e3a` v54 |
| **I** description 4 字段标签 | ❌ 未做 | ✅ **已补齐**（v50 I 10 处 + v60 step2 补 verify-payment 等） | `9a232ca` + `3eff22f` |
| **J** 角色权限白名单 | 🟡 | ✅ 100% 全部 verifyPermission 用具体角色 | `dbbca22` |
| **K** 支付密码强制生效 | 🟡 80% | ✅ **pay 路由已删,3 入口全切 verify-payment** | `0be90dc` v50.1-K |
| **L** 自动确认收货 cron | ❌ 未做 | ✅ **vercel.json + /api/cron/daily-tasks 全挂载** | `0e311c2` v50 L |
| **M** 退款全流程通知 | 🟡 67% | ✅ **退款申请通知也加了**（refund_submitted） | `455e77f` + `3c9e0be` |
| **N** OrderService 拆分 | ❌ 未做 | ✅ **lifecycle + notification 双拆分完成**（720+ → 238 行） | `85a7b36` + `efab44c` |
| **O** 收益明细过滤 + setInterval | 🟡 1/20+ | ✅ **过滤 refunded + setInterval 全局扫描通过** | `dd35842` + `8fc5e3a` |
| **P** earnings_void 独立 type | ❌ 未做 | ✅ **6 种 type + earningsVoided 独立记账** | `d000b53` v60 step1-3 |

### 🎯 结论：**12/12 全完成**

> `docs/项目清单.md` 已于本次同步重写（v60.1）至 2026-07-01 实际状态。

---

## 📈 项目健康度评估

| 维度 | 评分 | 说明 |
|------|------|------|
| **代码质量** | 🟢 优 | typecheck/lint/coverage 三门强制,71.85% 覆盖 |
| **架构清晰度** | 🟢 优 | OrderService 拆分 + 14 service 职责清晰 + facade 模式 |
| **鉴权完整度** | 🟢 100% | 50 个 admin API 全双保险,middleware JWT 真签（v55.2） |
| **业务正确性** | 🟢 优 | 业务 v2 终版落地 + 4 字段余额 + 5 级独立池 + 13 条铁律 |
| **测试覆盖** | 🟡 中 | 71.85% Stmts 但 Branches 仅 62.06%（README badge 没体现） |
| **文档同步** | 🔴 差 → ✅ 修 | 项目清单已重写,docs 中文文件名待 git mv |
| **TODO 处理** | 🟢 优 | 仅 2 处真实未对接（短信/邮件）,其他全清 |
| **CI/CD** | 🟢 优 | GitHub Actions + Vercel + typecheck/lint/coverage 三门 |
| **部署可靠性** | 🟢 优 | 13 条铁律保驾（commit/push 验证 + 截图） |

---

## ⚠️ 当前真正的待办 / 风险

### 🟡 P1 — 真实未完成
1. **短信 / 邮件真实通道对接**（`sendSms.ts:44` / `sendEmail.ts:46`） — 生产化前必做
2. **docs 目录中文文件名** — PowerShell 终端显示乱码（git 内部是正常 UTF-8,需要 git mv 重命名为 ASCII）

### 🟢 P2 — 优化建议
3. **测试分支覆盖率** — 整体 Branches 62%,距离 70% 还有距离,补 reward/order/withdrawal 分支测试
4. **历史调试脚本清理** — `scripts/` 13 个,部分是 v57 配套一次性脚本,部分可归档
5. **临时目录清理** — `tmp/`、`~暂存-DEEPSEEK`、`.arts/.codeflicker/.dumate/.joycode/.lingma/.trae` 等 IDE 配置目录 — 不影响功能,但显乱

### 🔵 战略性建议
6. **完整业务链路压测** — 188 commits + 13 铁律 + 71.85% 覆盖,但**没有真实 E2E**：
   - 用户注册 → 上传头像 → 推 3 个下线 → 买升级品 → 自动晋升经销商 → 触发 5 级独立池分红 → 提现 → 通知
   - 建议用 Playwright 跑 1 条 happy path 作为最后验收
7. **业务规则 v2 文档与代码双重核对** — 关键算法（dividend 5 池 / reward v4）需要规格说明书与代码 1:1 对应
8. **备份与灾备** — Supabase PostgreSQL 备份策略没在仓库体现

---

## 🎯 执行方案建议（v60.1 即刻落地）

### 🔴 P0 — mavis 立刻执行（本次 commit）

| # | 动作 | 工时 | 状态 |
|---|------|------|------|
| 1 | 铁律 13 写入 `AGENTS.md`（所有分析必含执行方案） | 5 分钟 | ✅ 本次已完成 |
| 2 | 重写 `docs/项目清单.md` 到 2026-07-01 实际状态 | 10 分钟 | ✅ 本次已完成 |
| 3 | 创建本报告 `docs/v60-盘点报告.md`（落盘） | 5 分钟 | ✅ 本次已完成 |
| 4 | `docs/` 顶层 4 个中文文件名 `git mv` 重命名为 ASCII | 5 分钟 | ⏳ 本次进行中 |
| 5 | `git add .` + `commit` + `push` + `git log origin/main` 验证（铁律 1） | 5 分钟 | ⏳ 本次进行中 |

**P0 整体工时**: 约 30 分钟 · **本 commit**:`docs/项目治理-v60.1-全量盘点收尾.md`

### 🟡 P1 — 近期执行（需胡子哥拍板）

| # | 动作 | 依赖 | 建议时间 |
|---|------|------|---------|
| 1 | ~~短信真实通道选型~~ | ❌ **胡子哥 2026-07-01 拍板不做**（只做站内信） | - |
| 2 | ~~邮件真实通道选型~~ | ❌ **胡子哥 2026-07-01 拍板不做**（只做站内信） | - |
| 3 | Playwright 跑 1 条 E2E happy path（注册→下单→分红→提现） | mavis 写脚本 + 胡子哥提供测试账号 | 1 周内 |
| 4 | 测试 Branches 覆盖率补到 70% | mavis 补 reward/order/withdrawal 分支 | 2 周内 |

> **2026-07-01 决策记录**:`sendSms.ts:44` / `sendEmail.ts:46` 两个 TODO **保持现状不实现**。站内信（v46.4-v46.12 全链路）已覆盖业务通知需求,等真实短信/邮件业务出现时再启动选型。详见 `docs/business-rules-v2-spec.md` §11.2。

### 🟢 P2 — 战略规划（下一轮讨论）

| # | 动作 | 备注 |
|---|------|------|
| 1 | 写一份《业务规则 v2 完整规格书》与代码对照 | 防业务知识丢失,优先级中 |
| 2 | 清理 `scripts/` + `tmp/` + IDE 配置目录（`.arts`/`.codeflicker` 等） | 代码整洁 |
| 3 | Supabase PostgreSQL 备份与灾备方案 | 生产化前必做 |
| 4 | 派单工具自动化（Mavis 派单 + 猫爪执行 + 自动 review） | 长期目标 |

---

## 📅 时间线（v60.1 收尾）

- **2026-07-01 12:49** — 胡子哥要求全量盘点 + 分析铁律
- **2026-07-01 12:55** — 报告完成,胡子哥要求"根据计划执行 + 以后报告必含执行方案"
- **2026-07-01 13:00** — 铁律 13 写入 AGENTS.md
- **2026-07-01 13:05** — docs/项目清单.md 重写完成
- **2026-07-01 13:10** — docs/v60-盘点报告.md 创建完成
- **2026-07-01 13:15** — docs/ 中文文件名 git mv + commit + push + 验证远程 hash

---

**报告完。v60.1 文档治理 commit 即将完成。**