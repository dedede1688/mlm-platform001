# v52 升级规则 + 审计（Bug A + B + C）

> **派单时间**：2026-06-23 12:20
> **执行 AI**：小金（GLM-5.1）
> **改动文件**：4 个业务代码
> **改动量**：约 80-120 行
> **风险**：低（逻辑修复 + 审计日志，不影响业务主流程）

---

## 0. 任务背景

胡子哥（2026-06-23 12:00-12:15）发现 3 个 bug：

1. **Bug A**：`directDistributorCount` 字段名误导 — 升级到"董事"也 +1，UI 显示"直推经销商"但实际是"直推升级者"
2. **Bug B**：超管用户 8000 显示"董事" — admin 不该走 level 字段
3. **Bug C**：`/api/admin/users/[id]` PUT 改 level 没审计日志 — 查不到"谁在什么时候改了谁的等级"

**Mavis 验证**（已查代码 + 截图）：
- v47 业务规则 + seed.ts：董事升级需要 `directSalesAmount >= 1000000`（100 万）
- 8001 累计收益才 ¥150 → 不可能自动升董事
- 8000/8001/8002 升董事必然是手动改的（admin 改 level 时没审计）

**v52 范围**：
- **A**：`user.service.ts:168` 触发条件修复（只升"经销商"时 +1）
- **B**：`/admin/users` 列表对超管不显示"等级"列
- **C**：`admin.service.ts:130` + `/api/admin/users/[id]/route.ts:125` 加 `logOperation`

---

## 1. 任务步骤（小金执行）

### 1.1 步骤 1：读相关代码 + 写小测试

```powershell
# 1. 读 user.service.ts 找 line 168 附近
Get-Content "D:\mlm-platform-source\mlm-platform\src\lib\services\user.service.ts" -TotalCount 200 | Select-Object -Skip 100

# 2. 读 admin.service.ts 找 updateUserLevel
Get-Content "D:\mlm-platform-source\mlm-platform\src\lib\services\admin.service.ts" | Select-String -Pattern "updateUserLevel" -Context 5,5

# 3. 读 admin/users/[id]/route.ts PUT
Get-Content "D:\mlm-platform-source\mlm-platform\src\app\api\admin\users\[id]\route.ts" | Select-String -Pattern "level" -Context 3,3
```

### 1.2 步骤 2：修复 Bug A — `user.service.ts` 改触发条件

**当前代码**（line 168）：
```ts
if (user.referrerId && newLevel >= MEMBER_LEVELS.DISTRIBUTOR && user.level < MEMBER_LEVELS.DISTRIBUTOR) {
  await prisma.user.update({
    where: { id: user.referrerId },
    data: { directDistributorCount: { increment: 1 } },
  })
}
```

**改为**：
```ts
// v52 Bug A 修复：只升级到"经销商"时 +1（升级到更高等级不算）
if (user.referrerId && newLevel === MEMBER_LEVELS.DISTRIBUTOR && user.level < MEMBER_LEVELS.DISTRIBUTOR) {
  await prisma.user.update({
    where: { id: user.referrerId },
    data: { directDistributorCount: { increment: 1 } },
  })
}
```

**改动**：把 `>=` 改成 `==`（只在新等级等于 DISTRIBUTOR 时 +1）

### 1.3 步骤 3：修复 Bug C-1 — `admin.service.ts` 加审计

**当前代码**（line 130-135）：
```ts
static async updateUserLevel(userId: string, newLevel: number) {
  return prisma.user.update({
    where: { id: userId },
    data: { level: newLevel },
  })
}
```

**改为**：
```ts
static async updateUserLevel(userId: string, newLevel: number, operatorId?: string) {
  // v52 Bug C 修复：记录操作日志
  const oldUser = await prisma.user.findUnique({
    where: { id: userId },
    select: { level: true },
  })

  const result = await prisma.user.update({
    where: { id: userId },
    data: { level: newLevel },
  })

  if (operatorId && oldUser) {
    await logOperation({
      userId: operatorId,
      action: 'UPDATE',
      module: 'user',
      targetId: userId,
      oldValue: { level: oldUser.level },
      newValue: { level: newLevel },
    })
  }

  return result
}
```

**顶部 import** 加：
```ts
import { logOperation } from '@/lib/utils/operation-log'
```

### 1.4 步骤 4：修复 Bug C-2 — `admin/users/[id]/route.ts` 加审计

**当前代码**（line 122-142）：
```ts
// 更新等级
const updatedUser = await prisma.user.update({
  where: { id },
  data: { level: newLevel },
  select: { ... },
})
```

**改为**：
```ts
// v52 Bug C 修复：记录操作日志
const oldUser = await prisma.user.findUnique({
  where: { id },
  select: { level: true },
})

const updatedUser = await prisma.user.update({
  where: { id },
  data: { level: newLevel },
  select: { ... },
})

await logOperation({
  userId: admin.id,
  action: 'UPDATE',
  module: 'user',
  targetId: id,
  oldValue: { level: oldUser?.level },
  newValue: { level: newLevel },
})
```

**顶部 import** 加：
```ts
import { logOperation } from '@/lib/utils/operation-log'
```

### 1.5 步骤 5：修复 Bug B — `admin/users/page.tsx` 超管不显示等级

**当前代码**：表格列定义里有"等级"列（搜索 `等级` 找到对应的 `<th>` 和 `<td>`）

**改为**：

**找到等级列的 `<th>` 标记**（约 line 400-500 区域），包一层条件渲染：

```tsx
{/* 等级列：超管不显示 */}
<th className="...">等级</th>
```

改为：

```tsx
{/* v52 Bug B 修复：超管不显示等级列 */}
{user.role !== 'admin' && user.role !== 'super_admin' && (
  <th className="...">等级</th>
)}
```

对应的 `<td>` 也包同样条件。

**注意**：必须在 `UserRow` 类型加 `role: string` 字段（line 30 附近）。如果没加，**先加**：

```ts
interface UserRow {
  // ... 现有字段 ...
  role: string  // v52 新增
}
```

**API 返回**也要确认 `/api/admin/users` 路由返回 user 列表时包含 `role` 字段。如果没有，**也要修**（`select` 加 `role: true`）。

### 1.6 步骤 6：build 验证

```powershell
cd D:\mlm-platform-source\mlm-platform
pnpm build
```

**判断**：
- ✅ build 通过 + 0 错误 → 进入步骤 7
- ❌ build 失败（TS 类型错误）→ 修代码 → 重新 build

### 1.7 步骤 7：commit + push

```powershell
git add -A
git commit -m "v52: 升级规则修复 + 审计日志（Bug A/B/C）

- Bug A: directDistributorCount 只在升级到经销商时 +1
- Bug B: 超管不显示等级列
- Bug C: admin 改 level 加 logOperation 审计"

git push origin main

# ⚠️ 关键：验证远程 commit（铁律 1）
git log origin/main --oneline -1
# 必须显示你刚 push 的 commit hash
```

### 1.8 步骤 8：Vercel 部署验证

打开 https://vercel.com/dashboard → `mlm-platform001` → Deployments
**判断**：
- ✅ 最新部署的 commit hash = 你刚 push 的 commit → Status: Ready
- ❌ 不一致 → 重新 push

### 1.9 步骤 9：测试（胡子哥验收前你做基本 sanity check）

**测试 Bug A**：
- 在 Supabase 查 8001.directDistributorCount 当前值（截图前）
- 手动跑 SQL 模拟：让 8001 升"董事"（level=7），看 referrer（无）是否被 +1
- 不应该 +1（v52 修复后只有升"经销商" level=2 才 +1）

**测试 Bug B**：
- 访问 /admin/users 列表
- 看 8000（超管）的行 → 等级列应该**不显示**（或显示"-"）
- 看 8001/8002 的行 → 等级列正常显示"董事"

**测试 Bug C**：
- 管理员登录 → 修改某个用户的 level（比如从 1 改成 2）
- 查 Supabase `operation_log` 表，看是否有新记录（`module='user'`, `action='UPDATE'`, `oldValue.level=1`, `newValue.level=2`）

---

## 2. 完成后告诉胡子哥

- commit hash
- Vercel 部署状态（commit hash + Ready）
- 4 个文件的具体改动行数
- Bug A/B/C 各自的验证截图或说明

## 完成后告诉胡子哥
