# v43-7 Batch 2：admin_adjust 管理员调账 BalanceRecord 流水

> **派单时间**：2026-06-21 00:48
> **执行 AI**：小金（GLM-5.1）
> **改动文件**：1 个（`src/app/api/admin/users/[id]/balance/route.ts`）
> **改动量**：~50 行新增

---

## 0. 任务
改 `src/app/api/admin/users/[id]/balance/route.ts`，在事务内加 BalanceRecord 写入。
**同时** 修复章节 6 标记的并发风险（`findUnique + update` → `updateMany + where`）。

参考 v43-7 设计文档：
- 章节 3 写入点 4（admin_adjust 字段值）
- 章节 6 风险点 3（并发防护）

---

## 1. 完整改动代码（JoyCode 编辑文件）

**改前先读**：`src/app/api/admin/users/[id]/balance/route.ts` 整个文件
**参考**：v43-7 章节 3 写入点 4 字段表

### 改动 1：事务部分（约第 47-72 行）

**原代码**（事务内）：
```typescript
const result = await prisma.$transaction(async (tx) => {
  const user = await tx.user.findUnique({ where: { id } })

  if (!user || user.status === 'deleted') {
    throw new Error('用户不存在')
  }

  // 扣减时检查是否会导致负数
  const currentVal = field === 'balance' ? user.balance : user.frozenBalance
  if (amount < 0 && currentVal + amount < 0) {
    const fieldLabel = field === 'balance' ? '余额' : '冻结余额'
    throw new Error(`${fieldLabel}不足，当前 ${currentVal}，扣减 ${Math.abs(amount)}`)
  }

  // 原值记录日志用
  const oldValue = { [field]: currentVal }

  const updated = await tx.user.update({
    where: { id },
    data: { [field]: { increment: amount } },
  })

  return { updated, oldValue, field }
})
```

**改为**：
```typescript
const result = await prisma.$transaction(async (tx) => {
  // 步骤 1：查旧值（v43-7 统一规则）
  const before = await tx.user.findUnique({ where: { id } })
  if (!before || before.status === 'deleted') {
    throw new Error('用户不存在')
  }

  // 步骤 2：变更（用 updateMany + where 防并发透支）
  const result = await tx.user.updateMany({
    where: {
      id,
      [field]: amount < 0 ? { gte: Math.abs(amount) } : undefined,
    },
    data: { [field]: { increment: amount } },
  })
  if (result.count === 0) {
    const fieldLabel = field === 'balance' ? '余额' : '冻结余额'
    throw new Error(`${fieldLabel}不足`)
  }

  // 步骤 3：用旧值+变动量计算新值（不依赖 update 后返回值）
  const newBalance = field === 'balance' ? before.balance + amount : before.balance
  const newFrozenBalance = field === 'frozenBalance' ? before.frozenBalance + amount : before.frozenBalance

  // 步骤 4：写 BalanceRecord
  const oldValue = { [field]: field === 'balance' ? before.balance : before.frozenBalance }
  await tx.balanceRecord.create({
    data: {
      userId: id,
      type: 'admin_adjust',
      amount,
      balance: newBalance,
      frozenBalance: newFrozenBalance,
      sourceType: 'admin',
      sourceId: admin.id,
      description: `管理员调账：${field === 'balance' ? '余额' : '冻结余额'}${amount > 0 ? '增加' : '扣减'} ¥${Math.abs(amount).toFixed(2)}，原因：${reason}`,
    },
  })

  const updated = await tx.user.findUnique({ where: { id } })
  return { updated, oldValue, field }
})
```

### 改动 2：返回值（result 后面用 newBalance 的部分）

注意：现在 `result.oldValue` 已经是旧值（before 时的值），不用再算 currentVal。
result.updated 已经是新值（after 时的值）。

```typescript
// 第 86-96 行（原代码）保持不变
```

---

## 2. 字段值参考

| BalanceRecord 字段 | 值 |
|---|---|
| userId | id（被调账用户） |
| type | 'admin_adjust' |
| amount | amount（直接用，正/负看方向） |
| balance | 变动后可用余额（before.balance + amount 或 before.balance） |
| frozenBalance | 变动后冻结余额（before.frozenBalance + amount 或 before.frozenBalance） |
| sourceType | 'admin' |
| sourceId | admin.id（不是 OperationLog id，logOperation 在事务外写） |
| description | 管理员调账描述，含字段名 + 方向 + 金额 + 原因 |

---

## 3. 验证（JoyCode PowerShell）

```powershell
npx next build 2>&1 | Select-String "Checking validity|error TS|Failed"
```

**期望**：看到 `✓ Checking validity of types` 通过，无 error TS 或 Failed。
**已知**：可能有预存 prerender 错误，与本次改动无关，可忽略。

---

## 4. git 三连 + 推送

```powershell
git add src/app/api/admin/users/[id]/balance/route.ts
git commit -m "feat(admin): 管理员调账 BalanceRecord 流水（v43-7 Batch 2）"
git push origin main
git log origin/main --oneline -1
```

**判断**：
- ✅ 看到新 commit hash + "feat(admin)..." → push 成功
- ❌ 看到旧 hash → push 静默失败，重跑 push

---

## 5. 完成后告诉胡子哥

格式：
> Batch 2 完成，commit hash: xxx，build 类型检查通过，等真实链路测试。

---

## 6. 严格禁止

| 禁止项 |
|---|
| ❌ 用 $queryRawUnsafe |
| ❌ 改 balance/route.ts 之外的文件 |
| ❌ 漏写 BalanceRecord |
| ❌ 改 schema.prisma（schema 已经够用） |
| ❌ 假装成功（不实际跑 build） |

---

## 7. 预计耗时

- 改代码：10-15 分钟
- 验证 + commit + push：5 分钟
- **总计 15-20 分钟**

---

## 8. 链路测试（胡子哥做）

待 Batch 2 commit + push 后，胡子哥：
1. admin/users → 找用户 13800138001 → 进详情
2. 资金调整 section → type=balance, amount=10, reason="Batch 2 测试增加"
3. 提交 → 验证 balance 增加 10，BalanceRecord 写了 admin_adjust

之后我给小金步骤 5 查 balance_records。