# v45.4 修复 CI 测试 7 个失败（withdrawal 4 + order 3 mock 不全）

> **派单时间**：2026-06-22 17:55
> **执行 AI**：小金（GLM-5.1）
> **触发原因**：v45.3 CI workflow 跑通（commit c1e1d25），测试报告 47 用例 40 ✅ / 7 ❌
> **改动文件**：2 个（`__tests__/services/withdrawal.test.ts` + `__tests__/services/order.test.ts`）
> **改动量**：约 15-20 行新增 / 5-8 行修改

---

## 0. 任务背景

v45.3 commit `c1e1d25` 集成 GitHub Actions CI（`.github/workflows/test.yml`），trigger `push/main`，跑 `pnpm test`。

CI 第一次跑测试报告：
- ✅ 40 通过（reward 16 + dividend 5 + admin 4 + points 11 + 4 个边界用例）
- ❌ 7 失败（withdrawal 4 + order 3）
- 两个测试文件 mockPrisma 缺 `balanceRecord` 链 + 缺 `user.findUnique` / `user.updateMany` 事务内 mock

**根因（不是业务代码 bug，是测试代码 mock 不全）**：
- 这 2 个文件 v44 测试覆盖时**根本没写进**（v44 只写了 reward/dividend/admin 3 个文件）
- v45.2 加 points.test.ts 时也没碰这 2 个文件
- 本地从来没跑过完整测试套件，CI 第一次跑就暴露 mock 漏洞
- 业务代码 `withdrawal.service.ts` / `order.service.ts` 完全正确，v43-7 BalanceRecord 修复已上线 16 个 release 都没出过问题

---

## 1. 完整改动（小金编辑文件）

### 改动 1：`__tests__/services/withdrawal.test.ts`

#### 1.1 加 `balanceRecord` 到 mockPrisma（约第 16-21 行）

**原代码**：
```typescript
const mockPrisma: any = {
  user: createMockChain(),
  withdrawal: createMockChain(),
  $transaction: vi.fn(),
  $queryRaw: vi.fn(),
}
```

**改为**：
```typescript
const mockPrisma: any = {
  user: createMockChain(),
  withdrawal: createMockChain(),
  balanceRecord: createMockChain(),
  $transaction: vi.fn(),
  $queryRaw: vi.fn(),
}
```

> **原因**：业务代码 `withdrawal.service.ts:37` 有 `tx.balanceRecord.create(...)`，mock 缺这个调用会报 `is not a function`。

#### 1.2 修 `should create withdrawal successfully with sufficient balance`（约第 36-49 行）

**原代码**：
```typescript
it('should create withdrawal successfully with sufficient balance', async () => {
  prisma.$queryRaw.mockResolvedValueOnce([{ count: 1 }])
  prisma.withdrawal.create.mockResolvedValueOnce({
    id: 'w1', userId: 'u1', amount: 100, status: 'pending',
  })

  const result = await WithdrawalService.createWithdrawal('u1', 100)
  expect(result).toBeDefined()
  expect(result.amount).toBe(100)
  expect(result.status).toBe('pending')
})
```

**改为**：
```typescript
it('should create withdrawal successfully with sufficient balance', async () => {
  // 事务外：用户存在 + 余额够
  prisma.user.findUnique.mockResolvedValueOnce({
    id: 'u1', balance: 1000, frozenBalance: 0,
  })
  // 事务内：updateMany 成功扣减
  prisma.user.updateMany.mockResolvedValueOnce({ count: 1 })
  // 事务内：创建提现记录
  prisma.withdrawal.create.mockResolvedValueOnce({
    id: 'w1', userId: 'u1', amount: 100, status: 'pending',
  })
  // 事务内：写 BalanceRecord
  prisma.balanceRecord.create.mockResolvedValueOnce({})

  const result = await WithdrawalService.createWithdrawal('u1', 100)
  expect(result).toBeDefined()
  expect(result.amount).toBe(100)
  expect(result.status).toBe('pending')
})
```

> **根因**：业务代码 `withdrawal.service.ts:9` 是 `await prisma.user.findUnique(...)`，第 13 行 `if (!user) throw new Error('用户不存在')`。测试没 mock 它 → undefined → 抛错。
> **错误信息**：`Error: 用户不存在 at src/lib/services/withdrawal.service.ts:13:22`

#### 1.3 修 `should throw error with insufficient balance`（约第 51-56 行）

**原代码**：
```typescript
it('should throw error with insufficient balance', async () => {
  prisma.$queryRaw.mockResolvedValueOnce([]) // No rows updated = insufficient balance

  await expect(WithdrawalService.createWithdrawal('u1', 10000))
    .rejects.toThrow('余额不足')
})
```

**改为**：
```typescript
it('should throw error with insufficient balance', async () => {
  // 用户存在但余额不够（业务代码 line 14 先校验一次）
  prisma.user.findUnique.mockResolvedValueOnce({
    id: 'u1', balance: 100, frozenBalance: 0,
  })

  await expect(WithdrawalService.createWithdrawal('u1', 10000))
    .rejects.toThrow('余额不足')
})
```

> **根因**：原代码 mock `$queryRaw.mockResolvedValueOnce([])` 是错误的，业务代码用 `tx.user.updateMany` 不是 `$queryRaw`。正确做法：mock user.balance=100 < amount=10000，让业务代码 line 14 `if (user.balance < amount) throw` 抛错。

#### 1.4 修 `should approve withdrawal and decrease frozenBalance`（约第 68-83 行）

**原代码**：
```typescript
it('should approve withdrawal and decrease frozenBalance', async () => {
  prisma.withdrawal.findUnique.mockResolvedValueOnce({
    id: 'w1', userId: 'u1', amount: 100, status: 'pending',
  })
  prisma.user.update.mockResolvedValueOnce({})
  prisma.withdrawal.update.mockResolvedValueOnce({
    id: 'w1', status: 'approved',
  })

  const result = await WithdrawalService.reviewWithdrawal('w1', true)
  expect(result.status).toBe('approved')
})
```

**改为**：
```typescript
it('should approve withdrawal and decrease frozenBalance', async () => {
  // 事务外：查提现记录
  prisma.withdrawal.findUnique.mockResolvedValueOnce({
    id: 'w1', userId: 'u1', amount: 100, status: 'pending',
  })
  // 事务内：查用户余额（balance + frozenBalance 用于 BalanceRecord）
  prisma.user.findUnique.mockResolvedValueOnce({
    balance: 500, frozenBalance: 100,
  })
  // 事务内：原子扣减 frozenBalance
  prisma.user.updateMany.mockResolvedValueOnce({ count: 1 })
  // 事务内：更新提现状态
  prisma.withdrawal.update.mockResolvedValueOnce({
    id: 'w1', status: 'approved',
  })
  // 事务内：写 BalanceRecord
  prisma.balanceRecord.create.mockResolvedValueOnce({})

  const result = await WithdrawalService.reviewWithdrawal('w1', true)
  expect(result.status).toBe('approved')
})
```

> **根因**：业务代码 `withdrawal.service.ts:68`（在事务内）调 `tx.user.findUnique(...)`，mock 序列没这个 → undefined → line 72 抛"用户不存在"。
> **错误信息**：`Error: 用户不存在 at src/lib/services/withdrawal.service.ts:72:26`

#### 1.5 修 `should reject withdrawal and refund balance`（约第 85-100 行）

**原代码**：
```typescript
it('should reject withdrawal and refund balance', async () => {
  prisma.withdrawal.findUnique.mockResolvedValueOnce({
    id: 'w1', userId: 'u1', amount: 100, status: 'pending',
  })
  prisma.user.update.mockResolvedValueOnce({})
  prisma.withdrawal.update.mockResolvedValueOnce({
    id: 'w1', status: 'rejected',
  })

  const result = await WithdrawalService.reviewWithdrawal('w1', false, '信息不完整')
  expect(result.status).toBe('rejected')
})
```

**改为**：
```typescript
it('should reject withdrawal and refund balance', async () => {
  prisma.withdrawal.findUnique.mockResolvedValueOnce({
    id: 'w1', userId: 'u1', amount: 100, status: 'pending',
  })
  // 事务内：查用户余额
  prisma.user.findUnique.mockResolvedValueOnce({
    balance: 500, frozenBalance: 100,
  })
  // 事务内：原子退回余额 + 扣减冻结余额
  prisma.user.updateMany.mockResolvedValueOnce({ count: 1 })
  // 事务内：更新提现状态
  prisma.withdrawal.update.mockResolvedValueOnce({
    id: 'w1', status: 'rejected',
  })
  // 事务内：写 BalanceRecord
  prisma.balanceRecord.create.mockResolvedValueOnce({})

  const result = await WithdrawalService.reviewWithdrawal('w1', false, '信息不完整')
  expect(result.status).toBe('rejected')
})
```

> **根因**：和 1.4 一样，事务内 `tx.user.findUnique` 没 mock。
> **错误信息**：`Error: 用户不存在 at src/lib/services/withdrawal.service.ts:113:26`

---

### 改动 2：`__tests__/services/order.test.ts`

#### 2.1 加 `balanceRecord` 到 mockPrisma（约第 14-23 行）

**原代码**：
```typescript
const mockPrisma: any = {
  user: createMockChain(),
  product: createMockChain(),
  order: createMockChain(),
  reward: createMockChain(),
  pointsRecord: createMockChain(),
  $transaction: vi.fn(),
  $queryRaw: vi.fn(),
}
```

**改为**：
```typescript
const mockPrisma: any = {
  user: createMockChain(),
  product: createMockChain(),
  order: createMockChain(),
  reward: createMockChain(),
  pointsRecord: createMockChain(),
  balanceRecord: createMockChain(),
  $transaction: vi.fn(),
  $queryRaw: vi.fn(),
}
```

> **原因**：`order.service.ts:192` 有 `tx.balanceRecord.create(...)`。

#### 2.2 修 `should create order successfully`（约第 62-117 行）

**原代码**：
```typescript
it('should create order successfully', async () => {
  // Mock user
  prisma.user.findUnique.mockResolvedValueOnce({
    id: 'u1', level: 1, unlockedPoints: 100,
  })
  // Mock products
  prisma.product.findMany.mockResolvedValueOnce([{
    id: 'p1', name: 'Test Product', memberPrice: 100, retailPrice: 120, stock: 10, maxPointsRatio: 50,
  }])
  // Transaction: re-check products
  prisma.product.findMany.mockResolvedValueOnce([{
    id: 'p1', name: 'Test Product', stock: 10,
  }])
  // Transaction: re-check user
  prisma.user.findUnique.mockResolvedValueOnce({
    id: 'u1', level: 1, unlockedPoints: 100, totalPoints: 200, lockedPoints: 100,
  })
  // Create order
  prisma.order.create.mockResolvedValueOnce({
    id: 'o1', orderNo: 'ORD123', userId: 'u1', totalAmount: 100, payAmount: 100, status: 'pending',
    items: [{ productId: 'p1', quantity: 1, unitPrice: 100, totalPrice: 100 }],
  })
  // Stock deduction
  prisma.$queryRaw.mockResolvedValueOnce([{ count: 1 }])  // ← 错误！

  const result = await OrderService.createOrder({
    userId: 'u1', items: [{ productId: 'p1', quantity: 1 }],
  })

  expect(result).toBeDefined()
  expect(result.id).toBe('o1')
})
```

**改为**：
```typescript
it('should create order successfully', async () => {
  // 事务外：查用户
  prisma.user.findUnique.mockResolvedValueOnce({
    id: 'u1', level: 1, unlockedPoints: 100,
  })
  // 事务外：查商品
  prisma.product.findMany.mockResolvedValueOnce([{
    id: 'p1', name: 'Test Product', memberPrice: 100, retailPrice: 120, stock: 10, maxPointsRatio: 50,
  }])
  // 事务内：重新查商品（防并发）
  prisma.product.findMany.mockResolvedValueOnce([{
    id: 'p1', name: 'Test Product', stock: 10,
  }])
  // 事务内：重新查用户（防并发）
  prisma.user.findUnique.mockResolvedValueOnce({
    id: 'u1', level: 1, unlockedPoints: 100, totalPoints: 200, lockedPoints: 100,
  })
  // 事务内：创建订单
  prisma.order.create.mockResolvedValueOnce({
    id: 'o1', orderNo: 'ORD123', userId: 'u1', totalAmount: 100, payAmount: 100, status: 'pending',
    items: [{ productId: 'p1', quantity: 1, unitPrice: 100, totalPrice: 100 }],
  })
  // 事务内：原子扣减库存（业务代码 line 128 是 tx.product.updateMany）
  prisma.product.updateMany.mockResolvedValueOnce({ count: 1 })

  const result = await OrderService.createOrder({
    userId: 'u1', items: [{ productId: 'p1', quantity: 1 }],
  })

  expect(result).toBeDefined()
  expect(result.id).toBe('o1')
})
```

> **根因**：业务代码 `order.service.ts:128` 是 `await tx.product.updateMany(...)`，line 137 访问 `result.count`。测试 mock 的是 `$queryRaw`，导致 updateMany 没 mock 返回 undefined，line 137 报 `Cannot read properties of undefined (reading 'count')`。
> **错误信息**：`TypeError: Cannot read properties of undefined (reading 'count') at src/lib/services/order.service.ts:137:20`

#### 2.3 修 `should pay order successfully`（约第 153-173 行）

**原代码**：
```typescript
it('should pay order successfully', async () => {
  prisma.order.updateMany.mockResolvedValueOnce({ count: 1 })

  prisma.order.findUnique.mockResolvedValueOnce({  // ← 第一次 findUnique
    id: 'o1', orderNo: 'ORD123', userId: 'u1', totalAmount: 100, payAmount: 100,
    status: 'paid',  // ← 错误！应该是 'pending'
    user: { email: 'test@test.com', phone: '13800000001', nickname: 'Test' },
    items: [{ product: { isUpgradeProduct: false, name: 'Test' } }],
  })

  const { RewardService } = await import('@/lib/services/reward.service')
  vi.mocked(RewardService.processOrderRewards).mockResolvedValueOnce(undefined as any)

  const result = await OrderService.payOrder('o1')
  expect(result).toBeDefined()
  expect(result.status).toBe('paid')
})
```

**改为**：
```typescript
it('should pay order successfully', async () => {
  // 第一次 findUnique（事务外，line 180）：查订单，必须 status='pending' 通过 line 182 检查
  prisma.order.findUnique.mockResolvedValueOnce({
    id: 'o1', orderNo: 'ORD123', userId: 'u1', totalAmount: 100, payAmount: 100,
    status: 'pending',
  })
  // 事务内：原子 updateMany 改 status=paid
  prisma.order.updateMany.mockResolvedValueOnce({ count: 1 })
  // 事务内：payAmount > 0 才需要查 user；这里是 100 > 0
  prisma.user.findUnique.mockResolvedValueOnce({
    balance: 1000, frozenBalance: 0,
  })
  // 事务内：原子扣减余额
  prisma.user.updateMany.mockResolvedValueOnce({ count: 1 })
  // 事务内：写 BalanceRecord
  prisma.balanceRecord.create.mockResolvedValueOnce({})
  // 事务内：第二次 findUnique 返回支付后的订单（line 194）
  prisma.order.findUnique.mockResolvedValueOnce({
    id: 'o1', orderNo: 'ORD123', userId: 'u1', totalAmount: 100, payAmount: 100,
    status: 'paid',
    user: { email: 'test@test.com', phone: '13800000001', nickname: 'Test' },
    items: [{ product: { isUpgradeProduct: false, name: 'Test' } }],
  })

  const { RewardService } = await import('@/lib/services/reward.service')
  vi.mocked(RewardService.processOrderRewards).mockResolvedValueOnce(undefined as any)

  const result = await OrderService.payOrder('o1')
  expect(result).toBeDefined()
  expect(result.status).toBe('paid')
})
```

> **根因**：业务代码 `order.service.ts:180` 先 `prisma.order.findUnique` 查订单，line 182 `if (order.status !== ORDER_STATUS.PENDING) throw new Error('订单不存在或状态已变更')`。原测试 mock `status: 'paid'` → 不通过 → 抛错。正确做法：第一次 mock 返回 `status: 'pending'`，事务内 updateMany 改 paid，第二次 findUnique 返回 paid（用于发邮件/短信）。

#### 2.4 修 `should throw error when order already paid`（约第 175-181 行）

**原代码**：
```typescript
it('should throw error when order already paid', async () => {
  prisma.order.updateMany.mockResolvedValueOnce({ count: 0 }) // No rows updated

  await expect(OrderService.payOrder('o1'))
    .rejects.toThrow('订单不存在或状态已变更')
})
```

**改为**：
```typescript
it('should throw error when order already paid', async () => {
  // 第一次 findUnique（事务外）：订单状态已经是 paid，业务代码 line 182 直接抛错
  prisma.order.findUnique.mockResolvedValueOnce({
    id: 'o1', orderNo: 'ORD123', userId: 'u1', totalAmount: 100, payAmount: 100,
    status: 'paid',  // 已经是 paid 状态
  })

  await expect(OrderService.payOrder('o1'))
    .rejects.toThrow('订单不存在或状态已变更')
})
```

> **根因**：业务代码 `order.service.ts:180-182`：
> ```
> const order = await prisma.order.findUnique(...)
> if (!order) throw new Error('订单不存在')           // ← line 181
> if (order.status !== ORDER_STATUS.PENDING) throw ...  // ← line 182
> ```
> 原测试没 mock findUnique → order=undefined → line 181 抛"订单不存在"。正确做法：mock `status: 'paid'` → 走 line 182 抛"订单不存在或状态已变更"。

---

## 2. 验证（小金本地跑）

### 2.1 先跑测试确认同样挂 7 个

```powershell
cd D:\mlm-platform-source\mlm-platform
pnpm test 2>&1 | Select-String "Test Files|Tests|FAIL|✓|×"
```

**期望看到**：
```
Test Files  2 failed | 4 passed (6 total)
Tests       7 failed | 40 passed (47 total)
```

（确认和 CI 报告一致，证明是测试代码问题不是环境问题）

### 2.2 改完后再跑测试，确认全绿

```powershell
pnpm test 2>&1 | Select-String "Test Files|Tests|FAIL|✓|×"
```

**期望看到**：
```
Test Files  6 passed (6 total)
Tests       47 passed (47 total)
```

### 2.3 build 验证

```powershell
npx next build 2>&1 | Select-String "Checking validity|error TS|Failed"
```

**期望**：`✓ Checking validity of types` 通过，无 `error TS`。

### 2.4 重要：用 grep 确认没漏掉 API

```powershell
# 确认 6 个测试文件都用了 mock 链路
Get-ChildItem __tests__\services -Filter *.test.ts | ForEach-Object {
  $f = $_.FullName
  Write-Output "=== $f ==="
  Select-String -Path $f -Pattern "balanceRecord|findUnique|updateMany" | Select-Object -First 5
}
```

---

## 3. git 三连 + 推送

```powershell
git add __tests__/services/withdrawal.test.ts __tests__/services/order.test.ts
git commit -m "test: 修复 withdrawal + order 测试 mock 不全（v45.4 CI 修复）"
git push origin main
git log origin/main --oneline -1
```

**判断**：
- ✅ 看到新 commit hash + "test:..." → push 成功
- ❌ 看到旧 hash `c1e1d25` → push 静默失败，重跑 push

---

## 4. 完成后告诉胡子哥

格式：
> v45.4 完成，commit hash: xxx，pnpm test 本地 47/47 通过，build 0 errors。等胡子哥在 GitHub Actions 页面点 "Re-run jobs" 验证 CI 全绿。

---

## 5. 严格禁止

| 禁止项 |
|---|
| ❌ 改 `withdrawal.service.ts` / `order.service.ts` 业务代码（业务代码没问题，是测试 mock 问题）|
| ❌ 改 `prisma/schema.prisma` |
| ❌ 改 `.github/workflows/test.yml` |
| ❌ 改其他测试文件（reward/dividend/admin/points） |
| ❌ 用 `$queryRawUnsafe`（铁律 5）|
| ❌ 假装成功（不实际跑 pnpm test / build）|

---

## 6. 预计耗时

- 阅读现状：5 分钟
- 改 2 个测试文件：15-20 分钟
- 本地 pnpm test + build：5 分钟
- commit + push：5 分钟
- **总计 30-35 分钟**

---

## 7. CI 验证（胡子哥做）

v45.4 push 完成后，胡子哥在 https://github.com/dedede1688/mlm-platform001/actions 页面：
1. 点 "Re-run jobs"（右上角按钮，让 v45.3 失败的 workflow 重跑）
2. 等等 1-2 分钟
3. 看到 status 是绿圈 ✅ → v45.4 收官
4. 看到还是红叉 ❌ → 把新日志截图发回

---

## 8. 真实事故参考

**v44 + v45.2 都没跑过 withdrawal/order 测试**，因为：
- v44 派单只列了 reward/dividend/admin 三个文件
- v45.2 只追加了 voidPoints 到 points.test.ts
- 这 2 个文件**本地从来没跑过完整测试套件**

**v45.3 CI 第一次跑就暴露**：
- 7 个失败 mock 错误（v45.4 修）
- 不是业务代码 bug（业务代码 v43-7 BalanceRecord 修复已上线 16 个 release 都没出过问题）

**教训**：
- 改业务代码时**必须同步加测试**（v43-7 改 BalanceRecord 时**没要求**补 withdrawal/order 测试 → 教训）
- 加测试时**必须本地跑一遍**（v44 加 reward/dividend/admin 跑了；withdrawal/order 没跑就 commit 了 → 教训）
- 改业务代码 + 跑测试 + 改 mock 是连贯动作，**不能拆**

---

## 9. v45.4 收官后候选下一步

- **v45.5**：CI 加 Vercel deploy preview（push PR 自动生成预览链接）
- **v45.6**：CI 加 lint / typecheck（强制 build 前先过）
- **v45.7**：测试覆盖率报告（vitest coverage badge）
- **v46**：转新功能（胡子哥定方向）
