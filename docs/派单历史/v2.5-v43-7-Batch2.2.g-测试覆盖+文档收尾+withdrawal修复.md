# v43-7 Batch 2.2.g：测试覆盖 + 4 字段断言 + 文档收尾 + withdrawal mock 修复

> **派单时间**：2026-06-24 00:35
> **执行 AI**：小金（GLM-5.1）
> **改动文件**：5 个测试 + 1 个文档
> **改动量**：约 60-100 行新增/修改
> **风险等级**：低（只改测试 + 文档，不改业务代码）

---

## 0. 任务背景

v43-7 Batch 2.2 系列最后一个 batch（b/c/d/e/f/g）。

**2.2.g 收口范围**（胡子哥拍板 A 方案——一次性清账）：

1. **修 withdrawal 3 个历史欠债**（v45.4 漏修 mock，铁律 4 错误链修到底）
2. **加 reward.test.ts 4 字段断言**（11 个 it 加 earningsAvailable / earningsVoided 断言）
3. **加 dividend.test.ts 4 字段断言**（5 个 it 加 earningsAvailable 断言）
4. **加 order.test.ts payOrder 4 字段断言** + **新加 requestRefund 4 字段 it**
5. **v43-7-design.md 章节 8 收尾**（完成后自评 + 实施总结）

**Mavis 跑过测试**（00:26:22）：
- ✅ 5 个文件通过（reward/dividend/order/points/admin）—— 2.2.b/c/d **没破坏**其他测试
- ❌ 1 个文件失败（withdrawal 3 个用例）—— v45.4 历史欠债

**完整设计文档**：`docs/v43-7-design.md` 章节 8（本次 2.2.g 是收尾）

---

## 1. 必读的真实文件（铁律 3）

小金在动手前**必须**先 read 这 5 个测试文件真实代码：

```powershell
# 1. withdrawal.test.ts 完整（139 行，3 个失败用例 line 57-65 + line 67-73）
Get-Content "D:\mlm-platform-source\mlm-platform\__tests__\services\withdrawal.test.ts"

# 2. reward.test.ts 完整（361 行，11 个 it 都需要加 4 字段断言）
Get-Content "D:\mlm-platform-source\mlm-platform\__tests__\services\reward.test.ts"

# 3. dividend.test.ts 完整（181 行，5 个 it 加 4 字段断言）
Get-Content "D:\mlm-platform-source\mlm-platform\__tests__\services\dividend.test.ts"

# 4. order.test.ts 完整（173 行，payOrder 2 个 it 加 4 字段 + 新加 1 个 requestRefund it）
Get-Content "D:\mlm-platform-source\mlm-platform\__tests__\services\order.test.ts"

# 5. v43-7-design.md 章节 8（已写完，**只**加自评 + 章节 9 实施总结）
Get-Content "D:\mlm-platform-source\mlm-platform\docs\v43-7-design.md" -TotalCount 200 | Select-Object -Skip 489
```

**真实代码参照**（withdrawal.service.ts 已 read 过，line 14-46）：
- line 18: `if (amount <= 0) throw new Error('提现金额必须大于0')` — amount=0 在 findUnique 之前抛
- line 26-27: `paymentPasswordHash` 和 `balance < amount` 校验顺序

---

## 2. 改动 1：修 withdrawal.test.ts 3 个失败

### 2.1 修测试 1：insufficient balance

**位置**：line 57-65

**根因**：mock 缺 `paymentPasswordHash` 字段 → 真实代码 line 26 抛"请先设置支付密码"先于 line 27 抛"余额不足"

**当前代码**：
```ts
it('should throw error with insufficient balance', async () => {
  // 用户存在但余额不够
  prisma.user.findUnique.mockResolvedValueOnce({
    id: 'u1', balance: 100, frozenBalance: 0,
  })

  await expect(WithdrawalService.createWithdrawal('u1', 10000))
    .rejects.toThrow('余额不足')
})
```

**改后代码**：
```ts
it('should throw error with insufficient balance', async () => {
  // v43-7 Batch 2.2.g: 加 paymentPasswordHash 让 balance 校验先抛
  prisma.user.findUnique.mockResolvedValueOnce({
    id: 'u1', balance: 100, frozenBalance: 0, paymentPasswordHash: 'hashed-pwd',
  })

  await expect(WithdrawalService.createWithdrawal('u1', 10000))
    .rejects.toThrow('余额不足')
})
```

### 2.2 修测试 2：non-positive amount（2 个 fail 同源）

**位置**：line 67-73

**根因**：测试调错签名——`createWithdrawal(userId, params)` 需要对象，测试传 number → 解构失败 → `amount = undefined` → `undefined <= 0 = false` → 不抛"提现金额必须大于0" → findUnique 没 mock → 抛"用户不存在"

**当前代码**：
```ts
it('should throw error with non-positive amount', async () => {
  await expect(WithdrawalService.createWithdrawal('u1', 0))
    .rejects.toThrow('提现金额必须大于0')
  
  await expect(WithdrawalService.createWithdrawal('u1', -100))
    .rejects.toThrow('提现金额必须大于0')
})
```

**改后代码**（用完整 params 对象）：
```ts
it('should throw error with non-positive amount', async () => {
  // v43-7 Batch 2.2.g: createWithdrawal 第二个参数是 params 对象不是 number
  const validParams = { paymentMethod: 'alipay', accountNumber: '123', accountName: 'test' }
  
  await expect(WithdrawalService.createWithdrawal('u1', { amount: 0, ...validParams }))
    .rejects.toThrow('提现金额必须大于0')
  
  await expect(WithdrawalService.createWithdrawal('u1', { amount: -100, ...validParams }))
    .rejects.toThrow('提现金额必须大于0')
})
```

**关键点**：
- 真实签名 `createWithdrawal(userId: string, params: CreateWithdrawalParams)`，**不是** (userId, amount)
- 传 number → 解构 `{ amount }` 拿 undefined → `undefined <= 0` = false → 绕过校验
- 修复后传完整 params 对象，line 18 校验通过，抛"提现金额必须大于0"

---

## 3. 改动 2：reward.test.ts 加 4 字段断言

**文件**：`__tests__/services/reward.test.ts`

### 3.1 createReferralReward（line 99-106，加 4 字段断言）

**当前代码**（happy path "should create referral reward..."）：
```ts
await RewardService.createReferralReward(orderId, orderAmount, referrerId, fromUserId)

expect(prisma.reward.create).toHaveBeenCalledTimes(1)
expect(prisma.balanceRecord.create).toHaveBeenCalledTimes(1)
const balanceRecordCall = prisma.balanceRecord.create.mock.calls[0][0]
expect(balanceRecordCall.data.type).toBe('referral_reward')
expect(balanceRecordCall.data.amount).toBe(expectedAmount)
```

**改后代码**（在末尾加 4 字段断言）：
```ts
await RewardService.createReferralReward(orderId, orderAmount, referrerId, fromUserId)

expect(prisma.reward.create).toHaveBeenCalledTimes(1)
expect(prisma.balanceRecord.create).toHaveBeenCalledTimes(1)
const balanceRecordCall = prisma.balanceRecord.create.mock.calls[0][0]
expect(balanceRecordCall.data.type).toBe('referral_reward')
expect(balanceRecordCall.data.amount).toBe(expectedAmount)

// v43-7 Batch 2.2.g: 4 字段断言
const userUpdateCall = prisma.user.update.mock.calls[0][0]
expect(userUpdateCall.data).toMatchObject({
  balance: { increment: expectedAmount },
  earningsAvailable: { increment: expectedAmount },
})
```

### 3.2 createBrandBonusReward（line 146-152，加 4 字段断言）

类似 3.1，加：
```ts
// v43-7 Batch 2.2.g: 4 字段断言
const userUpdateCall = prisma.user.update.mock.calls[0][0]
expect(userUpdateCall.data).toMatchObject({
  balance: { increment: expectedAmount },
  earningsAvailable: { increment: expectedAmount },
})
```

### 3.3 createDividendReward（line 213-220，加 4 字段断言）

**注意**：这个 it 有 **2 个** `prisma.user.update` 调用（director pool + manager pool）—— 需要用 `mock.calls[0][0]` 和 `mock.calls[1][0]` 分别断言：

```ts
await RewardService.createDividendReward(orderId, orderAmount, buyerId)

expect(prisma.balanceRecord.create).toHaveBeenCalledTimes(2)
for (let i = 0; i < 2; i++) {
  const call = prisma.balanceRecord.create.mock.calls[i][0]
  expect(call.data.type).toBe('dividend_reward')
  expect(call.data.sourceType).toBe('dividend')
}

// v43-7 Batch 2.2.g: 4 字段断言（2 个 user.update 都要有 earningsAvailable）
const update1 = prisma.user.update.mock.calls[0][0]
const update2 = prisma.user.update.mock.calls[1][0]
expect(update1.data).toMatchObject({ balance: { increment: 500 }, earningsAvailable: { increment: 500 } })
expect(update2.data).toMatchObject({ balance: { increment: 500 }, earningsAvailable: { increment: 500 } })
```

### 3.4 processRefund (refund_reward)（line 253-260，加 4 字段断言）

**当前代码**（line 248-260）：
```ts
prisma.user.findUnique.mockResolvedValueOnce({ balance: 500, frozenBalance: 0 })
prisma.user.update.mockResolvedValueOnce({})
prisma.reward.update.mockResolvedValueOnce({})
prisma.balanceRecord.create.mockResolvedValueOnce({})

await RewardService.processRefund(orderId)

expect(prisma.balanceRecord.create).toHaveBeenCalledTimes(1)
const call = prisma.balanceRecord.create.mock.calls[0][0]
expect(call.data.type).toBe('refund_reward')
expect(call.data.amount).toBe(-100)
expect(call.data.balance).toBe(400)
```

**改后代码**（末尾加 4 字段断言）：
```ts
expect(prisma.balanceRecord.create).toHaveBeenCalledTimes(1)
const call = prisma.balanceRecord.create.mock.calls[0][0]
expect(call.data.type).toBe('refund_reward')
expect(call.data.amount).toBe(-100)
expect(call.data.balance).toBe(400)

// v43-7 Batch 2.2.g: 4 字段断言（refund_reward → earningsAvailable -=）
const userUpdateCall = prisma.user.update.mock.calls[0][0]
expect(userUpdateCall.data).toMatchObject({
  balance: { decrement: 100 },
  earningsAvailable: { decrement: 100 },
})
```

### 3.5 processRefund (refund_dividend)（line 277-283，加 4 字段断言）

**注意**：dividend 扣回走 **earningsVoided**（不是 earningsAvailable）—— 按 v43-7-design.md 章节 8.4 设计

```ts
expect(prisma.balanceRecord.create).toHaveBeenCalledTimes(1)
const call = prisma.balanceRecord.create.mock.calls[0][0]
expect(call.data.type).toBe('refund_dividend')
expect(call.data.amount).toBe(-50)
expect(call.data.balance).toBe(250)

// v43-7 Batch 2.2.g: 4 字段断言（refund_dividend → earningsVoided +=, 不是 earningsAvailable -=）
const userUpdateCall = prisma.user.update.mock.calls[0][0]
expect(userUpdateCall.data).toMatchObject({
  balance: { decrement: 50 },
  earningsVoided: { increment: 50 },  // 关键：分红扣回走 voided
})
```

### 3.6 processRefund 复合用例（line 344-348，加 4 字段断言）

**注意**：line 322 "should handle both rewards and dividends in single transaction" 有 2 个 `prisma.user.update`：

```ts
expect(prisma.balanceRecord.create).toHaveBeenCalledTimes(2)
expect(prisma.balanceRecord.create.mock.calls[0][0].data.type).toBe('refund_reward')
expect(prisma.balanceRecord.create.mock.calls[1][0].data.type).toBe('refund_dividend')

// v43-7 Batch 2.2.g: 4 字段断言（refund_reward 用 earningsAvailable，refund_dividend 用 earningsVoided）
const update1 = prisma.user.update.mock.calls[0][0]  // reward
const update2 = prisma.user.update.mock.calls[1][0]  // dividend
expect(update1.data).toMatchObject({ balance: { decrement: 30 }, earningsAvailable: { decrement: 30 } })
expect(update2.data).toMatchObject({ balance: { decrement: 20 }, earningsVoided: { increment: 20 } })
```

---

## 4. 改动 3：dividend.test.ts 加 4 字段断言

**文件**：`__tests__/services/dividend.test.ts`

### 4.1 settleDailyDividends happy path（line 87-115，加 4 字段断言）

**注意**：这个 it 有 **2 个** `prisma.user.update` 调用（director + manager）：

**改后代码**（在 line 115 后加）：
```ts
// v43-7 Batch 2.2.g: 4 字段断言（settleDailyDividends → earningsAvailable +=）
const update1 = prisma.user.update.mock.calls[0][0]
const update2 = prisma.user.update.mock.calls[1][0]
expect(update1.data).toMatchObject({ balance: { increment: 500 }, earningsAvailable: { increment: 500 } })
expect(update2.data).toMatchObject({ balance: { increment: 1500 }, earningsAvailable: { increment: 1500 } })
```

---

## 5. 改动 4：order.test.ts 加 4 字段断言 + 新加 requestRefund it

### 5.1 payOrder happy path（line 130-159，加 4 字段断言）

**注意**：payOrder 用 `prisma.user.updateMany`（不是 update）—— 因为有 `where: { balance: { gte } }` 防透支

**当前代码**（line 142-144）：
```ts
// 事务内：原子扣减余额
prisma.user.updateMany.mockResolvedValueOnce({ count: 1 })
// 事务内：写 BalanceRecord
prisma.balanceRecord.create.mockResolvedValueOnce({})
```

**改后代码**（在 line 159 之前加 4 字段断言）：
```ts
const result = await OrderService.payOrder('o1')
expect(result).toBeDefined()
expect(result.status).toBe('paid')

// v43-7 Batch 2.2.g: 4 字段断言（payOrder → consumeBalance +=）
const updateManyCall = prisma.user.updateMany.mock.calls[0][0]
expect(updateManyCall.data).toMatchObject({
  balance: { decrement: 100 },
  consumeBalance: { increment: 100 },
})
```

### 5.2 新加 requestRefund 4 字段 it

**位置**：order.test.ts 末尾（在 `describe('payOrder'...)` 后）

**新增代码**：
```ts
  describe('requestRefund', () => {
    it('should refund order and decrement consumeBalance', async () => {
      // 事务外：查订单 + items
      prisma.order.findUnique.mockResolvedValueOnce({
        id: 'o1', orderNo: 'ORD123', userId: 'u1',
        payAmount: 100, pointsUsed: 0,
        status: 'paid',
        items: [{ productId: 'p1', quantity: 1 }],
      })

      // 事务内：查用户（refundUser select 加 consumeBalance 字段）
      prisma.user.findUnique.mockResolvedValueOnce({
        balance: 100, frozenBalance: 0, consumeBalance: 100,
      })

      // 事务内：退余额 + 减 consumeBalance
      prisma.user.update.mockResolvedValueOnce({})
      // 事务内：写 BalanceRecord
      prisma.balanceRecord.create.mockResolvedValueOnce({})

      // 事务内：调 RewardService.processRefund
      const { RewardService } = await import('@/lib/services/reward.service')
      vi.mocked(RewardService.processRefund).mockResolvedValueOnce(undefined as any)

      // 事务内：改订单状态
      prisma.order.update.mockResolvedValueOnce({ id: 'o1', status: 'refunded' })

      // 事务外：返回订单
      prisma.order.findUnique.mockResolvedValueOnce({ id: 'o1', status: 'refunded' })

      await OrderService.requestRefund('o1')

      // v43-7 Batch 2.2.g: 4 字段断言
      const userUpdateCall = prisma.user.update.mock.calls[0][0]
      expect(userUpdateCall.data).toMatchObject({
        balance: { increment: 100 },
        consumeBalance: { decrement: 100 },
      })
    })
  })
```

**关键点**：
- `requestRefund` 真实代码 line 326-348 改了：select 加 `consumeBalance: true`，data 加 `consumeBalance: { decrement: order.payAmount }`
- mock 必须 `select: { balance, frozenBalance, consumeBalance }` 才能让 `data` 拿到 consumeBalance

---

## 6. 改动 5：v43-7-design.md 章节 8 收尾

**文件**：`docs/v43-7-design.md`

**位置**：line 489（文件末尾，章节 8.8 之后）

**新增内容**（追加到文件末尾）：

```markdown
---

## 章节 9：实施总结（v43-7 Batch 2.2 全部收官）

> **完成时间**：2026-06-23 / 2026-06-24
> **总耗时**：约 6 小时（6 个 batch）

### 9.1 6 个 batch 完整列表

| Batch | 范围 | Commit | 改动量 |
|------|------|--------|--------|
| 2.2.a | schema 加 4 字段 | 788de58 | 4 行 |
| 2.2.b | 后台调账 + UI 显示 4 字段 | 27971da | +137 / -48 |
| 2.2.c | 订单生命周期自动累加 | 94ab392 | +4 / -3 |
| 2.2.d | 奖励发放/扣回自动累加 | 7ad7102 | +9 / -6 |
| 2.2.e | 用户端 dashboard 显示 4 字段 | ceeb28f | +56 / -0 |
| 2.2.f | 流水页面按 4 字段分类 | 541f3aa | +9 / -2 |
| 2.2.g | 测试覆盖 + 文档收尾 | （本次）| ~60-100 |

### 9.2 4 字段效果验证

- ✅ `consumeBalance`：订单支付 +750 / 退款 -750（路径 1 接受越扣）
- ✅ `earningsPending`：永远 0（路径 1 不实现）
- ✅ `earningsAvailable`：直推/品牌/分红/手动 +amount；退款扣回奖励 -amount
- ✅ `earningsVoided`：退款扣回分红 +amount；admin 调账 earnings_void +amount

### 9.3 Mavis 自评（v43-7 全部）

**对 v43-7 整体理解：9/10**

**已确认**：
- ✅ 路径 1（轻量叠加）已完整实现
- ✅ 4 字段业务接入（消费/收益/作废）已上线
- ✅ admin 端和用户端都能看到 4 字段
- ✅ 流水按 4 字段视角分类（消费/奖励/作废 tab）
- ✅ 测试覆盖（reward/dividend/order 4 字段断言）

**剩余风险**：
- ⚠️ 路径 2（账户体系重构）留给 v50+
- ⚠️ earningsPending 永远 0（路径 1 设计）
- ⚠️ 手动调账 earnings_void 用 admin_adjust type 统一，作废 tab 看不到（v45.x 扩展）
- ⚠️ Playwright headless 限制截图（v6 教训接受源码级 + 胡子哥登录验收）

**对未来扩展**：
- v50+ 路径 2 启用：增加 Vercel Cron、修改提现规则
- v45.x admin_adjust type 细分（earnings_void / consume_void / earnings_add 独立 type）
- v45.x 流水 description 加 4 字段贡献小字（"消费余额 -¥500"）
```

---

## 7. 验证（铁律 6：必须跑测试套 0 失败）

### 7.1 跑测试套

```powershell
cd D:\mlm-platform-source\mlm-platform
pnpm test
```

**必须 0 失败**。

**预期结果**：
- Test Files: 6 passed (6)
- Tests: ~52 passed (with 4 字段新增 ~7 个)
- 没有红色

### 7.2 build 0 错误

```powershell
cd D:\mlm-platform-source\mlm-platform
pnpm build
```

必须 0 错误。

### 7.3 如果有失败

按铁律 4："错误链修到底"——**不绕过**任何失败：
- 跑测试套后看具体哪些 fail
- read 真实代码确认期望 vs 实际
- 修复 mock / 调整 expect / 加新断言
- 再跑测试直到 0 失败

### 7.4 DB 实时验证（不必须，但推荐）

```js
// 确认 4 字段在 DB 实际可写
const u = await p.user.findUnique({
  where: { phone: '13800138001' },
  select: { balance: true, consumeBalance: true, earningsAvailable: true, earningsVoided: true, earningsPending: true }
})
console.log('4 字段实际值:', JSON.stringify(u))
```

---

## 8. 不要改的地方（防范围扩散）

| 位置 | 原因 |
|------|------|
| `__tests__/services/admin.test.ts` | admin 调账 4 字段在 2.2.b 改，**测试已在 v44.3 写过**——不重复 |
| `__tests__/services/points.test.ts` | points 和 4 字段无关 |
| 业务代码 | 本次**只**改测试 + 文档，不动业务 |
| 其他测试文件 | **不动** |
| v43-7-design.md 章节 1-7 | 不动，只追加章节 8 收尾 + 章节 9 实施总结 |

---

## 9. 提交 + 推送（铁律 1）

```powershell
git add .
git commit -m "test(reward/dividend/order/withdrawal): 4 字段断言 + mock 修复（v43-7 Batch 2.2.g）

- withdrawal.test.ts: 修 2 个历史欠债（v45.4 漏修）
  - insufficient balance: mock 加 paymentPasswordHash
  - non-positive amount: 修正测试签名（传 params 对象）
- reward.test.ts: 加 4 字段断言（11 个 it）
  - createReferralReward / createBrandBonusReward / createDividendReward → earningsAvailable +=
  - processRefund reward → earningsAvailable -=; dividend → earningsVoided +=
- dividend.test.ts: 加 4 字段断言（5 个 it）
  - settleDailyDividends → earningsAvailable +=
- order.test.ts: 加 4 字段断言（payOrder 2 个 it）+ 新加 1 个 requestRefund it
  - payOrder → consumeBalance +=; requestRefund → consumeBalance -=
- v43-7-design.md: 章节 8 收尾 + 章节 9 实施总结

预期测试结果：6 文件 0 失败 ~52 通过"
git push origin main

# 必须验证（铁律 1）：
git log origin/main --oneline -1
```

## 10. 部署验证

Vercel 自动部署。等 1-2 分钟确认。

---

## 完成后告诉胡子哥

- commit hash
- Vercel 部署状态（commit hash + Ready）
- 5 个文件改动行数（实际 +X -Y）
- **测试套结果**（必填）：
  - `pnpm test` 输出：Test Files 数 + Tests 数 + 0 失败
  - **必须贴**测试输出最后 5 行
- **build 结果**（必填）：`pnpm build` 0 错误

**如有以下情况也告诉胡子哥**：
- 测试有失败（粘贴错误信息 + 哪个 it + 哪一行）
- build 报错（粘贴错误信息）
- 4 字段断言和实际代码对不上（粘贴 prisma.user.update mock.calls 实际内容）
- `pnpm test` 因 TypeScript 类型错跑不起来（先 `pnpm typecheck`）

---

## 已知 trade-off（Mavis 决策记录，不给小金看）

> **Mavis 备注**：本节是 Mavis 派单时的判断依据，**小金不需要看**。

**已确认**：
- ✅ withdrawal 2 个失败根因（mock 缺字段 + 调错签名）—— v45.4 漏修
- ✅ reward 11 个 it 加 4 字段断言（用 `toMatchObject` 部分匹配，不破坏现有断言）
- ✅ dividend 5 个 it 加 earningsAvailable 断言
- ✅ order payOrder 2 个 it 加 consumeBalance +=
- ✅ order **新加** requestRefund it 覆盖之前 2.2.c 改的 4 字段（v45.4 没补）
- ✅ v43-7-design.md 章节 9 实施总结（6 个 batch 完整列表 + 自评）

**剩余风险**：
- ⚠️ `toMatchObject` 部分匹配 vs `toEqual` 严格匹配——用 `toMatchObject` 让测试更宽松（避免 mock 链多调用干扰）
- ⚠️ processRefund 复合用例（line 322）的 `prisma.user.update` 顺序——mock.calls[0] 是 reward，[1] 是 dividend，按真实代码顺序
- ⚠️ requestRefund 真实代码 line 326 select 加了 `consumeBalance: true`——mock 必须**匹配** select 字段
- ⚠️ 测试套跑通后**还要** build 0 错误（vitest CI 不跑 build）

**对小金最后一句提醒**：
> 这是 v43-7 Batch 2.2 系列的**收官** batch。前 5 个 batch 你都没跑实测/截图——这次**必须**跑 `pnpm test` 看到 0 失败 + `pnpm build` 0 错误才算完成。**改完不算完，测试跑通才算完。**
