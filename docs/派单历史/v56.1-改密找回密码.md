# v56.1：改密 + 找回密码

> **派单时间**：2026-06-28 00:33
> **执行 AI**：猫爪（GLM-5.1）
> **改动文件**：约 6-8 个
> **改动量**：约 350-500 行
> **风险等级**：🟡 中（鉴权 + 密码安全核心）
> **关联 commit**：`b04c807` (v55.2 最新基线)

---

## 决策记录

需求文档 line 62：
> | 11 | 改密 / 找回密码 | ❌ | 完整缺失 |

**v56.1 目标**：补全改密 + 找回密码两条用户端流程。

**当前现状（grep 验证）**：
- ✅ bcrypt 已用（register / login / payment-password / admin users password）
- ✅ 支付密码已有完整流程（`/lib/auth/payment-password.ts` + `/dashboard/payment-password/page.tsx`）
- ✅ admin 改密路由已存在（`/api/admin/users/[id]/password/route.ts`）—— **可直接参考模式**
- ❌ **无** 用户端改密 API
- ❌ **无** 找回密码 API（无验证码系统）
- ⚠️ **短信服务**：`src/lib/notification/sendSms.ts:44` 是 `// TODO: 对接真实短信发送服务` —— **真实短信未接通**

**业务决策**：
- **改密**（必做）：独立功能，不依赖短信服务
- **找回密码**（必做）：用 **mock 验证码**（开发/测试用 console.log + DB 存验证码 + 5 分钟过期），生产环境对接真实短信服务时只需替换 sendSms 调用

---

## 改动范围

### 后端 API

| 文件 | 改动 |
|------|------|
| `src/app/api/auth/change-password/route.ts` | **新增**——POST 改密（需登录 + 旧密码 + 新密码） |
| `src/app/api/auth/forgot-password/send-code/route.ts` | **新增**——POST 发送验证码（输入手机号） |
| `src/app/api/auth/forgot-password/verify-code/route.ts` | **新增**——POST 校验验证码 |
| `src/app/api/auth/forgot-password/reset/route.ts` | **新增**——POST 重置密码（手机号 + 验证码 + 新密码） |
| `src/lib/services/auth.service.ts` | **新增**——`AuthService` 类（changePassword / sendResetCode / verifyResetCode / resetPassword） |
| `prisma/schema.prisma` | 新增 `PasswordResetCode` model |

### 前端 UI

| 文件 | 改动 |
|------|------|
| `src/app/dashboard/profile/page.tsx` | **新增**——个人资料 + 改密入口 |
| `src/app/forgot-password/page.tsx` | **新增**——找回密码多步表单（输入手机号 → 收验证码 → 重置密码） |
| `src/app/login/page.tsx` | 加"忘记密码？"链接（指向 `/forgot-password`） |

### 中间件 / 权限

| 文件 | 改动 |
|------|------|
| `src/middleware.ts` | `/api/auth/change-password` 需登录（**middleware 不需要改**，因为登录鉴权是 verifyToken） |

### 测试

| 文件 | 改动 |
|------|------|
| `__tests__/services/auth.test.ts` | **新增**——5-7 个 it（改密 + 找回密码 4 个方法） |
| `__tests__/api/auth/change-password.test.ts` | **新增**——3 个 it（鉴权 + 旧密码校验 + 改密成功） |
| `__tests__/api/auth/forgot-password.test.ts` | **新增**——4 个 it（发码 + 校验 + 重置 + 过期） |

---

## 5 步执行链

```
Step 1: Schema（PasswordResetCode model）+ AuthService 后端
  ↓
Step 2: 改密 API（POST /api/auth/change-password）
  ↓
Step 3: 找回密码 3 个 API（send-code / verify-code / reset）
  ↓
Step 4: 前端 2 个页面（/dashboard/profile + /forgot-password）+ login 页加链接
  ↓
Step 5: 测试 + 验证 + commit + push
```

**每步必须 build 0 错误 + 测试通过**——**不要**堆到最后。

---

## Step 1：Schema + AuthService

### 1.1 PasswordResetCode model 设计

**位置**：`prisma/schema.prisma`（放在 User model 附近）

```prisma
model PasswordResetCode {
  id          String    @id @default(uuid())
  userId      String    @map("user_id")
  phone       String    @db.VarChar(20)
  code        String    @db.VarChar(6)        // 6 位数字
  expiresAt   DateTime  @map("expires_at")
  used        Boolean   @default(false)
  createdAt   DateTime  @default(now()) @map("created_at")

  user        User      @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([phone, code])
  @@index([expiresAt])
  @@map("password_reset_codes")
}
```

**User model 加反向关系**：

```prisma
model User {
  // ... 原有字段
  passwordResetCodes  PasswordResetCode[]
}
```

### 1.2 Migration

```bash
npx prisma migrate dev --name v56-1-password-reset
```

**铁律 v46.3.2**：deploy 后**不**自动跑 migration——胡子哥需在 Supabase SQL Editor 跑 migration SQL。

### 1.3 AuthService 设计

**位置**：`src/lib/services/auth.service.ts`（新建）

```typescript
import { prisma } from '@/lib/prisma'
import bcrypt from 'bcryptjs'
import { logger } from '@/lib/logger'
import { sendSms } from '@/lib/notification/sendSms'

const RESET_CODE_EXPIRY_MINUTES = 5
const SALT_ROUNDS = 10

export class AuthService {
  // ===========================
  // 改密（用户登录后）
  // ===========================
  static async changePassword(params: {
    userId: string
    oldPassword: string
    newPassword: string
  }): Promise<void> {
    const { userId, oldPassword, newPassword } = params

    // 1. 新密码强度校验
    if (newPassword.length < 8) {
      throw new Error('新密码至少 8 位')
    }
    if (newPassword === oldPassword) {
      throw new Error('新密码不能与旧密码相同')
    }

    // 2. 查用户
    const user = await prisma.user.findUnique({
      where: { id: userId },
      select: { passwordHash: true },
    })
    if (!user) throw new Error('用户不存在')

    // 3. 校验旧密码
    const isValid = await bcrypt.compare(oldPassword, user.passwordHash)
    if (!isValid) throw new Error('旧密码错误')

    // 4. 加密新密码
    const newPasswordHash = await bcrypt.hash(newPassword, SALT_ROUNDS)

    // 5. 更新
    await prisma.user.update({
      where: { id: userId },
      data: { passwordHash: newPasswordHash },
    })

    logger.info(`[v56.1 AuthService] 用户改密成功: ${userId}`)
  }

  // ===========================
  // 找回密码 - 发验证码
  // ===========================
  static async sendResetCode(phone: string): Promise<{ expiresIn: number }> {
    // 1. 查用户是否存在（不暴露用户存在性——统一返回成功）
    const user = await prisma.user.findUnique({
      where: { phone },
      select: { id: true },
    })
    if (!user) {
      // 安全考虑：手机号不存在也返回成功（避免泄露用户存在性）
      logger.warn(`[v56.1 AuthService] 找回密码手机号不存在: ${phone}`)
      return { expiresIn: RESET_CODE_EXPIRY_MINUTES * 60 }
    }

    // 2. 生成 6 位验证码
    const code = Math.floor(100000 + Math.random() * 900000).toString()

    // 3. 设置过期时间
    const expiresAt = new Date(Date.now() + RESET_CODE_EXPIRY_MINUTES * 60 * 1000)

    // 4. 失效旧验证码（防刷）
    await prisma.passwordResetCode.updateMany({
      where: { userId: user.id, used: false },
      data: { used: true },
    })

    // 5. 创建新验证码
    await prisma.passwordResetCode.create({
      data: {
        userId: user.id,
        phone,
        code,
        expiresAt,
      },
    })

    // 6. 发送短信（v56.1 mock 模式：开发环境 console.log 打印）
    await sendSms({
      to: phone,
      templateType: 'password_reset',
      variables: { code, expireMinutes: String(RESET_CODE_EXPIRY_MINUTES) },
    }).catch((err: unknown) => {
      logger.error('[v56.1 AuthService] 发送验证码短信失败', { phone, error: String(err) })
    })

    logger.info(`[v56.1 AuthService] 找回密码验证码已发送: ${phone} (mock: ${code})`)

    return { expiresIn: RESET_CODE_EXPIRY_MINUTES * 60 }
  }

  // ===========================
  // 找回密码 - 校验验证码
  // ===========================
  static async verifyResetCode(phone: string, code: string): Promise<{ valid: boolean; userId?: string }> {
    const record = await prisma.passwordResetCode.findFirst({
      where: {
        phone,
        code,
        used: false,
        expiresAt: { gt: new Date() },
      },
      orderBy: { createdAt: 'desc' },
    })

    if (!record) return { valid: false }
    return { valid: true, userId: record.userId }
  }

  // ===========================
  // 找回密码 - 重置密码
  // ===========================
  static async resetPassword(params: {
    phone: string
    code: string
    newPassword: string
  }): Promise<void> {
    const { phone, code, newPassword } = params

    // 1. 新密码强度校验
    if (newPassword.length < 8) {
      throw new Error('新密码至少 8 位')
    }

    // 2. 校验验证码
    const record = await prisma.passwordResetCode.findFirst({
      where: {
        phone,
        code,
        used: false,
        expiresAt: { gt: new Date() },
      },
      orderBy: { createdAt: 'desc' },
    })
    if (!record) throw new Error('验证码无效或已过期')

    // 3. 加密新密码
    const newPasswordHash = await bcrypt.hash(newPassword, SALT_ROUNDS)

    // 4. 事务：更新密码 + 标记验证码已用
    await prisma.$transaction([
      prisma.user.update({
        where: { id: record.userId },
        data: { passwordHash: newPasswordHash },
      }),
      prisma.passwordResetCode.update({
        where: { id: record.id },
        data: { used: true },
      }),
    ])

    logger.info(`[v56.1 AuthService] 用户重置密码成功: ${record.userId}`)
  }
}
```

**关键设计决策**：

1. **密码强度**：至少 8 位
2. **新密码 ≠ 旧密码**：防呆
3. **找回密码不暴露用户存在性**：手机号不存在也返回"成功"——避免泄露哪些手机号注册过
4. **验证码 5 分钟过期**：行业标准
5. **验证码防刷**：发新码前标记旧码已用
6. **重置密码事务化**：更新密码 + 标记验证码 同步完成

---

## Step 2：改密 API

### 2.1 `/api/auth/change-password` —— POST

**位置**：`src/app/api/auth/change-password/route.ts`（新建）

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { verifyToken } from '@/lib/utils/auth'
import { AuthService } from '@/lib/services/auth.service'
import { logOperation } from '@/lib/utils/operation-log'

export async function POST(request: NextRequest) {
  try {
    // 1. 鉴权（必须登录）
    const authUser = await verifyToken(request)
    if (!authUser) {
      return NextResponse.json(
        { success: false, error: '未登录或登录已过期' },
        { status: 401 }
      )
    }

    // 2. 解析 body
    const body = await request.json()
    const { oldPassword, newPassword } = body as {
      oldPassword: string
      newPassword: string
    }

    if (!oldPassword || !newPassword) {
      return NextResponse.json(
        { success: false, error: '旧密码和新密码必填' },
        { status: 400 }
      )
    }

    // 3. 调 service
    await AuthService.changePassword({
      userId: authUser.userId,
      oldPassword,
      newPassword,
    })

    // 4. 操作日志
    await logOperation({
      userId: authUser.userId,
      action: 'UPDATE',
      module: 'user',
      targetId: authUser.userId,
      newValue: { action: 'change_password' },
    })

    return NextResponse.json({
      success: true,
      message: '密码修改成功',
    })
  } catch (error) {
    const message = error instanceof Error ? error.message : '改密失败'
    return NextResponse.json(
      { success: false, error: message },
      { status: 400 }
    )
  }
}
```

---

## Step 3：找回密码 3 个 API

### 3.1 `/api/auth/forgot-password/send-code` —— POST

**位置**：`src/app/api/auth/forgot-password/send-code/route.ts`（新建）

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { AuthService } from '@/lib/services/auth.service'

export async function POST(request: NextRequest) {
  try {
    const body = await request.json()
    const { phone } = body as { phone: string }

    if (!phone || !/^1[3-9]\d{9}$/.test(phone)) {
      return NextResponse.json(
        { success: false, error: '请输入有效的手机号' },
        { status: 400 }
      )
    }

    const result = await AuthService.sendResetCode(phone)

    return NextResponse.json({
      success: true,
      message: '验证码已发送，请注意查收',
      data: { expiresIn: result.expiresIn },
    })
  } catch (error) {
    console.error('Send reset code error:', error)
    return NextResponse.json(
      { success: false, error: '发送验证码失败' },
      { status: 500 }
    )
  }
}
```

### 3.2 `/api/auth/forgot-password/verify-code` —— POST

**位置**：`src/app/api/auth/forgot-password/verify-code/route.ts`（新建）

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { AuthService } from '@/lib/services/auth.service'

export async function POST(request: NextRequest) {
  try {
    const body = await request.json()
    const { phone, code } = body as { phone: string; code: string }

    if (!phone || !code) {
      return NextResponse.json(
        { success: false, error: '手机号和验证码必填' },
        { status: 400 }
      )
    }

    const result = await AuthService.verifyResetCode(phone, code)

    return NextResponse.json({
      success: true,
      data: { valid: result.valid },
    })
  } catch (error) {
    console.error('Verify reset code error:', error)
    return NextResponse.json(
      { success: false, error: '校验失败' },
      { status: 500 }
    )
  }
}
```

### 3.3 `/api/auth/forgot-password/reset` —— POST

**位置**：`src/app/api/auth/forgot-password/reset/route.ts`（新建）

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { AuthService } from '@/lib/services/auth.service'

export async function POST(request: NextRequest) {
  try {
    const body = await request.json()
    const { phone, code, newPassword } = body as {
      phone: string
      code: string
      newPassword: string
    }

    if (!phone || !code || !newPassword) {
      return NextResponse.json(
        { success: false, error: '手机号、验证码、新密码必填' },
        { status: 400 }
      )
    }

    await AuthService.resetPassword({ phone, code, newPassword })

    return NextResponse.json({
      success: true,
      message: '密码重置成功，请使用新密码登录',
    })
  } catch (error) {
    const message = error instanceof Error ? error.message : '重置密码失败'
    return NextResponse.json(
      { success: false, error: message },
      { status: 400 }
    )
  }
}
```

---

## Step 4：前端页面

### 4.1 `/dashboard/profile` —— 个人资料 + 改密入口

**位置**：`src/app/dashboard/profile/page.tsx`（新建）

**关键功能**：
- 显示用户信息（手机号、昵称、头像、推荐人）
- 「修改密码」按钮 → 弹窗或独立页（输入旧密码 + 新密码 + 确认新密码）
- 提交调 `/api/auth/change-password`
- 成功后提示「请重新登录」（密码改了 token 可能失效，需要重新登录）

**参考样式**：`/dashboard/payment-password/page.tsx`（支付密码页风格）

### 4.2 `/forgot-password` —— 找回密码多步表单

**位置**：`src/app/forgot-password/page.tsx`（新建）

**3 步流程**：
1. **步骤 1**：输入手机号 → 调 `send-code` → 进入步骤 2
2. **步骤 2**：输入 6 位验证码 → 调 `verify-code` → 验证通过进入步骤 3
3. **步骤 3**：输入新密码 + 确认新密码 → 调 `reset` → 成功跳登录页

**UI 参考**：stepper 进度条 + 每步独立表单

### 4.3 `/login/page.tsx` 加"忘记密码？"链接

**位置**：`src/app/login/page.tsx`（修改）

在登录表单底部加：
```tsx
<Link href="/forgot-password" className="text-sm text-primary hover:underline">
  忘记密码？
</Link>
```

---

## Step 5：测试

### 5.1 单元测试 `__tests__/services/auth.test.ts`

```typescript
describe('AuthService.changePassword', () => {
  it('改密成功', async () => { /* mock user + bcrypt */ })
  it('旧密码错误', async () => {
    await expect(AuthService.changePassword({ /* wrong old */ })).rejects.toThrow('旧密码错误')
  })
  it('新密码 < 8 位', async () => {
    await expect(AuthService.changePassword({ newPassword: '123' })).rejects.toThrow('至少 8 位')
  })
  it('新密码 = 旧密码', async () => {
    await expect(AuthService.changePassword({ oldPassword: 'abc12345', newPassword: 'abc12345' })).rejects.toThrow('不能相同')
  })
})

describe('AuthService.sendResetCode', () => {
  it('发送验证码成功', async () => { /* mock user + create */ })
  it('手机号不存在也返回成功（安全）', async () => {
    const result = await AuthService.sendResetCode('13800138000')
    expect(result.expiresIn).toBe(300)
  })
  it('失效旧验证码', async () => { /* 验证 updateMany */ })
})

describe('AuthService.verifyResetCode', () => {
  it('验证码有效', async () => { /* mock valid record */ })
  it('验证码过期', async () => { /* mock expired */ })
  it('验证码已用', async () => { /* mock used */ })
})

describe('AuthService.resetPassword', () => {
  it('重置密码成功', async () => { /* mock + verify */ })
  it('验证码无效', async () => {
    await expect(AuthService.resetPassword({ /* invalid */ })).rejects.toThrow()
  })
})
```

**预计 +12 个 it**。

### 5.2 API 测试

**`__tests__/api/auth/change-password.test.ts`**：
- 未登录返回 401
- 旧密码错误返回 400
- 改密成功返回 200

**预计 +3 个 it**。

**`__tests__/api/auth/forgot-password.test.ts`**：
- send-code: 手机号无效返回 400
- verify-code: 验证码错误返回 valid=false
- reset: 验证码过期返回 400

**预计 +3 个 it**。

**总计 +18 个 it**（原 139 → 157）。

---

## 验证

### 6.1 Typecheck

```powershell
npx tsc --noEmit -p tsconfig.typecheck.json 2>&1 | Select-Object -Last 5
```

**必须 0 错误**。

### 6.2 测试套

```powershell
pnpm test
```

**必须 0 失败**，**预计 +18 个 it 全通过**。

### 6.3 Coverage

```powershell
pnpm test:coverage:check
```

**必须 ≥ 73.08%**（v55.2 baseline）。

### 6.4 本地 dev server + 真实链路测试

```powershell
pnpm dev
```

| 场景 | 操作 | 预期 |
|------|------|------|
| 改密成功 | 登录 → 个人中心 → 改密 → 输入旧密码 + 新密码 → 提交 | 提示「密码修改成功」+ 数据库 passwordHash 更新 |
| 旧密码错误 | 改密 → 输错旧密码 | 提示「旧密码错误」 |
| 新密码太短 | 改密 → 新密码 123 | 提示「至少 8 位」 |
| 找回密码发码 | 登录页 → 忘记密码 → 输入手机号 → 发送 | 提示「验证码已发送」+ console.log 打印验证码 |
| 找回密码校验 | 输入收到的验证码 → 校验 | 通过进入步骤 3 |
| 找回密码重置 | 输入新密码 → 提交 | 提示「重置成功」+ 数据库 passwordHash 更新 |
| 用旧密码登录 | 重置后用旧密码尝试登录 | 失败（密码已改） |
| 用新密码登录 | 重置后用新密码登录 | 成功 |

### 6.5 Build

```powershell
pnpm build
```

**必须 0 错误**。

---

## 提交 + 推送（铁律 1）

```bash
git add .
git commit -m "feat(v56.1): 改密 + 找回密码

- schema: 新增 PasswordResetCode model（验证码 + 5 分钟过期）
- AuthService: changePassword / sendResetCode / verifyResetCode / resetPassword
- API: 4 个路由（change-password / forgot-password/send-code / verify-code / reset）
- 前端: /dashboard/profile 个人中心 + 改密表单
- 前端: /forgot-password 多步表单（手机号 → 验证码 → 重置密码）
- /login 加「忘记密码？」链接
- 测试: +18 个 it（AuthService 12 + API 6）

需求文档 1.2 第 11 步「改密 / 找回密码」❌ 已补全。
找回密码用 mock 验证码（开发 console.log + DB 存码 + 5 分钟过期），生产对接真实短信服务时只需替换 sendSms 调用。"

git push origin main

# 必须验证（铁律 1）：
git log origin/main --oneline -1
```

---

## 部署验证

Vercel 自动部署。**Migration 不自动跑**——胡子哥需在 Supabase SQL Editor 跑：

```sql
-- prisma/migrations/v56-1-password-reset/migration.sql
-- 创建 password_reset_codes 表
```

---

## 完成后告诉胡子哥

- commit hash
- 远程 hash 验证通过（铁律 1）
- 改动文件清单（约 6-8 个）
- 测试 + coverage 结果（+18 个 it 通过）
- **真实截图**（必填，铁律 2）：
  - 个人中心改密表单
  - 改密成功提示
  - 找回密码步骤 1（输入手机号）
  - 找回密码步骤 2（输入验证码 + console.log 验证码）
  - 找回密码步骤 3（输入新密码）
  - 重置成功跳转登录页
- **migration SQL 文件路径**

---

## 风险点

1. **mock 验证码安全风险**：
   - 找回密码用 mock（console.log 打印验证码）—— **生产环境绝对不能用**
   - 派单里明确：生产环境对接 sendSms 真实服务时必须移除 mock
   - **必须**在 sendSms.ts 实现真实服务时立即接入

2. **改密后 token 失效**：
   - 密码改了，旧 token 可能还有效
   - 派单未处理——token 还在 JWT 过期前有效（7 天）
   - 如果要"改密立即踢出所有设备"，需要额外逻辑（jwt 黑名单 / 强制重登）

3. **找回密码防爆破**：
   - 验证码 6 位（100 万种可能）+ 5 分钟过期 + 用后失效 = **安全**
   - 但**没有频次限制**（同一手机号 1 秒发 100 次验证码）
   - 派单**未加 rate-limit**——如果需要，单独派单

4. **新密码与支付密码混淆**：
   - 用户改密是「登录密码」（`passwordHash`）
   - 支付密码是另一个字段（`paymentPasswordHash`）
   - 派单只动 `passwordHash`，不动 `paymentPasswordHash`——正确

5. **Migration 不自动跑**：
   - v46.3.2 教训：必须告诉胡子哥手动跑 Supabase SQL

6. **未触发 daily-tasks 自动清理过期验证码**：
   - `expiresAt` 过期的验证码不会自动删除（仅查询时过滤）
   - **v56.1 不做** —— 派单里 grep 时 where 加 `expiresAt: { gt: new Date() }` 即可
   - 如果数据量大需要清理，单独派单

---

## 派单前必做 4 步检查（Mavis 铁律 11）

1. ✅ **grep 业务 service 方法的真实调用入口**：
   - `AuthService.changePassword`：仅 `/api/auth/change-password/route.ts` 调用
   - `AuthService.sendResetCode`：仅 `/api/auth/forgot-password/send-code/route.ts` 调用
   - `AuthService.verifyResetCode`：仅 `/api/auth/forgot-password/verify-code/route.ts` 调用
   - `AuthService.resetPassword`：仅 `/api/auth/forgot-password/reset/route.ts` 调用
   - 不破坏现有调用入口

2. ✅ **grep 枚举值**：无新枚举（密码强度只有长度校验）

3. ✅ **read 相关工具函数**：
   - ✅ `/api/auth/login/route.ts`（bcrypt.compare 模式）
   - ✅ `/api/auth/register/route.ts`（bcrypt.hash 模式）
   - ✅ `/api/admin/users/[id]/password/route.ts`（admin 改密模式）
   - ✅ `/lib/auth/payment-password.ts`（支付密码工具）
   - ✅ `/lib/notification/sendSms.ts`（TODO 状态）
   - ✅ `/dashboard/payment-password/page.tsx`（前端改密样式参考）

4. ✅ **业务冲突检查**：
   - 业务规则需求文档 line 62 明确 ❌ 标记
   - 与 v50/v54/v55 已完成任务无冲突
   - 改密与支付密码独立（不同字段）

---

## 派单铁律清单

- **铁律 1**：commit + push 后必须 `git log origin/main --oneline -1` 验证远程 hash
- **铁律 2**：UI 改动必须本地 dev server 真实截图（改密表单 + 找回密码多步流程）
- **铁律 5**：默认禁止 `$queryRaw` / `$queryRawUnsafe`
- **铁律 7**：新建 admin 页面 fetch 必须含 `Authorization: Bearer ${token}` header（本任务无 admin 页）
- **铁律 8**：派单检查清单 6 步
- **铁律 11**：派单前 grep 业务 service 方法的真实调用入口（已完成）

---

## v56.1 决策记录

- 需求文档 1.2 第 11 步 ❌ 改密 / 找回密码 —— v56.1 补全
- 改密：参考 admin 改密模式 + bcrypt，独立 API + 前端表单
- 找回密码：mock 验证码（5 分钟过期 + 用后失效 + 防刷）
- 业务规则：手机号不存在也返回成功（不暴露用户存在性）
- 测试覆盖：+18 个 it（AuthService 12 + API 6）
- 总工作量：约 350-500 行，1 天

## 完成后告诉胡子哥
