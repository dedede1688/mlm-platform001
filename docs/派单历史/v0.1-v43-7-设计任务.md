# v43-7 设计任务：BalanceRecord 全流水对齐

> **派单时间**：2026-06-20 20:32
> **派单 AI**：mavis
> **执行 AI**：小京（GLM-5.1 chat-only）
> **执行类型**：只设计不改代码
> **后续**：JoyAI-Code-1.5 实施

---

## 0. 背景

v43-6 已完成 payOrder / requestRefund 的 BalanceRecord 流水写入。
但**还有 4 类余额变动没覆盖流水**：
- 提现相关（申请冻结 / 解冻 / 打款扣减）
- 管理员调账（admin_adjust）
- 手动奖励（manual_reward）
- 解冻（unfreeze，订单取消等场景）

任务：输出 v43-7 设计方案 markdown。

---

## 1. 你是谁 + 限制

你是小京（GLM-5.1 chat-only），刚完成入职任务。

**限制重申**：
- 你是 chat-only，**没有 file / shell / git 工具**
- 今天任务**只设计不执行**
- 不写代码、不改文件
- 资料由胡子哥**分段贴**给你

---

## 2. 胡子哥会贴给你的资料

按顺序贴：

1. `src/lib/services/withdrawal.service.ts`（约 5KB）—— 提现服务
2. `src/app/api/admin/users/[id]/balance/route.ts` —— 管理员调账 API（如不存在，问胡子哥确认）
3. `src/lib/services/admin.service.ts` 或 `manual-reward` 相关代码 —— 手动奖励入口
4. v43-6 已有的 BalanceRecord 写入代码片段（让小京对齐风格）—— 胡子哥贴 2-3 个典型片段
5. `prisma/schema.prisma` 中 BalanceRecord 模型完整定义（约 30 行）

每贴一段你**只输出对应章节的设计**，别等全部贴完再答。

---

## 3. 设计输出要求（7 章节）

### 章节 1：现状盘点

- 列出 v43-6 已有的 BalanceRecord 写入点（哪些 service 方法、哪些 type）
- 列出 v43-7 需要补齐的写入点（基于"限制"列出的 4 类）

### 章节 2：type 枚举设计

基于已有 type：`payment / refund / reward / withdraw / unfreeze / admin_adjust`

- v43-7 需要新增哪些 type 值？（如 `withdraw_freeze` / `withdraw_paid` / `withdraw_unfreeze` 等细分）
- 命名规范（snake_case、与已有 type 风格统一）
- 给每个 type 写清楚：
  - 触发场景
  - amount 正负约定
  - balance / frozenBalance 谁变

### 章节 3：各写入点详细设计

对每个写入点（**最少 4 个：withdraw / unfreeze / admin_adjust / manual_reward**）：

| 字段 | 内容 |
|------|------|
| 触发位置 | service 方法名 / API 路由 |
| 入参 | 参数 + 类型 + 含义 |
| 出参 | 返回类型 |
| BalanceRecord.type | 选哪个枚举值 |
| BalanceRecord.amount | 正/负 + 数值来源 |
| BalanceRecord.balance | 变动后可用余额 |
| BalanceRecord.frozenBalance | 变动后冻结余额 |
| BalanceRecord.sourceType | order / withdrawal / manual_reward / admin |
| BalanceRecord.sourceId | 对应记录 ID |
| BalanceRecord.description | 中文说明 |
| 事务边界 | 与哪些 DB 操作在同一事务 |

### 章节 4：API 路由影响

- 列出 v43-7 需要修改 / 新增的 API 路由
- 每个路由的：
  - 请求方法 + 路径
  - 入参 schema
  - 出参 schema
  - 错误码（参考 v43-6 的风格）

### 章节 5：测试场景

每个写入点至少 2 个测试用例：
- 1 个 happy path（正常流程）
- 1 个异常场景（余额不足 / 重复请求 / 并发冲突）

### 章节 6：风险点

- 数据一致性（BalanceRecord 与 User.balance 必须同步）
- 重复写入防护（幂等性）
- 并发场景（同一用户并发多笔交易）
- 历史数据迁移（v43-6 之前的余额变动没有流水，怎么办？）

### 章节 7：实施顺序

推荐先做哪个？为什么？
工作量评估（每个写入点大概多少行代码 / 多少小时）？

---

## 4. 自评要求

每章节末尾写：

> **自评**：我对本章节的理解：___/10，疑问点：___

最终报告末尾写：

> **总自评**：我对 v43-7 全局理解：___/10，最大疑问：___

---

## 5. 严格禁止

| 禁止项 | 原因 |
|--------|------|
| ❌ 不要写代码（chat-only 也写不了） | 你只设计 |
| ❌ 不要修改任何文件 | 设计任务 |
| ❌ 不要凭印象猜字段值 | 看胡子哥贴的资料 |
| ❌ 不要省略章节 | 7 个章节都要写 |
| ❌ 不要假设"应该这样实现" | 给方案 + 理由，让 JoyAI-Code-1.5 实施 |

---

## 6. 交付物

**输出路径**：`D:\mlm-platform-source\mlm-platform\docs\v43-7-design.md`
胡子哥手动复制你的 chat 输出到这个文件。

完成后回复：
> v43-7 设计方案已输出，等 mavis 审核。

预计耗时：45-75 分钟（资料多 + 7 个章节）

---

## 7. 审核标准（mavis 视角）

| 章节 | 通过标准 |
|------|---------|
| 1 现状盘点 | v43-6 已有写入点列全，v43-7 待补齐清单准确 |
| 2 type 枚举 | 与已有 6 个 type 风格统一，命名无歧义 |
| 3 各写入点 | 4 个写入点齐，每点 10 个字段不漏 |
| 4 API 路由 | 入参 / 出参与 v43-6 风格统一 |
| 5 测试场景 | 每点至少 happy + 异常各 1 个 |
| 6 风险点 | 至少提到 3 个真实风险（不只是"性能"） |
| 7 实施顺序 | 有理由，不只是"按列表顺序" |

---

## 8. 后续流程

小京设计完成 → mavis 审核 → 找 JoyAI-Code-1.5 实施 → 测试 → build → push → 验证部署

**预计全流程**：今晚 20:30 启动设计 → 21:45 设计完成 → 22:00 审核完成 → 明天 JoyAI-Code-1.5 实施