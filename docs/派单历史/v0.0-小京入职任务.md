# v0.0 — 小京入职任务：项目认知报告

> **派单时间**：2026-06-20 18:21
> **派单 AI**：mavis
> **执行 AI**：小京
> **用户**：胡子哥
> **项目**：D:\mlm-platform-source\mlm-platform
> **任务类型**：认知报告（只读不改）

---

## 0. 背景与目标

### 0.1 协作背景

| 角色 | 谁 | 职责 |
|------|-----|------|
| 拍板 | 胡子哥（用户） | 业务决策 + 验收 + 传话 |
| 方案 + 审核 | mavis | 出方案、写派单、审交付物 |
| 执行 | 小京 | 读文件、改代码、build、push、验证部署 |

之前的执行 AI 是"猫爪"，历史教训已沉淀到 `AGENTS.md`。小京入职必须先读 AGENTS.md。

### 0.2 任务目标

通过**只读不改**的方式，建立小京对项目的**基础认知**，避免后续改代码踩坑。

### 0.3 为什么需要这一步

小京做的初步分析报告存在 3 个问题：
1. 数字偏差（18 模型实际 20，后台 12 实际 13）
2. 不知道项目 6 条铁律
3. "待完善"建议泛化（国际化/支付/测试——任何 GPT 都能答）

如果直接派活改代码，可能重蹈 v6 部署事故、v17 $queryRaw 灾难、v43-4-修复-2 支付链路 bug。

---

## 1. 具体任务（4 个子任务）

### 任务 1：读 AGENTS.md，逐条复述项目铁律

**必做**：
1. 读完 `D:\mlm-platform-source\mlm-platform\AGENTS.md` 整个文件
2. 逐条列出所有"铁律"
3. 用自己的话复述每条（不要复制粘贴）
4. 每条铁律末尾说出：防什么坑？能想到真实事故例子吗？

**自评要求**：报告末尾写"我对铁律的理解：___/10，疑问点：___"

### 任务 2：读 prisma/schema.prisma，整理模型关系图

**必做**：
1. 读完 `prisma/schema.prisma`
2. 列出所有模型（不止 18 个，按文件实际数量）
3. 每个模型一句话中文说明用途
4. 画出关系图（1对多、多对1、关联表）
5. 特别关注：
   - User 模型所有字段
   - BalanceRecord（v43-6 新增）
   - Address（v43-5 新增）
   - Order 的 paymentVerified（v43-2）
   - 所有 @map() 映射的实际数据库表名

**自评要求**：报告末尾写"我对模型的理解：___/10，疑问点：___"

### 任务 3：读 reward.service.ts，列出核心方法

**必做**：
1. 读完 `src/lib/services/reward.service.ts`
2. 列出所有 export 方法
3. 每个方法：方法名 + 入参 + 出参 + 业务逻辑（用自己的话）
4. 特别关注：
   - 多级分销奖励怎么算
   - 状态机流转
   - 是否使用 `$queryRawUnsafe`（如果有，必须警惕 v17 教训）

**自评要求**：报告末尾写"我对奖励系统的理解：___/10，疑问点：___"

### 任务 4：读最近 5 个 commit message

**必做**：
1. 执行：`git log --oneline -5`
2. 列出 5 个 commit 完整标题
3. 一句话总结本周主线
4. 推测下一步：v43-7 或 v44 可能做什么

**自评要求**：报告末尾写"我对项目节奏的理解：___/10，疑问点：___"

---

## 2. 报告输出格式

**输出路径**：`D:\mlm-platform-source\mlm-platform\docs\小京入职报告.md`

**结构**：
```markdown
# 小京入职报告

> 生成时间：YYYY-MM-DD HH:MM
> 执行 AI：小京
> 审核：待 mavis 审核

---

## 一、项目铁律（基于 AGENTS.md）

### 铁律 1：<标题>
**复述**：<用自己的话说>
**防什么坑**：<真实事故或潜在风险>
**自评**：<理解到几成>

---

## 二、模型关系图（基于 schema.prisma）

| 模型名 | 实际表名（@map） | 中文用途 | 核心字段 |
|--------|----------------|---------|---------|
| ... | ... | ... | ... |

---

## 三、奖励系统核心方法（基于 reward.service.ts）

| 方法名 | 入参 | 出参 | 业务逻辑 |
|--------|------|------|---------|
| ... | ... | ... | ... |

---

## 四、最近 commit 主线（基于 git log）

```
<hash> <title>
...
```

**本周主线**：<一句话>
**推测下一步**：<一句话>
```

---

## 3. 严格禁止

| 禁止项 | 原因 |
|--------|------|
| 不要修改任何代码 | 这是认知任务 |
| 不要跑 build / dev | 浪费时间 |
| 不要 git add / commit / push | 没活可推 |
| 不要装新依赖 | 用现有的 |
| 不要创建/删除文件（除了报告本身） | docs 目录允许 |
| 不要凭空猜 | 读不懂就写"疑问点：___" |
| 不要省略子任务 | 4 个都要完成 |

---

## 4. 交付与审核

### 4.1 交付方式

小京把报告路径发回给胡子哥，例如：
> 报告已写到 `D:\mlm-platform-source\mlm-platform\docs\小京入职报告.md`

### 4.2 审核标准（mavis 视角）

| 子任务 | 通过标准 | 不通过常见情况 |
|--------|---------|--------------|
| 任务 1 | 6+ 条铁律全列出，每条用自己的话复述 + 真实事故例子 | 复制粘贴原文 / 没举例 |
| 任务 2 | 20 个模型全列出（含 banners），关系图清晰，@map 映射对 | 漏模型 / 关系错误 |
| 任务 3 | 所有 export 方法齐，入参出参与代码一致 | 漏方法 / 业务逻辑是复制代码 |
| 任务 4 | 5 个 commit hash 真实，标题完整，推测合理 | hash 是编的 / 推测是"做好做精" |

### 4.3 时间盒

**预计 30-45 分钟**。超时说明没读懂，需要在报告里写"我卡在 ___，需要 ___ 帮助"。

---

## 5. 后续动作

审核通过后 → 派第一个真正的活（v43-7 或 v44 第一个单子）。
审核不通过 → 打回对应章节重做。