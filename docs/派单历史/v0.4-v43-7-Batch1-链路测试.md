# v43-7 Batch 1 链路测试：3 条 BalanceRecord 验证

> **派单时间**：2026-06-20 23:30
> **执行 AI**：小金（GLM-5.1）
> **类型**：链路测试验证（不修改代码）

---

## 0. 背景
v43-7 Batch 1 重做已 commit `9683b23` + push。
现在跑完整链路测试，验证 3 条 BalanceRecord 流水都写入了：
- withdraw_freeze（用户申请提现）
- withdraw（admin 审核通过）
- unfreeze（admin 审核拒绝）

---

## 1. 步骤 1：确认 dev server 在跑

```powershell
# 检查 3000 端口
netstat -ano | Select-String ":3000"
```

**期望**：看到 LISTENING 状态。
**如果没跑**：重启 `pnpm dev`（后台跑，会卡住不返回——正常）。

```powershell
# 如果没跑，重启
pnpm dev
```

---

## 2. 步骤 2：找到测试账户

```powershell
# 在 JoyCode PowerShell 跑
# 用 prisma 直接查一个测试账户
node -e "const{PrismaClient}=require('@prisma/client');const p=new PrismaClient();p.user.findFirst({where:{phone:{contains:'138'}}}).then(u=>{console.log(JSON.stringify(u));p.\$disconnect()})"
```

**期望**：输出一个测试用户的 JSON（含 id / phone / balance）。
**告诉胡子哥**：测试账户的 userId 和 phone。

如果查不到，找一个真实测试账户（用手机号含 138 的人）。

---

## 3. 步骤 3：SQL 给测试账户加余额 200

```powershell
# 用 prisma 改 balance（需要 admin 权限确认环境变量）
node -e "const{PrismaClient}=require('@prisma/client');const p=new PrismaClient();p.user.update({where:{phone:'PHONE_HERE'},data:{balance:200,frozenBalance:0}}).then(u=>{console.log('Updated:',u.phone,'balance:',u.balance);p.\$disconnect()})"
```

把 `PHONE_HERE` 替换成步骤 2 找到的电话。

**期望**：`Updated: 138xxxx balance: 200`

---

## 4. 步骤 4：用 curl 模拟用户申请提现 30

```powershell
# 1. 用户登录拿 token
$body = @{phone='138xxxx';password='test123456'} | ConvertTo-Json -Compress
$resp = Invoke-RestMethod -Method POST -Uri 'http://localhost:3000/api/auth/login' -ContentType 'application/json' -Body $body
$userToken = $resp.data.token
Write-Host "User token: $userToken"

# 2. 申请提现 30
$body = @{amount=30} | ConvertTo-Json -Compress
$resp = Invoke-RestMethod -Method POST -Uri 'http://localhost:3000/api/withdrawals' -Headers @{Authorization="Bearer $userToken"} -ContentType 'application/json' -Body $body
$wd1Id = $resp.data.id
Write-Host "Withdrawal 1 id: $wd1Id"
```

**期望**：输出 token + withdrawal id。

---

## 5. 步骤 5：让胡子哥审核第 1 笔（通过）

**告诉胡子哥**：
> 测试数据准备好了，wd1Id=[xxxx]。
> 请用 admin 账号登录 → admin/finance → 提现审核 tab → 找到这条申请 → 点"通过"按钮。

等胡子哥操作完。

---

## 6. 步骤 6：用 curl 申请提现 50

```powershell
$body = @{amount=50} | ConvertTo-Json -Compress
$resp = Invoke-RestMethod -Method POST -Uri 'http://localhost:3000/api/withdrawals' -Headers @{Authorization="Bearer $userToken"} -ContentType 'application/json' -Body $body
$wd2Id = $resp.data.id
Write-Host "Withdrawal 2 id: $wd2Id"
```

---

## 7. 步骤 7：让胡子哥审核第 2 笔（拒绝）

**告诉胡子哥**：
> wd2Id=[xxxx]。
> 请去 admin/finance → 提现审核 → 找到这条 → 点"拒绝"按钮 → 填写拒绝原因（如"测试拒绝"）→ 确认。

---

## 8. 步骤 8：查 balance_records 验证

胡子哥操作完后，跑：

```powershell
node -e "const{PrismaClient}=require('@prisma/client');const p=new PrismaClient();p.balanceRecord.findMany({where:{user:{phone:'PHONE_HERE'}},orderBy:{createdAt:'desc'},take:5,include:{user:{select:{phone:true}}}}).then(rs=>{console.log(JSON.stringify(rs,null,2));p.\$disconnect()})"
```

**期望看到 3 条**：

```json
[
  {
    "type": "unfreeze",
    "amount": 50,        // 拒绝退回
    "balance": <用户余额>,
    "frozenBalance": 0,
    "sourceType": "withdrawal",
    "description": "提现拒绝，解冻退回 ¥50"
  },
  {
    "type": "withdraw",
    "amount": -30,       // 通过扣冻结
    "balance": 200,      // balance 不变
    "frozenBalance": 0,
    "sourceType": "withdrawal",
    "description": "提现通过，扣减冻结 ¥30"
  },
  {
    "type": "withdraw_freeze",
    "amount": -30,
    "balance": 170,      // 200 - 30
    "frozenBalance": 30, // 0 + 30
    "sourceType": "withdrawal",
    "description": "提现申请冻结 ¥30"
  }
]
```

---

## 9. 验证完成后告诉胡子哥

格式：
> 链路测试完成，balance_records 共 3 条：
> - withdraw_freeze ✅ / ❌
> - withdraw ✅ / ❌
> - unfreeze ✅ / ❌
> 
> 等胡子哥确认进 Batch 2。

---

## 10. 严格禁止

| 禁止项 |
|---|
| ❌ 修改任何代码文件 |
| ❌ 修改测试账户的真实数据（只改 balance 用于测试） |
| ❌ 跑 `git commit` / `git push` |
| ❌ 假装成功（balance_records 没 3 条必须告诉我哪里出问题） |

---

## 11. 预计耗时

- 步骤 1-7：10 分钟（让胡子哥操作 UI 间隔等待）
- 步骤 8 查 balance_records：2 分钟
- **总计 15-20 分钟**

---

## 12. 失败处理

如果某一步失败：
- API 调用报错 → 截图 / 复制错误信息给胡子哥
- 查不到用户 → 换一个手机号试试
- balance_records 数量不对 → 截图 + 给我看
- 不要自己 debug 代码（除非明显是 SQL 写错）