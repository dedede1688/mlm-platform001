# v45.7.2.b：补测 user.service.ts（核心算法 + 业务核心）

> **派单时间**：2026-06-24 02:05
> **执行 AI**：小金（GLM-5.1）
> **改动文件**：1 个新文件（__tests__/services/user.test.ts）
> **改动量**：约 250-350 行新增
> **风险等级**：中（findPlacementPosition BFS 算法 + checkAndUpgradeLevel 业务规则 mock 复杂）

---

## 0. 任务背景

v45.7 baseline 显示 `user.service.ts` **0% 覆盖**（v45.7 报告里没这文件，确认 0%）—— 234 行 / 8 个方法 / 包含**核心算法**（findPlacementPosition BFS 安置）和**业务核心**（checkAndUpgradeLevel 6 级升级）。

**v45.7.2.a 已完成**（commit 39918a5）—— product.service.ts 100% 覆盖，但 product 只有 70 行所以对总体贡献小。

**v45.7.2.b 目标**：补测 user.service.ts，**预期**整体覆盖率 60% → ~75%。

**Mavis 警告**：user.service.ts 比 product.service.ts **复杂 3 倍**——8 个方法，其中：
- `findPlacementPosition`（BFS 二叉树安置）—— **核心算法**
- `checkAndUpgradeLevel`（6 级升级 + 积分奖励 + 直推数累加）—— **业务核心**
- 其他 6 个方法（createUser / getPlacementChain / getReferrals / getTeam / addDirectSales / addUpgradeProductCount）相对简单

---

## 1. 必读的真实文件（铁律 3）

小金在动手前**必须**先 read 这 3 个文件真实代码：

```powershell
# 1. user.service.ts 完整（234 行，8 个方法）
Get-Content "D:\mlm-platform-source\mlm-platform\src\lib\services\user.service.ts"

# 2. MEMBER_LEVELS 常量（升级等级常量）
Get-Content "D:\mlm-platform-source\mlm-platform\src\lib\constants.ts" -ErrorAction SilentlyContinue

# 3. 已有测试参考（points.test.ts 完整，看 PointsService mock 模式）
Get-Content "D:\mlm-platform-source\mlm-platform\__tests__\services\points.test.ts"
```

**关键确认**：
- ✅ user.service.ts **8 个方法**已 read
- ✅ import：`prisma` / `MEMBER_LEVELS` / `PointsService` / `getBusinessConfig`
- ✅ 升级逻辑 `checkAndUpgradeLevel` 调 `PointsService.createPointsRecord` —— PointsService **必须 mock**
- ✅ `getBusinessConfig` 用于升级等级 + 积分奖励 —— **必须 mock**

---

## 2. 新建 user.test.ts

**文件**：`__tests__/services/user.test.ts`（**新文件**）

参考 points.test.ts 的 mock 模式（含 PointsService mock + getBusinessConfig mock）：

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'

// Mock prisma
vi.mock('@/lib/prisma', () => {
  const createMockChain = () => ({
    findUnique: vi.fn(),
    findMany: vi.fn(),
    findFirst: vi.fn(),
    create: vi.fn(),
    update: vi.fn(),
    updateMany: vi.fn(),
    count: vi.fn(),
    aggregate: vi.fn(),
    $transaction: vi.fn(),
  })

  const mockPrisma: any = {
    user: createMockChain(),
    pointsRecord: createMockChain(),
  }
  mockPrisma.$transaction.mockImplementation(async (fn: any) => fn(mockPrisma))
  return { prisma: mockPrisma }
})

// Mock PointsService（user.service.ts 调 PointsService.createPointsRecord）
vi.mock('@/lib/services/points.service', () => ({
  PointsService: {
    createPointsRecord: vi.fn(),
  },
}))

// Mock business config（升级等级 + 积分奖励用）
vi.mock('@/lib/config/business', () => ({
  getBusinessConfig: vi.fn().mockImplementation(async (_key: string, defaultValue: any) => defaultValue),
  invalidateBusinessConfigCache: vi.fn(),
}))

import { prisma } from '@/lib/prisma'
import { PointsService } from '@/lib/services/points.service'
import { UserService } from '@/lib/services/user.service'

describe('UserService', () => {
  beforeEach(() => {
    vi.clearAllMocks()
    prisma.$transaction.mockImplementation(async (fn: any) => fn(prisma))
  })

  // ========================================
  // createUser
  // ========================================
  describe('createUser', () => {
    it('should create user without referrer', async () => {
      const mockUser = { id: 'u1', phone: '13800138001', level: 1 }
      prisma.user.create.mockResolvedValueOnce(mockUser)

      const result = await UserService.createUser({
        phone: '13800138001',
        passwordHash: 'hashed',
        nickname: 'test',
      })

      expect(result).toEqual(mockUser)
      expect(prisma.user.create).toHaveBeenCalledWith({
        data: expect.objectContaining({
          phone: '13800138001',
          passwordHash: 'hashed',
          nickname: 'test',
          level: 1,
        }),
      })
    })

    it('should create user with referrer and find placement', async () => {
      // findPlacementPosition 内部：prisma.user.findMany
      prisma.user.findMany.mockResolvedValueOnce([])  // 简化场景
      const mockUser = { id: 'u2', parentId: 'referrer-1', position: 1 }
      prisma.user.create.mockResolvedValueOnce(mockUser)

      const result = await UserService.createUser({
        phone: '13800138002',
        passwordHash: 'hashed',
        referrerId: 'referrer-1',
      })

      expect(result).toEqual(mockUser)
      // 应有 referrerId / parentId / position
      expect(prisma.user.create).toHaveBeenCalledWith({
        data: expect.objectContaining({
          referrerId: 'referrer-1',
          parentId: 'referrer-1',
          position: 1,
        }),
      })
    })

    it('should auto-generate nickname when not provided', async () => {
      const mockUser = { id: 'u3', nickname: '用户8001' }
      prisma.user.findMany.mockResolvedValueOnce([])
      prisma.user.create.mockResolvedValueOnce(mockUser)

      await UserService.createUser({
        phone: '13800138001',
        passwordHash: 'hashed',
      })

      expect(prisma.user.create).toHaveBeenCalledWith({
        data: expect.objectContaining({
          nickname: '用户8001',  // phone.slice(-4)
        }),
      })
    })
  })

  // ========================================
  // findPlacementPosition（BFS 安置算法）
  // ========================================
  describe('findPlacementPosition', () => {
    it('should throw error for invalid uuid format', async () => {
      await expect(UserService.findPlacementPosition('invalid-id'))
        .rejects.toThrow('推荐人 ID 格式无效')
    })

    it('should return referrer as parent when no children exist', async () => {
      prisma.user.findMany.mockResolvedValueOnce([])

      const result = await UserService.findPlacementPosition('aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee')

      expect(result).toEqual({ parentId: 'aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee', position: 1 })
    })

    it('should find next available position in BFS', async () => {
      // 推荐人 uuid
      const referrerId = 'aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee'
      // 推荐人 + 已有 1 个子节点
      prisma.user.findMany.mockResolvedValueOnce([
        { id: referrerId, parentId: null, position: null },
        { id: 'child-1', parentId: referrerId, position: 1 },
      ])

      const result = await UserService.findPlacementPosition(referrerId)

      // 推荐人 pos 1 满了，pos 2 是下一个空位
      expect(result).toEqual({ parentId: referrerId, position: 2 })
    })
  })

  // ========================================
  // getPlacementChain
  // ========================================
  describe('getPlacementChain', () => {
    it('should return empty array for user with no parent', async () => {
      prisma.user.findUnique.mockResolvedValueOnce({ parentId: null })

      const result = await UserService.getPlacementChain('u1')

      expect(result).toEqual([])
    })

    it('should return chain of parents up to maxDepth', async () => {
      // 链条：u1 -> u2 -> u3 -> null
      prisma.user.findUnique
        .mockResolvedValueOnce({ parentId: 'u2' })
        .mockResolvedValueOnce({ parentId: 'u3' })
        .mockResolvedValueOnce({ parentId: null })

      const result = await UserService.getPlacementChain('u1', 5)

      expect(result).toEqual(['u2', 'u3'])
    })
  })

  // ========================================
  // checkAndUpgradeLevel（业务核心）
  // ========================================
  describe('checkAndUpgradeLevel', () => {
    it('should do nothing for non-existent user', async () => {
      prisma.user.findUnique.mockResolvedValueOnce(null)

      await UserService.checkAndUpgradeLevel('nonexistent')

      expect(prisma.user.update).not.toHaveBeenCalled()
    })

    it('should upgrade to DISTRIBUTOR when upgradeProductCount >= box_count', async () => {
      // user with 10 upgrade products (default box_count=10)
      prisma.user.findUnique.mockResolvedValueOnce({
        id: 'u1', level: 1, upgradeProductCount: 10, directSalesAmount: 0, referrerId: 'ref-1',
      })
      // defaultValue for box_count = 10 → 满足条件
      prisma.user.update.mockResolvedValueOnce({})  // 升级 user.level
      // 升级到经销商发积分
      vi.mocked(PointsService.createPointsRecord).mockResolvedValueOnce({} as any)
      // 累加推荐人 directDistributorCount
      prisma.user.update.mockResolvedValueOnce({})

      await UserService.checkAndUpgradeLevel('u1')

      // 应有 2 次 user.update（一次升级 user / 一次累加推荐人）
      expect(prisma.user.update).toHaveBeenCalledTimes(2)
    })

    it('should upgrade to DIRECTOR when directSalesAmount >= required', async () => {
      // 假设已设置 getBusinessConfig mock 返回 director.sales_amount=50000
      // user 已达 director 升级条件
      prisma.user.findUnique.mockResolvedValueOnce({
        id: 'u2', level: 2, upgradeProductCount: 5, directSalesAmount: 100000, referrerId: 'ref-1',
      })
      prisma.user.update.mockResolvedValueOnce({})  // 升级到 DIRECTOR

      await UserService.checkAndUpgradeLevel('u2')

      expect(prisma.user.update).toHaveBeenCalled()
    })
  })

  // ========================================
  // getReferrals / getTeam
  // ========================================
  describe('getReferrals', () => {
    it('should return list of users referred by userId', async () => {
      const mockReferrals = [{ id: 'r1', phone: '111' }, { id: 'r2', phone: '222' }]
      prisma.user.findMany.mockResolvedValueOnce(mockReferrals)

      const result = await UserService.getReferrals('u1')

      expect(result).toEqual(mockReferrals)
      expect(prisma.user.findMany).toHaveBeenCalledWith({
        where: { referrerId: 'u1' },
        orderBy: { createdAt: 'desc' },
      })
    })
  })

  describe('getTeam', () => {
    it('should return empty array for user with no children', async () => {
      prisma.user.findMany.mockResolvedValueOnce([])

      const result = await UserService.getTeam('u1')

      expect(result).toEqual([])
    })

    it('should return team members with depth', async () => {
      // u1 有 2 个子节点（depth 1）
      prisma.user.findMany.mockResolvedValueOnce([
        { id: 'child-1', level: 1 },
        { id: 'child-2', level: 1 },
      ])
      // child-1 有 1 个子节点（depth 2）
      prisma.user.findMany.mockResolvedValueOnce([
        { id: 'grand-1', level: 1 },
      ])
      // grand-1 无子节点
      prisma.user.findMany.mockResolvedValueOnce([])

      const result = await UserService.getTeam('u1', 5)

      expect(result).toContainEqual({ id: 'child-1', level: 1, depth: 1 })
      expect(result).toContainEqual({ id: 'child-2', level: 1, depth: 1 })
      expect(result).toContainEqual({ id: 'grand-1', level: 1, depth: 2 })
    })
  })

  // ========================================
  // addDirectSales / addUpgradeProductCount
  // ========================================
  describe('addDirectSales', () => {
    it('should increment directSalesAmount', async () => {
      prisma.user.update.mockResolvedValueOnce({})

      await UserService.addDirectSales('u1', 500)

      expect(prisma.user.update).toHaveBeenCalledWith({
        where: { id: 'u1' },
        data: { directSalesAmount: { increment: 500 } },
      })
    })
  })

  describe('addUpgradeProductCount', () => {
    it('should increment upgradeProductCount by 1 by default', async () => {
      prisma.user.update.mockResolvedValueOnce({})

      await UserService.addUpgradeProductCount('u1')

      expect(prisma.user.update).toHaveBeenCalledWith({
        where: { id: 'u1' },
        data: { upgradeProductCount: { increment: 1 } },
      })
    })

    it('should increment upgradeProductCount by custom count', async () => {
      prisma.user.update.mockResolvedValueOnce({})

      await UserService.addUpgradeProductCount('u1', 5)

      expect(prisma.user.update).toHaveBeenCalledWith({
        where: { id: 'u1' },
        data: { upgradeProductCount: { increment: 5 } },
      })
    })
  })
})
```

**关键点**：
- 14 个 it（createUser 3 + findPlacementPosition 3 + getPlacementChain 2 + checkAndUpgradeLevel 3 + getReferrals 1 + getTeam 2 + addDirectSales 1 + addUpgradeProductCount 2）
- `PointsService.createPointsRecord` **必须 mock**（user.service.ts 调它做升级积分奖励）
- `getBusinessConfig` **必须 mock**（升级等级 + 积分奖励用）
- `findPlacementPosition` 测试需要 uuid 格式（line 41-44 校验）

---

## 3. 不要改的地方

| 位置 | 原因 |
|------|------|
| `user.service.ts` | **不**改业务代码 |
| 其他测试文件 | **不**动 |
| coverage threshold | **不**加 |

---

## 4. 验证（铁律 6：必须跑测试 0 失败 + coverage 提升）

### 4.1 跑测试套

```powershell
cd D:\mlm-platform-source\mlm-platform
pnpm test
```

**必须 0 失败**。预期：Test Files 8 passed (8)，Tests 60 passed (46 + 9 + 14 - 9 product = 60)。

等等：之前是 55 tests（v45.7.2.a 加 9），现在加 14 = 69。但 46 + 9 + 14 = 69——不是 60。

**实际预期**：Tests 69 passed。

### 4.2 跑 coverage 看提升

```powershell
cd D:\mlm-platform-source\mlm-platform
pnpm test:coverage
```

**必须看到 user.service.ts 在覆盖率表里出现**（之前缺失）—— 预期：

```
  user.service.ts   |   80+ |   50+ |   80+ |   80+
```

**整体数字预期提升**（user.service.ts 234 行 / 8 方法）：
- Statements: 61.37% → ~78%
- Branches: 48.09% → ~55%
- Functions: 71.27% → ~80%
- Lines: 64.16% → ~80%

### 4.3 build 0 错误

```powershell
cd D:\mlm-platform-source\mlm-platform
pnpm build
```

---

## 5. 提交 + 推送（铁律 1）

```powershell
git add .
git commit -m "test(user): 补测 user.service.ts（v45.7.2.b）

- 新建 __tests__/services/user.test.ts
- 14 个 it 覆盖 8 个方法：
  - createUser (3, 无 referrer / 有 referrer / nickname auto-gen)
  - findPlacementPosition (3, BFS 安置算法 + uuid 校验)
  - getPlacementChain (2, 空链 + 多层链)
  - checkAndUpgradeLevel (3, 用户不存在 / 升经销商 / 升董事)
  - getReferrals (1)
  - getTeam (2, 空团队 + 多层)
  - addDirectSales (1)
  - addUpgradeProductCount (2, 默认 + 自定义)
- mock PointsService（升级积分）+ getBusinessConfig（升级规则）
- 预期覆盖率：61.37% → ~78% Statements"
git push origin main

# 必须验证（铁律 1）：
git log origin/main --oneline -1
```

---

## 6. 部署验证

Vercel 自动部署。CI 会在下次 push 触发（v45.7.2.b push 触发）—— 验证 CI 跑通（lint + typecheck + test 0 失败）。

---

## 7. 完成后告诉胡子哥

- commit hash
- 远程 hash 验证通过
- 1 个新文件 + 行数
- **测试套结果**（**必填**）：
  - `pnpm test` 输出最后 5 行（Test Files + Tests + 0 失败）
- **覆盖率结果**（**必填**）：
  - 贴 `pnpm test:coverage` 输出 user.service.ts 行 + 4 个整体数字
- **是否需要 v45.8**（threshold + badge / 其他）

---

## 8. 风险点

1. **`findPlacementPosition` BFS 算法复杂**——`while` 循环 + `queue.shift()`，**难** mock 全部分支
2. **`checkAndUpgradeLevel` 业务规则多**——6 个等级 + 积分奖励 + 直推数累加——**真实代码**复杂，mock 链长
3. **getBusinessConfig mock**——`upgrade.distributor.box_count` / `upgrade.director.sales_amount` 等多个 key——如果 mock 返回值不对，测试会失败
4. **CI 时间增加**—— 14 个 it + coverage 报告 ~5-10s

---

## 已知 trade-off（Mavis 决策记录，不给小金看）

> **Mavis 备注**：本节是 Mavis 派单时的判断依据，**小金不需要看**。

**已确认**：
- ✅ user.service.ts 234 行 / 8 方法
- ✅ 必 mock PointsService + getBusinessConfig
- ✅ findPlacementPosition BFS 算法难 mock——只测**关键**场景（uuid 校验 / 空 / BFS 第一层）
- ✅ checkAndUpgradeLevel 只测**关键**升级（DISTRIBUTOR / DIRECTOR），不测全 6 级

**剩余风险**：
- ⚠️ findPlacementPosition BFS **深**层测试（多层 queue）**没**覆盖——覆盖率可能 ~80% 而非 100%
- ⚠️ checkAndUpgradeLevel 6 级**没**全测——MANAGER/SUPERVISOR/PRESIDENT/BOARD 没 it——覆盖率 ~80%
- ⚠️ getTeam 复杂 BFS 跨层——只测**简单** 2 层
- ⚠️ 小金可能**只**复制上面代码——Mavis **救场**再跑一次看实际跑通

**对小金最后一句提醒**：
> 这是 v45.7.2.b（中单，1.5-2h）。user.service.ts 是**业务核心**（注册/安置/升级）——测试质量比覆盖率重要。**Mavis 必查关键场景**：findPlacementPosition BFS 第一层 + checkAndUpgradeLevel 升 DISTRIBUTOR。**改完不算完，跑通 + 贴数字才算完。**
