# v45.7.2.a：补测 product.service.ts（简单 CRUD）

> **派单时间**：2026-06-24 01:50
> **执行 AI**：小金（GLM-5.1）
> **改动文件**：1 个新文件（__tests__/services/product.test.ts）
> **改动量**：约 100-150 行新增
> **风险等级**：低（只新增测试文件，不改业务代码）

---

## 0. 任务背景

v45.7 baseline 显示 `product.service.ts` **0% 覆盖**（v45.7 报告里没这文件，确认 0%）—— 是测试盲区。

**v45.7.2.a 目标**：补测 product.service.ts 6 个方法（getAllProducts / getProductById / createProduct / updateProduct / deleteProduct / adjustStock）。

**预期覆盖率提升**：60.99% → 约 70% Statements。

**v45.7.2.b 后续**：补测 user.service.ts（234 行 / 8 个方法 / 复杂逻辑）—— 下个 batch。

---

## 1. 必读的真实文件（铁律 3）

小金在动手前**必须**先 read 这 2 个文件真实代码：

```powershell
# 1. product.service.ts 完整（70 行，6 个方法）
Get-Content "D:\mlm-platform-source\mlm-platform\src\lib\services\product.service.ts"

# 2. 已有测试参考（reward.test.ts 完整，看 mock 模式）
Get-Content "D:\mlm-platform-source\mlm-platform\__tests__\services\reward.test.ts"
```

**关键确认**：
- ✅ product.service.ts **6 个方法**全简单 CRUD
- ✅ reward.test.ts 有 mock 模式参考（`createMockChain` / `$transaction` mock）
- ✅ vitest 4.1.8 + @vitest/coverage-v8 已装（v45.7 已配）

---

## 2. 新建 product.test.ts

**文件**：`__tests__/services/product.test.ts`（**新文件**）

参考 reward.test.ts 的 mock 模式：

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'

// Mock prisma before importing service
vi.mock('@/lib/prisma', () => {
  const createMockChain = () => ({
    findUnique: vi.fn(),
    findMany: vi.fn(),
    create: vi.fn(),
    update: vi.fn(),
    updateMany: vi.fn(),
    delete: vi.fn(),
  })

  const mockPrisma: any = {
    product: createMockChain(),
  }
  return { prisma: mockPrisma }
})

import { prisma } from '@/lib/prisma'
import { ProductService } from '@/lib/services/product.service'

describe('ProductService', () => {
  beforeEach(() => {
    vi.clearAllMocks()
  })

  // ========================================
  // getAllProducts
  // ========================================
  describe('getAllProducts', () => {
    it('should return all products ordered by sortOrder', async () => {
      const mockProducts = [
        { id: 'p1', name: 'Product A', sortOrder: 1 },
        { id: 'p2', name: 'Product B', sortOrder: 2 },
      ]
      prisma.product.findMany.mockResolvedValueOnce(mockProducts)

      const result = await ProductService.getAllProducts()

      expect(result).toEqual(mockProducts)
      expect(prisma.product.findMany).toHaveBeenCalledWith({
        orderBy: { sortOrder: 'asc' },
      })
    })
  })

  // ========================================
  // getProductById
  // ========================================
  describe('getProductById', () => {
    it('should return product by id', async () => {
      const mockProduct = { id: 'p1', name: 'Product A' }
      prisma.product.findUnique.mockResolvedValueOnce(mockProduct)

      const result = await ProductService.getProductById('p1')

      expect(result).toEqual(mockProduct)
      expect(prisma.product.findUnique).toHaveBeenCalledWith({
        where: { id: 'p1' },
      })
    })

    it('should return null when product not found', async () => {
      prisma.product.findUnique.mockResolvedValueOnce(null)

      const result = await ProductService.getProductById('nonexistent')

      expect(result).toBeNull()
    })
  })

  // ========================================
  // createProduct
  // ========================================
  describe('createProduct', () => {
    it('should create product with all fields', async () => {
      const productData = {
        name: 'Test Product',
        description: 'A test product',
        retailPrice: 100,
        memberPrice: 80,
        stock: 50,
        isUpgradeProduct: false,
      }
      const mockProduct = { id: 'p1', ...productData }
      prisma.product.create.mockResolvedValueOnce(mockProduct)

      const result = await ProductService.createProduct(productData)

      expect(result).toEqual(mockProduct)
      expect(prisma.product.create).toHaveBeenCalledWith({
        data: productData,
      })
    })

    it('should create product with optional sortOrder', async () => {
      const productData = {
        name: 'Test',
        retailPrice: 100,
        memberPrice: 80,
        stock: 10,
        isUpgradeProduct: true,
        sortOrder: 5,
      }
      prisma.product.create.mockResolvedValueOnce({ id: 'p1', ...productData })

      await ProductService.createProduct(productData)

      expect(prisma.product.create).toHaveBeenCalledWith({
        data: productData,
      })
    })
  })

  // ========================================
  // updateProduct
  // ========================================
  describe('updateProduct', () => {
    it('should update product with partial fields', async () => {
      const updateData = { retailPrice: 120, stock: 100 }
      const mockUpdated = { id: 'p1', name: 'Test', ...updateData }
      prisma.product.update.mockResolvedValueOnce(mockUpdated)

      const result = await ProductService.updateProduct('p1', updateData)

      expect(result).toEqual(mockUpdated)
      expect(prisma.product.update).toHaveBeenCalledWith({
        where: { id: 'p1' },
        data: updateData,
      })
    })
  })

  // ========================================
  // deleteProduct
  // ========================================
  describe('deleteProduct', () => {
    it('should delete product by id', async () => {
      const mockDeleted = { id: 'p1', name: 'Test' }
      prisma.product.delete.mockResolvedValueOnce(mockDeleted)

      const result = await ProductService.deleteProduct('p1')

      expect(result).toEqual(mockDeleted)
      expect(prisma.product.delete).toHaveBeenCalledWith({
        where: { id: 'p1' },
      })
    })
  })

  // ========================================
  // adjustStock（业务关键：库存增减）
  // ========================================
  describe('adjustStock', () => {
    it('should increment stock by positive quantity', async () => {
      const mockUpdated = { id: 'p1', stock: 60 }
      prisma.product.update.mockResolvedValueOnce(mockUpdated)

      const result = await ProductService.adjustStock('p1', 10)

      expect(result).toEqual(mockUpdated)
      expect(prisma.product.update).toHaveBeenCalledWith({
        where: { id: 'p1' },
        data: { stock: { increment: 10 } },
      })
    })

    it('should decrement stock by negative quantity', async () => {
      const mockUpdated = { id: 'p1', stock: 40 }
      prisma.product.update.mockResolvedValueOnce(mockUpdated)

      const result = await ProductService.adjustStock('p1', -10)

      expect(result).toEqual(mockUpdated)
      expect(prisma.product.update).toHaveBeenCalledWith({
        where: { id: 'p1' },
        data: { stock: { increment: -10 } },
      })
    })
  })
})
```

**关键点**：
- 9 个 it（getAllProducts 1 + getProductById 2 + createProduct 2 + updateProduct 1 + deleteProduct 1 + adjustStock 2）
- mock 模式参考 reward.test.ts（createMockChain + vi.clearAllMocks）
- 不用 mock `$transaction`（product.service.ts 不用事务）
- 不用 mock `getBusinessConfig`（product.service.ts 不用配置）
- `adjustStock` 是**业务关键**——2 个 it（正负数都测）

---

## 3. 不要改的地方

| 位置 | 原因 |
|------|------|
| `product.service.ts` | **不**改业务代码——只新增测试 |
| 其他测试文件 | **不**动 |
| `vitest.config.ts` | **不**加——CLI 够用 |
| coverage threshold | **不**加（v45.7 baseline 没改） |

---

## 4. 验证（铁律 6：必须跑测试 0 失败 + coverage 提升）

### 4.1 跑测试套

```powershell
cd D:\mlm-platform-source\mlm-platform
pnpm test
```

**必须 0 失败**。预期：Test Files 7 passed (7)，Tests 55 passed (46 + 9 新)。

### 4.2 跑 coverage 看提升

```powershell
cd D:\mlm-platform-source\mlm-platform
pnpm test:coverage
```

**必须看到 product.service.ts 在覆盖率表里出现**（之前缺失）—— 预期：

```
  product.service.ts |  100.00 |    100.00 |    100.00 |  100.00
```

**整体数字预期提升**：
- Statements: 60.99% → ~70%
- Functions: 69.31% → ~75%
- Branches: 48.09% → ~52%
- Lines: 63.77% → ~72%

### 4.3 build 0 错误（不必须，但推荐）

```powershell
cd D:\mlm-platform-source\mlm-platform
pnpm build
```

---

## 5. 提交 + 推送（铁律 1）

```powershell
git add .
git commit -m "test(product): 补测 product.service.ts（v45.7.2.a）

- 新建 __tests__/services/product.test.ts
- 9 个 it 覆盖 6 个方法：
  - getAllProducts (1)
  - getProductById (2，含 null case)
  - createProduct (2，含 sortOrder 可选)
  - updateProduct (1，部分字段)
  - deleteProduct (1)
  - adjustStock (2，正负数)
- 预期覆盖率：60.99% → ~70% Statements"
git push origin main

# 必须验证（铁律 1）：
git log origin/main --oneline -1
```

---

## 6. 部署验证

Vercel 自动部署。这次**只新增测试文件**，Vercel 部署主要是同步 commit。

CI 会在下次 push 触发（v45.7.2.a push 触发）—— 验证 CI 跑通（lint + typecheck + test 0 失败）。

---

## 7. 完成后告诉胡子哥

- commit hash
- 远程 hash 验证通过
- 1 个新文件 + 行数
- **测试套结果**（**必填**）：
  - `pnpm test` 输出最后 5 行（Test Files + Tests + 0 失败）
- **覆盖率结果**（**必填**）：
  - 贴 `pnpm test:coverage` 输出 product.service.ts 行 + 4 个百分比 + 整体数字
  - 4 个整体数字：Stmts / Branch / Funcs / Lines
- **是否需要 v45.7.2.b**（user.service.ts 补测）

---

## 8. 风险点

1. **product.test.ts 不存在**——**新文件**，mock 模式参考 reward.test.ts
2. **vitest coverage 可能不完全覆盖 product.service.ts**—— 6 个方法都是简单 CRUD，**应该**能 100% 覆盖
3. **CI 时间增加**—— ~5s（新增 9 个 it + coverage 报告）
4. **build 0 错误**—— 测试文件不参与 build，**应该**不破坏

---

## 已知 trade-off（Mavis 决策记录，不给小金看）

> **Mavis 备注**：本节是 Mavis 派单时的判断依据，**小金不需要看**。

**已确认**：
- ✅ product.service.ts 70 行 / 6 个方法全简单 CRUD
- ✅ reward.test.ts 的 mock 模式可复用
- ✅ 不需要 mock $transaction / getBusinessConfig
- ✅ 拆分 a/b 是降低单 batch 风险（小金不跑实测模式下大单危险）

**剩余风险**：
- ⚠️ 9 个 it 可能不够覆盖 100%——`adjustStock` 边界 case 缺失（如 quantity=0 怎么办）—— 真实代码 `prisma.product.update` 接受 quantity=0 但 stock 不变，**没**显式校验
- ⚠️ createProduct 的 description 可选 / sortOrder 可选——只测 1 个完整 + 1 个带 sortOrder，**没**测 description 单独缺失
- ⚠️ 小金可能**只**复制上面代码——**Mavis 救场**再跑一次看实际跑通

**对小金最后一句提醒**：
> 这是 v45.7.2.a（小单）—— 9 个 it 简单 CRUD。**前 5 个 batch 你没交实测结果**——这次**必须**贴 `pnpm test` + `pnpm test:coverage` 数字。不交 = 退回。
