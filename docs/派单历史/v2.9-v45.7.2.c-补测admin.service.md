# v45.7.2.c：补测 admin.service.ts 其他 7 个方法

> **派单时间**：2026-06-24 02:25
> **执行 AI**：小金（GLM-5.1）
> **改动文件**：1 个扩展（__tests__/services/admin.test.ts 追加 describe 块）
> **改动量**：约 200-300 行新增
> **风险等级**：低-中（admin 现有 1 个 describe 块已测 settleDividends，**不**动它；只追加 7 个新 describe）

---

## 0. 任务背景

v45.7 baseline 显示 `admin.service.ts` **31.57% 覆盖**——v44.3 派单**只**测了 `settleDividends` 4 个 it（commit `f2f7d46` v44.3）。

**Mavis 核验**（v45.7.2.b 核验时）：admin.test.ts 142 行**只**有 1 个 describe 块（settleDividends），其他 7 个方法**完全没测**。

**v45.7.2.c 目标**：追加 7 个 describe 块（getSystemStats / getUsers / getUserDetail / updateUserLevel / updateUserStatus / getOrders / getRewards）——预计 +5-7% 整体覆盖率。

**预期**：60% → 65% → **70%+** Statements（v45 系列收口 70%）。

---

## 1. 必读的真实文件（铁律 3）

小金在动手前**必须**先 read 这 2 个文件真实代码：

```powershell
# 1. admin.service.ts 完整（317 行，8 个方法）—— 已 read by Mavis
Get-Content "D:\mlm-platform-source\mlm-platform\src\lib\services\admin.service.ts"

# 2. 现有 admin.test.ts 完整（142 行，1 个 describe 块 settleDividends）
Get-Content "D:\mlm-platform-source\mlm-platform\__tests__\services\admin.test.ts"
```

**关键确认**：
- ✅ admin.test.ts 有 `groupBy` mock（line 15）—— 已有 `getSystemStats` 需要
- ✅ 现有 `mockPrisma` 包含 `user / reward / dividend / order / balanceRecord` —— **缺** `balanceRecord` 也加（updateUserLevel 不调 balanceRecord，但 getRewards / getOrders 不调——OK 不需要）
- ✅ 现有 mock 缺 `logOperation` mock —— **需要新增**（updateUserLevel 调 logOperation）

---

## 2. 改动 1：admin.test.ts 顶部加 logOperation mock

**文件**：`__tests__/services/admin.test.ts`

**位置**：line 1-2 之后（import 之前）

**新增**：
```ts
// Mock logOperation（admin.service.ts:updateUserLevel 调）
vi.mock('@/lib/utils/operation-log', () => ({
  logOperation: vi.fn(),
}))
```

**关键点**：
- 必须在 import `AdminService` **之前** mock
- 现有 `mockPrisma` 不动 —— 复用

---

## 3. 改动 2：admin.test.ts 末尾追加 7 个 describe 块

**位置**：admin.test.ts 末尾（settleDividends describe 块之后，整个文件结尾）

**追加代码**：

```ts
  // ========================================
  // getSystemStats
  // ========================================
  describe('getSystemStats', () => {
    it('should return aggregate system stats', async () => {
      prisma.user.count.mockResolvedValueOnce(100)
      prisma.order.count.mockResolvedValueOnce(50)
      prisma.order.aggregate.mockResolvedValueOnce({ _sum: { payAmount: 50000 } })
      prisma.reward.aggregate.mockResolvedValueOnce({ _sum: { amount: 5000 } })
      prisma.user.groupBy.mockResolvedValueOnce([
        { level: 0, _count: { _all: 10 } },
        { level: 1, _count: { _all: 80 } },
        { level: 2, _count: { _all: 10 } },
      ])
      prisma.order.count.mockResolvedValueOnce(5)  // todayOrders
      prisma.order.aggregate.mockResolvedValueOnce({ _sum: { payAmount: 1000 } })  // todaySales

      const result = await AdminService.getSystemStats()

      expect(result).toEqual({
        totalUsers: 100,
        totalOrders: 50,
        totalSales: 50000,
        totalRewards: 5000,
        usersByLevel: { 0: 10, 1: 80, 2: 10 },
        todayOrders: 5,
        todaySales: 1000,
      })
    })

    it('should handle empty/zero stats', async () => {
      prisma.user.count.mockResolvedValueOnce(0)
      prisma.order.count.mockResolvedValueOnce(0)
      prisma.order.aggregate.mockResolvedValueOnce({ _sum: { payAmount: null } })
      prisma.reward.aggregate.mockResolvedValueOnce({ _sum: { amount: null } })
      prisma.user.groupBy.mockResolvedValueOnce([])
      prisma.order.count.mockResolvedValueOnce(0)
      prisma.order.aggregate.mockResolvedValueOnce({ _sum: { payAmount: null } })

      const result = await AdminService.getSystemStats()

      expect(result.totalUsers).toBe(0)
      expect(result.totalSales).toBe(0)
      expect(result.usersByLevel).toEqual({})
    })
  })

  // ========================================
  // getUsers（分页 + 过滤）
  // ========================================
  describe('getUsers', () => {
    it('should return paginated users with default filters', async () => {
      const mockUsers = [{ id: 'u1', phone: '111' }, { id: 'u2', phone: '222' }]
      prisma.user.findMany.mockResolvedValueOnce(mockUsers)
      prisma.user.count.mockResolvedValueOnce(100)

      const result = await AdminService.getUsers(1, 20)

      expect(result.users).toEqual(mockUsers)
      expect(result.pagination).toEqual({
        page: 1, limit: 20, total: 100, totalPages: 5,
      })
    })

    it('should apply level filter', async () => {
      prisma.user.findMany.mockResolvedValueOnce([])
      prisma.user.count.mockResolvedValueOnce(0)

      await AdminService.getUsers(1, 20, { level: 2 })

      expect(prisma.user.findMany).toHaveBeenCalledWith(
        expect.objectContaining({
          where: { level: 2 },
        })
      )
    })

    it('should apply search filter (phone/nickname)', async () => {
      prisma.user.findMany.mockResolvedValueOnce([])
      prisma.user.count.mockResolvedValueOnce(0)

      await AdminService.getUsers(1, 20, { search: '138' })

      expect(prisma.user.findMany).toHaveBeenCalledWith(
        expect.objectContaining({
          where: {
            OR: [
              { phone: { contains: '138' } },
              { nickname: { contains: '138' } },
            ],
          },
        })
      )
    })
  })

  // ========================================
  // getUserDetail
  // ========================================
  describe('getUserDetail', () => {
    it('should return user with referrals, orders, rewards', async () => {
      const mockDetail = {
        id: 'u1', phone: '111',
        referrals: [],
        orders: [],
        rewards: [],
      }
      prisma.user.findUnique.mockResolvedValueOnce(mockDetail)

      const result = await AdminService.getUserDetail('u1')

      expect(result).toEqual(mockDetail)
      expect(prisma.user.findUnique).toHaveBeenCalledWith({
        where: { id: 'u1' },
        include: {
          referrals: { orderBy: { createdAt: 'desc' }, take: 10 },
          orders: { orderBy: { createdAt: 'desc' }, take: 10 },
          rewards: { orderBy: { createdAt: 'desc' }, take: 10 },
        },
      })
    })
  })

  // ========================================
  // updateUserLevel（**调 logOperation** 审计）
  // ========================================
  describe('updateUserLevel', () => {
    it('should update user level and write operation log', async () => {
      prisma.user.findUnique.mockResolvedValueOnce({ level: 1 })
      prisma.user.update.mockResolvedValueOnce({ id: 'u1', level: 2 })

      const { logOperation } = await import('@/lib/utils/operation-log')
      vi.mocked(logOperation).mockResolvedValueOnce(undefined as any)

      const result = await AdminService.updateUserLevel('u1', 2, 'admin-1')

      expect(result).toEqual({ id: 'u1', level: 2 })
      expect(prisma.user.update).toHaveBeenCalledWith({
        where: { id: 'u1' },
        data: { level: 2 },
      })
      expect(logOperation).toHaveBeenCalledWith({
        userId: 'admin-1',
        action: 'UPDATE',
        module: 'user',
        targetId: 'u1',
        oldValue: { level: 1 },
        newValue: { level: 2 },
      })
    })

    it('should not write log when operatorId is not provided', async () => {
      prisma.user.findUnique.mockResolvedValueOnce({ level: 1 })
      prisma.user.update.mockResolvedValueOnce({ id: 'u1', level: 2 })

      const { logOperation } = await import('@/lib/utils/operation-log')
      vi.mocked(logOperation).mockClear()

      await AdminService.updateUserLevel('u1', 2)

      expect(logOperation).not.toHaveBeenCalled()
    })
  })

  // ========================================
  // updateUserStatus
  // ========================================
  describe('updateUserStatus', () => {
    it('should update user status', async () => {
      prisma.user.update.mockResolvedValueOnce({ id: 'u1', status: 'banned' })

      const result = await AdminService.updateUserStatus('u1', 'banned')

      expect(result).toEqual({ id: 'u1', status: 'banned' })
      expect(prisma.user.update).toHaveBeenCalledWith({
        where: { id: 'u1' },
        data: { status: 'banned' },
      })
    })
  })

  // ========================================
  // getOrders
  // ========================================
  describe('getOrders', () => {
    it('should return paginated orders with user and items', async () => {
      const mockOrders = [{ id: 'o1', orderNo: 'ORD1', user: { id: 'u1' }, items: [] }]
      prisma.order.findMany.mockResolvedValueOnce(mockOrders)
      prisma.order.count.mockResolvedValueOnce(50)

      const result = await AdminService.getOrders(1, 20)

      expect(result.orders).toEqual(mockOrders)
      expect(result.pagination.total).toBe(50)
    })

    it('should apply status filter', async () => {
      prisma.order.findMany.mockResolvedValueOnce([])
      prisma.order.count.mockResolvedValueOnce(0)

      await AdminService.getOrders(1, 20, { status: 'paid' })

      expect(prisma.order.findMany).toHaveBeenCalledWith(
        expect.objectContaining({
          where: { status: 'paid' },
        })
      )
    })

    it('should apply search filter on orderNo', async () => {
      prisma.order.findMany.mockResolvedValueOnce([])
      prisma.order.count.mockResolvedValueOnce(0)

      await AdminService.getOrders(1, 20, { search: 'ORD123' })

      expect(prisma.order.findMany).toHaveBeenCalledWith(
        expect.objectContaining({
          where: { orderNo: { contains: 'ORD123' } },
        })
      )
    })
  })

  // ========================================
  // getRewards
  // ========================================
  describe('getRewards', () => {
    it('should return paginated rewards with user and order', async () => {
      const mockRewards = [{ id: 'r1', user: { id: 'u1' }, order: { id: 'o1' } }]
      prisma.reward.findMany.mockResolvedValueOnce(mockRewards)
      prisma.reward.count.mockResolvedValueOnce(30)

      const result = await AdminService.getRewards(1, 20)

      expect(result.rewards).toEqual(mockRewards)
      expect(result.pagination.total).toBe(30)
    })

    it('should apply filters (type, status, userId)', async () => {
      prisma.reward.findMany.mockResolvedValueOnce([])
      prisma.reward.count.mockResolvedValueOnce(0)

      await AdminService.getRewards(1, 20, {
        type: 'referral',
        status: 'paid',
        userId: 'u1',
      })

      expect(prisma.reward.findMany).toHaveBeenCalledWith(
        expect.objectContaining({
          where: {
            type: 'referral',
            status: 'paid',
            userId: 'u1',
          },
        })
      )
    })
  })
})
```

**关键点**：
- 14 个 it（getSystemStats 2 + getUsers 3 + getUserDetail 1 + updateUserLevel 2 + updateUserStatus 1 + getOrders 3 + getRewards 2）
- mock `logOperation`（updateUserLevel 调）
- **不**碰现有 settleDividends 4 个 it
- 现有 `mockPrisma` 包含 `groupBy`（line 15）—— getSystemStats 需要

---

## 4. 不要改的地方

| 位置 | 原因 |
|------|------|
| `admin.service.ts` | **不**改业务代码 |
| `admin.test.ts` 现有 settleDividends describe 块 | **不**碰，v44.3 已测 |
| 其他测试文件 | **不**动 |

---

## 5. 验证（铁律 6：必须跑测试 0 失败 + coverage 提升）

### 5.1 跑测试套

```powershell
cd D:\mlm-platform-source\mlm-platform
pnpm test
```

**必须 0 失败**。预期：Test Files 8 passed (8)，Tests 83 passed (69 + 14)。

### 5.2 跑 coverage 看提升

```powershell
cd D:\mlm-platform-source\mlm-platform
pnpm test:coverage
```

**必须看到 admin.service.ts 在覆盖率表里**且**整体数字提升**：

预期：
- Statements: 64.79% → ~72%
- Branches: 51.91% → ~58%
- Functions: 72.11% → ~80%
- Lines: 67.46% → ~75%

admin.service.ts 单文件预期 70-80% 覆盖（之前 31.57%）。

### 5.3 build 0 错误

```powershell
cd D:\mlm-platform-source\mlm-platform
pnpm build
```

---

## 6. 提交 + 推送（铁律 1）

```powershell
git add .
git commit -m "test(admin): 补测 admin.service.ts 其他 7 方法（v45.7.2.c）

- 追加 7 个 describe 块（14 个 it）：
  - getSystemStats (2, 含空数据)
  - getUsers (3, 默认 + level filter + search filter)
  - getUserDetail (1)
  - updateUserLevel (2, 含 operatorId 调 logOperation 审计)
  - updateUserStatus (1)
  - getOrders (3, 默认 + status + search)
  - getRewards (2, 默认 + 三 filter)
- mock logOperation（updateUserLevel 审计）
- 不动现有 settleDividends 测试
- 预期覆盖率：64.79% → ~72% Statements"
git push origin main

# 必须验证（铁律 1）：
git log origin/main --oneline -1
```

---

## 7. 部署验证

Vercel 自动部署。CI 跑通。

---

## 8. 完成后告诉胡子哥

- commit hash
- 远程 hash 验证通过
- 1 个文件改动行数（实际 +X -Y）
- **测试套结果**（**必填**）：
  - `pnpm test` 输出最后 5 行
- **覆盖率结果**（**必填**）：
  - 贴 `pnpm test:coverage` 输出 admin.service.ts 行 + 4 个整体数字
- **是否需要 v45.8**（threshold + badge / 其他）

---

## 9. 风险点

1. **`logOperation` mock 时机**——必须在 `AdminService` import **之前** mock（line 1-2 后）
2. **`getSystemStats` Promise.all 7 个查询**——mock 调用次数**必须**对齐
3. **`getOrders` include 链**——`user: true` + `items: { include: { product: true } }` —— mock 需要返回**完整**链
4. **`updateUserLevel` operatorId 可选**——operatorId **缺**时**不**调 logOperation（line 142-152）

---

## 已知 trade-off（Mavis 决策记录，不给小金看）

> **Mavis 备注**：本节是 Mavis 派单时的判断依据，**小金不需要看**。

**已确认**：
- ✅ admin.test.ts 142 行 / 1 describe 块（settleDividends 4 it）
- ✅ admin.service.ts 8 方法：1 个已测 + 7 个新测
- ✅ 现有 mockPrisma 包含 groupBy（line 15）—— getSystemStats 需要
- ✅ 现有 mockPrisma 缺 logOperation —— 需 mock

**剩余风险**：
- ⚠️ `getOrders` / `getRewards` include 链 mock —— 测试要返回**完整**对象（user / items.product / order）—— mock **复杂**
- ⚠️ `getSystemStats` Promise.all 7 个查询 —— mock 调用次数**严格**对齐
- ⚠️ admin.service.ts 244 行（实际 317 行含空行）—— 7 个新方法 + 14 it —— 覆盖率可能 70-80% 而非 100%
- ⚠️ **v45.7.2.a/b/c 三连发**（v45.7.2.a 30 min / b 1.5-2h / c 1.5-2h）——小金节奏快，但 Mavis 救场也连着 3 次——**节奏风险**

**对小金最后一句提醒**：
> 这是 v45.7.2.c（v45.7.2 系列最后冲刺）——admin.service.ts 8 个方法 7 个新测。**改完不算完，跑通 + 贴数字 + 报告 v45.7.2 系列 3 批次对比**才算完。
