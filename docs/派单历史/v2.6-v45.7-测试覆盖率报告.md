# v45.7 测试覆盖率报告：vitest coverage + CI 集成

> **派单时间**：2026-06-24 01:10
> **执行 AI**：小金（GLM-5.1）
> **改动文件**：2 个
> **改动量**：约 8-12 行新增
> **风险等级**：低（不涉及业务代码）

---

## 0. 任务背景

v45.6 收官后，v45 系列最后一环：**测试覆盖率报告**。

**v45.7 目标**：让 vitest 跑覆盖率报告，CI 自动跑，看到当前覆盖率数字。

**重要决策**：
- **路径 1（简版）**：只生成报告，**不**加 threshold（不会卡 CI）
- **路径 2（强版）**：加 threshold（如 80%），没到就 fail CI
- **本次走路径 1**——先看数字，下次再决定阈值

**为什么是路径 1**：
- v44/v45 系列测试只覆盖 reward/dividend/order/withdrawal/admin 5 个 service
- 实际代码覆盖比例可能 30-50%——路径 2 threshold 80% 会立即 fail
- 路径 1 给数字不卡 CI，**符合 Mavis "看现状再定策略"** 原则

---

## 1. 必读的真实文件（铁律 3）

小金在动手前**必须**先 read 这 2 个文件真实代码：

```powershell
# 1. package.json（看 scripts 段 + vitest/coverage 依赖）
Get-Content "D:\mlm-platform-source\mlm-platform\package.json"

# 2. .github/workflows/test.yml（看 CI 测试步骤结构）
Get-Content "D:\mlm-platform-source\mlm-platform\.github\workflows\test.yml"
```

**关键确认**：
- ✅ `@vitest/coverage-v8@^4.1.8` 已装
- ✅ `vitest@^4.1.8` 已装
- ✅ `pnpm test = "vitest run"` 已有
- ❌ **没有** `vitest.config.ts` —— CLI 参数够用，**不动**
- ❌ **没有** `test:coverage` script
- ❌ **没有** coverage 步骤

---

## 2. 改动 1：package.json 加 test:coverage script

**文件**：`package.json`

**位置**：scripts 段（"test" 后面）

**当前代码**：
```json
"test": "vitest run",
"test:watch": "vitest",
"test:services": "npm run build && node test-services.js",
```

**改后代码**：
```json
"test": "vitest run",
"test:watch": "vitest",
"test:coverage": "vitest run --coverage",  // v45.7: 加 coverage 脚本
"test:services": "npm run build && node test-services.js",
```

**关键点**：
- 1 行新增
- 不动其他 script
- `vitest run --coverage` 用 v8 provider（性能好）

---

## 3. 改动 2：test.yml 加 coverage 步骤

**文件**：`.github/workflows/test.yml`

**位置**：Run tests 步骤之后

**当前代码**：
```yaml
      - name: Lint
        run: pnpm lint
      - name: TypeScript type check
        run: pnpm typecheck
      - name: Run tests
        run: pnpm test
```

**改后代码**：
```yaml
      - name: Lint
        run: pnpm lint
      - name: TypeScript type check
        run: pnpm typecheck
      - name: Run tests
        run: pnpm test
      - name: Run tests with coverage
        run: pnpm test:coverage
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage/
          retention-days: 30
```

**关键点**：
- 加 2 步：跑 coverage + 上传 artifact
- `if: always()` 即使测试失败也上传（方便看 coverage）
- `retention-days: 30` 30 天后自动清理
- `coverage/` 是 vitest 默认输出目录

---

## 4. 不要改的地方（防范围扩散）

| 位置 | 原因 |
|------|------|
| `vitest.config.ts` 新建 | **不需要**——CLI 参数够用 |
| coverage threshold | **不**加（v45.7.1 再做）|
| README badge | **不**加（v45.7.1 再做）|
| 测试代码 | **不**改——只生成现有测试的覆盖率 |
| 业务代码 | **不**改 |
| 覆盖率"补测" | **不**做——只让现有测试能跑覆盖率 |

---

## 5. 验证（铁律 6：必须跑测试 0 失败 + 跑 coverage 看到报告）

### 5.1 本地跑 coverage

```powershell
cd D:\mlm-platform-source\mlm-platform
pnpm test:coverage
```

**预期输出**：
```
 Test Files  6 passed (6)
      Tests  46 passed (46)
   Coverage:
   File           | % Stmts | % Branch | % Funcs | % Lines
   ------------------|---------|----------|---------|---------
   All files        |   XX.X  |    XX.X  |   XX.X  |   XX.X
   ...
```

**必须看到 4 列百分比**——这是 v45.7 的核心交付物。

### 5.2 测试套仍 0 失败

`pnpm test:coverage` 跑通的同时，`pnpm test` 也要 0 失败（不应该被 coverage 标志破坏）。

### 5.3 build 0 错误（不必须跑，但推荐）

```powershell
cd D:\mlm-platform-source\mlm-platform
pnpm build
```

### 5.4 coverage 报告位置

跑完会生成 `coverage/` 目录：
- `coverage/index.html` —— HTML 报告（可在浏览器看）
- `coverage/coverage-summary.json` —— JSON 摘要
- `coverage/lcov.info` —— lcov 格式（CI artifact 上传这个）

---

## 6. 提交 + 推送（铁律 1）

```powershell
git add .
git commit -m "ci: 加 vitest coverage 报告（v45.7）

- package.json: 加 test:coverage script (vitest run --coverage)
- .github/workflows/test.yml: 加 coverage 步骤 + 上传 artifact
- 路径 1 简版：只生成报告，不加 threshold（v45.7.1 再定）
- @vitest/coverage-v8@^4.1.8 已装（无需 npm install）"
git push origin main

# 必须验证（铁律 1）：
git log origin/main --oneline -1
```

确认 commit hash 在远程。

---

## 7. 部署验证

Vercel 自动部署。这次**不涉及**业务代码，**Vercel 部署主要是同步 commit**。

CI 会在下次 push 触发（v45.7 push 触发）—— 验证 CI 跑通 coverage 步骤。

---

## 8. 完成后告诉胡子哥

- commit hash
- 远程 hash 验证通过
- 2 个文件改动行数
- **覆盖率数字**（**必填**）：
  - 贴 `pnpm test:coverage` 输出最后 10-15 行（Coverage 表）
  - 4 个百分比：Stmts / Branch / Funcs / Lines
- **当前哪些 service 有测试覆盖 / 哪些没有**（观察结论）
- **是否需要在 v45.7.1 加 threshold**（你的建议）

---

## 9. 风险点

1. **coverage 数字可能很低**（< 30%）—— 现有测试只覆盖 5 个 service，**其他** service / route / page 都没测
2. **coverage report 噪音**—— vitest 默认会包含所有 .ts 文件（包括 lib/utils/ 下的工具函数）
3. **CI 时间增加**—— `pnpm test:coverage` 比 `pnpm test` 慢（v8 插桩），但应该 < 30s

---

## 10. 下一步建议（v45.7.1 候选）

**v45.7.1**（如果胡子哥决定继续）：
- 跑完 baseline 后**决定** threshold（如 50% 起步）
- CI 加 threshold check（没到 fail）
- README 加 coverage badge

**v45.7.2**：
- 给"覆盖率 < 50% 的 service"补测试（v44 系列风格的测试增量）
- 提升覆盖率到 60% / 70% / 80%

**v46**：
- 转新功能（胡子哥定方向）

---

## 已知 trade-off（Mavis 决策记录，不给小金看）

> **Mavis 备注**：本节是 Mavis 派单时的判断依据，**小金不需要看**。

**已确认**：
- ✅ `@vitest/coverage-v8` 已装（v4.1.8）
- ✅ vitest 4.1.8 已装
- ✅ `vitest run --coverage` 用 CLI 参数够用
- ✅ 路径 1（不卡 CI）vs 路径 2（卡 CI）—— 选路径 1

**剩余风险**：
- ⚠️ coverage 数字可能 < 30%——预期之内
- ⚠️ CI 时间增加 ~10-20s——可接受
- ⚠️ v45.7.1 决定 threshold 需要 Mavis 实战数字——Mavis 自己跑

**对小金最后一句提醒**：
> 这是 v45 系列收官 batch。前 6 个 batch 你没交测试结果/截图——这次 `pnpm test:coverage` 输出**必须**贴最后 10 行覆盖率数字，**不算完成**。改完不算完，跑通 + 贴数字才算完。
