# 猫爪_007号结果：收益作废提交前收尾

## 1. 最终结论

**完成。**

三个收尾点全部完成，86 个自动化测试全部通过，类型检查通过，构建通过，未修改数据库结构，未 commit/push/部署。

---

## 2. 修改文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/app/api/admin/users/[id]/balance/route.ts` | 修改 | catch 中映射"可用收益不足"为 400；保存 balanceRecord.create 返回值并带出 balanceRecordId；传给 notifyEarningsVoid |
| `src/lib/services/order-notification.service.ts` | 修改 | notifyEarningsVoid 新增 balanceRecordId 参数；sendInApp 增加 sourceType='balance_record' + sourceId |
| `__tests__/api/admin/users/balance.test.ts` | 修改 | 测试 13 改为断言 400；测试 16 加 balanceRecordId 断言；新增测试 19（updateMany 精确断言）和测试 20（balanceRecordId 传递链） |
| `__tests__/services/order-notification.test.ts` | 修改 | baseParams 加 balanceRecordId；测试 1 加 sourceType/sourceId 断言 |

---

## 3. 可用收益不足返回状态码

**400。**

修复前：`updateMany.count === 0` → 抛 `Error('可用收益不足')` → 外层 catch 统一返回 500。

修复后：
```typescript
} catch (error) {
  const message = error instanceof Error ? error.message : '资金调整失败'
  // v007: 可用收益不足属于业务校验失败，返回 400 而非 500
  const isBusinessError = message === '可用收益不足'
  return NextResponse.json(
    { success: false, message },
    { status: isBusinessError ? 400 : 500 }
  )
}
```

测试 13 验证：
- `res.status === 400` ✅
- `data.success === false` ✅
- `data.message` 包含"可用收益不足" ✅
- 不写流水 ✅
- 不写日志 ✅
- 不发通知 ✅

---

## 4. 其他未知异常返回状态码

**500。**

只有 `message === '可用收益不足'` 时返回 400，其他所有异常（数据库连接失败、事务回滚、未知错误等）仍返回 500。

---

## 5. balanceRecordId 如何从事务传到通知

### 事务内

```typescript
// v007: 保存资金流水创建结果，带出 balanceRecordId 给通知关联
const balanceRecord = await tx.balanceRecord.create({
  data: { ... }
})

return {
  updated,
  oldValue: actualOld,
  adjustType,
  mapping: TYPE_FIELD_MAP[adjustType],
  balanceRecordId: balanceRecord.id,  // ← 从 create 返回值拿 id
}
```

### 路由层

```typescript
await OrderNotificationService.notifyEarningsVoid({
  userId: id,
  amount,
  earningsAvailable: result.updated.earningsAvailable,
  earningsVoided: result.updated.earningsVoided,
  reason: reason.trim(),
  operatorId: admin.id,
  balanceRecordId: result.balanceRecordId!,  // ← 传给通知
}).catch(...)
```

### 通知方法

```typescript
static async notifyEarningsVoid(params: {
  userId: string
  amount: number
  earningsAvailable: number
  earningsVoided: number
  reason: string
  operatorId?: string
  balanceRecordId: string  // ← 新增参数
}) {
  // ...
  await sendInApp({
    userId: params.userId,
    templateType: 'earnings_voided',
    variables,
    batchId: b.id,
    senderId: params.operatorId,
    sourceType: 'balance_record',           // ← 关联资金流水
    sourceId: params.balanceRecordId,       // ← 本次唯一流水编号
  })
}
```

测试 20 验证：balanceRecord.create 返回 `{ id: 'br-special-999' }` → notifyEarningsVoid 收到 `balanceRecordId: 'br-special-999'` ✅

---

## 6. 通知 sourceType/sourceId 结果

| 字段 | 值 | 说明 |
|------|-----|------|
| `sourceType` | `'balance_record'` | 固定值，标识来源为资金流水 |
| `sourceId` | `params.balanceRecordId` | 本次操作创建的 BalanceRecord.id |

通知服务测试 1 验证：
```typescript
expect(notifications.sendCalls[0]).toMatchObject({
  sourceType: 'balance_record',
  sourceId: 'br-ev-001',
})
```
✅ 通过

---

## 7. 原子更新精确断言结果

测试 19 直接断言 `updateMany` 的 where 和 data：

```typescript
expect(tx.user.updateMany).toHaveBeenCalledWith({
  where: {
    id: 'u1',
    earningsAvailable: { gte: 40 },
  },
  data: {
    earningsAvailable: { decrement: 40 },
    earningsVoided: { increment: 40 },
  },
})
```

✅ 通过

---

## 8. 其他资金字段不变断言结果

测试 19 同时直接断言 `updateMany.data` 不包含其他字段：

```typescript
const updateManyCall = tx.user.updateMany.mock.calls[0][0]
expect(updateManyCall.data).not.toHaveProperty('balance')         // ✅
expect(updateManyCall.data).not.toHaveProperty('frozenBalance')   // ✅
expect(updateManyCall.data).not.toHaveProperty('consumeBalance')  // ✅
expect(updateManyCall.data).not.toHaveProperty('earningsFrozen')  // ✅
expect(updateManyCall.data).not.toHaveProperty('earningsPending') // ✅
```

此外测试 4/5/6 也从不同角度验证了字段不变（测试 4 检查 consumeBalance，测试 5 检查 earningsFrozen，测试 6 检查 earningsPending）。

---

## 9. 接口测试结果和数量

`__tests__/api/admin/users/balance.test.ts`：**20 个测试，全部通过。**

| # | 测试 | 结果 |
|---|------|------|
| 1 | 正常作废：earningsAvailable 减少、earningsVoided 增加 | ✅ |
| 2 | balance 不变 | ✅ |
| 3 | frozenBalance 不变 | ✅ |
| 4 | consumeBalance 不变 | ✅ |
| 5 | earningsFrozen 不变 | ✅ |
| 6 | earningsPending 不变 | ✅ |
| 7 | 金额 0 返回 400 | ✅ |
| 8 | 负数返回 400 | ✅ |
| 9 | 字符串返回 400 | ✅ |
| 10 | NaN 返回 400 | ✅ |
| 11 | Infinity 返回 400 | ✅ |
| 12 | -Infinity 返回 400 | ✅ |
| 13 | 可用收益不足返回 400 | ✅ |
| 14 | 写入 BalanceRecord | ✅ |
| 15 | OperationLog 前后值正确 | ✅ |
| 16 | 调用 notifyEarningsVoid 并传入 balanceRecordId | ✅ |
| 17 | notifyEarningsVoid 抛错时接口仍返回成功 | ✅ |
| 18 | earnings_add 正常路径不受影响 | ✅ |
| 19 | updateMany where/data 精确匹配 | ✅ |
| 20 | balanceRecordId 从流水创建结果传到通知 | ✅ |

---

## 10. 通知测试结果和数量

`__tests__/services/order-notification.test.ts`：**66 个测试，全部通过。**

其中 `notifyEarningsVoid` 的 7 个测试：

| # | 测试 | 结果 |
|---|------|------|
| 1 | 正常发送 earnings_voided 通知（含 sourceType/sourceId 断言） | ✅ |
| 2 | 模板变量包含金额、剩余可用收益和累计作废收益 | ✅ |
| 3 | 用户不存在时不发送通知 | ✅ |
| 4 | 用户查询失败时不向调用方抛错 | ✅ |
| 5 | 通知批次创建失败时不向调用方抛错 | ✅ |
| 6 | sendInApp 失败时不向调用方抛错 | ✅ |
| 7 | 失败时调用日志记录 | ✅ |

---

## 11. typecheck（类型检查）结果

```
npx tsc --noEmit -p tsconfig.typecheck.json
```

**通过。** 0 错误。

---

## 12. build（构建）结果

```
npx next build
```

**通过。**

```
✓ Compiled successfully in 96s
✓ Generating static pages (110/110)
```

---

## 13. git diff --check 结果

```
git diff --check
```

**通过。** 退出码 0。仅有 LF→CRLF 换行符警告（Windows 正常现象）。

---

## 14. git status --short 结果

本轮修改的文件（M）：
```
 M __tests__/services/order-notification.test.ts
 M src/app/api/admin/users/[id]/balance/route.ts
 M src/lib/services/order-notification.service.ts
 M prisma/seed.ts                    （005 保留）
 M src/app/admin/users/page.tsx      （005 保留）
 M docs/admin-api-audit.md           （非本轮改动）
 M docs/归档/...                     （非本轮改动）
```

新增的文件（??）：
```
?? __tests__/api/admin/users/balance.test.ts   （006 新建，本轮修改）
?? docs/agent-tasks/catpaw/todo/猫爪_007号任务.md
?? docs/agent-tasks/catpaw/done/猫爪_007号结果.md
```

未暂存任何文件（未 `git add`）。

---

## 15. 是否修改数据库结构

**否。**

- 未修改 `prisma/schema.prisma`
- 未新增数据库迁移
- 未执行生产数据库 SQL

---

## 16. 是否同步生产模板

**否。**

`prisma/seed.ts` 中存在 `earnings_voided` 模板定义，但未连接生产数据库，未运行全量 seed。

**部署前置项**：小 M 007 号复审通过后，必须对生产数据库执行 `earnings_voided` 单模板 upsert（存在则更新，不存在则新增），不得运行全量 seed。

---

## 17. 是否 commit（提交）

**否。** 未执行 `git commit`，未执行 `git add`。

---

## 18. 是否 push（推送）

**否。** 未执行 `git push`。

---

## 19. 是否部署

**否。** 未部署到 Vercel 或任何环境。

---

## 20. 遗留风险

1. **生产通知模板未同步**：`earnings_voided` 模板只在 `prisma/seed.ts` 中定义，生产数据库尚无此模板。部署前需执行单条 upsert。
2. **`notifyBalanceChange` 未加路由层 .catch()**：本轮只对 `notifyEarningsVoid` 加了路由层第二道保护，其他调账类型的 `notifyBalanceChange` 仍直接 await（任务要求不改造）。
3. **通用调账逻辑的并发快照**：本轮只修复了 `earnings_void` 的并发快照问题，通用调账逻辑仍使用 `before` 值构造流水（任务要求不扩大修改范围）。
4. **catch 中用错误消息字符串匹配状态码**：`message === '可用收益不足'` 是精确匹配，如果未来修改了错误消息文本，需要同步更新 catch 中的匹配条件。
