# 猫爪_027号结果

**执行时间**：2026-07-15 00:02
**基线 commit**：`baff1843a394fe1e2b21b9bc3e10ccc9d232b53b`
**状态**：✅ 全部闭合

---

## 修复清单

### P0-A：原始接口缺口类型守卫 ✅

**漏洞**：`Number(verifyErr.data?.shortage)` 将 `null`/`''`/`false` 误转为合法 0

**修复**（`src/app/products/[id]/page.tsx` L476-481）：
```typescript
// 修复前
const apiShortage = Number(verifyErr.data?.shortage)
if (Number.isFinite(apiShortage) && apiShortage >= 0) {

// 修复后
const rawApiShortage = verifyErr.data?.shortage
if (typeof rawApiShortage === 'number' && Number.isFinite(rawApiShortage) && rawApiShortage >= 0) {
  actualShortage = rawApiShortage
```

**TDD 记录**：
- 🔴 红灯：5 个新测试全部失败（`not.toMatch(/Number\s*\(\s*verifyErr/)`、`toMatch(/typeof\s+rawApiShortage/)` 等）
- 🟢 绿灯：修复后 47/47 通过

---

### P0-B：新订单禁止硬编码零缺口 ✅

**漏洞**：`shortage: 0` + `persistPendingPayment(orderId, 0)` 硬编码初始缺口为 0

**修复**（`src/app/products/[id]/page.tsx` L442-453）：
```typescript
// 修复前
currentPendingPayment = { orderId, payAmount: currentPayAmount, shortage: 0, ... }
persistPendingPayment(orderId, 0)

// 修复后
const initialShortage = calculatePendingShortage(currentPayAmount, user.balance)
if (initialShortage === null) {
  setRestoreStatus('validation_error')
  setRestoreError('资金数据异常，无法计算初始缺口')
  return null
}
currentPendingPayment = { orderId, payAmount: currentPayAmount, shortage: initialShortage, ... }
persistPendingPayment(orderId, initialShortage)
```

**TDD 记录**：
- 🔴 红灯：4 个新测试全部失败（`not.toMatch(/shortage:\s*0/)`、`not.toMatch(/persistPendingPayment\s*\(\s*orderId\s*,\s*0/)` 等）
- 🟢 绿灯：修复后 47/47 通过

---

### P1-A：锁定收货快照空白值校验与 trim ✅

**漏洞**：`!lockedShipping.recipientName` 对 `'   '` 空白字符串不拦截

**修复 1**（`CheckoutDialog.tsx` L194）— handleConfirm 校验加 `.trim()`：
```typescript
// 修复前
if (!lockedShipping || !lockedShipping.recipientName || !lockedShipping.recipientPhone || !lockedShipping.shippingAddress)

// 修复后
if (!lockedShipping || !lockedShipping.recipientName.trim() || !lockedShipping.recipientPhone.trim() || !lockedShipping.shippingAddress.trim())
```

**修复 2**（`CheckoutDialog.tsx` L220-221）— shippingSnapshot 提交前 trim：
```typescript
// 修复前
? lockedShipping!

// 修复后
? {
    recipientName: lockedShipping!.recipientName.trim(),
    recipientPhone: lockedShipping!.recipientPhone.trim(),
    shippingAddress: lockedShipping!.shippingAddress.trim(),
  }
```

**修复 3**（`CheckoutDialog.tsx` L491）— 确认按钮 disabled 加空白字段检查：
```typescript
// 修复前
disabled={submitting || (hasPendingOrder && !lockedShipping)}

// 修复后
disabled={submitting || (hasPendingOrder && (!lockedShipping || !lockedShipping.recipientName.trim() || !lockedShipping.recipientPhone.trim() || !lockedShipping.shippingAddress.trim()))}
```

**TDD 记录**：
- 🔴 红灯：5 个新测试全部失败
- 🟢 绿灯：修复后 27/27 通过

---

### P1-B：lint Error 修复 + 测试可信度清理 ✅

**修复 1**（`page.tsx` L538）— 删除未使用参数 `amount`：
```typescript
// 修复前
const handleEarningsTransferSuccess = async (amount?: number) => {

// 修复后
const handleEarningsTransferSuccess = async () => {
```

**修复 2** — 测试可信度清理（`product-pending-payment-flow.test.ts`）：

| 模式 | 修复前 | 修复后 |
|------|--------|--------|
| `if (nullBranchIdx === -1)` 兜底 | 6 处，跳过核心断言 | 改为 `expect(idx).toBeGreaterThanOrEqual(0)` 立即失败 |
| `substring(idx, idx + 200/300)` | 5 处固定字符窗口 | 改为 `extractBlock` 精确提取 |
| `A \|\| B` 宽松 OR | 1 处 `includes('null') \|\| includes('validation_error')` | 拆为两个精确 `toContain` 断言 |

**修复 3** — 测试可信度清理（`checkout-pending-order-lock.test.ts`）：

| 模式 | 修复前 | 修复后 |
|------|--------|--------|
| `substring(idx, idx + 300)` | 5 处固定字符窗口 | 改为 `extractBlock` 精确提取 |
| `find(l => ...)` + `toBeDefined()` | 1 处弱断言 | 改为 `expect(idx).toBeGreaterThan(0)` |

---

## 验证结果

| 验证项 | 结果 |
|--------|------|
| `npx tsc --noEmit -p tsconfig.typecheck.json` | ✅ 0 错误 |
| `npx next lint --file CheckoutDialog.tsx --file page.tsx` | ✅ 0 Error（2 Warning 为既有问题） |
| `npx vitest run product-pending-payment-flow.test.ts` | ✅ 47/47 |
| `npx vitest run checkout-pending-order-lock.test.ts` | ✅ 27/27 |
| **合计** | **74/74 全部通过** |

---

## 修改文件清单

| 文件 | 修改类型 |
|------|----------|
| `src/app/products/[id]/page.tsx` | P0-A typeof 守卫 + P0-B calculatePendingShortage 初始缺口 + P1-B 删除未使用参数 |
| `src/components/checkout/CheckoutDialog.tsx` | P1-A trim 校验 + trim 提交 + disabled 空白检查 |
| `__tests__/components/product-pending-payment-flow.test.ts` | P0-A/P0-B 新增 9 个测试 + 清理 6 处假通过模式 |
| `__tests__/components/checkout-pending-order-lock.test.ts` | P1-A 新增 5 个测试 + 清理 5 处固定字符窗口 |

---

## 未修改文件（符合约束）

- ❌ `src/lib/services/order.service.ts`
- ❌ `src/app/api/orders/[id]/route.ts`
- ❌ `src/lib/utils/pending-payment-session.ts`
- ❌ `src/app/cart/page.tsx`
- ❌ `src/app/api/orders/[id]/verify-payment/route.ts`
- ❌ `src/components/EarningsTransferModal.tsx`
- ❌ `prisma/schema.prisma`
- ❌ `AGENTS.md`