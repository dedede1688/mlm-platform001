# 猫爪_033号结果

## 任务概述

将订单详情页退款入口改造成紧凑售后服务卡片（OrderAfterSalesCard），并在前后端阻止待审核或已通过退款的重复申请。

## TDD 执行记录

### 任务1：退款接口进行中状态拦截

| 步骤 | 操作 | 结果 |
|------|------|------|
| 1.1 | 写 `__tests__/api/orders/refund-route.test.ts`（7 个测试） | 完成 |
| 1.2 | 运行确认失败 | 2 failed（pending/approved 拦截断言失败：旧文案"待审核"≠"进行中"） |
| 1.3 | 修改 `src/app/api/orders/[id]/refund/route.ts` | where 条件 `{ in: ['pending', 'approved'] }` + 错误文案统一 + 变量名 `existingActiveRefund` |
| 1.4 | 运行确认通过 | 7/7 passed |

### 任务2：售后服务组件与订单详情接入

| 步骤 | 操作 | 结果 |
|------|------|------|
| 2.1 | 写 `__tests__/components/order-after-sales-card.test.ts`（18 个测试） | 完成 |
| 2.2 | 运行确认失败 | 0 tests（组件不存在，导入失败） |
| 2.3 | 创建 `src/components/orders/OrderAfterSalesCard.tsx` | 完成 |
| 2.4 | 接入订单详情页：删除旧退款卡片 + 底部满宽橙色按钮，渲染 OrderAfterSalesCard | 完成 |
| 2.5 | 运行确认通过 | 18/18 passed |

### 任务3：回归验证

| 检查项 | 结果 |
|--------|------|
| 目标测试 25/25 | passed |
| 生产代码 typecheck | 0 errors |
| 测试文件 TS 类型错误 | 已有模式（vitest mock 类型），非本次引入 |
| ESLint | 项目无 eslint.config，跳过 |
| dev server | 启动成功 http://localhost:3000 |
| 满宽橙色按钮残留 | 0 处 |
| "退款申请审核中"重复提示残留 | 0 处 |
| 文件范围 | 正好 5 个文件（3 修改 + 2 新增），无越界 |

## 修改文件清单

### 修改

1. **`src/app/api/orders/[id]/refund/route.ts`**（L56-65）
   - `where: { orderId, status: 'pending' }` → `where: { orderId, status: { in: ['pending', 'approved'] } }`
   - 错误文案 `该订单已有待审核的退款申请` → `该订单已有进行中的退款申请`
   - 变量名 `existingPending` → `existingActiveRefund`

2. **`src/app/dashboard/orders/[id]/page.tsx`**
   - 新增 `import OrderAfterSalesCard`
   - 删除旧退款申请状态卡片（原 L430-504，含 pending 重复提示和 rejected 重新申请链接）
   - 删除底部满宽橙色"申请退款"按钮和灰色"退款申请审核中"提示（原 L537-557）
   - 在金额明细之后渲染一次 `<OrderAfterSalesCard>`

3. **`__tests__/components/order-after-sales-card.test.ts`**（新建）
   - 18 个测试覆盖：无退款/pending/approved/completed/rejected/不可退款/详情展开/样式规范

4. **`__tests__/api/orders/refund-route.test.ts`**（新建）
   - 7 个测试覆盖：pending 拦截/approved 拦截/rejected 允许/未登录/订单不存在/无权/状态不可退

5. **`src/components/orders/OrderAfterSalesCard.tsx`**（新建）
   - 紧凑售后服务卡片组件
   - 5 种状态渲染：无退款→申请退款、pending→审核中、approved→退款处理中、completed→退款已完成、rejected→重新申请
   - 原生 `<details>/<summary>` 展开退款详情
   - 桌面左右排列，移动端上下排列
   - 小号描边按钮，无满宽橙色大按钮

## 未完成 / 待胡子哥验收

- dev server 截图：Playwright headless 模式无法绕过 `/dashboard/orders/[id]` 登录拦截，需胡子哥登录后截图验收
- 验收项：金额明细下方出现"售后服务"区域、无满宽橙色按钮、每种退款状态只有一个状态表达和最多一个操作