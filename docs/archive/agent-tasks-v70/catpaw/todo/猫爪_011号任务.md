# 猫爪_011号任务：快速清零代码规范阻断错误

## 目标

用最小修改清除当前 15 个文件中的 21 个 `Lint Error（代码规范阻断错误）`。

本轮只执行快速修改和快速验证：

1. 一次性完成全部修改。
2. 运行代码规范检查。
3. 运行类型检查。
4. 写结果文件后停止。

不运行完整测试，不运行 build（构建），不提交，不推送，不部署。这些留到小 M 复审通过后的唯一一次发布验证。

## 精确修改清单

只允许以下修改：

1. `src/app/admin/dashboard/page.tsx`
   - 删除未使用导入 `FileText`。

2. `src/app/admin/finance/page.tsx`
   - 删除未使用导入 `FileText`、`Bell`、`SquareCheck`、`Square`。

3. `src/app/admin/layout.tsx`
   - 删除未使用导入 `FileText`。

4. `src/app/admin/notification-history/page.tsx`
   - 删除未使用导入 `Search`。

5. `src/app/admin/reports/page.tsx`
   - `funnel.funnel.map((step, i) => {` 改为 `funnel.funnel.map((step) => {`。
   - `const [textColor, bgColor] = color.split(' ')` 改为 `const [textColor] = color.split(' ')`。
   - 不得删除漏斗主体中正在使用的另一个 `bgColor`。

6. `src/app/api/admin/dashboard/summary/route.ts`
   - 从 `next/server` 导入中删除 `NextResponse`，保留 `NextRequest`。

7. `src/app/api/admin/role-permissions/route.ts`
   - 从 `next/server` 导入中删除 `NextResponse`，保留 `NextRequest`。

8. `src/app/api/admin/roles/route.ts`
   - 从 `next/server` 导入中删除 `NextResponse`，保留 `NextRequest`。

9. `src/app/api/admin/users/route.ts`
   - 将：
     `const { orders, _count, referrerId, ...rest } = u`
   - 改为：
     `const { orders: _orders, _count, referrerId: _referrerId, ...rest } = u`
   - 必须保留排除 `orders` 和 `referrerId` 的接口行为。

10. `src/app/api/admin/withdrawals/batch-review/route.ts`
    - 删除未使用导入 `WITHDRAWAL_STATUS`。

11. `src/app/api/admin/withdrawals/route.ts`
    - 删除未使用导入 `WithdrawalAuditLogService`、`NotificationService`。
    - 保留正在使用的 `WITHDRAWAL_STATUS`。

12. `src/app/dashboard/balance/page.tsx`
    - 删除未使用导入 `Loader2`。

13. `src/app/dashboard/page.tsx`
    - 从 `recharts` 导入中删除未使用的 `Legend`。

14. `src/app/forgot-password/page.tsx`
    - 删除未使用导入 `Loader2`。

15. `src/components/layout/Header.tsx`
    - `const { user, logout, syncFromStorage } = useAuthStore()` 改为 `const { user, logout } = useAuthStore()`。
    - 保留文件中 `useAuthStore.getState().syncFromStorage()` 的真实调用。

## 禁止事项

- 不处理任何 `Warning（警告）`。
- 不修改业务逻辑、页面布局、接口返回或权限。
- 不修改代码规范配置，不加忽略注释。
- 不修改上述 15 个文件以外的业务或测试文件。
- 不修改数据库、工作流、依赖、锁文件和 `AGENTS.md`。
- 不修改或清理旧文档、`docs/agent-tasks/` 其他文件和 `vercel_ls.txt`。
- 不运行完整测试。
- 不运行 build（构建）。
- 不执行 `git add`、commit（提交）、push（推送）或部署。

## 快速验证

修改完成后只运行：

```powershell
.\node_modules\.bin\next.cmd lint
npx tsc --noEmit -p tsconfig.typecheck.json
git diff --name-only
git diff --check
git status --short
```

通过标准：

- 代码规范检查退出码 0。
- `Error（阻断错误）`从 21 降为 0。
- 允许保留现有 `Warning（警告）`。
- 类型检查 0 错误。
- 业务修改范围恰好是上述 15 个文件。

## 结果文件

完成后写入：

```text
docs/agent-tasks/catpaw/done/猫爪_011号结果.md
```

结果必须包含：

1. 阻断错误从多少降到多少。
2. 实际修改的 15 个文件。
3. 是否处理警告。
4. 是否改变业务逻辑。
5. 代码规范检查结果。
6. 类型检查结果。
7. `git diff --check` 结果。
8. `git status --short` 结果。
9. 是否运行完整测试和构建。
10. 是否提交、推送或部署。

完成后立即停止，等待小 M 011 号快速只读复审。
