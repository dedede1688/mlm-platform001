# 猫爪_034号任务：清理订单售后卡片改造后的代码规范阻断

> **任务类型：** 033 号提交前极小修复  
> **执行角色：** 猫爪  
> **禁止：** commit、push、部署、完整测试、生产构建、数据库操作

---

## 一、背景

小 M 已完成 033 号独立只读复审，业务逻辑、126 个相关测试和正确类型检查均通过，但订单详情页存在 2 个代码规范 Error（错误），阻塞发布：

1. `AlertTriangle` 已导入但未使用；
2. `REFUND_STATUS_CONFIG` 已定义但未使用。

测试文件继续保留为：

```text
__tests__/components/order-after-sales-card.test.ts
```

原因：`vitest.config.ts` 当前只匹配 `__tests__/**/*.test.ts`。本任务禁止重命名测试文件、禁止修改测试配置。

---

## 二、固定工作目录和基线

```text
D:\mlm-platform-source\mlm-platform
```

开始时确认：

```text
本地 HEAD   = f7e01929feca42c092b60ef07677bc0fa3746ac2
origin/main = 0c076092f232c7aef3a27791c34daa27feb363ef
```

本地已有 033 号未提交业务改动和历史任务文件，不得 reset、回滚、覆盖或清理。

---

## 三、允许修改范围

只允许修改 1 个文件：

```text
src/app/dashboard/orders/[id]/page.tsx
```

只做两项删除：

1. 从 `lucide-react` 导入列表删除 `AlertTriangle`；
2. 删除整个未使用的 `REFUND_STATUS_CONFIG` 常量定义。

禁止修改任何业务判断、退款状态规则、组件属性、接口、测试、依赖、配置或样式。

---

## 四、执行顺序

1. 先运行目标代码规范检查，记录修复前 2 个 Error：

```powershell
npx next lint --file "src/app/dashboard/orders/[id]/page.tsx" --file "src/components/orders/OrderAfterSalesCard.tsx" --file "src/app/api/orders/[id]/refund/route.ts"
```

2. 完成上述两项最小删除。
3. 重新运行相同代码规范检查，必须达到 0 Error；既有 Warning（警告）如与本任务无关可如实记录。
4. 运行正确类型检查：

```powershell
npx tsc --noEmit -p tsconfig.typecheck.json
```

5. 运行最小相关回归：

```powershell
npx vitest run __tests__/api/orders/refund-route.test.ts __tests__/components/order-after-sales-card.test.ts __tests__/api/orders/order-detail-route.test.ts __tests__/services/order.test.ts __tests__/services/order-notification.test.ts
```

6. 运行：

```powershell
git diff --check -- "src/app/dashboard/orders/[id]/page.tsx"
git status --short
```

7. 确认 034 净修改只有订单详情页 1 个文件；033 原有 5 文件仍保持。

本任务不运行完整测试、不运行生产构建、不启动开发服务器。

---

## 五、结果文件

写入：

```text
docs/agent-tasks/catpaw/done/猫爪_034号结果.md
```

结果必须包含：

1. 修复前代码规范检查结果；
2. 删除的两项内容；
3. 修复后代码规范检查结果；
4. 类型检查结果；
5. 5 文件、126 个相关测试结果；
6. 034 净修改范围；
7. `git status --short`；
8. 未 commit、未 push、未部署确认。

写完立即停止，等待小 M 034 号独立只读复审。
