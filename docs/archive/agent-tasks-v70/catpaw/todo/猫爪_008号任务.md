# 猫爪_008号任务：补未知异常返回 500 测试

## 一、任务性质

这是收益作废功能提交前的最后一条自动化测试补充。

本任务只补测试，不允许修改任何业务代码。

完成后：

- 不允许 commit（提交）。
- 不允许 push（推送）。
- 不允许部署。
- 不允许连接或修改生产数据库。
- 等待小 M 008 号快速只读复审。

## 二、工作目录

确认当前目录：

```text
D:\mlm-platform-source\mlm-platform
```

先运行：

```powershell
git status --short
```

必须保留 005、006、007 号现有业务和测试改动，不得回退。

## 三、唯一允许修改的文件

```text
__tests__/api/admin/users/balance.test.ts
```

除该测试文件外，本轮不允许修改任何其他文件。

## 四、测试目标

当前路由逻辑为：

```text
可用收益不足 → 400
其他未知异常 → 500
```

现有测试已验证“可用收益不足返回 400”，但没有明确验证“其他未知异常返回 500”。

请在 `balance.test.ts` 末尾新增测试 21。

测试名称建议：

```text
21. 未知异常返回 500，不发送通知
```

测试场景：

1. 鉴权成功。
2. 请求类型为 `earnings_void`。
3. 金额为正数 40。
4. 原因不少于 5 个字。
5. 模拟 `prisma.$transaction` 抛出：

```typescript
new Error('数据库连接断开')
```

6. 调用真实路由 `POST`。

必须断言：

```typescript
expect(res.status).toBe(500)
expect(data.success).toBe(false)
expect(data.message).toBe('数据库连接断开')
expect(OrderNotificationService.notifyEarningsVoid).not.toHaveBeenCalled()
expect(logOperation).not.toHaveBeenCalled()
```

如果事务在进入内部逻辑前直接抛错，无法获取事务内 `balanceRecord.create` 的 mock（模拟对象），不必伪造该断言；路由没有拿到事务结果，自然不能进入日志和通知阶段。

## 五、禁止事项

本轮禁止：

1. 不要修改 `route.ts`。
2. 不要修改通知服务。
3. 不要修改前端页面。
4. 不要修改 `prisma/seed.ts`。
5. 不要修改 `prisma/schema.prisma`。
6. 不要修改 `AGENTS.md`。
7. 不要新增迁移。
8. 不要运行全量 seed。
9. 不要连接生产数据库。
10. 不要 commit、push 或部署。
11. 不要暂存任何文件。

## 六、验证命令

运行：

```powershell
npx vitest run __tests__/api/admin/users/balance.test.ts __tests__/services/order-notification.test.ts
npx tsc --noEmit -p tsconfig.typecheck.json
npx next build
git diff --check
git status --short
```

预期：

- 接口测试从 20 个增加到 21 个。
- 两个指定测试文件总数从 86 增加到 87。
- 类型检查通过。
- 构建通过。
- 只有原有 6 个业务/测试文件处于改动状态。

## 七、结果文件

完成后写入：

```text
docs/agent-tasks/catpaw/done/猫爪_008号结果.md
```

结果文件必须包含：

1. 最终结论。
2. 本轮实际修改文件。
3. 新增测试名称。
4. 未知异常最终状态码。
5. 是否不调用通知。
6. 是否不调用操作日志。
7. 接口测试数量和结果。
8. 两个指定测试文件总数量和结果。
9. typecheck（类型检查）结果。
10. build（构建）结果。
11. git diff --check 结果。
12. git status --short 结果。
13. 是否修改业务代码。
14. 是否修改数据库结构。
15. 是否同步生产模板。
16. 是否 commit（提交）。
17. 是否 push（推送）。
18. 是否部署。

## 八、完成条件

只有同时满足以下条件才能报告完成：

- 只修改 `balance.test.ts`。
- 新增未知异常返回 500 的真实路由测试。
- 测试断言不发送通知、不写操作日志。
- 所有指定测试通过。
- 类型检查通过。
- 构建通过。
- 未修改业务代码。
- 未 commit、未 push、未部署。
