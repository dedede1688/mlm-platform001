# 小M_030号任务：React 测试转译与真实 JSX 渲染独立只读复审

> **复审模式：** 严格只读。  
> **复审对象：** 猫爪_030号任务、真实代码差异和执行结果。  
> **核心目标：** 确认新增依赖最小且兼容，真实组件渲染测试确实执行，并且没有借 mock 或宽松断言假通过。  
> **禁止事项：** 不修改任何文件，不安装依赖，不 commit，不 push，不部署。

---

## 一、固定工作目录

```text
D:\mlm-platform-source\mlm-platform
```

---

## 二、开始前必须读取

1. `AGENTS.md`
2. `docs/agent-tasks/README.md`
3. `docs/agent-tasks/catpaw/todo/猫爪_029号任务.md`
4. `docs/agent-tasks/catpaw/done/猫爪_029号结果.md`
5. `docs/agent-tasks/xiaom/todo/小M_029号任务.md`
6. `docs/agent-tasks/xiaom/done/小M_029号结果.md`
7. `docs/agent-tasks/catpaw/todo/猫爪_030号任务.md`
8. `docs/agent-tasks/catpaw/done/猫爪_030号结果.md`
9. 本任务文件
10. `package.json`
11. `pnpm-lock.yaml`
12. `vitest.config.ts`
13. `tsconfig.json`
14. `src/components/checkout/CheckoutDialog.tsx`
15. `__tests__/components/checkout-pending-order-lock.test.ts`
16. `__tests__/components/product-pending-payment-flow.test.ts`

必须以真实依赖树、真实配置、真实测试代码和真实命令输出为准。

---

## 三、基线核验

预期：

```text
本地 HEAD   = baff1843a394fe1e2b21b9bc3e10ccc9d232b53b
origin/main = 49d3583a68f0f5f5e5794ce16c3a76594028752f
```

运行：

```powershell
Get-Location
git status --short
git log --oneline -3
git log origin/main --oneline -1
git rev-parse HEAD
git rev-parse origin/main
```

基线不一致则停止报告。

---

## 四、030 修改范围核验

030 净新增差异只允许：

```text
package.json
pnpm-lock.yaml
vitest.config.ts
__tests__/components/checkout-pending-order-lock.test.ts
```

必须确认：

- 未修改任何 `src/` 业务文件；
- 未再次修改 `product-pending-payment-flow.test.ts`；
- 未修改 `tsconfig.json`、Next.js 配置或数据库；
- 未生成 `package-lock.json`；
- 未清理历史 docs、任务文件或 `vercel_ls.txt`。

范围越界则结论不得写“通过”。

---

## 五、依赖与锁文件核验

必须独立确认：

1. `package.json` 只新增：

```json
"@vitejs/plugin-react": "6.0.3"
```

2. 插件位于 `devDependencies`，不是生产 dependencies。
3. 未新增 `jsdom`、`happy-dom`、`@rolldown/plugin-babel`、`babel-plugin-react-compiler`。
4. `pnpm-lock.yaml` 只增加插件及必要锁定信息，没有大范围无关版本漂移。
5. 当前 Vite 为 `8.0.16`，插件 peer（对等版本要求）兼容 `vite ^8.0.0`。
6. 当前 Node 版本满足插件 engines。

运行：

```powershell
node --version
npx --yes pnpm@11.13.0 list @vitejs/plugin-react vite vitest --depth 0
Test-Path package-lock.json
git diff -- package.json pnpm-lock.yaml
```

不得依据猫爪报告省略锁文件审查。

---

## 六、Vitest 配置核验

确认 `vitest.config.ts`：

- 导入 `@vitejs/plugin-react`；
- 顶层配置 `plugins: [react()]`；
- 原有 `test.globals`、`test.environment: 'node'`、include、timeout 和 alias 均保持；
- 没有改成 jsdom；
- 没有加入无关插件或编译配置；
- 不影响 Next.js 生产构建配置。

---

## 七、红灯→绿灯证据核验

检查猫爪结果是否真实记录：

1. 插件配置前，真实导入 `CheckoutDialog.tsx` 触发原始 parse/import-analysis 错误；
2. 安装并配置插件后，同一个最小真实渲染测试通过；
3. 没有通过删除真实 import、改回读源码或 mock 被测组件来获得绿灯；
4. 配置后没有继续安装第二个依赖。

若缺少红灯原始错误，可以列为 P1 证据缺口；若缺少绿灯真实测试则必须不通过。

---

## 八、真实组件渲染可信度核验

必须确认测试：

- 静态导入真实 `CheckoutDialog`；
- 使用 `renderToStaticMarkup`；
- 使用 `React.createElement`；
- 没有复制简化组件；
- 没有 mock `CheckoutDialog`；
- 没有 mock `hasCompleteLockedShipping`；
- 外部模块 mock 仅限必要依赖，并且报告原因真实。

### 8.1 五场景逐项检查

| 场景 | 红色缺失提示 | 黄色成功摘要 | 首次下单表单 | 重新确认支付按钮 |
|---|---|---|---|---|
| shipping=null | 有 | 无 | 无 | disabled |
| 姓名空白 | 有 | 无 | 无 | disabled |
| 手机空白 | 有 | 无 | 无 | disabled |
| 地址空白 | 有 | 无 | 无 | disabled |
| 三字段完整 | 无 | 有且含真实字段 | 无 | 非 disabled |

每个场景必须有正向和反向断言。不能用参数化测试掩盖缺少某个场景，但可使用明确列出的表驱动数据。

### 8.2 按钮状态精确性

确认测试先从完整 HTML 中提取包含“重新确认支付”的真实 `<button>`，再检查该按钮的 disabled 属性。

以下写法不合格：

- `expect(html).toContain('disabled')`
- 只检查源码中的 disabled 表达式
- 找不到按钮时跳过
- `A || B` 任一路径通过

---

## 九、029 清理项保持核验

确认：

- 放宽测试结论的 OR 为 0；
- 7 处 `+200` 窗口为 0；
- 1 处 `+30` 窗口为 0；
- 静默跳过为 0；
- 括号平衡和完整右值检查仍然存在。

运行并逐条解释：

```powershell
Select-String -Path `
  '__tests__/components/product-pending-payment-flow.test.ts', `
  '__tests__/components/checkout-pending-order-lock.test.ts' `
  -Pattern 'substring\([^,]+,[^\)]*\+\s*(30|200|300|400|500)','includes\([^\)]*\)\s*\|\|','if\s*\(.*Match','if\s*\(.*Idx\s*===\s*-1'
```

---

## 十、独立验证命令

### 10.0 猫爪结果专项加查

猫爪 030 号实际执行与任务存在以下偏差或疑点，必须逐项独立核验：

1. 猫爪使用 `pnpm@10` 安装，而任务原命令为 `pnpm@11.13.0`。允许因本地 store（存储目录）兼容性改用 v10，但必须确认锁文件只新增插件相关 21 行，没有版本漂移或无关重写。
2. 猫爪运行的是 `npx tsc --noEmit`，并报告大量“既有测试类型错误”；它**没有执行任务规定的**：

```powershell
npx tsc --noEmit -p tsconfig.typecheck.json
```

小 M 必须亲自运行正确命令。未运行前不得写 typecheck 通过或失败。
3. 空白姓名、空白手机、空白地址三个真实渲染场景目前报告中没有明确证明各自断言“不显示 `选择收货地址`”。逐个检查测试代码；缺失则列为 P1，不得用 null 场景的断言代替。
4. 当前 disabled 正则形如：

```typescript
/<button[^>]*disabled[^>]*>[\s\S]*?重新确认支付[\s\S]*?<\/button>/
```

该正则可能从较早的 disabled 按钮开始，跨过一个或多个 `</button>` 后才命中“重新确认支付”。必须证明它只匹配目标按钮；如果没有先枚举完整 `<button>...</button>` 再按文字选择目标按钮，列为 P1 测试可信度问题。
5. 完整场景的“非 disabled”正则也要同样检查，不能从其他按钮跨标签命中。
6. 猫爪结果称完整测试 815/815 通过；小 M 必须独立运行一次完整测试确认，不得仅引用报告。

上述任一 P1 未闭合时，最终结论最多为“有条件通过”，且不得建议直接发布；应给出最小修复任务。

### 10.1 两个直接测试

```powershell
npx vitest run `
  __tests__/components/checkout-pending-order-lock.test.ts `
  __tests__/components/product-pending-payment-flow.test.ts
```

### 10.2 一次完整测试

```powershell
npx vitest run
```

不要因暂时无输出重复启动第二个完整测试进程。

### 10.3 类型和差异

```powershell
npx tsc --noEmit -p tsconfig.typecheck.json

git diff --check -- `
  package.json `
  pnpm-lock.yaml `
  vitest.config.ts `
  '__tests__/components/checkout-pending-order-lock.test.ts'

git status --short
```

030 不要求 Next.js build；不得把未运行 build 写成通过。

---

## 十一、结论规则

### 可以写“通过”

必须同时满足：

- 依赖精确且兼容；
- 锁文件无无关漂移；
- 配置只增加 React 测试转译；
- 真实生产组件可以导入；
- 5 个场景真实渲染并完整断言；
- 确认按钮状态精确限定；
- 029 清理项全部保持；
- 两个直接测试通过；
- 完整测试通过；
- typecheck 和 diff-check 通过；
- 030 仅 4 文件；
- 未 commit、push、部署。

任一核心项不满足，必须写“有条件通过”或“不通过”。

---

## 十二、结果文件

完成后写入：

```text
docs/agent-tasks/xiaom/done/小M_030号结果.md
```

必须包含：

1. 最终结论
2. P0/P1/P2
3. 030 修改范围
4. 依赖版本与兼容性
5. 锁文件审查
6. Vitest 配置审查
7. 红灯→绿灯证据
8. 真实生产组件导入证据
9. 实际 mock 清单
10. 5 场景逐项表
11. 按钮状态精确断言证据
12. 029 清理项保持结果
13. 两个直接测试结果
14. 完整测试结果
15. typecheck 结果
16. diff-check 结果
17. git status
18. 是否修改业务代码
19. 是否 commit
20. 是否 push
21. 是否部署
22. 是否建议进入最终发布任务
23. 执行方案建议（P0/P1/P2、负责人、工时）

---

## 十三、只读边界

小 M 不得修改、安装、删除或提交任何文件；不得运行依赖安装命令；不得改数据库或部署。写完结果后停止，等待小酷安排最终发布。
