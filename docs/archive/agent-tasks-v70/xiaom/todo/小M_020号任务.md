# 小M_020号任务：支付密码重置测试可信度快速只读复审

## 一、任务身份

你是本任务的独立只读复审代理。本次只复审猫爪 020 号对测试可信度的收口，不重复执行 018/019 的全量业务审计。

严格只读：

- 不允许修改业务代码或测试代码。
- 不允许删除文件。
- 不允许 commit（提交）。
- 不允许 push（推送）。
- 不允许部署。
- 不允许写生产数据库或同步通知模板。
- 只允许写结果文件。

## 二、复审对象

猫爪 020 号只允许修改：

```text
__tests__/api/admin/users/detail.test.ts
__tests__/components/payment-password-reset-flow.test.ts
```

结果文件：

```text
docs/agent-tasks/catpaw/done/猫爪_020号结果.md
```

猫爪报告称：

- 三处 `if (match) { expect(...) }` 假通过已改为强制 `not.toBeNull()`。
- 详情接口测试原始记录已加入登录密码哈希和支付密码哈希。
- 新增 Prisma `select` 顶层投影 mock。
- 4 个测试文件共 151 项测试通过。
- 未修改业务代码。

不要直接相信报告，必须独立核验真实文件和真实命令。

## 三、范围检查

运行：

```powershell
git status --short
git diff --name-status
```

结合 019 号结果确认：

1. 020 号是否只改变上述两个测试文件。
2. 是否出现新的 `src/`、`prisma/` 或其他测试文件改动。
3. 仓库内是否遗留新的测试输出文件。
4. 猫爪报告中的 `D:/vitest-020-output.txt` 位于仓库外，不作为仓库污染；只需确认仓库内无同类临时文件。
5. 旧文档、任务文件和 `vercel_ls.txt` 只记录，不得处理。

## 四、三处假通过专项核验

读取：

```text
__tests__/components/payment-password-reset-flow.test.ts
```

逐项检查：

1. 忘记支付密码弹窗关闭回调：
   - 匹配失败时必须失败。
   - 包含 `setShowForgotPayPwdModal(false)`。
   - 不包含 `fetch`、`onClose` 和 `/api/`。
2. 会员详情右上角 X 关闭按钮：
   - 匹配失败时必须失败。
   - 必须使用 `closeDetailModal`。
3. 会员详情底部“关闭”按钮：
   - 匹配失败时必须失败。
   - 必须使用 `closeDetailModal`。
4. 上述三处不得残留：

```typescript
if (closeMatch)
if (closeBtnMatch)
if (closeBtnArea)
```

5. 正则范围是否足以指向目标函数或目标弹窗，不能明显跨越到其他按钮后误匹配。

任一关键匹配仍可静默跳过，结论必须为“不通过”。

## 五、登录密码哈希安全测试核验

读取：

```text
__tests__/api/admin/users/detail.test.ts
src/app/api/admin/users/[id]/route.ts
```

逐项检查：

1. 原始测试记录真实包含：

```typescript
passwordHash: 'login_password_hash_should_never_leave_db'
```

2. `applyTopLevelSelect` 是否根据真实 `select` 的 key 投影返回对象。
3. `mockFindUniqueWithSelect` 是否读取路由传入的 `args.select`，而不是使用写死的白名单。
4. 业务路由的 `select` 是否确实不包含 `passwordHash`。
5. 投影后的 Prisma mock 返回值是否确实不包含 `passwordHash`。
6. 最终响应是否不包含 `passwordHash`。
7. 测试是否形成“数据库原始记录有字段 → 路由 select 排除 → Prisma 返回排除 → 响应排除”的证据链。
8. 测试不能只断言自己写的 helper 输出；必须真实调用 `GET` 路由并检查其 Prisma 调用参数和响应。

如果测试只是 helper 自证、没有真实调用路由，结论为 P1（重要测试缺口）。

## 六、支付密码哈希转布尔值测试核验

逐项检查：

1. 原始测试记录真实包含：

```typescript
paymentPasswordHash: 'payment_password_hash_should_never_leave_db'
```

2. 路由 `select` 真实包含 `paymentPasswordHash`。
3. 投影后的 mock 返回值真实包含该字段。
4. 真实调用 `GET` 路由后：
   - 响应不含 `paymentPasswordHash`。
   - 响应 `hasPaymentPassword === true`。
5. 原有空哈希场景仍验证 `hasPaymentPassword === false`。

## 七、测试实现质量检查

重点检查：

1. `applyTopLevelSelect` 对当前测试是否足够，不要求实现完整 Prisma 引擎。
2. 嵌套关系直接从测试记录取值是否会破坏原有关系测试。
3. 是否把所有原先直接返回 `FULL_USER` 的 GET 场景改为通过 select 投影。
4. 用户不存在、已删除、数据库异常等特殊 mock 是否仍保持原语义。
5. 新增测试是否存在重复但不增加证据的空壳断言。
6. 不能因为测试通过就默认业务安全，必须同时读取真实路由 `select`。

## 八、独立验证命令

### 8.1 针对性测试

运行：

```powershell
npx vitest run __tests__/api/admin/users/detail.test.ts __tests__/api/admin/users/payment-password-reset.test.ts __tests__/components/payment-password-reset-flow.test.ts __tests__/services/order-notification.test.ts
```

记录测试文件数、测试数、失败数和退出码。

### 8.2 类型检查

运行：

```powershell
npx tsc --noEmit -p tsconfig.typecheck.json
```

### 8.3 差异检查

运行：

```powershell
git diff --check -- __tests__/api/admin/users/detail.test.ts __tests__/components/payment-password-reset-flow.test.ts
git status --short
```

本任务不运行完整测试、不运行覆盖率、不运行构建。

## 九、结论标准

### 通过

- 三处假通过全部消除。
- 两条哈希安全证据链真实调用路由。
- 无业务代码改动。
- 针对性测试和类型检查通过。

### 有条件通过

- 核心测试真实有效，仅有不影响发布的 P2（一般观察）。

### 不通过

- 任一关键断言仍可能静默跳过。
- 测试数据仍没有真实敏感字段。
- 投影 mock 未使用路由传入的 select。
- 安全测试没有真实调用 GET 路由。
- 020 号越界修改业务代码。

## 十、结果文件

只允许写入：

```text
docs/agent-tasks/xiaom/done/小M_020号结果.md
```

结果必须包含：

1. 最终结论。
2. P0（最高优先级）问题。
3. P1（重要）问题。
4. P2（一般观察）。
5. 020 号真实修改范围。
6. 三处假通过核验结果。
7. 登录密码哈希安全证据链。
8. 支付密码哈希转布尔值证据链。
9. 投影 mock 有效性评价。
10. 针对性测试结果。
11. 类型检查结果。
12. `git diff --check` 结果。
13. 仓库内是否遗留临时输出文件。
14. 是否修改业务代码。
15. 是否 commit、push、部署或同步模板。
16. 是否建议进入唯一一次发布验证阶段。
17. 执行方案建议：问题、负责人和工时估算。

完成后停止，等待胡子老师和小酷决定下一步。
