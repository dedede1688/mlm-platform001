# 小M_016号任务：空订单编号防御修复快速只读复审

## 一、任务性质

这是猫爪 016 号窄范围修复后的最终快速只读复审。

只允许读取和验证：不修改代码、不 commit（提交）、不 push（推送）、不部署。

## 二、先读取

```text
docs/agent-tasks/catpaw/todo/猫爪_016号任务.md
docs/agent-tasks/catpaw/done/猫爪_016号结果.md
docs/agent-tasks/xiaom/done/小M_015号结果.md
src/app/cart/page.tsx
__tests__/components/cart-earnings-transfer-flow.test.ts
```

## 三、必须检查的 8 项

### 1. 修改范围

确认 016 号只继续修改：

```text
src/app/cart/page.tsx
__tests__/components/cart-earnings-transfer-flow.test.ts
```

### 2. 空订单编号兜底彻底移除

提取 `handleEarningsTransferSuccess` 到 `handleCheckoutClose` 的真实函数体，确认不包含：

```typescript
orderId: prev[checkoutItem.id]?.orderId ?? ''
```

也不包含其他给 `orderId` 设置空字符串的等价写法。

### 3. 记录不存在时不写入

确认函数式状态更新中真实存在严格等价逻辑：

```typescript
const current = prev[checkoutItem.id]
if (!current) return prev
```

记录不存在时必须返回原状态，不新增键、不显示为待支付订单。

### 4. 记录存在时保留真实订单编号

确认更新使用：

```typescript
...current
```

或 `orderId: current.orderId`，并且只修改当前商品的 `shortage`。

同时确认 `...prev` 保留其他商品记录。

### 5. 不自动支付

确认收益转入成功回调仍然：

- 不包含 `verify-payment`。
- 不调用 `handleCheckoutConfirm`。
- 刷新用户资金。
- 提示用户手动重新确认支付。

### 6. 015 字典化逻辑未被破坏

快速确认：

- `pendingPayments` 仍按 `cartItemId` 保存 `orderId + shortage`。
- A→B→A 仍复用各自订单。
- 支付成功仍只删除当前商品记录。
- 余额不足仍只更新当前商品记录。

### 7. 测试真实性

确认新增测试通过真实函数体提取检查：

- 不含 `?? ''`。
- 从 `prev[checkoutItem.id]` 读取记录。
- 记录不存在时 `return prev`。
- 使用 `current.orderId` 或 `...current` 保留订单编号。

如果测试只检查全文件字符串而没有限定函数体，应列为测试缺口。

### 8. 禁止范围和发布状态

确认未修改后端、数据库、支付事务、后台、`AGENTS.md`；当前仍未 commit、未 push、未部署。

## 四、只读验证命令

只运行：

```powershell
npx vitest run __tests__/components/cart-earnings-transfer-flow.test.ts __tests__/api/user/earnings-transfer-route.test.ts __tests__/api/orders/verify-payment-route.test.ts
npx tsc --noEmit -p tsconfig.typecheck.json
git diff --check
git status --short
```

不要运行完整测试、覆盖率或 `next build`（项目构建）。

## 五、结果文件

完成后写入：

```text
docs/agent-tasks/xiaom/done/小M_016号结果.md
```

结果必须包含：

1. 最终结论。
2. P0/P1/P2 问题。
3. 空订单编号风险是否闭合。
4. 015 多商品字典逻辑是否保持。
5. 测试真实性评价。
6. 针对性测试结果。
7. 类型检查结果。
8. `git diff --check` 和 `git status --short`。
9. 是否修改代码、commit、push、部署。
10. 是否建议进入唯一一次完整发布验证阶段。

结果写完后立即停止，等待胡子老师和小酷决定发布。
