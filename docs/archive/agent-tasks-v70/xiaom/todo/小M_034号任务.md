# 小M_034号任务：订单售后卡片代码规范清理独立只读复审

> **模式：** 严格只读  
> **复审对象：** 猫爪_034号执行结果  
> **禁止：** 修改代码或测试、commit、push、部署、数据库操作

---

## 一、读取材料

1. `docs/agent-tasks/catpaw/todo/猫爪_034号任务.md`
2. `docs/agent-tasks/catpaw/done/猫爪_034号结果.md`
3. `docs/agent-tasks/xiaom/done/小M_033号结果.md`
4. 033 号 5 个目标业务和测试文件

---

## 二、独立核验

1. 034 净修改必须只有：

```text
src/app/dashboard/orders/[id]/page.tsx
```

2. 确认只删除：
   - 未使用的 `AlertTriangle` 导入；
   - 未使用的 `REFUND_STATUS_CONFIG` 常量。
3. 确认没有修改退款业务规则、组件接入、状态判断、样式、接口或测试。
4. 独立运行目标代码规范检查，必须 0 Error。
5. 独立运行：

```powershell
npx tsc --noEmit -p tsconfig.typecheck.json
```

6. 独立运行 5 个最小相关回归测试，预期 126 个测试全部通过。
7. 核验 `.test.ts` 后缀继续保留，且没有修改 `vitest.config.ts`。
8. 运行 `git diff --check` 和 `git status --short`。
9. 确认未 commit、未 push、未部署。

---

## 三、结论标准

以下任一项存在时不得通过：

- 目标代码规范检查仍有 Error；
- 类型检查失败；
- 相关测试失败；
- 034 修改超过 1 个业务文件；
- 删除内容之外还有业务变化；
- 修改测试后缀、依赖或配置；
- 已 commit、push 或部署。

---

## 四、结果文件

写入：

```text
docs/agent-tasks/xiaom/done/小M_034号结果.md
```

必须包含最终结论、P0/P1/P2、修改范围、代码规范检查、类型检查、126 个测试、差异检查和提交状态。写完立即停止，等待胡子老师和小酷审核。
