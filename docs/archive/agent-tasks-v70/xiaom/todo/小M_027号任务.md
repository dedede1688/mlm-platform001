# 小M_027号任务：待支付恢复最终安全闭环独立只读复审

## 一、任务性质

本任务是**独立只读复审**，复审猫爪 027 号执行结果。

禁止：

- 修改业务代码或测试代码；
- 修改数据库或执行迁移；
- commit（提交）；
- push（推送）；
- 部署；
- 清理历史文档、任务文件或 `vercel_ls.txt`；
- 因测试、类型检查或构建通过而忽略业务链路问题。

完成后写入：

```text
docs/agent-tasks/xiaom/done/小M_027号结果.md
```

---

## 二、固定工作目录与必读资料

固定目录：

```text
D:\mlm-platform-source\mlm-platform
```

依次读取：

1. `AGENTS.md`
2. `docs/agent-tasks/README.md`
3. `docs/agent-tasks/catpaw/猫爪执行岗位说明.md`
4. `docs/agent-tasks/catpaw/todo/猫爪_026号任务.md`
5. `docs/agent-tasks/catpaw/done/猫爪_026号结果.md`
6. `docs/agent-tasks/xiaom/todo/小M_026号任务.md`
7. `docs/agent-tasks/xiaom/done/小M_026号结果.md`
8. `docs/agent-tasks/catpaw/todo/猫爪_027号任务.md`
9. `docs/agent-tasks/catpaw/done/猫爪_027号结果.md`
10. 本任务文件

必须读取真实源码和测试，不得只复述执行报告。

---

## 三、基线与范围

预期：

```text
本地 HEAD   = baff1843a394fe1e2b21b9bc3e10ccc9d232b53b
origin/main = 49d3583a68f0f5f5e5794ce16c3a76594028752f
```

本地领先远程 1 个设计文档提交属于预期，不得重置、修改或推送。

024 至 027 累计业务/测试范围预期仍严格为原 10 个文件：

```text
src/lib/services/order.service.ts
src/app/api/orders/[id]/route.ts
src/components/checkout/CheckoutDialog.tsx
src/app/products/[id]/page.tsx
src/lib/utils/pending-payment-session.ts
__tests__/services/order.test.ts
__tests__/api/orders/order-detail-route.test.ts
__tests__/lib/pending-payment-session.test.ts
__tests__/components/checkout-pending-order-lock.test.ts
__tests__/components/product-pending-payment-flow.test.ts
```

027 净修改只允许：

```text
src/components/checkout/CheckoutDialog.tsx
src/app/products/[id]/page.tsx
__tests__/components/checkout-pending-order-lock.test.ts
__tests__/components/product-pending-payment-flow.test.ts
```

---

## 四、先核验已报告完成的四项修复

### 4.1 原始接口缺口类型守卫

确认：

- 使用 `rawApiShortage = verifyErr.data?.shortage`；
- 先判断 `typeof rawApiShortage === 'number'`；
- 再判断 `Number.isFinite` 和非负；
- 不再使用 `Number(verifyErr.data?.shortage)`；
- `null`、空字符串、字符串 `'0'`、`false`、对象、数组、NaN、Infinity、负数均不得直接采用；
- 非法接口值进入本地兜底计算；
- 兜底计算失败时不更新缺口、不持久化、不打开收益弹窗、不自动支付。

### 4.2 新订单初始缺口

确认：

- 不再有新订单 `shortage: 0`；
- 不再有 `persistPendingPayment(orderId, 0)`；
- 使用 `calculatePendingShortage(currentPayAmount, user.balance)`；
- 合法初始缺口写入内存和 sessionStorage；
- 支付接口合法缺口仍能覆盖初始缺口。

### 4.3 锁定收货快照

确认：

- 三个字段均使用 `.trim()` 校验；
- 传给 `onConfirm` 前均执行 `.trim()`；
- 缺失或空白字段时 `handleConfirm` return；
- 按钮对缺失或空白字段禁用；
- 非待支付地址逻辑未被破坏。

### 4.4 lint Error

确认 `handleEarningsTransferSuccess` 已改为无参数函数，且定向 lint 0 Error。

---

## 五、最高优先级加查 A：订单已创建后缺口计算失败

小酷独立读取真实代码发现当前顺序为：

1. `POST /api/orders` 已成功创建订单；
2. 已取得 `orderId`；
3. 才调用 `calculatePendingShortage(currentPayAmount, user.balance)`；
4. 若返回 `null`，直接进入 `validation_error` 并 `return null`；
5. 此时尚未执行 `setPendingPayment` 和 `persistPendingPayment`。

请完整推演并回答：

1. 数据库中的订单是否已经存在；
2. React 内存是否保存该订单编号；
3. sessionStorage 是否保存该订单编号；
4. 用户关闭弹窗或再次点击购买会不会再次调用 `POST /api/orders`；
5. 是否存在重复创建待支付订单风险；
6. 是否违反 027 任务第 8.2 条“不能因为修复零缺口而重新引入重复创建订单风险”；
7. 是否应该在创建订单前先验证 `finalPrice` 和 `user.balance`，保证创建后初始缺口计算不可能为 null；
8. 现有测试是否真实执行并覆盖该完整状态链。

如果确认订单已创建但编号未保留，按支付/订单铁律评为 P0，不得降级为一般体验问题。

---

## 六、最高优先级加查 B：空白锁定快照的 UI 分支

小酷独立读取真实 JSX 发现当前仍是：

```tsx
{hasPendingOrder ? (
  lockedShipping ? (
    // 显示“订单已创建”的锁定摘要
  ) : (
    // 显示只读错误
  )
) : (...)}
```

但是按钮和 `handleConfirm` 使用 `.trim()` 判断完整性。

必须检查：

1. 当 `lockedShipping` 对象存在，但姓名为 `'   '` 时，JSX 显示哪个分支；
2. 手机号为空白时显示哪个分支；
3. 地址为空白时显示哪个分支；
4. 是否出现“订单已创建”但收货字段为空的误导摘要；
5. 按钮虽然禁用，UI 是否仍违反 027 要求“对象存在但字段空白时显示只读错误”；
6. 027 新增测试是否真实检查 JSX 分支，还是只检查 handler、snapshot 和 disabled 字符串。

该问题至少列为 P1。若它会让用户无法理解为何按钮不可点，必须明确写“不建议发布”。

---

## 七、最高优先级加查 C：测试清理报告真实性

猫爪报告声称：

- 清理 6 处 `if (nullBranchIdx === -1)`；
- 清理 5 处固定 200/300 字符窗口；
- 清理 1 处宽松 `A || B`；
- `checkout` 测试清理 5 处固定窗口。

小酷独立 grep（搜索）仍看到以下模式：

- 多处 `source.substring(...)`；
- `substring(buttonIdx, buttonIdx + 400)`；
- `const hasOrderIdPreservation = !... || ...`；
- `const hasNullGuard = ...includes('null') || ...includes('validation_error')`；
- 部分 `find(...)` 后使用非空断言 `!`；
- 测试仍以源码字符串契约为主，没有真实执行页面状态机。

请逐项核验：

1. 哪些 substring 只是安全提取，哪些仍是固定窗口假通过；
2. 哪些 `||` 断言仍允许任一路径碰巧成立；
3. 核心标记找不到时是否全部真实失败；
4. “初始缺口为 null 时不进入 verify-payment”测试是否只是检查字符串顺序；
5. 是否真实模拟“订单接口成功后初始缺口异常”的运行行为；
6. 是否真实模拟空白 lockedShipping 的 JSX 输出；
7. 猫爪结果文件关于“全部清理”的陈述是否准确。

测试全绿不能替代此项判断。

---

## 八、业务链路完整推演

报告必须至少推演以下 16 步：

1. 用户进入商品详情；
2. 加载用户余额、收益和商品金额；
3. 首次填写收货地址；
4. 点击确认下单；
5. 创建订单；
6. 计算初始缺口；
7. 保存订单编号和真实初始缺口；
8. 调用支付密码验证；
9. 接口返回合法余额不足缺口并更新会话；
10. 接口返回非法缺口类型时走本地兜底；
11. 初始缺口计算为 null 时的真实结果；
12. 用户关闭并重新打开弹窗；
13. 收货快照完整时只输入支付密码复用原订单；
14. 收货快照对象存在但字段空白时的 UI、按钮和 handler 行为；
15. 收益转入成功但资金刷新失败时保留原订单；
16. 支付成功后才清除会话，全程不自动支付、不绕过密码、不创建第二个订单。

---

## 九、必须运行的只读验证

```powershell
npx vitest run `
  __tests__/services/order.test.ts `
  __tests__/api/orders/order-detail-route.test.ts `
  __tests__/lib/pending-payment-session.test.ts `
  __tests__/components/checkout-pending-order-lock.test.ts `
  __tests__/components/product-pending-payment-flow.test.ts `
  __tests__/components/cart-earnings-transfer-flow.test.ts `
  __tests__/api/orders/verify-payment-route.test.ts

npx tsc --noEmit -p tsconfig.typecheck.json

npx next lint `
  --file src/components/checkout/CheckoutDialog.tsx `
  --file src/app/products/[id]/page.tsx

npx next build

git diff --check -- src __tests__
git status --short
```

### 小酷独立验证证据

小酷已运行：

- 7 个相关测试文件：`190 passed`；
- 正确项目类型检查：退出码 0；
- 定向 lint：退出码 0，只有 2 个既有 Warning；
- Next.js build（生产构建）：成功，110/110 页面生成；
- 未执行 commit、push、部署。

请独立复跑，不得只引用以上结果。

---

## 十、最终判定规则

以下任一项确认存在，最终结论必须为“不通过”，并明确“不建议提交发布”：

1. 订单已创建但初始缺口异常时订单编号未保存，重试会再建订单；
2. 空白 lockedShipping 仍显示成功锁定摘要；
3. 核心测试仍可通过固定窗口、宽松 OR 或找不到标记后的兜底；
4. 任何 lint Error、类型错误、测试失败或构建失败；
5. 027 净修改越出 4 个文件；
6. 修改支付事务、订单状态机、数据库或禁区文件。

---

## 十一、结果文件格式

结果文件必须包含：

1. 最终结论：通过 / 有条件通过 / 不通过；
2. P0 问题；
3. P1 问题；
4. P2 观察；
5. 基线与文件范围；
6. 四项报告修复的独立核验；
7. 小酷加查 A、B、C 的逐项结论；
8. 16 步完整链路推演；
9. 测试可信度结论；
10. 测试、正确类型检查、lint、build、diff 结果；
11. `git status --short` 原文；
12. 是否 commit、push、部署；
13. 是否建议进入提交发布阶段；
14. 可执行修复方案：负责人、文件范围、预计工时。

完成后立即停止，等待小酷安排下一任务。
