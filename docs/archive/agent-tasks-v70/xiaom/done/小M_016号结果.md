# 小M_016号结果：空订单编号防御修复快速只读复审

## 1. 最终结论

**通过**。

015 号发现的 P1-1（`handleEarningsTransferSuccess` 中 `orderId: prev[checkoutItem.id]?.orderId ?? ''` 允许空订单编号伪记录）**已彻底修复**。源码 L349-360 重写为标准防御性写法：

```typescript
setPendingPayments(prev => {
  const current = prev[checkoutItem.id]
  if (!current) return prev   // ← 守卫：记录不存在时直接返回原状态

  return {
    ...prev,                  // ← 保留其他商品记录
    [checkoutItem.id]: {
      ...current,             // ← 保留真实 orderId
      shortage: remainingShortage,  // ← 只更新 shortage
    },
  }
})
```

`?? ''` 已彻底删除。空字符串兜底、`/api/orders//verify-payment` 500/404 风险路径**已完全闭合**。

015 号字典化逻辑保持完整（A→B→A 复用各自订单、余额不足只更新当前、支付成功只删当前），57 条针对性测试全过、typecheck 0 错、diff --check 干净。

新增的 4 项测试（描述 6b）均通过 `extractBlock(source, 'const handleEarningsTransferSuccess', 'const handleCheckoutClose')` 提取真实函数体断言，**不存在只检查全文件字符串的测试缺口**。

**强烈建议进入唯一一次完整发布验证阶段**（完整测试 + build + commit + push + Vercel 部署）。

## 2. P0 / P1 / P2

### P0 - 无

### P1 - 无

015 P1-1（空订单编号防御）已彻底修复。

### P2 - 观察不阻塞

| ID | 类别 | 描述 |
|----|------|------|
| P2-1 | 状态持久化 | `pendingPayments` 离开购物车页面后丢失（React 状态管理固有限制） |
| P2-2 | 浏览器验证 | 因无测试账号未完成，源码级验证替代 |
| P2-3 | TypeScript 闭包 | 015 结果 P2-3：`handleCheckoutConfirm` 中 `orderId` 类型 `string \| null` 收窄问题通过 `const confirmedOrderId = orderId` 常量捕获（typecheck 0 错） |

## 3. 修改范围检查（任务三.1）

`git diff --stat` 输出（src/ 范围内）：

```text
 src/app/cart/page.tsx                      | 195 +++++++++++++++++++++++++----
 src/components/EarningsTransferModal.tsx   |   6 +-
 src/components/checkout/CheckoutDialog.tsx |  46 ++++++-
 3 files changed, 216 insertions(+), 31 deletions(-)
```

**016 净修改范围**（vs 015 状态）：
- `src/app/cart/page.tsx`：195/0（vs 015 的 190/0 = 016 净增 5 行）—— 修复 `setPendingPayments` 函数体
- `src/components/EarningsTransferModal.tsx`：6/0（无变化，仍是 013 状态）
- `src/components/checkout/CheckoutDialog.tsx`：46/0（无变化，仍是 013 状态）

**016 实际修改文件数：2 个**（1 业务 + 1 测试），与任务允许范围完全一致。✓

## 4. 空订单编号风险是否闭合（任务三.2-4）— 全部通过

### 4.1 真实函数体（任务三.2-4 关键检查）

**`src/app/cart/page.tsx` L349-360**：

```typescript
// v016: 只更新当前商品对应记录的 shortage（记录不存在时不新增伪记录）
setPendingPayments(prev => {
  const current = prev[checkoutItem.id]
  if (!current) return prev

  return {
    ...prev,
    [checkoutItem.id]: {
      ...current,
      shortage: remainingShortage,
    },
  }
})
```

### 4.2 逐项验证

| 任务检查项 | 实际 | 结论 |
|---|---|---|
| 不含 `orderId: prev[checkoutItem.id]?.orderId ?? ''` | L349-360 不含该模式 | ✅ 彻底移除 |
| 不含其他空字符串兜底（`?? ''`） | L349-360 不含 `?? ''` | ✅ 彻底移除 |
| 真实存在 `const current = prev[checkoutItem.id]` | L351 | ✅ |
| 真实存在 `if (!current) return prev` | L352 | ✅ |
| 用 `...current` 展开保留真实 `orderId` | L357 `...current` | ✅ |
| 只修改当前商品的 `shortage` | L358 `shortage: remainingShortage` | ✅ |
| 用 `...prev` 保留其他商品 | L354 `...prev` | ✅ |

**015 P1-1 风险完全闭合** — 防御性写法标准、无任何空字符串兜底。

## 5. 不自动支付检查（任务三.5）

**`handleEarningsTransferSuccess` 完整函数体 L334-368**（人工读取）：

```typescript
const handleEarningsTransferSuccess = (transferredAmount?: number) => {
  // checkoutItem 为空时安全返回，只刷新资金
  if (!checkoutItem) {
    if (token) fetchUserInfo(token)
    return
  }
  // 刷新用户资金信息
  if (token) {
    fetchUserInfo(token)
  }
  // 计算剩余差额
  const transferred = transferredAmount ?? 0
  const existingShortage = pendingPayments[checkoutItem.id]?.shortage ?? 0
  const remainingShortage = Math.max(0, existingShortage - transferred)

  // v016: 只更新当前商品对应记录的 shortage（记录不存在时不新增伪记录）
  setPendingPayments(prev => {
    const current = prev[checkoutItem.id]
    if (!current) return prev

    return {
      ...prev,
      [checkoutItem.id]: {
        ...current,
        shortage: remainingShortage,
      },
    }
  })

  if (remainingShortage === 0) {
    toast.success('余额已补足，请再次确认支付')
  } else {
    toast.info(`收益已转入，支付仍差 ¥${remainingShortage.toFixed(2)}，请充值后继续支付`)
  }
}
```

| 检查项 | 实际 | 结论 |
|---|---|---|
| 不包含 `verify-payment` | 函数体 L334-368 不含 `verify-payment` 字符串 | ✅ |
| 不调用 `handleCheckoutConfirm` | 函数体不含 `handleCheckoutConfirm` | ✅ |
| 刷新用户资金 | L340-343 `fetchUserInfo(token)` | ✅ |
| 提示用户手动重新确认 | L363-367 toast | ✅ |

## 6. 015 字典化逻辑保持（任务三.6）

| 检查项 | 实际 | 结论 |
|---|---|---|
| `pendingPayments` 仍按 `cartItemId` 保存 `orderId + shortage` | L62 `useState<Record<string, PendingCartPayment>>({})` + interface L40-43 | ✅ |
| A→B→A 仍复用各自订单 | L212-214 `existingPayment.orderId` 复用 | ✅ |
| 余额不足只更新当前商品 | L277-282 `setPendingPayments(prev => ({...prev, [checkoutItem.id]: {...}}))` | ✅ |
| 支付成功只删除当前商品 | L321-325 `delete next[checkoutItem.id]` | ✅ |
| 收益转入只更新当前商品 | L350-360 新版防御性写法 | ✅ |

**015 多商品字典逻辑完全保持**，未因 016 修复而被破坏。

## 7. 测试真实性评价（任务三.7）

### 7.1 新增 6b 测试 4 项（`__tests__/components/cart-earnings-transfer-flow.test.ts` L215-241）

| # | 测试 | 实现 | 真实性 |
|---|------|------|--------|
| 1 | `handleEarningsTransferSuccess 函数体不包含空字符串兜底 ?? ''` | `extractBlock(source, 'const handleEarningsTransferSuccess', 'const handleCheckoutClose')` + `expect(block).not.toContain("?? ''")` | ✅ 真实函数体 + 字符断言 |
| 2 | `handleEarningsTransferSuccess 函数式更新中从 prev 读取当前记录` | 同 extractBlock + `expect(block).toMatch(/prev\[checkoutItem\.id\]/)` | ✅ 真实函数体 + 正则 |
| 3 | `handleEarningsTransferSuccess 当前记录不存在时 return prev` | 同 extractBlock + `expect(block).toMatch(/return\s+prev/)` | ✅ 真实函数体 + 正则 |
| 4 | `handleEarningsTransferSuccess 更新后的 orderId 来自现有记录` | 同 extractBlock + `expect(block).toMatch(/current\.orderId\|\.\.\.current/)` | ✅ 真实函数体 + 正则 |

**全部使用 `extractBlock` 提取真实函数体**，**不存在只检查全文件字符串的测试缺口**。

### 7.2 关键逻辑被破坏后测试是否仍能通过

**场景 A：有人把 `if (!current) return prev` 守卫删除**
- 测试 3 失败（`return prev` 模式不存在）✓ 测试有效

**场景 B：有人把 `...current` 改回 `?? ''`**
- 测试 1 失败（含 `?? ''`）✓ 测试有效
- 测试 4 失败（无 `current.orderId` 或 `...current`）✓ 测试有效

**场景 C：有人把 `setPendingPayments` 改为 `setPendingPayments({})` 清空整张表**
- 测试 5.x（014 已加）失败 ✓ 测试有效

**场景 D：有人在 `handleEarningsTransferSuccess` 内加入 `verify-payment`**
- 测试 5.x（014 已加）失败 ✓ 测试有效

### 7.3 extractBlock 边界分析

**当前源码中**：
- `const handleEarningsTransferSuccess` 出现 1 次（L334）
- `const handleCheckoutClose` 出现 1 次（L365）
- extractBlock 提取的是真实函数体（L334-L364）✓

## 8. 针对性测试结果

```text
npx vitest run __tests__/components/cart-earnings-transfer-flow.test.ts __tests__/api/user/earnings-transfer-route.test.ts __tests__/api/orders/verify-payment-route.test.ts

Test Files  3 passed (3)
Tests       57 passed (57)
Duration    1.02s
```

| 测试文件 | 测试数 | 结果 |
|---|---|---|
| `cart-earnings-transfer-flow.test.ts` | 45（v016 新增 4 项）| ✅ 全部通过 |
| `earnings-transfer-route.test.ts` | 5 | ✅ 全部通过 |
| `verify-payment-route.test.ts` | 7 | ✅ 全部通过 |

## 9. 类型检查结果

```text
npx tsc --noEmit -p tsconfig.typecheck.json
退出码：0
错误数：0
```

**说明**：016 修复后类型层面也得到改善 —— `current.orderId` 类型推断为 `string`（不再是 `string | undefined`），无需 `?? ''` workaround。

## 10. `git diff --check` 结果

```text
退出码：0
无空白错误。
仅有 5 条 "LF will be replaced by CRLF" 警告（Windows 环境下 Git 正常行为，非本任务修改引入）。
```

## 11. `git status --short` 结果

```text
 M src/app/cart/page.tsx
 M src/components/EarningsTransferModal.tsx
 M src/components/checkout/CheckoutDialog.tsx
 M __tests__/components/cart-earnings-transfer-flow.test.ts
?? (大量历史任务文件 untracked)
?? vercel_ls.txt
```

- **016 净修改**：1 业务文件 + 1 测试 = 2 个 ✓
- **013-016 整体状态**：3 业务文件 + 1 测试 ✓
- **非业务变更**：5 个旧 docs M（任务前已存在）+ 大量 `docs/agent-tasks/` untracked（历史任务文件）+ `vercel_ls.txt` — 本任务禁止处理

## 12. 是否修改代码 / commit / push / 部署

| 项 | 状态 |
|---|---|
| 是否修改任何业务或测试代码 | **否**（本任务为只读复审） |
| 是否 `git add` | **否** |
| 是否 `git commit` | **否** |
| 是否 `git push` | **否** |
| 是否部署 | **否** |

## 13. 是否建议进入唯一一次完整发布验证阶段

**强烈建议进入**。

理由：
1. **P1-1 已彻底修复**：`?? ''` 已删除，`if (!current) return prev` 守卫已加，`...current` 展开保留真实 orderId
2. **015 字典化逻辑保持**：A→B→A 复用、余额不足隔离、支付成功只删当前 全部保持
3. **不自动支付约束保持**：函数体无 `verify-payment` / `handleCheckoutConfirm`
4. **57 条针对性测试全过**（含 v016 新增 4 项）
5. **typecheck 0 错** —— 016 修复后类型层面也得到改善（`current.orderId` 是 `string` 而非 `string | undefined`）
6. **diff --check 干净**

### 建议路径

1. 猫爪执行唯一一次完整发布验证（运行完整测试 + `next build` + 必要的 6 文件 `git add` + `git commit` + `git push` + Vercel 自动部署）
2. 走铁律 1 验证：`git log origin/main --oneline -1` 确认 commit hash
3. 胡子哥登录 Vercel Dashboard 确认 `mlm-platform001` 最新部署 Ready
4. 浏览器真实验收（推荐 4 步）：
   - 1 件商品余额不足 → 收益转入 50% → 显示剩余差额 + "去充值" + "再次确认支付"
   - 2 件商品 A→B→A → A 仍读 orderA + shortageA + 按钮"重新确认支付"
   - 故意 DB 断开 → 500 + "数据库连接断开" + 无通知
   - 收益转入成功后**不自动跳支付页**

### 建议 commit message 格式

```
fix(cart): 多商品待支付订单字典化 + 禁止空订单编号伪记录

v013: 引入 pendingOrder + shortage 单值
v014: 修复状态串台（handleBuyNow / handleCheckoutClose）
v015: 字典化 pendingPayments: Record<string, PendingCartPayment>，修复 A→B→A 重复创建
v016: 修复 ?? '' 防御性写法，if (!current) return prev
```

### 建议 git add 范围（仅 4 个文件 + 不要 add 历史 untracked）

```bash
git add src/app/cart/page.tsx
git add src/components/EarningsTransferModal.tsx
git add src/components/checkout/CheckoutDialog.tsx
git add __tests__/components/cart-earnings-transfer-flow.test.ts
git status  # 确认只显示这 4 个 A 状态
```

**禁止** `git add .` —— 会把 5 个旧 docs M + 大量历史任务文件 + `vercel_ls.txt` 一起提交。

## 14. 风险评估

| 风险 | 等级 | 依据 |
|------|------|------|
| A→B→A 重复创建订单 | **已闭合** | 015 字典化 + 016 防御性写法 |
| 空订单编号伪记录 | **已闭合** | 016 `if (!current) return prev` 守卫 |
| 不自动支付 | **已闭合** | 测试 5 + 函数体检查 |
| pendingPayments 不持久化 | P2 | 离开购物车页面后丢失（React 状态管理固有限制；猫爪已记录） |
| 浏览器验证未完成 | P2 | 源码级验证替代（猫爪已记录） |

## 15. 结论

**通过**。**强烈建议进入唯一一次完整发布验证阶段**（运行完整测试 + `next build` + `git add` 4 文件 + commit + push + Vercel 部署）。013/014/015/016 四轮迭代累计修复：状态串台、字典化、伪记录防御、测试真实性。所有 P0/P1 已闭合，无 P2 阻塞项。
