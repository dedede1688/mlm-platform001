# 小M_006号任务：收益作废风险修复第二轮只读复审报告

**复审对象**：猫爪 005 + 006 号任务合并后的真实工作区代码
**复审时间**：2026-07-13 02:20
**复审范围**：6 个业务/测试文件的真实代码改动（不依据猫爪报告下结论）
**本次结论**：✅ **通过（条件性）**——P0 全清，剩 1 个 P1 + 2 个 P2 不阻塞，**可进入生产模板同步 → 提交部署阶段**

---

## 1. 最终结论

**✅ 通过（有条件）**

**核心结论**：
- 005 号 P0（双重作废风险）已通过 **通知方法整体 try/catch + 路由层 .catch() 第二道保护** 彻底修复
- 005 号 P1-1（Infinity 校验）已用 `Number.isFinite` 修复
- 005 号 P1-2（并发下 oldValue 过期）已用"读取更新后真实值 + 反推前值"修复
- 005 号 P1-3（无自动化测试）已新建 `__tests__/api/admin/users/balance.test.ts`（18 场景）+ 补 `order-notification.test.ts` 7 个 notifyEarningsVoid 测试
- 005 号 P1-4（生产通知模板）确认尚未同步，**列为部署前置项**（胡子哥在 Supabase 跑单条 SQL upsert）

**新增 1 个 P1（任务文件第 16 项明确允许列为 P1/不要误判 P0）**：
- "可用收益不足"当前返回 500，应该是 400/409 业务校验错误（影响 admin 体验，不影响资金安全）

**新增 2 个 P2**：
- `notifyEarningsVoid` 调用 `sendInApp` 未传 `sourceType`/`sourceId`（其他 4 个通知方法都传）
- 路由层 `notifyBalanceChange` 没加 `.catch()` 保护（与 `notifyEarningsVoid` 不一致）

---

## 2. P0 问题

**无 P0。** 005 号 P0（双重作废风险）已修复。

修复证据：
1. `notifyEarningsVoid` 整个方法被 `try/catch` 覆盖（line 760-808，diff 760-808）
2. 路由层 `await ... .catch(...)` 第二道保护（route.ts line 232-242）
3. 测试 17 真实验证 P0：mock notifyEarningsVoid 抛 `Error('通知服务内部爆炸')` → 断言 `res.status === 200 + data.success === true`

---

## 3. P1 问题

### 🟡 P1-1：「可用收益不足」当前返回 500，应为 400 或 409

**位置**：`src/app/api/admin/users/[id]/balance/route.ts` line 111

```typescript
if (voidResult.count === 0) throw new Error('可用收益不足')
// → 抛错走到外层 catch → 返回 500
```

**证据**：`balance.test.ts` line 304 `expect(res.status).toBe(500)`

**影响**：
- admin 前端看到 500 错误会误以为是"系统故障"，实际是业务校验错误
- 影响 admin 体验，不影响资金安全（updateMany gte 正确拒绝，流水/日志/通知**均不写入**）
- 测试 13 验证了关键安全边界：可用收益不足时 `tx.balanceRecord.create` / `logOperation` / `notifyEarningsVoid` **全部未调用** ✓

**修复动作**：
```typescript
if (voidResult.count === 0) {
  return NextResponse.json(
    { success: false, message: '可用收益不足' },
    { status: 400 }  // 或 409 Conflict
  )
}
```

**任务文件第 261 行明确指引**："请判断这是否应该作为业务校验错误返回 400 或 409。该问题不影响资金安全，但如果确认存在，应列为 P2 或 P1 体验问题，不要误列为 P0。"

**判定**：列为 P1（不阻塞提交，但应在下一轮修复）。**原因**：通用调账逻辑（line 172）也用同样的 `throw new Error(...)` 返回 500，是历史遗留的模式，不只是 earnings_void 独有的问题。

---

## 4. P2 观察

### 🟢 P2-1：notifyEarningsVoid.sendInApp 缺 sourceType/sourceId

**位置**：`src/lib/services/order-notification.service.ts` line 790-797

```typescript
await sendInApp({
  userId: params.userId,
  templateType: 'earnings_voided',
  variables,
  batchId: b.id,
  senderId: params.operatorId,
  // ❌ 缺 sourceType / sourceId
})
```

**对比其他通知方法**（都传了 sourceType/sourceId）：
- `notifyRechargeApproved` line 647-648: `sourceType: 'recharge_request', sourceId: params.rechargeId`
- `notifyRechargeRejected` line 697-698: 同上
- `notifyRechargeSubmitted` line 744-745: 同上
- `notifyEarningsTransferred` line 844-845: `sourceType: 'earnings_transfer', sourceId: params.userId`

**影响**：用户端「消息中心」点击通知时，无法跳转到对应的"我的收益作废记录"详情页（如果有）。可追踪性受损。

**修复动作**（不阻塞本轮）：
```typescript
await sendInApp({
  ...,
  sourceType: 'earnings_void',
  sourceId: params.operatorId,  // 或后续加一个 adjustmentId 字段
})
```

**任务文件第 264-267 行明确**："如果没有，列为 P2 可追踪性建议，不阻塞本轮资金功能"。

### 🟢 P2-2：notifyBalanceChange 路由层未加 .catch() 保护

**位置**：`src/app/api/admin/users/[id]/balance/route.ts` line 245-252

```typescript
// v46.11: 触发余额变动通知（修复调账路由没调 sendInApp 的死代码问题）
await OrderNotificationService.notifyBalanceChange({...})
// ❌ 没有 .catch() 第二道保护（与 notifyEarningsVoid 不一致）
```

**影响**：虽然 `notifyBalanceChange` 内部有 try/catch，但未来代码修改可能漏；如果漏了，异常会冒泡到外层 catch → 资金已 commit 但接口 500。

**修复动作**（不阻塞本轮）：与 P2-1 一起在下一轮派单中统一为 `OrderNotificationService.notifyXxx(...).catch(...)` 模式。

---

## 5. 修改范围检查

| 预期文件 | 实际状态 | 一致？ |
|---|---|---|
| `src/app/api/admin/users/[id]/balance/route.ts` | M（+119/-19） | ✅ |
| `src/lib/services/order-notification.service.ts` | M（+52/-0） | ✅ |
| `prisma/seed.ts` | M（保留 005 改动） | ✅ |
| `src/app/admin/users/page.tsx` | M（保留 005 改动） | ✅ |
| `__tests__/api/admin/users/balance.test.ts` | ??（新建，427 行） | ✅ |
| `__tests__/services/order-notification.test.ts` | M（+117/-0） | ✅ |

**6 个预期文件全在位**。

**非业务改动（不属于本轮，不应混入提交）**：
- `docs/admin-api-audit.md`（历史 M）
- `docs/归档/报告/xiaojing-onboarding.md`（历史 M）
- `docs/归档/计划/plan-v47/48/49-*.md`（历史 M）
- `vercel_ls.txt`（临时文件）
- `docs/agent-tasks/`（协作文件夹）
- 5 个历史 M 的 docs **已确认与 005/006 业务代码无关**（diff 0 行业务相关）

**注**：`git diff --name-only` 不会列 `balance.test.ts`（因为是 `??` 状态），需要 `git status --short` 才能看到。任务文件第 70 行已明确提示。

---

## 6. P0 通知失败隔离检查

### 通知方法整体 try/catch 覆盖

`notifyEarningsVoid`（diff line 754-808）：

```typescript
static async notifyEarningsVoid(params: {...}) {
  try {  // ← line 766，整个方法体在 try 内
    const user = await prisma.user.findUnique({...})  // ✅ 在 try 内
    if (!user) return

    const b = await prisma.notificationBatch.create({...})  // ✅ 在 try 内
    await sendInApp({...})  // ✅ 在 try 内
  } catch (err) {
    console.error('[v006 notifyEarningsVoid]', {...})
    logger.error('收益作废通知失败', { error: String(err) })
  }
}
```

**所有 3 个关键调用都在 try/catch 内**：findUnique / batch.create / sendInApp ✓
**未 await 的 IIFE 已删除** ✓
**方法内任何异常都被吞掉** ✓

### 路由层第二道保护

`route.ts` line 232-242：

```typescript
await OrderNotificationService.notifyEarningsVoid({...}).catch((err) => {
  console.error('[v006 notifyEarningsVoid route catch]', { error: String(err) })
  logger.error('收益作废通知路由层捕获异常', { error: String(err) })
})
```

**保留 await** ✓
**保留 .catch() 兜底** ✓
**资金事务已 commit，不受通知失败影响** ✓

### 测试 17 真实验证

`balance.test.ts` line 378-399：
```typescript
;(OrderNotificationService.notifyEarningsVoid as any).mockRejectedValueOnce(
  new Error('通知服务内部爆炸')
)
// 关键断言：资金已成功，接口必须返回 200 + success=true
expect(res.status).toBe(200)
expect(data.success).toBe(true)
```

**测试通过** ✓（84/84）

---

## 7. 金额校验检查

`route.ts` line 73：

```typescript
if (typeof amount !== 'number' || !Number.isFinite(amount) || amount === 0) {
  return NextResponse.json(
    { success: false, message: 'amount 必须为非零有限数字' },
    { status: 400 }
  )
}
```

**`Number.isFinite` 已使用** ✓
**拒绝所有非法值**：
- 字符串 `typeof !== 'number'` ✓
- `null` / `undefined` `typeof !== 'number'` ✓
- `NaN` `!isFinite` ✓
- `Infinity` / `-Infinity` `!isFinite` ✓
- `0` `=== 0` ✓
- 负数（earnings_void 专用）line 81 `amount <= 0` ✓

**测试 7-12 全部通过** ✓（虽然有 JSON.stringify 把 NaN/Infinity 转 null 的 P2 测试方法学问题，但**源码确实用了 `Number.isFinite`**，任务文件第 125 行允许"同时依靠源码检查确认"）

---

## 8. 资金原子性检查

`route.ts` line 104-110（diff）：

```typescript
const voidResult = await tx.user.updateMany({
  where: { id, earningsAvailable: { gte: amount } },
  data: {
    earningsAvailable: { decrement: amount },
    earningsVoided: { increment: amount },
  },
})
if (voidResult.count === 0) throw new Error('可用收益不足')
```

**使用 updateMany 条件更新** ✓
**where 含 `earningsAvailable: { gte: amount }`** ✓
**data 含 `earningsAvailable: { decrement }` + `earningsVoided: { increment }`** ✓
**count === 0 拒绝** ✓
**不使用 $queryRaw**（grep 0 匹配）✓
**不使用普通 update**（grep 0 匹配）✓

**不修改其他字段的证据（测试 4-6）**：
```typescript
// balance.test.ts line 192-194
const updateManyCall = tx.user.updateMany.mock.calls[0][0]
expect(updateManyCall.data).not.toHaveProperty('consumeBalance')

// line 208-209
expect(updateManyCall.data).not.toHaveProperty('earningsFrozen')

// line 224-225
expect(updateManyCall.data).not.toHaveProperty('earningsPending')
```

**测试 2-3 验证 balance / frozenBalance 不变**（用真实 balanceRecord.create 调用的 balance/frozenBalance 字段）✓

**测试 13 验证可用收益不足时不写流水/日志/通知** ✓

---

## 9. 并发真实快照检查

`route.ts` line 113-134（diff）：

```typescript
// v006: 读取更新后的真实值，不用 before 伪造快照
const updated = await tx.user.findUnique({ where: { id } })

// v006: 反推本次操作前的真实值（并发安全）
const actualOld = {
  earningsAvailable: updated.earningsAvailable + amount,
  earningsVoided: updated.earningsVoided - amount,
}

const after4Field = {
  consumeBalance: updated.consumeBalance,
  earningsAvailable: updated.earningsAvailable,
  earningsPending: updated.earningsPending,
  earningsVoided: updated.earningsVoided,
}
const before4Field = {
  consumeBalance: updated.consumeBalance,
  earningsAvailable: actualOld.earningsAvailable,  // ✅ 反推真实前值
  earningsPending: updated.earningsPending,
  earningsVoided: actualOld.earningsVoided,  // ✅ 反推真实前值
}
await tx.balanceRecord.create({
  data: {
    balance: updated.balance,           // ✅ 真实快照
    frozenBalance: updated.frozenBalance, // ✅ 真实快照
    description: `...${format4FieldDelta(before4Field, after4Field)}`,
  },
})

return {
  updated,
  oldValue: actualOld,  // ✅ 返回反推真实前值
  ...
}
```

**BalanceRecord**：
- `balance` / `frozenBalance` 用 `updated.balance` / `updated.frozenBalance`（事务更新后真实值）✓
- `format4FieldDelta(before4Field, after4Field)` 两者都用 updated 派生（前值是反推）✓

**OperationLog**：
- `oldValue: actualOld`（反推真实前值）→ 测试 15 line 349-350 验证 `100 / 30` ✓
- `newValue: { earningsAvailable: result.updated.earningsAvailable, earningsVoided: result.updated.earningsVoided }`（数据库真实后值）→ 测试 15 line 352-353 验证 `60 / 70` ✓

**通知**：
- `notifyEarningsVoid({...earningsAvailable: result.updated.earningsAvailable, earningsVoided: result.updated.earningsVoided})` ✓
- 测试 16 line 373-374 验证 `60 / 70` ✓

**任务文件第 165-172 行要求全部达成** ✓

---

## 10. BalanceRecord（资金流水）检查

`route.ts` line 135-146（diff）：

```typescript
await tx.balanceRecord.create({
  data: {
    userId: id,
    type: 'earnings_void',       // ✅
    amount,                      // ✅ 正数（amount 校验通过 > 0）
    balance: updated.balance,    // ✅ 真实快照
    frozenBalance: updated.frozenBalance,  // ✅ 真实快照
    sourceType: 'admin',         // ✅ 可追踪
    sourceId: admin.id,          // ✅ 可追踪
    description: `管理员调账：作废收益 ¥${amount.toFixed(2)}，原因：${reason}${format4FieldDelta(before4Field, after4Field)}`,
  },
})
```

**type = 'earnings_void'** ✓ （测试 14 line 328 验证）
**amount 为正数** ✓ （测试 14 line 329-330 验证 `amount === 40, amount > 0`）
**sourceType = 'admin'** ✓
**sourceId = admin.id** ✓
**原因和字段变化完整** ✓
**与资金更新同一事务** ✓（在 `$transaction` 内部 line 95-208）

---

## 11. OperationLog（操作日志）检查

`route.ts` line 216-227（diff）：

```typescript
await logOperation({
  userId: admin.id,
  action: 'UPDATE',
  module: 'user',
  targetId: id,
  oldValue: result.oldValue,  // actualOld = { earningsAvailable, earningsVoided }
  newValue: adjustType === 'earnings_void'
    ? { earningsAvailable: result.updated.earningsAvailable, earningsVoided: result.updated.earningsVoided }
    : { [result.mapping.main]: result.updated[result.mapping.main] },
  ip: ...,
  userAgent: ...,
})
```

**记录管理员** ✓
**记录目标用户** ✓
**oldValue 含 earningsAvailable + earningsVoided** ✓
**newValue 含 earningsAvailable + earningsVoided** ✓

**测试 15 验证真实值**：
- `oldValue.earningsAvailable === 100`（反推真实前值）✓
- `oldValue.earningsVoided === 30`（反推真实前值）✓
- `newValue.earningsAvailable === 60`（DB 真实后值）✓
- `newValue.earningsVoided === 70`（DB 真实后值）✓

**logOperation 内部异常保护**（`src/lib/utils/operation-log.ts` line 29, 42）：
```typescript
export async function logOperation(params: LogOperationParams): Promise<void> {
  try {
    await prisma.operationLog.create({...})
  } catch (error) {
    logger.error('[OperationLog] 记录操作日志失败', {...})
  }
}
```

✓ 任务文件第 189 行明确"logOperation 已有内部异常保护，不要把已解决的问题误报为阻塞项"——不构成 P1 风险。

---

## 12. 前端回归检查

`src/app/admin/users/page.tsx` 保留 005 号全部改动：
- `balanceType` state 含 `'earnings_void'` ✓
- 下拉选项含「作废收益」 ✓
- 前端校验：`amount > 0` + `amount <= detailUser.earningsAvailable` ✓
- 大额确认弹窗展示作废金额/当前可用收益/作废后可用收益/作废后累计作废 ✓
- 原有 `earnings_add` 路径未改（005 之前就存在）✓

**测试 18 真实验证 earnings_add 路径未受影响**：
```typescript
expect(OrderNotificationService.notifyBalanceChange).toHaveBeenCalledOnce()
expect(OrderNotificationService.notifyEarningsVoid).not.toHaveBeenCalled()
```

---

## 13. 接口测试真实性检查

**关键断言**（不是只检查 mock 返回值）：

| 测试 | 真实断言 | 文件:行 |
|---|---|---|
| 1 | `data.data.earningsAvailable === 60` + `data.data.earningsVoided === 70` | line 142-143 |
| 2 | `brCall.data.balance === 1000`（BalanceRecord.balance 真实值） | line 159-160 |
| 3 | `brCall.data.frozenBalance === 200` | line 175-176 |
| **4** | **`updateManyCall.data` 不含 `consumeBalance`**（用 `not.toHaveProperty`） | line 192-193 |
| **5** | **`updateManyCall.data` 不含 `earningsFrozen`** | line 208-209 |
| **6** | **`updateManyCall.data` 不含 `earningsPending`** | line 224-225 |
| 7-12 | 6 种非法金额返回 400 + 不进入 `$transaction` | line 229-281 |
| 13 | 可用收益不足时不写流水/日志/通知 | line 304-311 |
| 14 | `brCall.data.type === 'earnings_void'` + `amount === 40` | line 328-329 |
| **15** | **OperationLog.oldValue = 100/30, newValue = 60/70** | line 349-353 |
| 16 | notifyEarningsVoid 参数 userId/u1, amount/40, earningsAvailable/60, earningsVoided/70 | line 371-374 |
| **17** | **mockRejectedValue → status=200 + success=true**（P0 核心） | line 397-398 |
| 18 | earnings_add 走 notifyBalanceChange 不走 notifyEarningsVoid | line 424-425 |

**测试真实性评估**：高。**测试 4-6 直接断言 `updateMany.data` 不含 3 个字段**（这是任务文件第 215-234 行明确要求的检查项），**测试 15 直接断言 oldValue/newValue 真实值**（这是任务文件第 168-172 行要求的反推真实前值），**测试 17 直接断言 P0 失败时接口仍 200**。

**唯一 P2 观察**（任务文件第 125 行已警示）：测试 10/11/12（NaN/Infinity/-Infinity）因 `JSON.stringify(NaN) === 'null'`，实际后端收到 `null`，是通过 `typeof null !== 'number'` 而非 `Number.isFinite` 被拦截的。**不是真测 Number.isFinite**。但**源码 line 73 已确认有 `!Number.isFinite(amount)`**，任务文件允许"同时依靠源码检查确认"。

---

## 14. 通知测试真实性检查

`__tests__/services/order-notification.test.ts` 新增 7 个测试（diff line 1102-1222）：

| # | 测试 | 真实断言 |
|---|---|---|
| 1 | 正常发送 | `batches[0]` 含 `templateType: 'earnings_voided'` + `batches[0].content` 含 `¥40.00 已被后台作废` + `sendCalls[0]` 含 `userId/batchId` |
| 2 | 模板变量 | `sendCalls[0].variables` 完全等于 `{ userName, amount: '40.00', earningsAvailable: '60.00', earningsVoided: '70.00' }` |
| 3 | 用户不存在 | `notificationBatch.create` 和 `sendInApp` 都 `not.toHaveBeenCalled` |
| **4** | **findUnique 抛错** | **`resolves.toBeUndefined()` + console.error + logger.error** |
| **5** | **batch.create 抛错** | **`resolves.toBeUndefined()` + logger.error** |
| **6** | **sendInApp 抛错** | **`resolves.toBeUndefined()` + logger.error** |
| 7 | 失败时调 logger | `logger.error` 被调 1 次 + 验证参数 |

**测试真实性评估**：高。所有 3 种失败场景（findUnique / batch.create / sendInApp）都真实抛出，断言 `resolves.toBeUndefined()`（不是抛错），断言 `logger.error` 被调用。

---

## 15. 通知模板与生产同步状态

| 检查项 | 状态 |
|---|---|
| `prisma/seed.ts` 含 `earnings_voided` 模板（type/channel/subject/content/enabled） | ✅ seed.ts line 225-231 |
| **生产数据库 `notification_template` 表是否含 `earnings_voided` 记录** | ❌ **未同步**（本轮不允许连生产 DB） |

**部署前必做（胡子哥）**：

```sql
-- 在 Supabase SQL Editor 手动跑，不依赖 prisma db seed 全量
INSERT INTO "notification_template" (id, type, channel, subject, content, variables, enabled, created_at, updated_at)
VALUES (
  'seed-tpl-earnings_voided-in_app',
  'earnings_voided',
  'in_app',
  '收益作废通知',
  '【敏维科技】{{userName}} 您好，您的可用收益 ¥{{amount}} 已被后台作废。剩余可用收益：¥{{earningsAvailable}}，累计作废收益：¥{{earningsVoided}}。如有疑问请联系客服。',
  '["userName","amount","earningsAvailable","earningsVoided"]',
  true,
  NOW(),
  NOW()
)
ON CONFLICT (type, channel) DO UPDATE SET
  subject = EXCLUDED.subject,
  content = EXCLUDED.content,
  updated_at = NOW();
```

**禁止运行 `npx prisma db seed`**（全量 seed 可能写入其他测试或初始化数据）

**验证 SQL**：
```sql
SELECT * FROM "notification_template" WHERE type = 'earnings_voided';
-- 应返回 1 条
```

---

## 16. 测试命令结果和测试数量

```
npx vitest run __tests__/api/admin/users/balance.test.ts __tests__/services/order-notification.test.ts
```

**结果**：
- Test Files: 2 passed (2)
- Tests: 84 passed (84)
- Duration: 4.72s

**测试数量**：
- `balance.test.ts` 新建：**18 个**（任务文件要求 18 个）
- `order-notification.test.ts` 新增：**7 个** notifyEarningsVoid（任务文件要求 7 个）
- `order-notification.test.ts` 原有：59 个（未受影响）
- 合计：**84 个**（18 + 7 + 59）

---

## 17. typecheck（类型检查）结果

```
npx tsc --noEmit -p tsconfig.typecheck.json
```

**结果：EXIT 0，0 错误**（无输出 = 全部通过）

注：项目整体 typecheck 仍存在约 918 行测试文件 mock 类型错误（25 个测试文件），与本次业务代码无关，是历史遗留。本次业务代码 0 错误。

---

## 18. build（构建）结果

```
npx next build
```

**结果：EXIT 0**
- ✓ Compiled successfully in 53s
- ✓ Generating static pages (110/110)
- 无 error、无 warning

---

## 19. git diff --check 结果

```
git diff --check
```

**结果**：仅有 LF→CRLF 换行符警告（Windows 正常现象，不影响功能）。无冲突标记、无空白错误。

---

## 20. git status --short 结果

```
 M __tests__/services/order-notification.test.ts
 M docs/admin-api-audit.md
 M "docs/归档/报告/xiaojing-onboarding.md"
 M "docs/归档/计划/plan-v47-system-optimization.md"
 M "docs/归档/计划/plan-v48-comprehensive.md"
 M "docs/归档/计划/plan-v49-engineering.md"
 M prisma/seed.ts
 M src/app/admin/users/page.tsx
 M src/app/api/admin/users/[id]/balance/route.ts
 M src/lib/services/order-notification.service.ts
?? __tests__/api/admin/users/balance.test.ts  ← 本轮新建
?? docs/agent-tasks/...（协作文件夹）
?? vercel_ls.txt
```

**6 个业务/测试文件全部就位**：5 M（已修改）+ 1 ??（新建）。**业务代码改动全部在 working tree 中（未 commit）**。

---

## 21. 是否 commit（提交）

**否。** 业务代码改动全部在 working tree，未执行 `git add` / `git commit`。

---

## 22. 是否 push（推送）

**否。** 未执行 `git push`。

---

## 23. 是否建议进入模板同步和提交部署阶段

**✅ 是。** 建议按以下顺序执行：

### 🔴 步骤 1（胡子哥，10 min）— 部署前置

在 Supabase SQL Editor 跑通知模板 upsert SQL（见第 15 节）。**禁止跑 `npx prisma db seed` 全量**。

### 🟡 步骤 2（胡子哥，30 min）— 可选

如想同步修复 P1-1（可用收益不足返回 500）：

```typescript
// src/app/api/admin/users/[id]/balance/route.ts line 111
if (voidResult.count === 0) {
  return NextResponse.json(
    { success: false, message: '可用收益不足' },
    { status: 400 }  // 改为 400
  )
}
```

并补 1 个测试断言 `expect(res.status).toBe(400)`。

**此步骤可选**——P1 不阻塞本轮提交。但**强烈建议**与 P2-1/P2-2 一起在下一轮派单统一修复。

### 🟢 步骤 3（猫爪，10 min）— 提交推送

```bash
git add src/app/api/admin/users/[id]/balance/route.ts
git add src/lib/services/order-notification.service.ts
git add prisma/seed.ts
git add src/app/admin/users/page.tsx
git add __tests__/api/admin/users/balance.test.ts
git add __tests__/services/order-notification.test.ts
git commit -m "fix(balance): prevent duplicate earnings void via notify exception isolation

- 通知方法整体 try/catch 覆盖 findUnique/batch.create/sendInApp
- 路由层 .catch() 第二道保护
- Number.isFinite 校验替代 isNaN
- 读取更新后真实值 + 反推真实前值（并发安全）
- 新建 18 个资金接口测试 + 7 个通知服务测试"
git push origin main
```

### 🚀 步骤 4（Vercel 自动部署）

Vercel 会自动从 main 分支部署。

### ✅ 步骤 5（胡子哥，10 min）— 部署后必做验证

参考 v68.6 铁律：
1. 强刷浏览器 → 后台 /admin/users/[id] → 资金账户 → 资金调整 → 选「作废收益」→ 提交一笔小额
2. 验证：可用收益减少、累计作废增加、资金流水 1 条、操作日志 1 条、用户端「消息中心」1 条通知
3. 检查 Supabase `notification_template` 表已有 `earnings_voided` 记录
4. 用测试账号登录 → 确认铃铛 +1 → 点开能看到「收益作废通知」
5. 故意提交 `amount: 1e308` 或 `amount: -Infinity` → 验证 400 拒绝
6. 故意提交 `amount > 用户可用收益` → 验证 500 返回（验证 P1-1 现象）或 400 拒绝（如已修）

---

## 24. 执行方案建议（修复动作 + 负责人 + 工时）

### 🔴 P0 - 已修复

无 P0 遗留。

### 🟡 P1 - 1 项（不阻塞，但建议下一轮修复）

| 问题 | 修复动作 | 负责人 | 工时 |
|---|---|---|---|
| P1-1 可用收益不足返回 500 | route.ts line 111 改 `throw` 为 `return NextResponse.json({...}, { status: 400 })`，通用调账 line 172 同样修复 | 猫爪 | 10 min（含 1 个新测试断言） |

### 🟢 P2 - 2 项（不阻塞，列入下轮派单）

| 问题 | 修复动作 | 负责人 | 工时 |
|---|---|---|---|
| P2-1 notifyEarningsVoid.sendInApp 缺 sourceType/sourceId | line 790-797 加 `sourceType: 'earnings_void', sourceId: params.operatorId` | 猫爪 | 5 min |
| P2-2 notifyBalanceChange 路由层未加 .catch() 保护 | route.ts line 245-252 加 `.catch()` | 猫爪 | 5 min |

### 📋 部署后必做（胡子哥）

| 步骤 | 动作 | 工时 |
|---|---|---|
| 1 | Supabase SQL Editor 跑通知模板 upsert | 10 min |
| 2 | 浏览器走 6 步验证 | 10 min |
| 3 | 部署后通知里子哥确认通知收到 | 5 min |

---

## 25. 总结

**本次 005 + 006 合并复审通过**。

- 005 号 P0（双重作废）已通过 **通知方法整体 try/catch + 路由层 .catch() 双层防御** 彻底修复，并经测试 17 真实验证
- 005 号 4 个 P1 全部修复（Number.isFinite / 反推真实前值 / 18 个接口测试 / 7 个通知测试）
- 005 号 P1-4（生产模板同步）确认尚未同步，已明确为部署前置步骤（胡子哥跑 SQL）

**新增 1 个 P1 + 2 个 P2 均不阻塞提交**：
- P1-1（可用收益不足 500 状态码）—— 业务校验错误应返 400/409
- P2-1（缺 sourceType/sourceId）—— 可追踪性建议
- P2-2（notifyBalanceChange 一致性）—— 与 notifyEarningsVoid 模式不一致

**所有验证命令通过**：typecheck EXIT 0 / build EXIT 0 / 84 个测试全过 / git diff --check 无冲突。

**建议下一步**：
1. 胡子哥在 Supabase 跑通知模板 upsert SQL（10 min，部署前置）
2. 猫爪 commit + push
3. Vercel 自动部署
4. 胡子哥按步骤 5 走浏览器 6 步验证
5. P1-1 / P2-1 / P2-2 列入下一轮派单统一修复

---

**复审完成时间**：2026-07-13 02:25
**本次复审完全只读**，未修改任何业务代码、测试代码、AGENTS.md、prisma/schema.prisma、迁移文件，未 commit，未 push，未部署。
